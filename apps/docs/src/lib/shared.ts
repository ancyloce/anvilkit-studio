// Shared, framework-agnostic constants for the Fumadocs/TanStack Start docs app.
// Docs are served at the SITE ROOT (`baseUrl: '/'`) to preserve the live
// Starlight site's URLs (e.g. `/getting-started`, `/guides/...`) — Starlight
// also serves docs from the root, not under a `/docs` prefix.
export const appName = "AnvilKit";
export const docsRoute = "/";

export const gitConfig = {
	user: "ancyloce",
	repo: "anvilkit-studio",
	branch: "main",
};
