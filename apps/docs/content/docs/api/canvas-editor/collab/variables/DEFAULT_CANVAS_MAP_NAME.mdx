---
title: "DEFAULT_CANVAS_MAP_NAME"
description: "@anvilkit/canvas-editor · DEFAULT_CANVAS_MAP_NAME"
---

```ts
const DEFAULT_CANVAS_MAP_NAME: "anvilkit-canvas" = "anvilkit-canvas";
```

Defined in: [packages/capabilities/canvas/editor/src/collab/keys.ts:9](https://github.com/ancyloce/anvilkit-studio/blob/main/src/collab/keys.ts#L9)

Y.Doc slot constants for the canvas collab binding (I3-1).

Prototype encoding is whole-document JSON-blob last-writer-wins: the entire
`CanvasIR` lives under a single [CANVAS\_IR\_KEY](CANVAS_IR_KEY.md) as key-sorted
JSON, and the latest writer's identity under [LAST\_PEER\_KEY](LAST_PEER_KEY.md). The
native per-node Y.Map tree (per-prop CRDT merge) is the GA follow-up.
