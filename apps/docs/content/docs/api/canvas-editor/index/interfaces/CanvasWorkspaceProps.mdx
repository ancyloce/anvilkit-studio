---
title: "CanvasWorkspaceProps"
description: "@anvilkit/canvas-editor · CanvasWorkspaceProps"
---

Defined in: [packages/capabilities/canvas/editor/src/workspace/layout/CanvasWorkspace.tsx:102](https://github.com/ancyloce/anvilkit-studio/blob/main/src/workspace/layout/CanvasWorkspace.tsx#L102)

## Extends

- `Omit`\&lt;[`CanvasStudioProps`](CanvasStudioProps.md), `"renderShell"` \| `"renderErrorDetails"`\&gt;

## Properties

| Property | Type | Description | Inherited from | Defined in |
| ------ | ------ | ------ | ------ | ------ |
| &lt;a id="assetpicker"&gt;&lt;/a&gt; `assetPicker?` | [`CanvasAssetPicker`](CanvasAssetPicker.md) | FR-090 asset picker adapter (B-10). When present it supersedes `onPickAsset` for tools (single pick) and powers multi-select flows; the legacy `onPickAsset` keeps working unchanged. | [`CanvasStudioProps`](CanvasStudioProps.md).[`assetPicker`](CanvasStudioProps.md#assetpicker) | [packages/capabilities/canvas/editor/src/CanvasStudio.tsx:351](https://github.com/ancyloce/anvilkit-studio/blob/main/src/CanvasStudio.tsx#L351) |
| &lt;a id="assetuploader"&gt;&lt;/a&gt; `assetUploader?` | [`CanvasAssetUploader`](CanvasAssetUploader.md) | FR-091 upload adapter (B-10) — enables drag-and-drop + the Uploads panel. | [`CanvasStudioProps`](CanvasStudioProps.md).[`assetUploader`](CanvasStudioProps.md#assetuploader) | [packages/capabilities/canvas/editor/src/CanvasStudio.tsx:353](https://github.com/ancyloce/anvilkit-studio/blob/main/src/CanvasStudio.tsx#L353) |
| &lt;a id="autolayout"&gt;&lt;/a&gt; `autoLayout?` | \| `boolean` \| [`CanvasAutoLayoutFlagOptions`](CanvasAutoLayoutFlagOptions.md) | T-M4-10 (AL-COMPAT-003): opt-in Auto Layout creation/conversion UI. **Default OFF for the whole alpha/beta line** — flipping the default on is a releasable behaviour change requiring release notes (PRD §19 Phase 4). Gates ONLY creation/conversion affordances: reading, rendering, editing existing intent, and exporting are never gated at any phase. Prop name provisional under OQ-5 (assumption A-2). | [`CanvasStudioProps`](CanvasStudioProps.md).[`autoLayout`](CanvasStudioProps.md#autolayout) | [packages/capabilities/canvas/editor/src/CanvasStudio.tsx:278](https://github.com/ancyloce/anvilkit-studio/blob/main/src/CanvasStudio.tsx#L278) |
| &lt;a id="autosave"&gt;&lt;/a&gt; `autoSave?` | `boolean` \| [`CanvasAutoSaveOptions`](CanvasAutoSaveOptions.md) | FR-162 auto-save tuning. `false` = manual saves only. Default on. | [`CanvasStudioProps`](CanvasStudioProps.md).[`autoSave`](CanvasStudioProps.md#autosave) | [packages/capabilities/canvas/editor/src/CanvasStudio.tsx:336](https://github.com/ancyloce/anvilkit-studio/blob/main/src/CanvasStudio.tsx#L336) |
| &lt;a id="avatarsslot"&gt;&lt;/a&gt; `avatarsSlot?` | `ReactNode` | Collaborator avatars slot, rendered in the header. | - | [packages/capabilities/canvas/editor/src/workspace/layout/CanvasWorkspace.tsx:123](https://github.com/ancyloce/anvilkit-studio/blob/main/src/workspace/layout/CanvasWorkspace.tsx#L123) |
| &lt;a id="brandgovernance"&gt;&lt;/a&gt; `brandGovernance?` | `CanvasBrandPolicyContext` | Host brand-policy context (plan 0021 T-040): capability snapshot, enforcement mode, opaque policy revision. Omit to run ungoverned — every affordance stays available, which is the pre-M4 behaviour. Presentation input only; the command layer enforces the same policy independently, so hiding a button is never the enforcement. | [`CanvasStudioProps`](CanvasStudioProps.md).[`brandGovernance`](CanvasStudioProps.md#brandgovernance) | [packages/capabilities/canvas/editor/src/CanvasStudio.tsx:425](https://github.com/ancyloce/anvilkit-studio/blob/main/src/CanvasStudio.tsx#L425) |
| &lt;a id="brandkit"&gt;&lt;/a&gt; `brandKit?` | [`BrandKit`](BrandKit.md) | Shared brand colors + fonts (I3-4). Hosts map their Studio config to a [BrandKit](BrandKit.md) and pass it here; the editor surfaces it via [useBrandKit](../functions/useBrandKit). Omit to run with no brand kit. | [`CanvasStudioProps`](CanvasStudioProps.md).[`brandKit`](CanvasStudioProps.md#brandkit) | [packages/capabilities/canvas/editor/src/CanvasStudio.tsx:391](https://github.com/ancyloce/anvilkit-studio/blob/main/src/CanvasStudio.tsx#L391) |
| &lt;a id="children"&gt;&lt;/a&gt; `children?` | `ReactNode` | Optional host UI rendered *inside* the editor's context provider, so it can call [useCanvasStudio](../functions/useCanvasStudio) to drive tool selection, read the live selection/IR, or mount the exported `<LayerPanel>` / `<PropertyInspector>` against this instance's stores. The editor ships no toolbar of its own (tool selection is host-driven, PRD §3.4); this slot is how a host wires one without recomposing the stage. Rendered as a sibling of the stage root so the host owns its own layout. | [`CanvasStudioProps`](CanvasStudioProps.md).[`children`](CanvasStudioProps.md#children) | [packages/capabilities/canvas/editor/src/CanvasStudio.tsx:554](https://github.com/ancyloce/anvilkit-studio/blob/main/src/CanvasStudio.tsx#L554) |
| &lt;a id="clipboard"&gt;&lt;/a&gt; `clipboard?` | [`CanvasClipboardAdapter`](CanvasClipboardAdapter.md) | §11.1 clipboard adapter override. When present, `clipboard-actions.ts` uses it instead of `system-clipboard.ts`'s `navigator.clipboard` wrapper — e.g. for an Electron/native bridge where the Web Clipboard API isn't available or isn't the right transport. Omit to use the built-in system clipboard (with its existing internal-clipboard fallback). | [`CanvasStudioProps`](CanvasStudioProps.md).[`clipboard`](CanvasStudioProps.md#clipboard) | [packages/capabilities/canvas/editor/src/CanvasStudio.tsx:385](https://github.com/ancyloce/anvilkit-studio/blob/main/src/CanvasStudio.tsx#L385) |
| &lt;a id="componentprovider"&gt;&lt;/a&gt; `componentProvider?` | [`CanvasComponentProvider`](CanvasComponentProvider.md) | Host-injected catalog for the Libraries source of the Components panel (plan 0021 T-018/T-019). Threaded exactly like [CanvasStudioProps.templateProvider](CanvasStudioProps.md#templateprovider). Omit it and the Libraries source is simply absent — a document that already contains external components still opens, renders and exports, because resolution reads the document's own snapshots and never a Provider. | [`CanvasStudioProps`](CanvasStudioProps.md).[`componentProvider`](CanvasStudioProps.md#componentprovider) | [packages/capabilities/canvas/editor/src/CanvasStudio.tsx:485](https://github.com/ancyloce/anvilkit-studio/blob/main/src/CanvasStudio.tsx#L485) |
| &lt;a id="componentvariants"&gt;&lt;/a&gt; `componentVariants?` | `boolean` | Plan 0021 rollout flag for component VARIANTS, default false. The third of T-053's flags, and it gates authoring only — exactly like `externalComponents` and for the same reason. A document that already carries `variantSelection` must keep resolving to the same variant with the flag off, because variant resolution is a READ concern: turning the flag off to roll back authoring must not silently re-render every instance with its default variant, which would be a visual regression indexed across every page at once. `rollback-rehearsal.test.ts` is what holds that line. | [`CanvasStudioProps`](CanvasStudioProps.md).[`componentVariants`](CanvasStudioProps.md#componentvariants) | [packages/capabilities/canvas/editor/src/CanvasStudio.tsx:508](https://github.com/ancyloce/anvilkit-studio/blob/main/src/CanvasStudio.tsx#L508) |
| &lt;a id="continuouscreation"&gt;&lt;/a&gt; `continuousCreation?` | `boolean` | FR-012 (A-10): keep the creation tool active after it commits an element (continuous creation). Default false — the editor returns to Select. | [`CanvasStudioProps`](CanvasStudioProps.md).[`continuousCreation`](CanvasStudioProps.md#continuouscreation) | [packages/capabilities/canvas/editor/src/CanvasStudio.tsx:269](https://github.com/ancyloce/anvilkit-studio/blob/main/src/CanvasStudio.tsx#L269) |
| &lt;a id="disablelocalassetfallback"&gt;&lt;/a&gt; `disableLocalAssetFallback?` | `boolean` | cp1-004 (PLAN-0035 §5 P1): opt OUT of the built-in local asset fallback. There are three states, not two: 1. **A host adapter is present** — any of [assetPicker](CanvasStudioProps.md#assetpicker), [assetUploader](CanvasStudioProps.md#assetuploader) or the legacy [onPickAsset](CanvasStudioProps.md#onpickasset). The host's adapter is used and the fallback is never even constructed. This flag is irrelevant here; a host adapter already wins. 2. **No host adapter, flag unset (the default)** — images are ingested into browser-local storage (IndexedDB, degrading to memory), so a bare `<CanvasStudio initialIR={…} />` can accept a drop and un-gate the Image tool with no wiring at all. 3. **No host adapter, flag `true`** — images are genuinely unavailable: the Image tool stays disabled and a drop reports "no upload service configured", which is the pre-cp1-004 behaviour. Set this when local storage would be the wrong promise — a host whose documents must be portable across devices, or one under a policy that forbids writing user content to the browser. Never set this to suppress the fallback while ALSO passing an adapter: state 1 already does that, and the flag would be misleading. | [`CanvasStudioProps`](CanvasStudioProps.md).[`disableLocalAssetFallback`](CanvasStudioProps.md#disablelocalassetfallback) | [packages/capabilities/canvas/editor/src/CanvasStudio.tsx:377](https://github.com/ancyloce/anvilkit-studio/blob/main/src/CanvasStudio.tsx#L377) |
| &lt;a id="dockitems"&gt;&lt;/a&gt; `dockItems?` | readonly [`DockItem`](DockItem.md)[] | Panel Dock entries, in order. Defaults to [DockItem](DockItem.md)[] config. | - | [packages/capabilities/canvas/editor/src/workspace/layout/CanvasWorkspace.tsx:132](https://github.com/ancyloce/anvilkit-studio/blob/main/src/workspace/layout/CanvasWorkspace.tsx#L132) |
| &lt;a id="elementactions"&gt;&lt;/a&gt; `elementActions?` | [`ElementActions`](ElementActions.md) | Optional host handlers for the Element Controls "more" menu (§2). | - | [packages/capabilities/canvas/editor/src/workspace/layout/CanvasWorkspace.tsx:138](https://github.com/ancyloce/anvilkit-studio/blob/main/src/workspace/layout/CanvasWorkspace.tsx#L138) |
| &lt;a id="extensions"&gt;&lt;/a&gt; `extensions?` | readonly [`CanvasEditorExtension`](CanvasEditorExtension.md)[] | Domain extensions (Area 1). Each may contribute renderers/inspectors for custom node kinds; they are threaded to `<CanvasNodeRenderer>` and the inspector via context. Pair with canvas-core's `createCanvasRuntime` for the matching schema/command/serializer extensions. | [`CanvasStudioProps`](CanvasStudioProps.md).[`extensions`](CanvasStudioProps.md#extensions) | [packages/capabilities/canvas/editor/src/CanvasStudio.tsx:531](https://github.com/ancyloce/anvilkit-studio/blob/main/src/CanvasStudio.tsx#L531) |
| &lt;a id="externalcomponents"&gt;&lt;/a&gt; `externalComponents?` | `boolean` | Plan 0021 rollout flag for EXTERNAL component libraries, default false. Parallel to `localComponents`, and gates only the authoring affordances (the Libraries source, insert, recovery). Never gates read/render/export: a document using external components must stay readable when the flag is off, or turning it off would be data loss rather than a rollback. | [`CanvasStudioProps`](CanvasStudioProps.md).[`externalComponents`](CanvasStudioProps.md#externalcomponents) | [packages/capabilities/canvas/editor/src/CanvasStudio.tsx:494](https://github.com/ancyloce/anvilkit-studio/blob/main/src/CanvasStudio.tsx#L494) |
| &lt;a id="fontcatalog"&gt;&lt;/a&gt; `fontCatalog?` | [`CanvasFontCatalog`](CanvasFontCatalog.md) | Extra font families the picker offers and the SVG exporter may embed (`cp2-007`). Build one with `createFontCatalog(entries)`; omit it to run on the built-in `DEFAULT_FONT_CATALOG` alone. **Merged, not replaced.** The editor resolves `mergeCatalogs(DEFAULT_FONT_CATALOG, fontCatalog)` once and hands the result to BOTH the font picker and the export `@font-face` manifest. Precedence is **brand &gt; host &gt; default** and rides on each record's `origin` — `createFontCatalog` stamps `"host"` by default, and `createFontCatalog(entries, { origin: "brand" })` outranks that — so argument order at any call site is irrelevant across tiers. A same-named family is replaced WHOLE-ENTRY (never field-merged, so a licence is never inherited); a family the default catalog does not know is added. This prop cannot *remove* a default family. **The default catalog ships metadata, not bytes.** All 37 default entries carry a Google Fonts stylesheet URL and no `source.files`, so they need network access to render and an SVG export emits **no** `@font-face` rule for them — those families fall back to system metrics with core's existing `FONT_NOT_IN_MANIFEST` warning. To get a family EMBEDDED in an SVG export, pass an entry whose `source.files` point at real font files. See `docs/typography.md`. | [`CanvasStudioProps`](CanvasStudioProps.md).[`fontCatalog`](CanvasStudioProps.md#fontcatalog) | [packages/capabilities/canvas/editor/src/CanvasStudio.tsx:416](https://github.com/ancyloce/anvilkit-studio/blob/main/src/CanvasStudio.tsx#L416) |
| &lt;a id="headerplugins"&gt;&lt;/a&gt; `headerPlugins?` | readonly [`CanvasHeaderPlugin`](CanvasHeaderPlugin.md)[] | Header plugins rendered in the header's right cluster. Pass the built-in export popover via [createCanvasExportPlugin](../functions/createCanvasExportPlugin). | - | [packages/capabilities/canvas/editor/src/workspace/layout/CanvasWorkspace.tsx:128](https://github.com/ancyloce/anvilkit-studio/blob/main/src/workspace/layout/CanvasWorkspace.tsx#L128) |
| &lt;a id="height"&gt;&lt;/a&gt; `height?` | `number` | - | [`CanvasStudioProps`](CanvasStudioProps.md).[`height`](CanvasStudioProps.md#height) | [packages/capabilities/canvas/editor/src/CanvasStudio.tsx:185](https://github.com/ancyloce/anvilkit-studio/blob/main/src/CanvasStudio.tsx#L185) |
| &lt;a id="hidepagenavigator"&gt;&lt;/a&gt; `hidePageNavigator?` | `boolean` | Suppress the built-in `<PageNavigator>` (e.g. hosts that bring their own). | [`CanvasStudioProps`](CanvasStudioProps.md).[`hidePageNavigator`](CanvasStudioProps.md#hidepagenavigator) | [packages/capabilities/canvas/editor/src/CanvasStudio.tsx:253](https://github.com/ancyloce/anvilkit-studio/blob/main/src/CanvasStudio.tsx#L253) |
| &lt;a id="initialactivepageid"&gt;&lt;/a&gt; `initialActivePageId?` | `string` | Initial active page id. Defaults to `initialIR.pages[0].id`. Uncontrolled — after mount the `pagesStore` owns the active id; switch pages via the `<PageNavigator>` or by calling `useCanvasStudio().pagesStore.getState().setActivePageId(...)`. | [`CanvasStudioProps`](CanvasStudioProps.md).[`initialActivePageId`](CanvasStudioProps.md#initialactivepageid) | [packages/capabilities/canvas/editor/src/CanvasStudio.tsx:183](https://github.com/ancyloce/anvilkit-studio/blob/main/src/CanvasStudio.tsx#L183) |
| &lt;a id="initialir"&gt;&lt;/a&gt; `initialIR` | [`CanvasIR`](https://github.com/ancyloce/anvilkit-canvas-core/blob/main/src/ir/types.ts) | Initial IR. Uncontrolled — subsequent prop updates do not replace the internal IR. Use `onChange` to mirror state into a host store. | [`CanvasStudioProps`](CanvasStudioProps.md).[`initialIR`](CanvasStudioProps.md#initialir) | [packages/capabilities/canvas/editor/src/CanvasStudio.tsx:177](https://github.com/ancyloce/anvilkit-studio/blob/main/src/CanvasStudio.tsx#L177) |
| &lt;a id="initialtool"&gt;&lt;/a&gt; `initialTool?` | [`ToolId`](../type-aliases/ToolId) | - | [`CanvasStudioProps`](CanvasStudioProps.md).[`initialTool`](CanvasStudioProps.md#initialtool) | [packages/capabilities/canvas/editor/src/CanvasStudio.tsx:186](https://github.com/ancyloce/anvilkit-studio/blob/main/src/CanvasStudio.tsx#L186) |
| &lt;a id="initialworkspacestate"&gt;&lt;/a&gt; `initialWorkspaceState?` | `Partial`\&lt;[`CanvasWorkspaceState`](../type-aliases/CanvasWorkspaceState)\&gt; | PRD §11.1 seed for a FRESH workspace mount (no prior `localStorage` value for `storeId`). An existing persisted layout always wins over this seed for the persisted fields (`activeDockId`/`inspectorCollapsed`/ `panelWidth`/`recentTemplateIds`) — see `workspace/state/workspace-ui-store.ts`'s `CreateWorkspaceUiStoreOptions.initialWorkspaceState` for the full precedence rule. Read only on first render, like `storeId`. | - | [packages/capabilities/canvas/editor/src/workspace/layout/CanvasWorkspace.tsx:115](https://github.com/ancyloce/anvilkit-studio/blob/main/src/workspace/layout/CanvasWorkspace.tsx#L115) |
| &lt;a id="inspector"&gt;&lt;/a&gt; `inspector?` | `boolean` | Render the right `PropertyInspector` column. Defaults to `true`. | - | [packages/capabilities/canvas/editor/src/workspace/layout/CanvasWorkspace.tsx:136](https://github.com/ancyloce/anvilkit-studio/blob/main/src/workspace/layout/CanvasWorkspace.tsx#L136) |
| &lt;a id="localcomponents"&gt;&lt;/a&gt; `localComponents?` | `boolean` | Plan 0023 M6-07 (PRD §19): opt-in Local Components AUTHORING UI. **Default OFF**, and gated exactly like `autoLayout`: it controls creation and Source-editing affordances only. Everything a document already contains keeps working at every stage with the flag off — instances resolve, render, hit-test, export, stay override-editable and stay detachable — because rollback must never strip Registry or instance data (PRD §19 rollback). Concretely, OFF hides the Components dock (the id stays in `DOCK_IDS` so the persisted-state union and its migration are stable in both directions, M5-01) and suppresses "create component from selection". ON adds the dock and the create affordance. Staged enablement: 1 parse/render → 2 internal create → 3 beta overrides/nesting/templates/detach → 4 default-on. | [`CanvasStudioProps`](CanvasStudioProps.md).[`localComponents`](CanvasStudioProps.md#localcomponents) | [packages/capabilities/canvas/editor/src/CanvasStudio.tsx:294](https://github.com/ancyloce/anvilkit-studio/blob/main/src/CanvasStudio.tsx#L294) |
| &lt;a id="messages"&gt;&lt;/a&gt; `messages?` | `Readonly`\&lt;`Record`\&lt;`string`, `string`\&gt;\&gt; | Host-injected i18n catalog (P7). A flat `canvas.*` → string map for the active locale; the editor resolves chrome strings via [useCanvasT](../functions/useCanvasT) (host override wins, else the inline English fallback). Omit to render the bundled English defaults. canvas-editor stays standalone — the host (e.g. plugin-canvas-studio) selects the catalog by locale and passes it. | [`CanvasStudioProps`](CanvasStudioProps.md).[`messages`](CanvasStudioProps.md#messages) | [packages/capabilities/canvas/editor/src/CanvasStudio.tsx:524](https://github.com/ancyloce/anvilkit-studio/blob/main/src/CanvasStudio.tsx#L524) |
| &lt;a id="onactivepagechange"&gt;&lt;/a&gt; `onActivePageChange?` | (`pageId`) =&gt; `void` | Fires whenever the active page (artboard) changes, with the new page id. Used by hosts that want to mirror the active artboard out (e.g. preview-export bridges that need to tag exports with the artboard id). | [`CanvasStudioProps`](CanvasStudioProps.md).[`onActivePageChange`](CanvasStudioProps.md#onactivepagechange) | [packages/capabilities/canvas/editor/src/CanvasStudio.tsx:202](https://github.com/ancyloce/anvilkit-studio/blob/main/src/CanvasStudio.tsx#L202) |
| &lt;a id="onaiintent"&gt;&lt;/a&gt; `onAiIntent?` | (`intent`) =&gt; `void` | Fires when an AI tool (`ai-image` / `ai-brush`, I1-7) captures a gesture. Hosts wire this to the AI panel / job client. Omit it to leave the AI tools as inert gesture-capture (the marquee/selection still render). | [`CanvasStudioProps`](CanvasStudioProps.md).[`onAiIntent`](CanvasStudioProps.md#onaiintent) | [packages/capabilities/canvas/editor/src/CanvasStudio.tsx:227](https://github.com/ancyloce/anvilkit-studio/blob/main/src/CanvasStudio.tsx#L227) |
| &lt;a id="onanalyticsevent"&gt;&lt;/a&gt; `onAnalyticsEvent?` | [`CanvasAnalyticsSink`](../type-aliases/CanvasAnalyticsSink) | Product analytics sink (plan 0021 T-050, PRD §13). Sits alongside `onChange`/`onExport` as a host callback. Every event name is `anvilkit.canvas.*`-prefixed and every payload is redacted per §13 — no credentials, raw content, text values, asset URLs, or unredacted identity; library and component identifiers arrive hashed. A throwing sink cannot break an edit: emission is wrapped, because a host's analytics callback is third-party code on a user-interaction path and losing a metric beats losing the user's work. | [`CanvasStudioProps`](CanvasStudioProps.md).[`onAnalyticsEvent`](CanvasStudioProps.md#onanalyticsevent) | [packages/capabilities/canvas/editor/src/CanvasStudio.tsx:449](https://github.com/ancyloce/anvilkit-studio/blob/main/src/CanvasStudio.tsx#L449) |
| &lt;a id="onback"&gt;&lt;/a&gt; `onBack?` | () =&gt; `void` | Header back action (hidden when omitted). | - | [packages/capabilities/canvas/editor/src/workspace/layout/CanvasWorkspace.tsx:117](https://github.com/ancyloce/anvilkit-studio/blob/main/src/workspace/layout/CanvasWorkspace.tsx#L117) |
| &lt;a id="onchange"&gt;&lt;/a&gt; `onChange?` | (`ir`, `command`) =&gt; `void` | Fires after every committed command with the new IR + the command. | [`CanvasStudioProps`](CanvasStudioProps.md).[`onChange`](CanvasStudioProps.md#onchange) | [packages/capabilities/canvas/editor/src/CanvasStudio.tsx:188](https://github.com/ancyloce/anvilkit-studio/blob/main/src/CanvasStudio.tsx#L188) |
| &lt;a id="onchanges"&gt;&lt;/a&gt; `onChanges?` | (`changes`, `ir`) =&gt; `void` | Fires after every commit with the granular change records + the new IR. Complements [onChange](CanvasStudioProps.md#onchange) for autosave / dirty-tracking / collab that wants deltas rather than the whole command. A batch commit reports the flattened per-command changes. | [`CanvasStudioProps`](CanvasStudioProps.md).[`onChanges`](CanvasStudioProps.md#onchanges) | [packages/capabilities/canvas/editor/src/CanvasStudio.tsx:195](https://github.com/ancyloce/anvilkit-studio/blob/main/src/CanvasStudio.tsx#L195) |
| &lt;a id="oncomponentevent"&gt;&lt;/a&gt; `onComponentEvent?` | [`CanvasComponentEventHandler`](../../internal/type-aliases/CanvasComponentEventHandler) | Plan 0023 M6-08: host observer for the eight PRD §12 component events. No transport ships in this package — the editor emits, the host delivers. | [`CanvasStudioProps`](CanvasStudioProps.md).[`onComponentEvent`](CanvasStudioProps.md#oncomponentevent) | [packages/capabilities/canvas/editor/src/CanvasStudio.tsx:306](https://github.com/ancyloce/anvilkit-studio/blob/main/src/CanvasStudio.tsx#L306) |
| &lt;a id="oncreatedocument"&gt;&lt;/a&gt; `onCreateDocument?` | (`document`) =&gt; `void` | FR-132 "Open as a new document": `<CanvasStudio>` owns one live document, so creating a brand-new document is a HOST action. When wired, the Templates panel surfaces an "Open as new document" choice and hands the host the instantiated `CanvasIR` (e.g. to open a new tab/route). Omit it and the choice is hidden — Replace / Add-as-new-pages still work. | [`CanvasStudioProps`](CanvasStudioProps.md).[`onCreateDocument`](CanvasStudioProps.md#oncreatedocument) | [packages/capabilities/canvas/editor/src/CanvasStudio.tsx:516](https://github.com/ancyloce/anvilkit-studio/blob/main/src/CanvasStudio.tsx#L516) |
| &lt;a id="onerror"&gt;&lt;/a&gt; `onError?` | (`error`, `info`) =&gt; `void` | FR-172 host error callback (B-15): fires when the canvas subtree throws during render and the error boundary catches it. Wire to telemetry. | [`CanvasStudioProps`](CanvasStudioProps.md).[`onError`](CanvasStudioProps.md#onerror) | [packages/capabilities/canvas/editor/src/CanvasStudio.tsx:232](https://github.com/ancyloce/anvilkit-studio/blob/main/src/CanvasStudio.tsx#L232) |
| &lt;a id="onexport"&gt;&lt;/a&gt; `onExport?` | (`result`) =&gt; `void` | PRD §11.1 export observer: fires with the completed [CanvasExportResult](CanvasExportResult.md) after every successful export — both the headless `useCanvasStudioActions().export()` action and the built-in export dialog's user-driven export. | [`CanvasStudioProps`](CanvasStudioProps.md).[`onExport`](CanvasStudioProps.md#onexport) | [packages/capabilities/canvas/editor/src/CanvasStudio.tsx:345](https://github.com/ancyloce/anvilkit-studio/blob/main/src/CanvasStudio.tsx#L345) |
| &lt;a id="ongovernanceauditevent"&gt;&lt;/a&gt; `onGovernanceAuditEvent?` | `CanvasGovernanceAuditSink` | Governance audit sink (TD §24.2). Distinct from [CanvasStudioProps.onAnalyticsEvent](CanvasStudioProps.md#onanalyticsevent): different retention, different consumer, different redaction. Carries no actor — the host adds authenticated identity in its own audit system. | [`CanvasStudioProps`](CanvasStudioProps.md).[`onGovernanceAuditEvent`](CanvasStudioProps.md#ongovernanceauditevent) | [packages/capabilities/canvas/editor/src/CanvasStudio.tsx:456](https://github.com/ancyloce/anvilkit-studio/blob/main/src/CanvasStudio.tsx#L456) |
| &lt;a id="ongovernancedeeplink"&gt;&lt;/a&gt; `onGovernanceDeepLink?` | (`code`) =&gt; `void` | Called when the user asks for more detail about a blocked operation (T-040 step 3). Receives the stable deny code, never localized copy and never the policy decision's log-only `detail`. | [`CanvasStudioProps`](CanvasStudioProps.md).[`onGovernanceDeepLink`](CanvasStudioProps.md#ongovernancedeeplink) | [packages/capabilities/canvas/editor/src/CanvasStudio.tsx:462](https://github.com/ancyloce/anvilkit-studio/blob/main/src/CanvasStudio.tsx#L462) |
| &lt;a id="onlayoutevent"&gt;&lt;/a&gt; `onLayoutEvent?` | [`CanvasLayoutEventHandler`](../type-aliases/CanvasLayoutEventHandler) | T-M4-11 (A-3, PRD §12): one optional callback carrying the six layout events. `canvas.layout.diagnostic` fires on commit only — never during preview — deduped within a commit by `(code, nodeId, axis)`. Payloads carry no document content. Prop name provisional under OQ-5. | [`CanvasStudioProps`](CanvasStudioProps.md).[`onLayoutEvent`](CanvasStudioProps.md#onlayoutevent) | [packages/capabilities/canvas/editor/src/CanvasStudio.tsx:301](https://github.com/ancyloce/anvilkit-studio/blob/main/src/CanvasStudio.tsx#L301) |
| &lt;a id="onloaderror"&gt;&lt;/a&gt; `onLoadError?` | (`error`) =&gt; `void` | Fires when a document could not be brought into the editor: either [CanvasPersistenceAdapter.load](CanvasPersistenceAdapter.md#load) rejected, or the document it resolved failed to parse, migrate, or validate (T-M0-04) — or a [CanvasRecoveryAdapter](CanvasRecoveryAdapter.md) snapshot failed the same pipeline and was discarded rather than restored (T-M0-05). Separate from [onError](CanvasStudioProps.md#onerror) on purpose: that one is the FR-172 render error-boundary callback and carries a `React.ErrorInfo`, which a load failure has no honest value for. A rejected load leaves `initialIR` mounted and editable rather than breaking the editor, so the host needs a way to hear about it — otherwise a document silently fails to load and the user edits the wrong content. | [`CanvasStudioProps`](CanvasStudioProps.md).[`onLoadError`](CanvasStudioProps.md#onloaderror) | [packages/capabilities/canvas/editor/src/CanvasStudio.tsx:327](https://github.com/ancyloce/anvilkit-studio/blob/main/src/CanvasStudio.tsx#L327) |
| &lt;a id="onpickasset"&gt;&lt;/a&gt; `onPickAsset?` | () =&gt; `Promise`\&lt;`string`\&gt; | Required for the image tool (MVP-6 Task 8). Host opens picker, returns asset id. | [`CanvasStudioProps`](CanvasStudioProps.md).[`onPickAsset`](CanvasStudioProps.md#onpickasset) | [packages/capabilities/canvas/editor/src/CanvasStudio.tsx:221](https://github.com/ancyloce/anvilkit-studio/blob/main/src/CanvasStudio.tsx#L221) |
| &lt;a id="onsavestatechange"&gt;&lt;/a&gt; `onSaveStateChange?` | (`state`) =&gt; `void` | Save-state observer (PRD §11.1). | [`CanvasStudioProps`](CanvasStudioProps.md).[`onSaveStateChange`](CanvasStudioProps.md#onsavestatechange) | [packages/capabilities/canvas/editor/src/CanvasStudio.tsx:338](https://github.com/ancyloce/anvilkit-studio/blob/main/src/CanvasStudio.tsx#L338) |
| &lt;a id="onselectionchange"&gt;&lt;/a&gt; `onSelectionChange?` | (`nodeId`) =&gt; `void` | Fires when the SINGLE selected node changes, with its id — or `null` whenever there is no single node to name: nothing selected (deselection), or a multi-selection, where naming one of N would be arbitrary. Hosts wire this to surfaces that operate on exactly one node, e.g. the AI panel's `image.replace` round-trip, which needs `AiLayerContext.selectedNodeId`. Fires once on mount with the initial value — like [onActivePageChange](CanvasStudioProps.md#onactivepagechange) — and after that ONLY when the reported id actually changes. Re-selecting the node that is already selected, or any selection churn that leaves the derived id equal, is not reported again. Reports the PERSISTENT-node projection (`selectionStore.selectedIds`), so a virtual node selected inside a component instance reports the owning instance id — the id a `node.*` / `image.replace` command may target. | [`CanvasStudioProps`](CanvasStudioProps.md).[`onSelectionChange`](CanvasStudioProps.md#onselectionchange) | [packages/capabilities/canvas/editor/src/CanvasStudio.tsx:219](https://github.com/ancyloce/anvilkit-studio/blob/main/src/CanvasStudio.tsx#L219) |
| &lt;a id="onstageready"&gt;&lt;/a&gt; `onStageReady?` | (`stage`) =&gt; `void` | Fires once after `<CanvasStage>` has constructed the Konva.Stage, and again with `null` when the stage tears down. Hosts use this to drive export pipelines (e.g. `stage.toDataURL()`) without reaching into the editor's internals. | [`CanvasStudioProps`](CanvasStudioProps.md).[`onStageReady`](CanvasStudioProps.md#onstageready) | [packages/capabilities/canvas/editor/src/CanvasStudio.tsx:249](https://github.com/ancyloce/anvilkit-studio/blob/main/src/CanvasStudio.tsx#L249) |
| &lt;a id="ontitlechange"&gt;&lt;/a&gt; `onTitleChange?` | (`next`) =&gt; `void` | Commit a rename; makes the header name click-to-edit when provided. | - | [packages/capabilities/canvas/editor/src/workspace/layout/CanvasWorkspace.tsx:121](https://github.com/ancyloce/anvilkit-studio/blob/main/src/workspace/layout/CanvasWorkspace.tsx#L121) |
| &lt;a id="panels"&gt;&lt;/a&gt; `panels?` | `Partial`\&lt;`Record`\&lt; \| `"text"` \| `"brand"` \| `"templates"` \| `"components"` \| `"ai"` \| `"layers"` \| `"elements"` \| `"uploads"`, [`CanvasPanelDescriptor`](../type-aliases/CanvasPanelDescriptor)\&gt;\&gt; | Tab Panel registry overrides merged over the defaults. | - | [packages/capabilities/canvas/editor/src/workspace/layout/CanvasWorkspace.tsx:134](https://github.com/ancyloce/anvilkit-studio/blob/main/src/workspace/layout/CanvasWorkspace.tsx#L134) |
| &lt;a id="persistenceadapter"&gt;&lt;/a&gt; `persistenceAdapter?` | [`CanvasPersistenceAdapter`](CanvasPersistenceAdapter.md) | FR-160 host persistence (B-08). When present, edits mark the document dirty, auto-save runs per `autoSave` (default on), a beforeunload guard warns while unsaved (FR-163), and pending changes flush on unmount. | [`CanvasStudioProps`](CanvasStudioProps.md).[`persistenceAdapter`](CanvasStudioProps.md#persistenceadapter) | [packages/capabilities/canvas/editor/src/CanvasStudio.tsx:312](https://github.com/ancyloce/anvilkit-studio/blob/main/src/CanvasStudio.tsx#L312) |
| &lt;a id="quarantinedsnapshotkeys"&gt;&lt;/a&gt; `quarantinedSnapshotKeys?` | readonly `string`[] | Snapshot keys quarantined by `loadCanvasDocumentWithDiagnostics` (plan 0021 T-045). The host owns the load call — it is what produces `initialIR` — so it is also what holds the diagnostics. Threading the keys back in is what makes a failed integrity check block EXPORT rather than only render a placeholder: without them, export preparation sees a snapshot that is present and exports content that failed verification. | [`CanvasStudioProps`](CanvasStudioProps.md).[`quarantinedSnapshotKeys`](CanvasStudioProps.md#quarantinedsnapshotkeys) | [packages/capabilities/canvas/editor/src/CanvasStudio.tsx:436](https://github.com/ancyloce/anvilkit-studio/blob/main/src/CanvasStudio.tsx#L436) |
| &lt;a id="recoveryadapter"&gt;&lt;/a&gt; `recoveryAdapter?` | [`CanvasRecoveryAdapter`](CanvasRecoveryAdapter.md) | FR-164 local recovery (C-10). When present, the editor mirrors the document into this adapter (debounced after each commit), clears it on a successful save, and offers to restore a newer snapshot on mount. `createIndexedDbRecoveryAdapter()` is the ready-made browser impl. | [`CanvasStudioProps`](CanvasStudioProps.md).[`recoveryAdapter`](CanvasStudioProps.md#recoveryadapter) | [packages/capabilities/canvas/editor/src/CanvasStudio.tsx:334](https://github.com/ancyloce/anvilkit-studio/blob/main/src/CanvasStudio.tsx#L334) |
| &lt;a id="runtime"&gt;&lt;/a&gt; `runtime?` | `CanvasRuntime` | Injected Core runtime (P0-7). When supplied, the commit/history pipeline (`commit`/`commitBatch`/`undo`/`redo`) dispatches through `runtime.apply` instead of core's built-in-only `applyCommand`, so custom commands registered on this runtime participate in undo/redo exactly like built-ins. Pair it with a matching `createCanvasRuntime(...)` on the `extensions` prop's renderer/inspector side and with the SAME runtime at decode/serialize time (`@anvilkit/canvas-editor/collab`'s `decodeCanvasIR`, core's `serializePageToSvg`) — a runtime is a single per-document config, not one-per-concern. Omit to use the default built-in-only runtime (unchanged from before this prop existed). | [`CanvasStudioProps`](CanvasStudioProps.md).[`runtime`](CanvasStudioProps.md#runtime) | [packages/capabilities/canvas/editor/src/CanvasStudio.tsx:544](https://github.com/ancyloce/anvilkit-studio/blob/main/src/CanvasStudio.tsx#L544) |
| &lt;a id="shareslot"&gt;&lt;/a&gt; `shareSlot?` | `ReactNode` | Share / Export / Publish slot, rendered in the header. | - | [packages/capabilities/canvas/editor/src/workspace/layout/CanvasWorkspace.tsx:130](https://github.com/ancyloce/anvilkit-studio/blob/main/src/workspace/layout/CanvasWorkspace.tsx#L130) |
| &lt;a id="shortcuts"&gt;&lt;/a&gt; `shortcuts?` | `boolean` \| [`CanvasShortcutOptions`](CanvasShortcutOptions.md) | Workspace shortcut registry (A-04, FR-040). Default `true` — the shell installs the built-in bindings on its root element. `false` disables every workspace shortcut; an options object extends/overrides bindings. Headless `<CanvasStudio>` embeds are unaffected either way (they never mount this registry). | - | [packages/capabilities/canvas/editor/src/workspace/layout/CanvasWorkspace.tsx:146](https://github.com/ancyloce/anvilkit-studio/blob/main/src/workspace/layout/CanvasWorkspace.tsx#L146) |
| &lt;a id="storeid"&gt;&lt;/a&gt; `storeId?` | `string` | Namespaces the persisted UI slice. Pass a per-design id to isolate state. | - | [packages/capabilities/canvas/editor/src/workspace/layout/CanvasWorkspace.tsx:105](https://github.com/ancyloce/anvilkit-studio/blob/main/src/workspace/layout/CanvasWorkspace.tsx#L105) |
| &lt;a id="templateprovider"&gt;&lt;/a&gt; `templateProvider?` | [`CanvasTemplateProvider`](CanvasTemplateProvider.md) | Provider-backed template source (C-06, FR-131) for remote/paginated catalogs. Takes precedence over `templates`; the static array keeps working without it. | [`CanvasStudioProps`](CanvasStudioProps.md).[`templateProvider`](CanvasStudioProps.md#templateprovider) | [packages/capabilities/canvas/editor/src/CanvasStudio.tsx:475](https://github.com/ancyloce/anvilkit-studio/blob/main/src/CanvasStudio.tsx#L475) |
| &lt;a id="templates"&gt;&lt;/a&gt; `templates?` | readonly [`CanvasTemplateEntry`](CanvasTemplateEntry.md)[] | Host-supplied template catalog (canvas-m0-009). Plain data consumed by the Templates dock panel; structurally compatible with `@anvilkit/canvas-templates`' catalog values. Omit to show the panel's empty state. | [`CanvasStudioProps`](CanvasStudioProps.md).[`templates`](CanvasStudioProps.md#templates) | [packages/capabilities/canvas/editor/src/CanvasStudio.tsx:469](https://github.com/ancyloce/anvilkit-studio/blob/main/src/CanvasStudio.tsx#L469) |
| &lt;a id="title"&gt;&lt;/a&gt; `title?` | `string` | Controlled document title (defaults to `ir.title`). | - | [packages/capabilities/canvas/editor/src/workspace/layout/CanvasWorkspace.tsx:119](https://github.com/ancyloce/anvilkit-studio/blob/main/src/workspace/layout/CanvasWorkspace.tsx#L119) |
| &lt;a id="toolregistry"&gt;&lt;/a&gt; `toolRegistry?` | `Partial`\&lt;`Record`\&lt;[`ToolId`](../type-aliases/ToolId), [`Tool`](../../internal/interfaces/Tool)\&gt;\&gt; | Tool registry override (mainly for tests). Defaults to the built-in registry. | [`CanvasStudioProps`](CanvasStudioProps.md).[`toolRegistry`](CanvasStudioProps.md#toolregistry) | [packages/capabilities/canvas/editor/src/CanvasStudio.tsx:251](https://github.com/ancyloce/anvilkit-studio/blob/main/src/CanvasStudio.tsx#L251) |
| &lt;a id="toolstrip"&gt;&lt;/a&gt; `toolStrip?` | `boolean` \| [`CanvasToolStripOptions`](CanvasToolStripOptions.md) | The floating tool strip (B-06, FR-010). Default `true`; `false` hides it (hosts with their own tool chrome keep the pre-M2 canvas untouched). An options object keeps the strip but composes it — mirroring `shortcuts`: `items` filters/reorders the rail (extension tool ids may be promoted into it); `renderer` replaces the rendering entirely while the workspace still positions the cluster. Extension-registered tools not in the rail surface under the strip's "More tools" overflow. | - | [packages/capabilities/canvas/editor/src/workspace/layout/CanvasWorkspace.tsx:156](https://github.com/ancyloce/anvilkit-studio/blob/main/src/workspace/layout/CanvasWorkspace.tsx#L156) |
| &lt;a id="width"&gt;&lt;/a&gt; `width?` | `number` | - | [`CanvasStudioProps`](CanvasStudioProps.md).[`width`](CanvasStudioProps.md#width) | [packages/capabilities/canvas/editor/src/CanvasStudio.tsx:184](https://github.com/ancyloce/anvilkit-studio/blob/main/src/CanvasStudio.tsx#L184) |
