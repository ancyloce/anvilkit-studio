---
title: "CanvasErrorBoundaryProps"
description: "@anvilkit/canvas-editor · CanvasErrorBoundaryProps"
---

Defined in: [packages/capabilities/canvas/editor/src/CanvasErrorBoundary.tsx:38](https://github.com/ancyloce/anvilkit-studio/blob/main/src/CanvasErrorBoundary.tsx#L38)

## Properties

| Property | Type | Description | Defined in |
| ------ | ------ | ------ | ------ |
| &lt;a id="children"&gt;&lt;/a&gt; `children` | `ReactNode` | - | [packages/capabilities/canvas/editor/src/CanvasErrorBoundary.tsx:39](https://github.com/ancyloce/anvilkit-studio/blob/main/src/CanvasErrorBoundary.tsx#L39) |
| &lt;a id="fallback"&gt;&lt;/a&gt; `fallback?` | (`error`, `reset`) =&gt; `ReactNode` | Custom fallback. Receives the thrown error and a `reset` callback that re-mounts the children. When omitted, a minimal inline message renders. | [packages/capabilities/canvas/editor/src/CanvasErrorBoundary.tsx:44](https://github.com/ancyloce/anvilkit-studio/blob/main/src/CanvasErrorBoundary.tsx#L44) |
| &lt;a id="label"&gt;&lt;/a&gt; `label?` | `string` | Heading for the default fallback (already localized by the caller). Also tags the console output. | [packages/capabilities/canvas/editor/src/CanvasErrorBoundary.tsx:64](https://github.com/ancyloce/anvilkit-studio/blob/main/src/CanvasErrorBoundary.tsx#L64) |
| &lt;a id="labels"&gt;&lt;/a&gt; `labels?` | `CanvasErrorBoundaryLabels` | Localized button labels for the default fallback. English defaults. | [packages/capabilities/canvas/editor/src/CanvasErrorBoundary.tsx:66](https://github.com/ancyloce/anvilkit-studio/blob/main/src/CanvasErrorBoundary.tsx#L66) |
| &lt;a id="onerror"&gt;&lt;/a&gt; `onError?` | (`error`, `info`) =&gt; `void` | Reported when a child throws — wire to host telemetry/logging. | [packages/capabilities/canvas/editor/src/CanvasErrorBoundary.tsx:46](https://github.com/ancyloce/anvilkit-studio/blob/main/src/CanvasErrorBoundary.tsx#L46) |
| &lt;a id="onexportrecovery"&gt;&lt;/a&gt; `onExportRecovery?` | () =&gt; `void` | FR-172 "Export recovery JSON": shown as a button in the default fallback when provided. The canvas mount wires this to a download of the current IR so no work is lost even when rendering is wedged. | [packages/capabilities/canvas/editor/src/CanvasErrorBoundary.tsx:59](https://github.com/ancyloce/anvilkit-studio/blob/main/src/CanvasErrorBoundary.tsx#L59) |
| &lt;a id="onreloaddocument"&gt;&lt;/a&gt; `onReloadDocument?` | () =&gt; `void` | FR-172 "Reload document": shown as a button in the default fallback when provided. The canvas mount wires this to `replaceDocument(getIR(), "recovery")`, which resets every editor store around the current IR. The boundary resets itself after calling it. | [packages/capabilities/canvas/editor/src/CanvasErrorBoundary.tsx:53](https://github.com/ancyloce/anvilkit-studio/blob/main/src/CanvasErrorBoundary.tsx#L53) |
| &lt;a id="rendererrordetails"&gt;&lt;/a&gt; `renderErrorDetails?` | (`info`) =&gt; `ReactNode` | FR-171 "View details": renders the full error-details dialog when the boundary's "View details" action is triggered. `CanvasErrorBoundary` is a leaf module (see this file's module doc) and cannot import dialog-class UI itself, so the actual dialog is composed by whichever ancestor CAN legally reach it (e.g. `<CanvasWorkspace>` / `<TabPanel>`, which import `ErrorDetailsDialog` and hand it down through this prop). Omit it and the "View details" button itself is hidden — there is nothing dead to click (e.g. a headless `<CanvasStudio>` mounted without `<CanvasWorkspace>`'s shell has no dialog surface to open one into). | [packages/capabilities/canvas/editor/src/CanvasErrorBoundary.tsx:83](https://github.com/ancyloce/anvilkit-studio/blob/main/src/CanvasErrorBoundary.tsx#L83) |
| &lt;a id="resetkey"&gt;&lt;/a&gt; `resetKey?` | `unknown` | Clears the error and re-mounts children whenever this value changes — e.g. pass the active page / panel id so navigating away recovers automatically. | [packages/capabilities/canvas/editor/src/CanvasErrorBoundary.tsx:71](https://github.com/ancyloce/anvilkit-studio/blob/main/src/CanvasErrorBoundary.tsx#L71) |
