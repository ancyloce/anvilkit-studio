---
title: "CanvasExportContext"
description: "@anvilkit/canvas-editor · CanvasExportContext"
---

Defined in: [packages/capabilities/canvas/editor/src/header/types.ts:30](https://github.com/ancyloce/anvilkit-studio/blob/main/src/header/types.ts#L30)

The live editor state an exporter reads from (sourced from the studio context).

## Properties

| Property | Modifier | Type | Description | Defined in |
| ------ | ------ | ------ | ------ | ------ |
| &lt;a id="activepageid"&gt;&lt;/a&gt; `activePageId` | `readonly` | `string` | Active artboard id — exporters that emit a single page use this. | [packages/capabilities/canvas/editor/src/header/types.ts:34](https://github.com/ancyloce/anvilkit-studio/blob/main/src/header/types.ts#L34) |
| &lt;a id="brandkit"&gt;&lt;/a&gt; `brandKit?` | `readonly` | [`BrandKit`](BrandKit.md) | The editor's brand kit, when the host wired one. The built-in SVG/PDF exporters resolve `BrandTokenRef` fills/fonts against it so exports agree with the live canvas; additive and optional (older host exporters ignore it). | [packages/capabilities/canvas/editor/src/header/types.ts:43](https://github.com/ancyloce/anvilkit-studio/blob/main/src/header/types.ts#L43) |
| &lt;a id="fontcatalog"&gt;&lt;/a&gt; `fontCatalog?` | `readonly` | [`CanvasFontCatalog`](CanvasFontCatalog.md) | The editor's RESOLVED font catalog (`cp2-007`) — `DEFAULT_FONT_CATALOG` extended by `<CanvasStudio fontCatalog>`. The built-in SVG exporter derives its `@font-face` manifest from it, so a family the picker offered is the same family the export can embed. Additive and optional (older host exporters ignore it); a host-built context that omits it gets exactly the pre-`cp2-007` behaviour. **Only entries carrying `source.files` can be embedded.** Every default entry is a stylesheet URL with no files, so the default catalog alone derives an EMPTY manifest — byte-identical output to passing nothing, with core's existing `FONT_NOT_IN_MANIFEST` warning per painted family. | [packages/capabilities/canvas/editor/src/header/types.ts:57](https://github.com/ancyloce/anvilkit-studio/blob/main/src/header/types.ts#L57) |
| &lt;a id="ir"&gt;&lt;/a&gt; `ir` | `readonly` | [`CanvasIR`](https://github.com/ancyloce/anvilkit-canvas-core/blob/main/src/ir/types.ts) | Current IR snapshot (`useCanvasStudio().getIR()`). | [packages/capabilities/canvas/editor/src/header/types.ts:32](https://github.com/ancyloce/anvilkit-studio/blob/main/src/header/types.ts#L32) |
| &lt;a id="stage"&gt;&lt;/a&gt; `stage` | `readonly` | `Stage` \| `null` | The live Konva stage; `null` until the canvas mounts. Raster formats need it. | [packages/capabilities/canvas/editor/src/header/types.ts:36](https://github.com/ancyloce/anvilkit-studio/blob/main/src/header/types.ts#L36) |
