---
title: "ElementsPanelProps"
description: "@anvilkit/canvas-editor · ElementsPanelProps"
---

Defined in: [packages/capabilities/canvas/editor/src/panels/ElementsPanel.tsx:267](https://github.com/ancyloce/anvilkit-studio/blob/main/src/panels/ElementsPanel.tsx#L267)

## Properties

| Property | Type | Description | Defined in |
| ------ | ------ | ------ | ------ |
| &lt;a id="classname"&gt;&lt;/a&gt; `className?` | `string` | - | [packages/capabilities/canvas/editor/src/panels/ElementsPanel.tsx:297](https://github.com/ancyloce/anvilkit-studio/blob/main/src/panels/ElementsPanel.tsx#L297) |
| &lt;a id="elementprovider"&gt;&lt;/a&gt; `elementProvider?` | [`CanvasElementProvider`](CanvasElementProvider.md) | Element catalog. Defaults to the built-in ~425-entry catalog, fetched on first query rather than at editor mount. A host supplying its own provider pays nothing for the built-in one: the default is never constructed, so its chunk is never requested. | [packages/capabilities/canvas/editor/src/panels/ElementsPanel.tsx:282](https://github.com/ancyloce/anvilkit-studio/blob/main/src/panels/ElementsPanel.tsx#L282) |
| &lt;a id="onselect"&gt;&lt;/a&gt; `onSelect?` | (`entry`) =&gt; `void` | Activation callback — click, Enter or Space on a grid cell. OVERRIDES the built-in insert (`cp3-004`) rather than observing it: a supplied handler owns the interaction, which is what an element PICKER — a host dialog that returns a choice instead of editing the document — needs. Absent (the normal case) inserts the entry at the viewport centre as one undo entry and selects it. A host that wants both its own handler AND the default behaviour calls `insertElementAtViewportCenter(ctx, entry)` itself; it is exported from the package root for exactly that. | [packages/capabilities/canvas/editor/src/panels/ElementsPanel.tsx:296](https://github.com/ancyloce/anvilkit-studio/blob/main/src/panels/ElementsPanel.tsx#L296) |
| &lt;a id="search"&gt;&lt;/a&gt; `search?` | `string` | Free-text query. Supplied by the Tab Panel's own search box, which the `elements` descriptor already enables (`searchable: true`), so the panel does not render a second one. Matched across name, tags AND keywords by the provider. | [packages/capabilities/canvas/editor/src/panels/ElementsPanel.tsx:274](https://github.com/ancyloce/anvilkit-studio/blob/main/src/panels/ElementsPanel.tsx#L274) |
