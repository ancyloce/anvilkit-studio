---
title: "InstanceComplianceLookup"
description: "@anvilkit/canvas-editor · InstanceComplianceLookup"
---

Defined in: [packages/capabilities/canvas/editor/src/brand-governance/use-instance-compliance.ts:32](https://github.com/ancyloce/anvilkit-studio/blob/main/src/brand-governance/use-instance-compliance.ts#L32)

## File

Per-node compliance lookup (plan 0021 T-043).

## The bug this replaces

`brand-warnings.tsx` memoized a **whole-document** compliance scan on
`[ctx.ir, definition, nodes, t]` and then kept only the selected nodes'
issues. `nodes` changes on every selection change, so clicking around a
1,000-node document re-scanned the entire document on every click and threw
almost all of the result away.

The fix is not a cache behind that call — the caller was asking a different
question each time. It is to memoize on **`[ir, brandKit]` only** and expose a
per-node lookup, so a selection change is a `Map.get` rather than a scan.

`t` is deliberately NOT a dependency: it only affects how an issue is
*rendered*, never which issues exist, and including it made a locale change
re-scan the document.

## Properties

| Property | Modifier | Type | Description | Defined in |
| ------ | ------ | ------ | ------ | ------ |
| &lt;a id="bycomponent"&gt;&lt;/a&gt; `byComponent` | `readonly` | `ReadonlyMap`\&lt;`string`, `"warning"` \| `"blocking"`\&gt; | Worst severity per component definition id (T-044 step 5), for the Components panel's per-row status. Absent means clean. Computed in the SAME pass as the per-node index rather than by a second walk: the Components panel would otherwise run its own whole-document scan, which is the exact shape of the bug T-043 removed from the Inspector. | [packages/capabilities/canvas/editor/src/brand-governance/use-instance-compliance.ts:46](https://github.com/ancyloce/anvilkit-studio/blob/main/src/brand-governance/use-instance-compliance.ts#L46) |
| &lt;a id="scancount"&gt;&lt;/a&gt; `scanCount` | `readonly` | `number` | How many document scans this lookup has performed. Test/diagnostic only. | [packages/capabilities/canvas/editor/src/brand-governance/use-instance-compliance.ts:48](https://github.com/ancyloce/anvilkit-studio/blob/main/src/brand-governance/use-instance-compliance.ts#L48) |

## Methods

### forNode()

```ts
forNode(nodeId): readonly BrandComplianceIssue[];
```

Defined in: [packages/capabilities/canvas/editor/src/brand-governance/use-instance-compliance.ts:34](https://github.com/ancyloce/anvilkit-studio/blob/main/src/brand-governance/use-instance-compliance.ts#L34)

Issues for one node id. Empty array when clean.

#### Parameters

| Parameter | Type |
| ------ | ------ |
| `nodeId` | `string` |

#### Returns

readonly `BrandComplianceIssue`[]

***

### forNodes()

```ts
forNodes(nodeIds): readonly BrandComplianceIssue[];
```

Defined in: [packages/capabilities/canvas/editor/src/brand-governance/use-instance-compliance.ts:36](https://github.com/ancyloce/anvilkit-studio/blob/main/src/brand-governance/use-instance-compliance.ts#L36)

Issues for a selection, deduplicated by node.

#### Parameters

| Parameter | Type |
| ------ | ------ |
| `nodeIds` | readonly `string`[] |

#### Returns

readonly `BrandComplianceIssue`[]
