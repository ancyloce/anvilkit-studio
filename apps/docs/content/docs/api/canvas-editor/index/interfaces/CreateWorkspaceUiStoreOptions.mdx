---
title: "CreateWorkspaceUiStoreOptions"
description: "@anvilkit/canvas-editor · CreateWorkspaceUiStoreOptions"
---

Defined in: [packages/capabilities/canvas/editor/src/workspace/state/workspace-ui-store.ts:206](https://github.com/ancyloce/anvilkit-studio/blob/main/src/workspace/state/workspace-ui-store.ts#L206)

## Properties

| Property | Modifier | Type | Description | Defined in |
| ------ | ------ | ------ | ------ | ------ |
| &lt;a id="initialworkspacestate"&gt;&lt;/a&gt; `initialWorkspaceState?` | `readonly` | `Partial`\&lt;[`CanvasWorkspaceState`](../type-aliases/CanvasWorkspaceState)\&gt; | PRD §11.1 seed for a host embedding a FRESH workspace (`storeId` with no prior `localStorage` value). Precedence: this seed is the store's baseline; persist's own rehydrate `merge` (below) always lets an EXISTING persisted value win over it for the 5 persisted fields (`activeDockId`/`inspectorCollapsed`/ `panelWidth`/`recentTemplateIds`/`recentFontFamilies`) — so a returning user's saved layout is never clobbered by a host's seed. The 2 transient fields (`panelOpen`/`panelSearch`) are never persisted, so this seed (or the hardcoded default) always applies for them. This is an ADDITIONAL seam: `restoreLayout()` (FR-002 "restore default layout") still resets to the hardcoded INITIAL\_STATE, never to this seed. | [packages/capabilities/canvas/editor/src/workspace/state/workspace-ui-store.ts:223](https://github.com/ancyloce/anvilkit-studio/blob/main/src/workspace/state/workspace-ui-store.ts#L223) |
| &lt;a id="storeid"&gt;&lt;/a&gt; `storeId` | `readonly` | `string` | - | [packages/capabilities/canvas/editor/src/workspace/state/workspace-ui-store.ts:207](https://github.com/ancyloce/anvilkit-studio/blob/main/src/workspace/state/workspace-ui-store.ts#L207) |
