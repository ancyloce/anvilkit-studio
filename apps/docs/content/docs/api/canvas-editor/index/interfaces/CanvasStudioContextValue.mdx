---
title: "CanvasStudioContextValue"
description: "@anvilkit/canvas-editor · CanvasStudioContextValue"
---

Defined in: [packages/capabilities/canvas/editor/src/context/canvas-studio-context.tsx:106](https://github.com/ancyloce/anvilkit-studio/blob/main/src/context/canvas-studio-context.tsx#L106)

## Properties

| Property | Type | Description | Defined in |
| ------ | ------ | ------ | ------ |
| &lt;a id="activepageid"&gt;&lt;/a&gt; `activePageId` | `string` | Live active page id — derived from `pagesStore` via `useSyncExternalStore` in `<CanvasStudio>`. Equivalent to `pagesStore.getState().activePageId` but reactive: consumers using `useCanvasStudio()` re-render when it changes. | [packages/capabilities/canvas/editor/src/context/canvas-studio-context.tsx:468](https://github.com/ancyloce/anvilkit-studio/blob/main/src/context/canvas-studio-context.tsx#L468) |
| &lt;a id="aijobstore"&gt;&lt;/a&gt; `aiJobStore` | [`AiJobStoreApi`](../../internal/type-aliases/AiJobStoreApi) | Transient registry of in-flight AI jobs backing `ai-placeholder` nodes (I1-10). The host registers an abort handle when it starts a job; the placeholder's on-canvas Cancel button calls `aiJobStore.cancel(jobId)`. Always provided by `<CanvasStudio>` (W2 — required). | [packages/capabilities/canvas/editor/src/context/canvas-studio-context.tsx:132](https://github.com/ancyloce/anvilkit-studio/blob/main/src/context/canvas-studio-context.tsx#L132) |
| &lt;a id="assetpicker"&gt;&lt;/a&gt; `assetPicker?` | [`CanvasAssetPicker`](CanvasAssetPicker.md) | FR-090/091 asset adapters (B-10). Present when the host wires them; `pickAsset` keeps its legacy single-uri contract for existing tools. | [packages/capabilities/canvas/editor/src/context/canvas-studio-context.tsx:403](https://github.com/ancyloce/anvilkit-studio/blob/main/src/context/canvas-studio-context.tsx#L403) |
| &lt;a id="assetuploader"&gt;&lt;/a&gt; `assetUploader?` | [`CanvasAssetUploader`](CanvasAssetUploader.md) | - | [packages/capabilities/canvas/editor/src/context/canvas-studio-context.tsx:404](https://github.com/ancyloce/anvilkit-studio/blob/main/src/context/canvas-studio-context.tsx#L404) |
| &lt;a id="autolayoutcreationenabled"&gt;&lt;/a&gt; `autoLayoutCreationEnabled?` | `boolean` | T-M4-10: true when the host opted into the Auto Layout creation/conversion UI (`<CanvasStudio autoLayout>`), default false. Gates ONLY creation/conversion affordances — never read/render/export. | [packages/capabilities/canvas/editor/src/context/canvas-studio-context.tsx:334](https://github.com/ancyloce/anvilkit-studio/blob/main/src/context/canvas-studio-context.tsx#L334) |
| &lt;a id="brandgovernance"&gt;&lt;/a&gt; `brandGovernance?` | `CanvasBrandPolicyContext` | Host brand-policy context (plan 0021 T-040) — the capability snapshot, the enforcement mode and the opaque policy revision. Absent means "governance not wired", which resolves to the permissive context: every affordance stays available, exactly as before M4. Read it through `useEffectivePolicyContext()` rather than directly, so that default lives in one place. This is presentation input only. Enforcement happens in the command layer via `CommandApplyOptions.brandPolicy`, which re-evaluates the same policy on every mutation path including undo/redo, batch and the public command API. | [packages/capabilities/canvas/editor/src/context/canvas-studio-context.tsx:262](https://github.com/ancyloce/anvilkit-studio/blob/main/src/context/canvas-studio-context.tsx#L262) |
| &lt;a id="brandkit"&gt;&lt;/a&gt; `brandKit?` | [`BrandKit`](BrandKit.md) | Shared brand colors + fonts sourced from the host's Studio config (I3-4). Optional — absent when the host configures no brand kit. Prefer reading it via [useBrandKit](../functions/useBrandKit), which normalizes the absent case to an empty kit. | [packages/capabilities/canvas/editor/src/context/canvas-studio-context.tsx:232](https://github.com/ancyloce/anvilkit-studio/blob/main/src/context/canvas-studio-context.tsx#L232) |
| &lt;a id="canleave"&gt;&lt;/a&gt; `canLeave?` | () =&gt; `boolean` | - | [packages/capabilities/canvas/editor/src/context/canvas-studio-context.tsx:390](https://github.com/ancyloce/anvilkit-studio/blob/main/src/context/canvas-studio-context.tsx#L390) |
| &lt;a id="clipboard"&gt;&lt;/a&gt; `clipboard?` | [`CanvasClipboardAdapter`](CanvasClipboardAdapter.md) | §11.1 clipboard adapter override. Present when the host wires it; `clipboard-actions.ts` prefers it over `system-clipboard.ts`'s `navigator.clipboard` wrapper, falling back to the latter when absent — clipboard behavior otherwise stays identical either way. | [packages/capabilities/canvas/editor/src/context/canvas-studio-context.tsx:413](https://github.com/ancyloce/anvilkit-studio/blob/main/src/context/canvas-studio-context.tsx#L413) |
| &lt;a id="commit"&gt;&lt;/a&gt; `commit` | (`cmd`) =&gt; [`CanvasIR`](https://github.com/ancyloce/anvilkit-canvas-core/blob/main/src/ir/types.ts) | - | [packages/capabilities/canvas/editor/src/context/canvas-studio-context.tsx:149](https://github.com/ancyloce/anvilkit-studio/blob/main/src/context/canvas-studio-context.tsx#L149) |
| &lt;a id="commitbatch"&gt;&lt;/a&gt; `commitBatch` | (`commands`, `label?`) =&gt; [`CanvasIR`](https://github.com/ancyloce/anvilkit-canvas-core/blob/main/src/ir/types.ts) | Apply many commands as one undoable transaction — a single undo step. Mirrors [commit](#commit) for multi-command gestures (multi-select move, transform commit, ungroup). Fires `onChange`/`onChanges` once for the batch. | [packages/capabilities/canvas/editor/src/context/canvas-studio-context.tsx:155](https://github.com/ancyloce/anvilkit-studio/blob/main/src/context/canvas-studio-context.tsx#L155) |
| &lt;a id="commitcoalesced"&gt;&lt;/a&gt; `commitCoalesced?` | (`cmd`, `mergeKey`) =&gt; [`CanvasIR`](https://github.com/ancyloce/anvilkit-canvas-core/blob/main/src/ir/types.ts) | Coalescing commit — the §10 field-input contract's commit half (B-12, first production consumer of the history store's coalesced-commit API). Successive calls sharing `mergeKey` within the history store's merge window fold into ONE undo entry (e.g. rapid re-commits of the same inspector field). Optional so hand-built partial test contexts keep working — consumers fall back to [commit](#commit). | [packages/capabilities/canvas/editor/src/context/canvas-studio-context.tsx:167](https://github.com/ancyloce/anvilkit-studio/blob/main/src/context/canvas-studio-context.tsx#L167) |
| &lt;a id="componentprovider"&gt;&lt;/a&gt; `componentProvider?` | [`CanvasComponentProvider`](CanvasComponentProvider.md) | Plan 0021 T-019: the host-injected component catalog, or undefined when no Provider was supplied. Absent simply means the Libraries source is not offered — never that an existing external component stops rendering. | [packages/capabilities/canvas/editor/src/context/canvas-studio-context.tsx:349](https://github.com/ancyloce/anvilkit-studio/blob/main/src/context/canvas-studio-context.tsx#L349) |
| &lt;a id="componentscopestore"&gt;&lt;/a&gt; `componentScopeStore?` | [`ComponentScopeStoreApi`](../../internal/type-aliases/ComponentScopeStoreApi) | Component Source editing scope STACK (plan 0023 M4-05, LC-CREATE-002). UI state only — a scope never enters the IR, and entering or leaving one is never a document command. Distinct from [isolationStore](#isolationstore), which validates its path against the PAGE tree and would discard a component scope. Always provided by `<CanvasStudio>`; optional for partial test contexts, where Source editing simply stays off. | [packages/capabilities/canvas/editor/src/context/canvas-studio-context.tsx:434](https://github.com/ancyloce/anvilkit-studio/blob/main/src/context/canvas-studio-context.tsx#L434) |
| &lt;a id="componentvariantsenabled"&gt;&lt;/a&gt; `componentVariantsEnabled?` | `boolean` | Plan 0021 variant-authoring flag (T-053). Gates authoring affordances only — variant RESOLUTION is unconditional, so a rollback never changes what an existing document renders. | [packages/capabilities/canvas/editor/src/context/canvas-studio-context.tsx:365](https://github.com/ancyloce/anvilkit-studio/blob/main/src/context/canvas-studio-context.tsx#L365) |
| &lt;a id="continuouscreation"&gt;&lt;/a&gt; `continuousCreation?` | `boolean` | FR-012 (A-10): when true, creation tools stay active after committing an element (the pre-PRD-0012 behavior). Default false — tools return to Select on completion. | [packages/capabilities/canvas/editor/src/context/canvas-studio-context.tsx:328](https://github.com/ancyloce/anvilkit-studio/blob/main/src/context/canvas-studio-context.tsx#L328) |
| &lt;a id="cropstore"&gt;&lt;/a&gt; `cropStore` | [`CropStoreApi`](../../internal/type-aliases/CropStoreApi) | Drives the interactive image-crop editor (I3-2). Always provided by `<CanvasStudio>` (W2 — required). | [packages/capabilities/canvas/editor/src/context/canvas-studio-context.tsx:137](https://github.com/ancyloce/anvilkit-studio/blob/main/src/context/canvas-studio-context.tsx#L137) |
| &lt;a id="documentreadonly"&gt;&lt;/a&gt; `documentReadOnly?` | `boolean` | AC-010 (T-M5-03): true when the document declares a capability this build does not implement. The commit pipeline blocks mutating commands; render and export stay available (read-only materialized preview). | [packages/capabilities/canvas/editor/src/context/canvas-studio-context.tsx:382](https://github.com/ancyloce/anvilkit-studio/blob/main/src/context/canvas-studio-context.tsx#L382) |
| &lt;a id="draftstore"&gt;&lt;/a&gt; `draftStore` | [`DraftStoreApi`](../../internal/type-aliases/DraftStoreApi) | - | [packages/capabilities/canvas/editor/src/context/canvas-studio-context.tsx:114](https://github.com/ancyloce/anvilkit-studio/blob/main/src/context/canvas-studio-context.tsx#L114) |
| &lt;a id="editingstore"&gt;&lt;/a&gt; `editingStore` | [`EditingStoreApi`](../../internal/type-aliases/EditingStoreApi) | - | [packages/capabilities/canvas/editor/src/context/canvas-studio-context.tsx:115](https://github.com/ancyloce/anvilkit-studio/blob/main/src/context/canvas-studio-context.tsx#L115) |
| &lt;a id="exportrequeststore"&gt;&lt;/a&gt; `exportRequestStore?` | `ExportRequestStoreApi` | Export invocation channel (FR-031/FR-032): entry points such as the node context menu's "Export selection" and the page menu's "Export page" post a scoped request here; the export UI mounted by `createCanvasExportPlugin` consumes it and opens preselected. Always provided by `<CanvasStudio>`; optional for partial test contexts. | [packages/capabilities/canvas/editor/src/context/canvas-studio-context.tsx:442](https://github.com/ancyloce/anvilkit-studio/blob/main/src/context/canvas-studio-context.tsx#L442) |
| &lt;a id="externalcomponentsenabled"&gt;&lt;/a&gt; `externalComponentsEnabled?` | `boolean` | Plan 0021: true when the host opted into EXTERNAL component libraries (`<CanvasStudio externalComponents>`), default false. Gates only authoring affordances — the Libraries source, insert, and recovery. Never read/render/export, for the same reason [localComponentsEnabled](#localcomponentsenabled) does not: turning the flag back off must be a rollback, not data loss. | [packages/capabilities/canvas/editor/src/context/canvas-studio-context.tsx:359](https://github.com/ancyloce/anvilkit-studio/blob/main/src/context/canvas-studio-context.tsx#L359) |
| &lt;a id="fieldpreviewstore"&gt;&lt;/a&gt; `fieldPreviewStore?` | `FieldPreviewStoreApi` | §10 field-input contract, preview half (B-12): transient per-node patches a mid-edit field renders through `CanvasNodeRenderer` without touching history. Optional for partial test contexts; always provided by `<CanvasStudio>`. | [packages/capabilities/canvas/editor/src/context/canvas-studio-context.tsx:174](https://github.com/ancyloce/anvilkit-studio/blob/main/src/context/canvas-studio-context.tsx#L174) |
| &lt;a id="flush"&gt;&lt;/a&gt; `flush?` | () =&gt; `Promise`\&lt;`boolean`\&gt; | FR-160/163: save-if-dirty for host routing guards. Unlike `save()`, the save this starts is protected from teardown — `flush()` followed by unmount cannot abort it — so hosts can `await flush()` in a route-leave hook. Resolves `true` when nothing needed saving. Without a `persistenceAdapter` it resolves `true`. | [packages/capabilities/canvas/editor/src/context/canvas-studio-context.tsx:398](https://github.com/ancyloce/anvilkit-studio/blob/main/src/context/canvas-studio-context.tsx#L398) |
| &lt;a id="focusstore"&gt;&lt;/a&gt; `focusStore` | `CanvasFocusStoreApi` | Roving keyboard focus (a11y), distinct from selection. | [packages/capabilities/canvas/editor/src/context/canvas-studio-context.tsx:111](https://github.com/ancyloce/anvilkit-studio/blob/main/src/context/canvas-studio-context.tsx#L111) |
| &lt;a id="fontcatalog"&gt;&lt;/a&gt; `fontCatalog?` | [`CanvasFontCatalog`](CanvasFontCatalog.md) | The RESOLVED font catalog (`cp2-007`): `DEFAULT_FONT_CATALOG` extended by the host's `<CanvasStudio fontCatalog>`, merged ONCE by the studio. Note the asymmetry with the prop of the same name: the PROP carries only the host's own entries, this field carries the merge. Both the font picker and the SVG export manifest read this one value, which is what keeps "the picker offered it" and "the export embedded it" from diverging. Optional because a partial test context may omit it. Prefer [useCanvasFontCatalog](../functions/useCanvasFontCatalog), which normalizes the absent case to `DEFAULT_FONT_CATALOG` — exactly as [useBrandKit](../functions/useBrandKit) normalizes an absent kit. | [packages/capabilities/canvas/editor/src/context/canvas-studio-context.tsx:247](https://github.com/ancyloce/anvilkit-studio/blob/main/src/context/canvas-studio-context.tsx#L247) |
| &lt;a id="getir"&gt;&lt;/a&gt; `getIR` | [`CanvasIRGetter`](../type-aliases/CanvasIRGetter) | - | [packages/capabilities/canvas/editor/src/context/canvas-studio-context.tsx:148](https://github.com/ancyloce/anvilkit-studio/blob/main/src/context/canvas-studio-context.tsx#L148) |
| &lt;a id="guidesstore"&gt;&lt;/a&gt; `guidesStore` | [`GuidesStoreApi`](../../internal/type-aliases/GuidesStoreApi) | - | [packages/capabilities/canvas/editor/src/context/canvas-studio-context.tsx:113](https://github.com/ancyloce/anvilkit-studio/blob/main/src/context/canvas-studio-context.tsx#L113) |
| &lt;a id="hasimagepicker"&gt;&lt;/a&gt; `hasImagePicker?` | `boolean` | FR-011: whether the Image tool can pick an asset — `assetPicker` OR the legacy `onPickAsset` callback is wired. Drives the Tool Strip's disabled state; `true` when unset (lightweight test contexts default to usable). | [packages/capabilities/canvas/editor/src/context/canvas-studio-context.tsx:220](https://github.com/ancyloce/anvilkit-studio/blob/main/src/context/canvas-studio-context.tsx#L220) |
| &lt;a id="historystore"&gt;&lt;/a&gt; `historyStore` | [`HistoryStoreApi`](../../internal/type-aliases/HistoryStoreApi) | - | [packages/capabilities/canvas/editor/src/context/canvas-studio-context.tsx:107](https://github.com/ancyloce/anvilkit-studio/blob/main/src/context/canvas-studio-context.tsx#L107) |
| &lt;a id="ir"&gt;&lt;/a&gt; `ir` | [`CanvasIR`](https://github.com/ancyloce/anvilkit-canvas-core/blob/main/src/ir/types.ts) | Current IR. Reactive — context value changes on every commit. | [packages/capabilities/canvas/editor/src/context/canvas-studio-context.tsx:470](https://github.com/ancyloce/anvilkit-studio/blob/main/src/context/canvas-studio-context.tsx#L470) |
| &lt;a id="isolationstore"&gt;&lt;/a&gt; `isolationStore?` | `IsolationStoreApi` | Container isolation stack (C-09, FR-055). UI state only — never enters IR. Always provided by `<CanvasStudio>`; optional for partial test contexts, where isolation features simply stay off. | [packages/capabilities/canvas/editor/src/context/canvas-studio-context.tsx:425](https://github.com/ancyloce/anvilkit-studio/blob/main/src/context/canvas-studio-context.tsx#L425) |
| &lt;a id="kindinspectors"&gt;&lt;/a&gt; `kindInspectors?` | `Readonly`\&lt;`Record`\&lt;`string`, [`CanvasKindInspector`](CanvasKindInspector.md)\&gt;\&gt; | Inspector field renderers for custom node kinds, keyed by kind. | [packages/capabilities/canvas/editor/src/context/canvas-studio-context.tsx:313](https://github.com/ancyloce/anvilkit-studio/blob/main/src/context/canvas-studio-context.tsx#L313) |
| &lt;a id="kindrenderers"&gt;&lt;/a&gt; `kindRenderers?` | `Readonly`\&lt;`Record`\&lt;`string`, [`CanvasKindRenderer`](CanvasKindRenderer.md)\&gt;\&gt; | Renderers for custom (extension) node kinds, keyed by kind. Consulted by `<CanvasNodeRenderer>` for any node whose `type` is not a built-in kind. | [packages/capabilities/canvas/editor/src/context/canvas-studio-context.tsx:311](https://github.com/ancyloce/anvilkit-studio/blob/main/src/context/canvas-studio-context.tsx#L311) |
| &lt;a id="layerrenamestore"&gt;&lt;/a&gt; `layerRenameStore?` | `LayerRenameStoreApi` | Layer-rename channel (FR-031): the node context menu's "Rename layer" posts a node id here for the mounted `<LayerPanel>` to enter inline rename. UI state only; optional for partial test contexts. | [packages/capabilities/canvas/editor/src/context/canvas-studio-context.tsx:448](https://github.com/ancyloce/anvilkit-studio/blob/main/src/context/canvas-studio-context.tsx#L448) |
| &lt;a id="localcomponentsenabled"&gt;&lt;/a&gt; `localComponentsEnabled?` | `boolean` | Plan 0023 M6-07: true when the host opted into the Local Components AUTHORING UI (`<CanvasStudio localComponents>`), default false. Gates ONLY creation and Source-editing affordances. Never read/render/ export, never override editing, never detach — rollback must leave every existing instance readable, editable and detachable (PRD §19). | [packages/capabilities/canvas/editor/src/context/canvas-studio-context.tsx:343](https://github.com/ancyloce/anvilkit-studio/blob/main/src/context/canvas-studio-context.tsx#L343) |
| &lt;a id="onanalyticsevent"&gt;&lt;/a&gt; `onAnalyticsEvent?` | [`CanvasAnalyticsSink`](../type-aliases/CanvasAnalyticsSink) | Product analytics sink (plan 0021 T-050). Absent means "do not emit". | [packages/capabilities/canvas/editor/src/context/canvas-studio-context.tsx:269](https://github.com/ancyloce/anvilkit-studio/blob/main/src/context/canvas-studio-context.tsx#L269) |
| &lt;a id="oncomponentevent"&gt;&lt;/a&gt; `onComponentEvent?` | [`CanvasComponentEventHandler`](../../internal/type-aliases/CanvasComponentEventHandler) | Plan 0023 M6-08: host observer for the eight PRD §12 COMPONENT events. Same convention as [onLayoutEvent](#onlayoutevent) and `onChange` — the editor emits, the host delivers. No analytics transport ships in this package, and the payload types carry counts, enums and HASHED ids only, so no text, image URI, document body or raw override value can be emitted. | [packages/capabilities/canvas/editor/src/context/canvas-studio-context.tsx:376](https://github.com/ancyloce/anvilkit-studio/blob/main/src/context/canvas-studio-context.tsx#L376) |
| &lt;a id="oncreatedocument"&gt;&lt;/a&gt; `onCreateDocument?` | (`document`) =&gt; `void` | FR-132 host "open as new document" seam. Present when the host wired `<CanvasStudio onCreateDocument>`; the Templates panel uses it to offer the "Open as new document" choice. | [packages/capabilities/canvas/editor/src/context/canvas-studio-context.tsx:296](https://github.com/ancyloce/anvilkit-studio/blob/main/src/context/canvas-studio-context.tsx#L296) |
| &lt;a id="onexport"&gt;&lt;/a&gt; `onExport?` | (`result`) =&gt; `void` | PRD §11.1 host export observer: fires with the completed [CanvasExportResult](CanvasExportResult.md) after EVERY successful export — both the headless `useCanvasStudioActions().export()` action (`header/export-action.ts`) and the built-in `<ExportDialog>`'s user-driven export call it, so a host sees every export regardless of which entry point triggered it. Present when the host wired `<CanvasStudio onExport>`. | [packages/capabilities/canvas/editor/src/context/canvas-studio-context.tsx:306](https://github.com/ancyloce/anvilkit-studio/blob/main/src/context/canvas-studio-context.tsx#L306) |
| &lt;a id="ongovernanceauditevent"&gt;&lt;/a&gt; `onGovernanceAuditEvent?` | `CanvasGovernanceAuditSink` | Governance audit sink (TD §24.2). Absent means "do not emit". | [packages/capabilities/canvas/editor/src/context/canvas-studio-context.tsx:271](https://github.com/ancyloce/anvilkit-studio/blob/main/src/context/canvas-studio-context.tsx#L271) |
| &lt;a id="ongovernancedeeplink"&gt;&lt;/a&gt; `onGovernanceDeepLink?` | (`code`) =&gt; `void` | Optional deep link for a blocked operation (T-040 step 3). Receives the STABLE deny code — never localized copy, never the decision's log-only `detail`. There is deliberately no approval inbox: a host that knows who grants exceptions owns that flow. | [packages/capabilities/canvas/editor/src/context/canvas-studio-context.tsx:278](https://github.com/ancyloce/anvilkit-studio/blob/main/src/context/canvas-studio-context.tsx#L278) |
| &lt;a id="onlayoutevent"&gt;&lt;/a&gt; `onLayoutEvent?` | [`CanvasLayoutEventHandler`](../type-aliases/CanvasLayoutEventHandler) | T-M4-11: host observer for the six PRD §12 layout events. | [packages/capabilities/canvas/editor/src/context/canvas-studio-context.tsx:367](https://github.com/ancyloce/anvilkit-studio/blob/main/src/context/canvas-studio-context.tsx#L367) |
| &lt;a id="pagesstore"&gt;&lt;/a&gt; `pagesStore` | [`PagesStoreApi`](../../internal/type-aliases/PagesStoreApi) | - | [packages/capabilities/canvas/editor/src/context/canvas-studio-context.tsx:116](https://github.com/ancyloce/anvilkit-studio/blob/main/src/context/canvas-studio-context.tsx#L116) |
| &lt;a id="patheditstore"&gt;&lt;/a&gt; `pathEditStore` | [`PathEditStoreApi`](../../internal/type-aliases/PathEditStoreApi) | On-stage path point-editing mode (I3-2). Always provided by `<CanvasStudio>` (W2 — required). The `PathEditOverlay` reads it. | [packages/capabilities/canvas/editor/src/context/canvas-studio-context.tsx:147](https://github.com/ancyloce/anvilkit-studio/blob/main/src/context/canvas-studio-context.tsx#L147) |
| &lt;a id="penstore"&gt;&lt;/a&gt; `penStore` | [`PenStoreApi`](../../internal/type-aliases/PenStoreApi) | Multi-click pen-path state (I3-2). Always provided by `<CanvasStudio>` (W2 — required). The `path` tool and `PenToolOverlay` read it. | [packages/capabilities/canvas/editor/src/context/canvas-studio-context.tsx:142](https://github.com/ancyloce/anvilkit-studio/blob/main/src/context/canvas-studio-context.tsx#L142) |
| &lt;a id="pickasset"&gt;&lt;/a&gt; `pickAsset` | () =&gt; `Promise`\&lt;`string`\&gt; | - | [packages/capabilities/canvas/editor/src/context/canvas-studio-context.tsx:206](https://github.com/ancyloce/anvilkit-studio/blob/main/src/context/canvas-studio-context.tsx#L206) |
| &lt;a id="pickassets"&gt;&lt;/a&gt; `pickAssets?` | () =&gt; `Promise`\&lt;readonly [`CanvasPickedAsset`](CanvasPickedAsset.md)[]\&gt; | FR-090 (B-10) multi-select pick: present only when a full `assetPicker` adapter is wired (the legacy single-uri `onPickAsset` callback has no multi-select concept). Optional — lightweight tool-test contexts, and hosts without an `assetPicker`, omit it; callers fall back to [pickAsset](#pickasset). | [packages/capabilities/canvas/editor/src/context/canvas-studio-context.tsx:214](https://github.com/ancyloce/anvilkit-studio/blob/main/src/context/canvas-studio-context.tsx#L214) |
| &lt;a id="quarantinedsnapshotkeys"&gt;&lt;/a&gt; `quarantinedSnapshotKeys?` | readonly `string`[] | Snapshot keys quarantined at load (plan 0021 T-045). Read by the export preflight so a failed integrity check blocks export, not only rendering. | [packages/capabilities/canvas/editor/src/context/canvas-studio-context.tsx:267](https://github.com/ancyloce/anvilkit-studio/blob/main/src/context/canvas-studio-context.tsx#L267) |
| &lt;a id="redo"&gt;&lt;/a&gt; `redo?` | () =&gt; [`CanvasIR`](https://github.com/ancyloce/anvilkit-canvas-core/blob/main/src/ir/types.ts) | **See** undo | [packages/capabilities/canvas/editor/src/context/canvas-studio-context.tsx:205](https://github.com/ancyloce/anvilkit-studio/blob/main/src/context/canvas-studio-context.tsx#L205) |
| &lt;a id="replacedocument"&gt;&lt;/a&gt; `replaceDocument` | (`ir`, `source`) =&gt; `void` | Replace the WHOLE document with an unrelated `CanvasIR` snapshot (P0-9) — not a normal edit, so it does not go through [commit](#commit). Resets undo/ redo history, clears selection/focus/draft/editing/crop/pen/path-edit/ guides, aborts stale AI jobs, swaps the IR, and reconciles the active page. Use this — never `sceneStore.getState().setIR(ir)` directly — for a host-driven document switch, loading a template as a new document, recovery, or wiring a `./collab` binding's `stores` option. | [packages/capabilities/canvas/editor/src/context/canvas-studio-context.tsx:195](https://github.com/ancyloce/anvilkit-studio/blob/main/src/context/canvas-studio-context.tsx#L195) |
| &lt;a id="requestaiintent"&gt;&lt;/a&gt; `requestAiIntent?` | (`intent`) =&gt; `void` | Hand an AI gesture to the host (I1-7). Optional — present only when the editor is mounted with an AI host. See [AiToolIntent](../../internal/type-aliases/AiToolIntent). | [packages/capabilities/canvas/editor/src/context/canvas-studio-context.tsx:225](https://github.com/ancyloce/anvilkit-studio/blob/main/src/context/canvas-studio-context.tsx#L225) |
| &lt;a id="resolveddocumentstore"&gt;&lt;/a&gt; `resolvedDocumentStore?` | `ResolvedDocumentStoreApi` | T-M3-05: the ONE resolved layout document per render context, derived from `sceneStore` + `fieldPreviewStore` (+ the font manifest). Every geometry consumer — renderer, hit-test, snap, selection, a11y tree, thumbnails, export — reads THIS store rather than computing geometry from raw IR, which is what guarantees they all see one resolved tree. Optional for partial test contexts; always provided by `<CanvasStudio>`. Consumers should memoise on record identity — untouched records are reference-identical across resolutions (TD §5.4). | [packages/capabilities/canvas/editor/src/context/canvas-studio-context.tsx:185](https://github.com/ancyloce/anvilkit-studio/blob/main/src/context/canvas-studio-context.tsx#L185) |
| &lt;a id="rulerguidestore"&gt;&lt;/a&gt; `rulerGuideStore?` | [`RulerGuideStoreApi`](../../internal/type-aliases/RulerGuideStoreApi) | Ruler/guide chrome state (C-02, FR-110/111/113). Always provided by `<CanvasStudio>`; optional in the type only for partial test contexts — ruler/guide surfaces render nothing without it. | [packages/capabilities/canvas/editor/src/context/canvas-studio-context.tsx:419](https://github.com/ancyloce/anvilkit-studio/blob/main/src/context/canvas-studio-context.tsx#L419) |
| &lt;a id="runtime"&gt;&lt;/a&gt; `runtime?` | `CanvasRuntime` | The Core runtime this instance was created with (P0-7), when one was supplied via `<CanvasStudio runtime>`. `commit`/`commitBatch`/undo/redo already dispatch through it internally; exposed here so host code (a custom tool, an inspector) can consult `runtime.nodeKinds`/`.commands` without threading it through separately. Absent when the default built-in-only runtime is in use. | [packages/capabilities/canvas/editor/src/context/canvas-studio-context.tsx:322](https://github.com/ancyloce/anvilkit-studio/blob/main/src/context/canvas-studio-context.tsx#L322) |
| &lt;a id="save"&gt;&lt;/a&gt; `save?` | () =&gt; `Promise`\&lt;`boolean`\&gt; | - | [packages/capabilities/canvas/editor/src/context/canvas-studio-context.tsx:389](https://github.com/ancyloce/anvilkit-studio/blob/main/src/context/canvas-studio-context.tsx#L389) |
| &lt;a id="savestatusstore"&gt;&lt;/a&gt; `saveStatusStore?` | [`SaveStatusStoreApi`](../type-aliases/SaveStatusStoreApi) | Save lifecycle (B-08, FR-160/161). Always provided by `<CanvasStudio>`; optional in the type only for partial test contexts. Without a `persistenceAdapter`, `save()` resolves false and `canLeave()` is true. | [packages/capabilities/canvas/editor/src/context/canvas-studio-context.tsx:388](https://github.com/ancyloce/anvilkit-studio/blob/main/src/context/canvas-studio-context.tsx#L388) |
| &lt;a id="scenestore"&gt;&lt;/a&gt; `sceneStore` | [`SceneStoreApi`](#) | Owns the live [CanvasIR](https://github.com/ancyloce/anvilkit-canvas-core/blob/main/src/ir/types.ts) scene. Always provided by `<CanvasStudio>` (W2 — required, not optional, so app code gets type safety without defensive guards). The Yjs collab prototype (I3-1) binds this store to a `Y.Doc`. Prefer [getIR](#getir)/[commit](#commit)/[ir](#ir) for normal reads and mutations — `sceneStore` is the collab seam. Tests that need a partial context should fill the stores (e.g. via `createSceneStore`). | [packages/capabilities/canvas/editor/src/context/canvas-studio-context.tsx:125](https://github.com/ancyloce/anvilkit-studio/blob/main/src/context/canvas-studio-context.tsx#L125) |
| &lt;a id="selectionstore"&gt;&lt;/a&gt; `selectionStore` | [`SelectionStoreApi`](../../internal/type-aliases/SelectionStoreApi) | - | [packages/capabilities/canvas/editor/src/context/canvas-studio-context.tsx:109](https://github.com/ancyloce/anvilkit-studio/blob/main/src/context/canvas-studio-context.tsx#L109) |
| &lt;a id="stage"&gt;&lt;/a&gt; `stage` | `Stage` \| `null` | Konva.Stage instance — null until &lt;CanvasStage&gt;'s onReady fires. | [packages/capabilities/canvas/editor/src/context/canvas-studio-context.tsx:462](https://github.com/ancyloce/anvilkit-studio/blob/main/src/context/canvas-studio-context.tsx#L462) |
| &lt;a id="t"&gt;&lt;/a&gt; `t?` | [`CanvasT`](../type-aliases/CanvasT) | i18n resolver (P7). Optional — `<CanvasStudio>` provides one backed by its `messages` prop; absent in partial test contexts. Read it via [useCanvasT](../functions/useCanvasT), which falls back to the inline English default so callers always get a string. | [packages/capabilities/canvas/editor/src/context/canvas-studio-context.tsx:477](https://github.com/ancyloce/anvilkit-studio/blob/main/src/context/canvas-studio-context.tsx#L477) |
| &lt;a id="templateprovider"&gt;&lt;/a&gt; `templateProvider?` | [`CanvasTemplateProvider`](CanvasTemplateProvider.md) | Provider-backed template source (C-06, FR-131). Takes precedence over `templates` when both are set; a plain `templates` array is wrapped in `createStaticTemplateProvider` by the panel, so hosts never need this unless their catalog is remote/paginated. | [packages/capabilities/canvas/editor/src/context/canvas-studio-context.tsx:290](https://github.com/ancyloce/anvilkit-studio/blob/main/src/context/canvas-studio-context.tsx#L290) |
| &lt;a id="templates"&gt;&lt;/a&gt; `templates?` | readonly [`CanvasTemplateEntry`](CanvasTemplateEntry.md)[] | Host-supplied template catalog (canvas-m0-009). Plain data — rendered by the Templates dock panel; absent/empty shows the panel's empty state. | [packages/capabilities/canvas/editor/src/context/canvas-studio-context.tsx:283](https://github.com/ancyloce/anvilkit-studio/blob/main/src/context/canvas-studio-context.tsx#L283) |
| &lt;a id="toolregistry"&gt;&lt;/a&gt; `toolRegistry?` | `Partial`\&lt;`Record`\&lt;[`ToolId`](../type-aliases/ToolId), [`Tool`](../../internal/interfaces/Tool)\&gt;\&gt; | The EFFECTIVE tool registry (FR-010): default tools + extension- contributed tools + the `toolRegistry` prop override — the SAME registry `ToolInteractionLayer` dispatches through, so the chrome (the tool strip and its "More tools" overflow — the only tool surfaces since `cp3-009`) lists exactly the tools that can actually run. Referentially stable (memoized by `<CanvasStudio>`; changes only when `extensions`/`toolRegistry` change). Optional for partial test contexts, where chrome falls back to the built-in rail alone. | [packages/capabilities/canvas/editor/src/context/canvas-studio-context.tsx:460](https://github.com/ancyloce/anvilkit-studio/blob/main/src/context/canvas-studio-context.tsx#L460) |
| &lt;a id="toolstore"&gt;&lt;/a&gt; `toolStore` | [`ToolStoreApi`](../../internal/type-aliases/ToolStoreApi) | - | [packages/capabilities/canvas/editor/src/context/canvas-studio-context.tsx:108](https://github.com/ancyloce/anvilkit-studio/blob/main/src/context/canvas-studio-context.tsx#L108) |
| &lt;a id="undo"&gt;&lt;/a&gt; `undo?` | () =&gt; [`CanvasIR`](https://github.com/ancyloce/anvilkit-canvas-core/blob/main/src/ir/types.ts) | Undo the last commit through `historyStore` AND fire `onChange`/ `onChanges` (E-20) — use this, never `historyStore.getState().undo(ir)` + `sceneStore.getState().setIR(ir)` directly, or a host mirroring the document silently misses every undo. Optional so hand-built partial test contexts keep working; always provided by `<CanvasStudio>`. | [packages/capabilities/canvas/editor/src/context/canvas-studio-context.tsx:203](https://github.com/ancyloce/anvilkit-studio/blob/main/src/context/canvas-studio-context.tsx#L203) |
| &lt;a id="uploadstore"&gt;&lt;/a&gt; `uploadStore?` | `UploadStoreApi` | Upload task registry (B-10). Provided by `<CanvasStudio>`. | [packages/capabilities/canvas/editor/src/context/canvas-studio-context.tsx:406](https://github.com/ancyloce/anvilkit-studio/blob/main/src/context/canvas-studio-context.tsx#L406) |
| &lt;a id="viewportstore"&gt;&lt;/a&gt; `viewportStore` | [`ViewportStoreApi`](../../internal/type-aliases/ViewportStoreApi) | - | [packages/capabilities/canvas/editor/src/context/canvas-studio-context.tsx:112](https://github.com/ancyloce/anvilkit-studio/blob/main/src/context/canvas-studio-context.tsx#L112) |
