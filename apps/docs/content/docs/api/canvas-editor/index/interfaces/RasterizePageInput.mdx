---
title: "RasterizePageInput"
description: "@anvilkit/canvas-editor · RasterizePageInput"
---

Defined in: [packages/capabilities/canvas/editor/src/render/rasterize-page.tsx:26](https://github.com/ancyloce/anvilkit-studio/blob/main/src/render/rasterize-page.tsx#L26)

## Properties

| Property | Modifier | Type | Description | Defined in |
| ------ | ------ | ------ | ------ | ------ |
| &lt;a id="assets"&gt;&lt;/a&gt; `assets?` | `readonly` | `Record`\&lt;`string`, `CanvasAssetRef`\&gt; | Asset map keyed by `assetId`. Image nodes look up their asset here. Defaults to an empty map; image nodes without an asset entry render nothing (matches the editor's behavior). | [packages/capabilities/canvas/editor/src/render/rasterize-page.tsx:33](https://github.com/ancyloce/anvilkit-studio/blob/main/src/render/rasterize-page.tsx#L33) |
| &lt;a id="brandkit"&gt;&lt;/a&gt; `brandKit?` | `readonly` | [`BrandKit`](BrandKit.md) | Brand kit to resolve `BrandTokenRef` fills/fonts against (canvas-m1-013) — the SAME resolution `<CanvasNodeRenderer>` performs on the live stage, via `CanvasBrandKitContext`. Defaults to an empty kit (every token degrades to its neutral fallback, never throws). | [packages/capabilities/canvas/editor/src/render/rasterize-page.tsx:40](https://github.com/ancyloce/anvilkit-studio/blob/main/src/render/rasterize-page.tsx#L40) |
| &lt;a id="includebackground"&gt;&lt;/a&gt; `includeBackground?` | `readonly` | `boolean` | Paint the page background (default `true`). `false` renders content only — the FR-150 "transparent background" / "include background" export options. JPEG has no alpha channel, so a background-less JPEG flattens to black; the export dialog disables the option there. | [packages/capabilities/canvas/editor/src/render/rasterize-page.tsx:60](https://github.com/ancyloce/anvilkit-studio/blob/main/src/render/rasterize-page.tsx#L60) |
| &lt;a id="mimetype"&gt;&lt;/a&gt; `mimeType?` | `readonly` | `"image/jpeg"` \| `"image/png"` \| `"image/webp"` | Defaults to `"image/png"`. | [packages/capabilities/canvas/editor/src/render/rasterize-page.tsx:51](https://github.com/ancyloce/anvilkit-studio/blob/main/src/render/rasterize-page.tsx#L51) |
| &lt;a id="page"&gt;&lt;/a&gt; `page` | `readonly` | `CanvasPage` | - | [packages/capabilities/canvas/editor/src/render/rasterize-page.tsx:27](https://github.com/ancyloce/anvilkit-studio/blob/main/src/render/rasterize-page.tsx#L27) |
| &lt;a id="pixelratio"&gt;&lt;/a&gt; `pixelRatio?` | `readonly` | \| `number` \| \{ `x`: `number`; `y`: `number`; \} | Defaults to 2 (retina-quality preview). Pass an `{x, y}` pair for independent horizontal/vertical scale — FR-153's custom width × height export, where an unlocked aspect ratio stretches non-proportionally. Implemented via Konva's own `stage.scaleX`/`scaleY` (no custom pixel resampling): the off-screen stage this function builds is torn down immediately after, so mutating its scale here is safe. | [packages/capabilities/canvas/editor/src/render/rasterize-page.tsx:49](https://github.com/ancyloce/anvilkit-studio/blob/main/src/render/rasterize-page.tsx#L49) |
| &lt;a id="quality"&gt;&lt;/a&gt; `quality?` | `readonly` | `number` | Only honored for image/jpeg + image/webp. Defaults to 0.92. | [packages/capabilities/canvas/editor/src/render/rasterize-page.tsx:53](https://github.com/ancyloce/anvilkit-studio/blob/main/src/render/rasterize-page.tsx#L53) |
| &lt;a id="resolveddocument"&gt;&lt;/a&gt; `resolvedDocument?` | `readonly` | `CanvasResolvedDocument` | T-M3-10: a resolution of the document this page belongs to. When present the offscreen render draws RESOLVED geometry (via `CanvasResolvedDocumentContext`) — the same tree the live stage shows — without mounting a studio context, so the pass stays non-interactive and preview-free. Omit for documents without layout intent. | [packages/capabilities/canvas/editor/src/render/rasterize-page.tsx:68](https://github.com/ancyloce/anvilkit-studio/blob/main/src/render/rasterize-page.tsx#L68) |
