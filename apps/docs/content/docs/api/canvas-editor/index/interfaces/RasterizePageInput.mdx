---
title: "RasterizePageInput"
description: "@anvilkit/canvas-editor · RasterizePageInput"
---

Defined in: [packages/capabilities/canvas/editor/src/render/rasterize-page.tsx:22](https://github.com/ancyloce/anvilkit-studio/blob/main/src/render/rasterize-page.tsx#L22)

## Properties

| Property | Modifier | Type | Description | Defined in |
| ------ | ------ | ------ | ------ | ------ |
| &lt;a id="assets"&gt;&lt;/a&gt; `assets?` | `readonly` | `Record`\&lt;`string`, `CanvasAssetRef`\&gt; | Asset map keyed by `assetId`. Image nodes look up their asset here. Defaults to an empty map; image nodes without an asset entry render nothing (matches the editor's behavior). | [packages/capabilities/canvas/editor/src/render/rasterize-page.tsx:29](https://github.com/ancyloce/anvilkit-studio/blob/main/src/render/rasterize-page.tsx#L29) |
| &lt;a id="brandkit"&gt;&lt;/a&gt; `brandKit?` | `readonly` | [`BrandKit`](BrandKit.md) | Brand kit to resolve `BrandTokenRef` fills/fonts against (canvas-m1-013) — the SAME resolution `<CanvasNodeRenderer>` performs on the live stage, via `CanvasBrandKitContext`. Defaults to an empty kit (every token degrades to its neutral fallback, never throws). | [packages/capabilities/canvas/editor/src/render/rasterize-page.tsx:36](https://github.com/ancyloce/anvilkit-studio/blob/main/src/render/rasterize-page.tsx#L36) |
| &lt;a id="includebackground"&gt;&lt;/a&gt; `includeBackground?` | `readonly` | `boolean` | Paint the page background (default `true`). `false` renders content only — the FR-150 "transparent background" / "include background" export options. JPEG has no alpha channel, so a background-less JPEG flattens to black; the export dialog disables the option there. | [packages/capabilities/canvas/editor/src/render/rasterize-page.tsx:56](https://github.com/ancyloce/anvilkit-studio/blob/main/src/render/rasterize-page.tsx#L56) |
| &lt;a id="mimetype"&gt;&lt;/a&gt; `mimeType?` | `readonly` | `"image/png"` \| `"image/jpeg"` \| `"image/webp"` | Defaults to `"image/png"`. | [packages/capabilities/canvas/editor/src/render/rasterize-page.tsx:47](https://github.com/ancyloce/anvilkit-studio/blob/main/src/render/rasterize-page.tsx#L47) |
| &lt;a id="page"&gt;&lt;/a&gt; `page` | `readonly` | `CanvasPage` | - | [packages/capabilities/canvas/editor/src/render/rasterize-page.tsx:23](https://github.com/ancyloce/anvilkit-studio/blob/main/src/render/rasterize-page.tsx#L23) |
| &lt;a id="pixelratio"&gt;&lt;/a&gt; `pixelRatio?` | `readonly` | \| `number` \| \{ `x`: `number`; `y`: `number`; \} | Defaults to 2 (retina-quality preview). Pass an `{x, y}` pair for independent horizontal/vertical scale — FR-153's custom width × height export, where an unlocked aspect ratio stretches non-proportionally. Implemented via Konva's own `stage.scaleX`/`scaleY` (no custom pixel resampling): the off-screen stage this function builds is torn down immediately after, so mutating its scale here is safe. | [packages/capabilities/canvas/editor/src/render/rasterize-page.tsx:45](https://github.com/ancyloce/anvilkit-studio/blob/main/src/render/rasterize-page.tsx#L45) |
| &lt;a id="quality"&gt;&lt;/a&gt; `quality?` | `readonly` | `number` | Only honored for image/jpeg + image/webp. Defaults to 0.92. | [packages/capabilities/canvas/editor/src/render/rasterize-page.tsx:49](https://github.com/ancyloce/anvilkit-studio/blob/main/src/render/rasterize-page.tsx#L49) |
