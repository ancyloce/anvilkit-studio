---
title: "CanvasStudioProps"
description: "@anvilkit/canvas-editor · CanvasStudioProps"
---

Defined in: [packages/capabilities/canvas/editor/src/CanvasStudio.tsx:75](https://github.com/ancyloce/anvilkit-studio/blob/main/src/CanvasStudio.tsx#L75)

## Properties

| Property | Type | Description | Defined in |
| ------ | ------ | ------ | ------ |
| &lt;a id="brandkit"&gt;&lt;/a&gt; `brandKit?` | [`BrandKit`](BrandKit.md) | Shared brand colors + fonts (I3-4). Hosts map their Studio config to a [BrandKit](BrandKit.md) and pass it here; the editor surfaces it via [useBrandKit](../functions/useBrandKit). Omit to run with no brand kit. | [packages/capabilities/canvas/editor/src/CanvasStudio.tsx:141](https://github.com/ancyloce/anvilkit-studio/blob/main/src/CanvasStudio.tsx#L141) |
| &lt;a id="children"&gt;&lt;/a&gt; `children?` | `ReactNode` | Optional host UI rendered *inside* the editor's context provider, so it can call [useCanvasStudio](../functions/useCanvasStudio) to drive tool selection, read the live selection/IR, or mount the exported `<LayerPanel>` / `<PropertyInspector>` against this instance's stores. The editor ships no toolbar of its own (tool selection is host-driven, PRD §3.4); this slot is how a host wires one without recomposing the stage. Rendered as a sibling of the stage root so the host owns its own layout. | [packages/capabilities/canvas/editor/src/CanvasStudio.tsx:173](https://github.com/ancyloce/anvilkit-studio/blob/main/src/CanvasStudio.tsx#L173) |
| &lt;a id="extensions"&gt;&lt;/a&gt; `extensions?` | readonly [`CanvasEditorExtension`](CanvasEditorExtension.md)[] | Domain extensions (Area 1). Each may contribute renderers/inspectors for custom node kinds; they are threaded to `<CanvasNodeRenderer>` and the inspector via context. Pair with canvas-core's `createCanvasRuntime` for the matching schema/command/serializer extensions. | [packages/capabilities/canvas/editor/src/CanvasStudio.tsx:163](https://github.com/ancyloce/anvilkit-studio/blob/main/src/CanvasStudio.tsx#L163) |
| &lt;a id="height"&gt;&lt;/a&gt; `height?` | `number` | - | [packages/capabilities/canvas/editor/src/CanvasStudio.tsx:88](https://github.com/ancyloce/anvilkit-studio/blob/main/src/CanvasStudio.tsx#L88) |
| &lt;a id="hidepagenavigator"&gt;&lt;/a&gt; `hidePageNavigator?` | `boolean` | Suppress the built-in `<PageNavigator>` (e.g. hosts that bring their own). | [packages/capabilities/canvas/editor/src/CanvasStudio.tsx:124](https://github.com/ancyloce/anvilkit-studio/blob/main/src/CanvasStudio.tsx#L124) |
| &lt;a id="initialactivepageid"&gt;&lt;/a&gt; `initialActivePageId?` | `string` | Initial active page id. Defaults to `initialIR.pages[0].id`. Uncontrolled — after mount the `pagesStore` owns the active id; switch pages via the `<PageNavigator>` or by calling `useCanvasStudio().pagesStore.getState().setActivePageId(...)`. | [packages/capabilities/canvas/editor/src/CanvasStudio.tsx:86](https://github.com/ancyloce/anvilkit-studio/blob/main/src/CanvasStudio.tsx#L86) |
| &lt;a id="initialir"&gt;&lt;/a&gt; `initialIR` | [`CanvasIR`](https://github.com/ancyloce/anvilkit-canvas-core/blob/main/src/ir/types.ts) | Initial IR. Uncontrolled — subsequent prop updates do not replace the internal IR. Use `onChange` to mirror state into a host store. | [packages/capabilities/canvas/editor/src/CanvasStudio.tsx:80](https://github.com/ancyloce/anvilkit-studio/blob/main/src/CanvasStudio.tsx#L80) |
| &lt;a id="initialtool"&gt;&lt;/a&gt; `initialTool?` | [`ToolId`](../type-aliases/ToolId) | - | [packages/capabilities/canvas/editor/src/CanvasStudio.tsx:89](https://github.com/ancyloce/anvilkit-studio/blob/main/src/CanvasStudio.tsx#L89) |
| &lt;a id="messages"&gt;&lt;/a&gt; `messages?` | `Readonly`\&lt;`Record`\&lt;`string`, `string`\&gt;\&gt; | Host-injected i18n catalog (P7). A flat `canvas.*` → string map for the active locale; the editor resolves chrome strings via [useCanvasT](../functions/useCanvasT) (host override wins, else the inline English fallback). Omit to render the bundled English defaults. canvas-editor stays standalone — the host (e.g. plugin-canvas-studio) selects the catalog by locale and passes it. | [packages/capabilities/canvas/editor/src/CanvasStudio.tsx:156](https://github.com/ancyloce/anvilkit-studio/blob/main/src/CanvasStudio.tsx#L156) |
| &lt;a id="onactivepagechange"&gt;&lt;/a&gt; `onActivePageChange?` | (`pageId`) =&gt; `void` | Fires whenever the active page (artboard) changes, with the new page id. Used by hosts that want to mirror the active artboard out (e.g. preview-export bridges that need to tag exports with the artboard id). | [packages/capabilities/canvas/editor/src/CanvasStudio.tsx:105](https://github.com/ancyloce/anvilkit-studio/blob/main/src/CanvasStudio.tsx#L105) |
| &lt;a id="onaiintent"&gt;&lt;/a&gt; `onAiIntent?` | (`intent`) =&gt; `void` | Fires when an AI tool (`ai-image` / `ai-brush`, I1-7) captures a gesture. Hosts wire this to the AI panel / job client. Omit it to leave the AI tools as inert gesture-capture (the marquee/selection still render). | [packages/capabilities/canvas/editor/src/CanvasStudio.tsx:113](https://github.com/ancyloce/anvilkit-studio/blob/main/src/CanvasStudio.tsx#L113) |
| &lt;a id="onchange"&gt;&lt;/a&gt; `onChange?` | (`ir`, `command`) =&gt; `void` | Fires after every committed command with the new IR + the command. | [packages/capabilities/canvas/editor/src/CanvasStudio.tsx:91](https://github.com/ancyloce/anvilkit-studio/blob/main/src/CanvasStudio.tsx#L91) |
| &lt;a id="onchanges"&gt;&lt;/a&gt; `onChanges?` | (`changes`, `ir`) =&gt; `void` | Fires after every commit with the granular change records + the new IR. Complements [onChange](#onchange) for autosave / dirty-tracking / collab that wants deltas rather than the whole command. A batch commit reports the flattened per-command changes. | [packages/capabilities/canvas/editor/src/CanvasStudio.tsx:98](https://github.com/ancyloce/anvilkit-studio/blob/main/src/CanvasStudio.tsx#L98) |
| &lt;a id="onpickasset"&gt;&lt;/a&gt; `onPickAsset?` | () =&gt; `Promise`\&lt;`string`\&gt; | Required for the image tool (MVP-6 Task 8). Host opens picker, returns asset id. | [packages/capabilities/canvas/editor/src/CanvasStudio.tsx:107](https://github.com/ancyloce/anvilkit-studio/blob/main/src/CanvasStudio.tsx#L107) |
| &lt;a id="onstageready"&gt;&lt;/a&gt; `onStageReady?` | (`stage`) =&gt; `void` | Fires once after `<CanvasStage>` has constructed the Konva.Stage, and again with `null` when the stage tears down. Hosts use this to drive export pipelines (e.g. `stage.toDataURL()`) without reaching into the editor's internals. | [packages/capabilities/canvas/editor/src/CanvasStudio.tsx:120](https://github.com/ancyloce/anvilkit-studio/blob/main/src/CanvasStudio.tsx#L120) |
| &lt;a id="rendershell"&gt;&lt;/a&gt; `renderShell?` | (`stage`) =&gt; `ReactNode` | Opt-in chrome composition (I3-5). When provided, `<CanvasStudio>` renders `renderShell(stage)` *inside* its context provider instead of the bare stacked layout — so any rail/panel/inspector returned by the shell is a provider child and can call [useCanvasStudio](../functions/useCanvasStudio). The callback is a pure composition seam (no hooks) that receives the ready-to-mount Konva stage node and decides where to place it (e.g. the centre column of a grid). Omit it to keep the bare-stage layout. `<CanvasWorkspace>` wraps this with the full editor shell. | [packages/capabilities/canvas/editor/src/CanvasStudio.tsx:135](https://github.com/ancyloce/anvilkit-studio/blob/main/src/CanvasStudio.tsx#L135) |
| &lt;a id="templates"&gt;&lt;/a&gt; `templates?` | readonly [`CanvasTemplateEntry`](CanvasTemplateEntry.md)[] | Host-supplied template catalog (canvas-m0-009). Plain data consumed by the Templates dock panel; structurally compatible with `@anvilkit/canvas-templates`' catalog values. Omit to show the panel's empty state. | [packages/capabilities/canvas/editor/src/CanvasStudio.tsx:148](https://github.com/ancyloce/anvilkit-studio/blob/main/src/CanvasStudio.tsx#L148) |
| &lt;a id="toolregistry"&gt;&lt;/a&gt; `toolRegistry?` | `Partial`\&lt;`Record`\&lt;[`ToolId`](../type-aliases/ToolId), [`Tool`](../../internal/interfaces/Tool)\&gt;\&gt; | Tool registry override (mainly for tests). Defaults to the built-in registry. | [packages/capabilities/canvas/editor/src/CanvasStudio.tsx:122](https://github.com/ancyloce/anvilkit-studio/blob/main/src/CanvasStudio.tsx#L122) |
| &lt;a id="width"&gt;&lt;/a&gt; `width?` | `number` | - | [packages/capabilities/canvas/editor/src/CanvasStudio.tsx:87](https://github.com/ancyloce/anvilkit-studio/blob/main/src/CanvasStudio.tsx#L87) |
