---
title: "CanvasStudioProps"
description: "@anvilkit/canvas-editor · CanvasStudioProps"
---

Defined in: [packages/canvas/editor/src/CanvasStudio.tsx:74](https://github.com/ancyloce/anvilkit-studio/blob/main/src/CanvasStudio.tsx#L74)

## Properties

| Property | Type | Description | Defined in |
| ------ | ------ | ------ | ------ |
| &lt;a id="brandkit"&gt;&lt;/a&gt; `brandKit?` | [`BrandKit`](BrandKit.md) | Shared brand colors + fonts (I3-4). Hosts map their Studio config to a [BrandKit](BrandKit.md) and pass it here; the editor surfaces it via [useBrandKit](../functions/useBrandKit). Omit to run with no brand kit. | [packages/canvas/editor/src/CanvasStudio.tsx:140](https://github.com/ancyloce/anvilkit-studio/blob/main/src/CanvasStudio.tsx#L140) |
| &lt;a id="children"&gt;&lt;/a&gt; `children?` | `ReactNode` | Optional host UI rendered *inside* the editor's context provider, so it can call [useCanvasStudio](../functions/useCanvasStudio) to drive tool selection, read the live selection/IR, or mount the exported `<LayerPanel>` / `<PropertyInspector>` against this instance's stores. The editor ships no toolbar of its own (tool selection is host-driven, PRD §3.4); this slot is how a host wires one without recomposing the stage. Rendered as a sibling of the stage root so the host owns its own layout. | [packages/canvas/editor/src/CanvasStudio.tsx:165](https://github.com/ancyloce/anvilkit-studio/blob/main/src/CanvasStudio.tsx#L165) |
| &lt;a id="extensions"&gt;&lt;/a&gt; `extensions?` | readonly [`CanvasEditorExtension`](CanvasEditorExtension.md)[] | Domain extensions (Area 1). Each may contribute renderers/inspectors for custom node kinds; they are threaded to `<CanvasNodeRenderer>` and the inspector via context. Pair with canvas-core's `createCanvasRuntime` for the matching schema/command/serializer extensions. | [packages/canvas/editor/src/CanvasStudio.tsx:155](https://github.com/ancyloce/anvilkit-studio/blob/main/src/CanvasStudio.tsx#L155) |
| &lt;a id="height"&gt;&lt;/a&gt; `height?` | `number` | - | [packages/canvas/editor/src/CanvasStudio.tsx:87](https://github.com/ancyloce/anvilkit-studio/blob/main/src/CanvasStudio.tsx#L87) |
| &lt;a id="hidepagenavigator"&gt;&lt;/a&gt; `hidePageNavigator?` | `boolean` | Suppress the built-in `<PageNavigator>` (e.g. hosts that bring their own). | [packages/canvas/editor/src/CanvasStudio.tsx:123](https://github.com/ancyloce/anvilkit-studio/blob/main/src/CanvasStudio.tsx#L123) |
| &lt;a id="initialactivepageid"&gt;&lt;/a&gt; `initialActivePageId?` | `string` | Initial active page id. Defaults to `initialIR.pages[0].id`. Uncontrolled — after mount the `pagesStore` owns the active id; switch pages via the `<PageNavigator>` or by calling `useCanvasStudio().pagesStore.getState().setActivePageId(...)`. | [packages/canvas/editor/src/CanvasStudio.tsx:85](https://github.com/ancyloce/anvilkit-studio/blob/main/src/CanvasStudio.tsx#L85) |
| &lt;a id="initialir"&gt;&lt;/a&gt; `initialIR` | [`CanvasIR`](https://github.com/ancyloce/anvilkit-canvas-core/blob/main/src/types.ts) | Initial IR. Uncontrolled — subsequent prop updates do not replace the internal IR. Use `onChange` to mirror state into a host store. | [packages/canvas/editor/src/CanvasStudio.tsx:79](https://github.com/ancyloce/anvilkit-studio/blob/main/src/CanvasStudio.tsx#L79) |
| &lt;a id="initialtool"&gt;&lt;/a&gt; `initialTool?` | [`ToolId`](../type-aliases/ToolId) | - | [packages/canvas/editor/src/CanvasStudio.tsx:88](https://github.com/ancyloce/anvilkit-studio/blob/main/src/CanvasStudio.tsx#L88) |
| &lt;a id="messages"&gt;&lt;/a&gt; `messages?` | `Readonly`\&lt;`Record`\&lt;`string`, `string`\&gt;\&gt; | Host-injected i18n catalog (P7). A flat `canvas.*` → string map for the active locale; the editor resolves chrome strings via [useCanvasT](../functions/useCanvasT) (host override wins, else the inline English fallback). Omit to render the bundled English defaults. canvas-editor stays standalone — the host (e.g. plugin-canvas-studio) selects the catalog by locale and passes it. | [packages/canvas/editor/src/CanvasStudio.tsx:148](https://github.com/ancyloce/anvilkit-studio/blob/main/src/CanvasStudio.tsx#L148) |
| &lt;a id="onactivepagechange"&gt;&lt;/a&gt; `onActivePageChange?` | (`pageId`) =&gt; `void` | Fires whenever the active page (artboard) changes, with the new page id. Used by hosts that want to mirror the active artboard out (e.g. preview-export bridges that need to tag exports with the artboard id). | [packages/canvas/editor/src/CanvasStudio.tsx:104](https://github.com/ancyloce/anvilkit-studio/blob/main/src/CanvasStudio.tsx#L104) |
| &lt;a id="onaiintent"&gt;&lt;/a&gt; `onAiIntent?` | (`intent`) =&gt; `void` | Fires when an AI tool (`ai-image` / `ai-brush`, I1-7) captures a gesture. Hosts wire this to the AI panel / job client. Omit it to leave the AI tools as inert gesture-capture (the marquee/selection still render). | [packages/canvas/editor/src/CanvasStudio.tsx:112](https://github.com/ancyloce/anvilkit-studio/blob/main/src/CanvasStudio.tsx#L112) |
| &lt;a id="onchange"&gt;&lt;/a&gt; `onChange?` | (`ir`, `command`) =&gt; `void` | Fires after every committed command with the new IR + the command. | [packages/canvas/editor/src/CanvasStudio.tsx:90](https://github.com/ancyloce/anvilkit-studio/blob/main/src/CanvasStudio.tsx#L90) |
| &lt;a id="onchanges"&gt;&lt;/a&gt; `onChanges?` | (`changes`, `ir`) =&gt; `void` | Fires after every commit with the granular change records + the new IR. Complements [onChange](#onchange) for autosave / dirty-tracking / collab that wants deltas rather than the whole command. A batch commit reports the flattened per-command changes. | [packages/canvas/editor/src/CanvasStudio.tsx:97](https://github.com/ancyloce/anvilkit-studio/blob/main/src/CanvasStudio.tsx#L97) |
| &lt;a id="onpickasset"&gt;&lt;/a&gt; `onPickAsset?` | () =&gt; `Promise`\&lt;`string`\&gt; | Required for the image tool (MVP-6 Task 8). Host opens picker, returns asset id. | [packages/canvas/editor/src/CanvasStudio.tsx:106](https://github.com/ancyloce/anvilkit-studio/blob/main/src/CanvasStudio.tsx#L106) |
| &lt;a id="onstageready"&gt;&lt;/a&gt; `onStageReady?` | (`stage`) =&gt; `void` | Fires once after `<CanvasStage>` has constructed the Konva.Stage, and again with `null` when the stage tears down. Hosts use this to drive export pipelines (e.g. `stage.toDataURL()`) without reaching into the editor's internals. | [packages/canvas/editor/src/CanvasStudio.tsx:119](https://github.com/ancyloce/anvilkit-studio/blob/main/src/CanvasStudio.tsx#L119) |
| &lt;a id="rendershell"&gt;&lt;/a&gt; `renderShell?` | (`stage`) =&gt; `ReactNode` | Opt-in chrome composition (I3-5). When provided, `<CanvasStudio>` renders `renderShell(stage)` *inside* its context provider instead of the bare stacked layout — so any rail/panel/inspector returned by the shell is a provider child and can call [useCanvasStudio](../functions/useCanvasStudio). The callback is a pure composition seam (no hooks) that receives the ready-to-mount Konva stage node and decides where to place it (e.g. the centre column of a grid). Omit it to keep the bare-stage layout. `<CanvasWorkspace>` wraps this with the full editor shell. | [packages/canvas/editor/src/CanvasStudio.tsx:134](https://github.com/ancyloce/anvilkit-studio/blob/main/src/CanvasStudio.tsx#L134) |
| &lt;a id="toolregistry"&gt;&lt;/a&gt; `toolRegistry?` | `Partial`\&lt;`Record`\&lt;[`ToolId`](../type-aliases/ToolId), [`Tool`](../../internal/interfaces/Tool)\&gt;\&gt; | Tool registry override (mainly for tests). Defaults to the built-in registry. | [packages/canvas/editor/src/CanvasStudio.tsx:121](https://github.com/ancyloce/anvilkit-studio/blob/main/src/CanvasStudio.tsx#L121) |
| &lt;a id="width"&gt;&lt;/a&gt; `width?` | `number` | - | [packages/canvas/editor/src/CanvasStudio.tsx:86](https://github.com/ancyloce/anvilkit-studio/blob/main/src/CanvasStudio.tsx#L86) |
