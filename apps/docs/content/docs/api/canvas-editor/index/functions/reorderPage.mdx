---
title: "reorderPage"
description: "@anvilkit/canvas-editor · reorderPage"
---

```ts
function reorderPage(
   ctx, 
   pageId, 
   toIndex): void;
```

Defined in: [packages/canvas/editor/src/pages/page-actions.ts:96](https://github.com/ancyloce/anvilkit-studio/blob/main/src/pages/page-actions.ts#L96)

Reorder a page in the IR. No-op when the page is already at `toIndex`,
the page id is unknown, or `toIndex` is out of range. Active page is
preserved (reordering does not change which page is editable).

## Parameters

| Parameter | Type |
| ------ | ------ |
| `ctx` | [`CanvasStudioContextValue`](../interfaces/CanvasStudioContextValue) |
| `pageId` | `string` |
| `toIndex` | `number` |

## Returns

`void`
