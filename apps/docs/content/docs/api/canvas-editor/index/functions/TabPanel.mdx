---
title: "TabPanel"
description: "@anvilkit/canvas-editor · TabPanel"
---

```ts
function TabPanel(__namedParameters): Element;
```

Defined in: [packages/canvas/editor/src/workspace/layout/TabPanel.tsx:29](https://github.com/ancyloce/anvilkit-studio/blob/main/src/workspace/layout/TabPanel.tsx#L29)

Tab Panel (Aside, col 2). Renders the registry descriptor for the active
dock id, with an optional search box, and dispatches by descriptor kind
(built-in / plugin / remote / search). Search resets when the dock changes
so each panel opens fresh.

## Parameters

| Parameter | Type |
| ------ | ------ |
| `__namedParameters` | [`TabPanelProps`](../interfaces/TabPanelProps) |

## Returns

`Element`
