---
title: "insertTemplateAsNewPages"
description: "@anvilkit/canvas-editor · insertTemplateAsNewPages"
---

```ts
function insertTemplateAsNewPages(ctx, entry): TemplateActionResult;
```

Defined in: [packages/capabilities/canvas/editor/src/panels/template-actions.ts:77](https://github.com/ancyloce/anvilkit-studio/blob/main/src/panels/template-actions.ts#L77)

Insert a template's pages as NEW pages alongside the current document's
existing pages, as ONE undo entry — the "create new" counterpart to
[loadTemplate](loadTemplate.md)'s "replace" (FR-023, UX-001). `<CanvasStudio>` owns one
live multi-page scene rather than a document tab set, so "create a new
document" is expressed here as appending a fresh, non-destructive set of
pages rather than discarding the current ones.

## Parameters

| Parameter | Type |
| ------ | ------ |
| `ctx` | [`CanvasStudioContextValue`](../interfaces/CanvasStudioContextValue) |
| `entry` | [`CanvasTemplateEntry`](../interfaces/CanvasTemplateEntry) |

## Returns

[`TemplateActionResult`](../type-aliases/TemplateActionResult)
