---
title: "ElementsPanel"
description: "@anvilkit/canvas-editor · ElementsPanel"
---

```ts
function ElementsPanel(__namedParameters): Element;
```

Defined in: [packages/capabilities/canvas/editor/src/panels/ElementsPanel.tsx:34](https://github.com/ancyloce/anvilkit-studio/blob/main/src/panels/ElementsPanel.tsx#L34)

The Canva-shell "Elements" panel — the new home for the drawing tools.
Renders each effective tool (built-in rail + extension-registered tools,
FR-010) as a button bound to `toolStore`; the active tool is highlighted.
It is the drawing-tool surface for the `CanvasWorkspace` shell (decision
§1.3.2). The `tools` prop still overrides the list entirely.

## Parameters

| Parameter | Type |
| ------ | ------ |
| `__namedParameters` | [`ElementsPanelProps`](../interfaces/ElementsPanelProps) |

## Returns

`Element`
