---
title: "ElementsPanel"
description: "@anvilkit/canvas-editor · ElementsPanel"
---

```ts
function ElementsPanel(__namedParameters): Element;
```

Defined in: [packages/capabilities/canvas/editor/src/panels/ElementsPanel.tsx:26](https://github.com/ancyloce/anvilkit-studio/blob/main/src/panels/ElementsPanel.tsx#L26)

The Canva-shell "Elements" panel — the new home for the drawing tools.
Renders each [TOOL\_RAIL\_ITEMS](../../internal/variables/TOOL_RAIL_ITEMS) entry as a button bound to `toolStore`;
the active tool is highlighted. It is the drawing-tool surface for the
`CanvasWorkspace` shell (decision §1.3.2).

## Parameters

| Parameter | Type |
| ------ | ------ |
| `__namedParameters` | [`ElementsPanelProps`](../interfaces/ElementsPanelProps) |

## Returns

`Element`
