---
title: "useCommitPatch"
description: "@anvilkit/canvas-editor · useCommitPatch"
---

```ts
function useCommitPatch(): CommitPatch;
```

Defined in: [packages/canvas/editor/src/panels/fields.tsx:209](https://github.com/ancyloce/anvilkit-studio/blob/main/src/panels/fields.tsx#L209)

Returns a stable [CommitPatch](../type-aliases/CommitPatch) bound to the current studio context.
Identical to the inspector's former local `commitPatch` — every field
mutation flows through `ctx.commit({ type: "node.update", … })`.

## Returns

[`CommitPatch`](../type-aliases/CommitPatch)
