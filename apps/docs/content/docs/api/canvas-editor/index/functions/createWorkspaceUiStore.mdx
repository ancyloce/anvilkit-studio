---
title: "createWorkspaceUiStore"
description: "@anvilkit/canvas-editor · createWorkspaceUiStore"
---

```ts
function createWorkspaceUiStore(options): WorkspaceUiStoreApi;
```

Defined in: [packages/capabilities/canvas/editor/src/workspace/state/workspace-ui-store.ts:85](https://github.com/ancyloce/anvilkit-studio/blob/main/src/workspace/state/workspace-ui-store.ts#L85)

Build a fresh per-instance store. The persistence key is namespaced by
`storeId` so concurrent stores never collide in `localStorage`.

## Parameters

| Parameter | Type |
| ------ | ------ |
| `options` | [`CreateWorkspaceUiStoreOptions`](../interfaces/CreateWorkspaceUiStoreOptions) |

## Returns

[`WorkspaceUiStoreApi`](../type-aliases/WorkspaceUiStoreApi)
