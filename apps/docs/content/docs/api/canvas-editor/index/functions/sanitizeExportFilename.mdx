---
title: "sanitizeExportFilename"
description: "@anvilkit/canvas-editor · sanitizeExportFilename"
---

```ts
function sanitizeExportFilename(stem, fallback?): string;
```

Defined in: [packages/capabilities/canvas/editor/src/header/download-artifact.ts:24](https://github.com/ancyloce/anvilkit-studio/blob/main/src/header/download-artifact.ts#L24)

§14.5 export file-name sanitization: strip path separators, characters
illegal on common filesystems, control characters, and leading dots, then
cap the length. An empty result falls back to `fallback`.

## Parameters

| Parameter | Type | Default value |
| ------ | ------ | ------ |
| `stem` | `string` | `undefined` |
| `fallback` | `string` | `"canvas"` |

## Returns

`string`
