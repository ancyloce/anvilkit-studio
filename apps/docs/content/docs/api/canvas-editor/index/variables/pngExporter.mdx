---
title: "pngExporter"
description: "@anvilkit/canvas-editor · pngExporter"
---

```ts
const pngExporter: CanvasExporter;
```

Defined in: [packages/capabilities/canvas/editor/src/header/exporters.ts:20](https://github.com/ancyloce/anvilkit-studio/blob/main/src/header/exporters.ts#L20)

Built-in PNG exporter. Reads the live Konva stage directly — no extra deps.
`resolution` scales the (retina) pixel ratio; `quality` is a no-op for PNG.
