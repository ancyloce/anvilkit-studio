---
title: "defaultToolRegistry"
description: "@anvilkit/canvas-editor · defaultToolRegistry"
---

```ts
const defaultToolRegistry: ToolRegistry;
```

Defined in: [packages/canvas/editor/src/tools/tool-registry.ts:17](https://github.com/ancyloce/anvilkit-studio/blob/main/src/tools/tool-registry.ts#L17)

Default tool registry — the PRD FR-009 tools wired. The AI tools
(`ai-image`, `ai-brush`) emit intents to the host rather than committing IR.
