---
title: "TOOL_LABELS"
description: "@anvilkit/canvas-editor · TOOL_LABELS"
---

```ts
const TOOL_LABELS: Record<ToolId, string>;
```

Defined in: [packages/canvas/editor/src/a11y/tool-labels.ts:9](https://github.com/ancyloce/anvilkit-studio/blob/main/src/a11y/tool-labels.ts#L9)

Human-readable names for the PRD FR-009 tools, used for screen-reader
announcements (and reusable by hosts that build a tool palette — the editor
itself ships no toolbar; tool selection is host-driven). Keep in sync with
[ToolId](../../index/type-aliases/ToolId).
