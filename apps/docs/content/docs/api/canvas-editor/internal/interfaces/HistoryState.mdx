---
title: "HistoryState"
description: "@anvilkit/canvas-editor · HistoryState"
---

Defined in: [packages/capabilities/canvas/editor/src/stores/history-store.ts:79](https://github.com/ancyloce/anvilkit-studio/blob/main/src/stores/history-store.ts#L79)

## Properties

| Property | Type | Description | Defined in |
| ------ | ------ | ------ | ------ |
| &lt;a id="canredo"&gt;&lt;/a&gt; `canRedo` | () =&gt; `boolean` | - | [packages/capabilities/canvas/editor/src/stores/history-store.ts:116](https://github.com/ancyloce/anvilkit-studio/blob/main/src/stores/history-store.ts#L116) |
| &lt;a id="canundo"&gt;&lt;/a&gt; `canUndo` | () =&gt; `boolean` | - | [packages/capabilities/canvas/editor/src/stores/history-store.ts:115](https://github.com/ancyloce/anvilkit-studio/blob/main/src/stores/history-store.ts#L115) |
| &lt;a id="commit"&gt;&lt;/a&gt; `commit` | (`ir`, `cmd`) =&gt; [`CanvasIR`](https://github.com/ancyloce/anvilkit-canvas-core/blob/main/src/ir/types.ts) | - | [packages/capabilities/canvas/editor/src/stores/history-store.ts:83](https://github.com/ancyloce/anvilkit-studio/blob/main/src/stores/history-store.ts#L83) |
| &lt;a id="commitbatch"&gt;&lt;/a&gt; `commitBatch` | (`ir`, `commands`, `label?`) =&gt; [`CanvasIR`](https://github.com/ancyloce/anvilkit-canvas-core/blob/main/src/ir/types.ts) | Apply many commands as ONE undo entry (a composite `batch` inverse). An empty `commands` array is a no-op and records nothing. | [packages/capabilities/canvas/editor/src/stores/history-store.ts:88](https://github.com/ancyloce/anvilkit-studio/blob/main/src/stores/history-store.ts#L88) |
| &lt;a id="commitcoalesced"&gt;&lt;/a&gt; `commitCoalesced` | (`ir`, `cmd`, `mergeKey`) =&gt; [`CanvasIR`](https://github.com/ancyloce/anvilkit-canvas-core/blob/main/src/ir/types.ts) | Apply a command, folding it into the previous undo entry when it shares `mergeKey` with the prior commit within the merge window (e.g. a drag or a held arrow key). Intended for single-target edits whose inverse restores an absolute value (move/rotate/resize/update) — not for structural commands. | [packages/capabilities/canvas/editor/src/stores/history-store.ts:99](https://github.com/ancyloce/anvilkit-studio/blob/main/src/stores/history-store.ts#L99) |
| &lt;a id="future"&gt;&lt;/a&gt; `future` | [`AnyCanvasCommand`](../type-aliases/AnyCanvasCommand)[] | - | [packages/capabilities/canvas/editor/src/stores/history-store.ts:81](https://github.com/ancyloce/anvilkit-studio/blob/main/src/stores/history-store.ts#L81) |
| &lt;a id="getstateid"&gt;&lt;/a&gt; `getStateId` | () =&gt; `number` | Identity of the CURRENT document state (B-08, FR-161). Every commit — including each coalesced fold — mints a fresh id; undo/redo walk back to the ids they restore. Dirty tracking compares ids, never documents. | [packages/capabilities/canvas/editor/src/stores/history-store.ts:122](https://github.com/ancyloce/anvilkit-studio/blob/main/src/stores/history-store.ts#L122) |
| &lt;a id="isatsavecheckpoint"&gt;&lt;/a&gt; `isAtSaveCheckpoint` | () =&gt; `boolean` | True when the current state IS the last-saved one (undo-to-clean). | [packages/capabilities/canvas/editor/src/stores/history-store.ts:131](https://github.com/ancyloce/anvilkit-studio/blob/main/src/stores/history-store.ts#L131) |
| &lt;a id="limit"&gt;&lt;/a&gt; `limit` | `number` | - | [packages/capabilities/canvas/editor/src/stores/history-store.ts:82](https://github.com/ancyloce/anvilkit-studio/blob/main/src/stores/history-store.ts#L82) |
| &lt;a id="marksavecheckpoint"&gt;&lt;/a&gt; `markSaveCheckpoint` | (`stateId?`) =&gt; `void` | Record a save checkpoint (FR-161). Pass the state id CAPTURED when the save snapshot was taken so late-arriving responses checkpoint the state they actually persisted; defaults to the current id. `reset()` (document replacement) re-checkpoints the fresh state as clean. | [packages/capabilities/canvas/editor/src/stores/history-store.ts:129](https://github.com/ancyloce/anvilkit-studio/blob/main/src/stores/history-store.ts#L129) |
| &lt;a id="past"&gt;&lt;/a&gt; `past` | [`AnyCanvasCommand`](../type-aliases/AnyCanvasCommand)[] | - | [packages/capabilities/canvas/editor/src/stores/history-store.ts:80](https://github.com/ancyloce/anvilkit-studio/blob/main/src/stores/history-store.ts#L80) |
| &lt;a id="redo"&gt;&lt;/a&gt; `redo` | (`ir`) =&gt; [`CanvasIR`](https://github.com/ancyloce/anvilkit-canvas-core/blob/main/src/ir/types.ts) | **See** undo — the same stale-entry drop applies to `future`. | [packages/capabilities/canvas/editor/src/stores/history-store.ts:113](https://github.com/ancyloce/anvilkit-studio/blob/main/src/stores/history-store.ts#L113) |
| &lt;a id="reset"&gt;&lt;/a&gt; `reset` | () =&gt; `void` | - | [packages/capabilities/canvas/editor/src/stores/history-store.ts:114](https://github.com/ancyloce/anvilkit-studio/blob/main/src/stores/history-store.ts#L114) |
| &lt;a id="undo"&gt;&lt;/a&gt; `undo` | (`ir`) =&gt; [`CanvasIR`](https://github.com/ancyloce/anvilkit-canvas-core/blob/main/src/ir/types.ts) | Replays the top inverse. A STALE inverse (e.g. it references a node a remote peer has since deleted, E-20) cannot replay — that entry is dropped and `ir` is returned unchanged instead of throwing, so a corrupted history doesn't get stuck retrying the same broken entry on every subsequent undo. | [packages/capabilities/canvas/editor/src/stores/history-store.ts:111](https://github.com/ancyloce/anvilkit-studio/blob/main/src/stores/history-store.ts#L111) |
