---
title: "CreateHistoryStoreOptions"
description: "@anvilkit/canvas-editor · CreateHistoryStoreOptions"
---

Defined in: [packages/capabilities/canvas/editor/src/stores/history-store.ts:44](https://github.com/ancyloce/anvilkit-studio/blob/main/src/stores/history-store.ts#L44)

## Properties

| Property | Type | Description | Defined in |
| ------ | ------ | ------ | ------ |
| &lt;a id="apply"&gt;&lt;/a&gt; `apply?` | [`CommandApplyFn`](../type-aliases/CommandApplyFn) | Command dispatcher (P0-7). Defaults to core's built-in-only `applyCommand`. Pass a `CanvasRuntime`'s `apply` (from `createCanvasRuntime`) so commands registered on that runtime — custom node kinds, custom command types — participate in commit/batch/undo/redo the same way built-ins do. | [packages/capabilities/canvas/editor/src/stores/history-store.ts:66](https://github.com/ancyloce/anvilkit-studio/blob/main/src/stores/history-store.ts#L66) |
| &lt;a id="enforcelocked"&gt;&lt;/a&gt; `enforceLocked?` | `boolean` | FR-024 / §20.13 lock enforcement. When true, every command applied through this store passes core's `enforceLocked` option, so mutating a locked node throws a typed `node-locked` `CanvasCommandError` at the command boundary (the unlock patch — setting `locked:false` — is exempted, so unlocking a node always works). The editor enables this for user-initiated commits; the commit pipeline catches the rejection and no-ops. Default false keeps the pre-PRD behavior for existing/test consumers. | [packages/capabilities/canvas/editor/src/stores/history-store.ts:76](https://github.com/ancyloce/anvilkit-studio/blob/main/src/stores/history-store.ts#L76) |
| &lt;a id="limit"&gt;&lt;/a&gt; `limit?` | `number` | Maximum number of inverse commands kept in `past`. When exceeded, the oldest entry is dropped. `future` is bounded by `past` (it can never be longer than the most recent run of undos), so a single `limit` suffices. | [packages/capabilities/canvas/editor/src/stores/history-store.ts:50](https://github.com/ancyloce/anvilkit-studio/blob/main/src/stores/history-store.ts#L50) |
| &lt;a id="mergewindowms"&gt;&lt;/a&gt; `mergeWindowMs?` | `number` | Time window (ms) within which a `commitCoalesced` call sharing the previous call's merge key folds into the same undo entry. Default 400. | [packages/capabilities/canvas/editor/src/stores/history-store.ts:57](https://github.com/ancyloce/anvilkit-studio/blob/main/src/stores/history-store.ts#L57) |
| &lt;a id="now"&gt;&lt;/a&gt; `now?` | () =&gt; `string` | Optional deterministic clock plumbed into every apply call. | [packages/capabilities/canvas/editor/src/stores/history-store.ts:52](https://github.com/ancyloce/anvilkit-studio/blob/main/src/stores/history-store.ts#L52) |
| &lt;a id="nowms"&gt;&lt;/a&gt; `nowMs?` | () =&gt; `number` | Injectable millisecond clock for coalescing (default `Date.now`). | [packages/capabilities/canvas/editor/src/stores/history-store.ts:59](https://github.com/ancyloce/anvilkit-studio/blob/main/src/stores/history-store.ts#L59) |
