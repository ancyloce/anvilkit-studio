---
title: "PagesState"
description: "@anvilkit/canvas-editor · PagesState"
---

Defined in: [packages/canvas/editor/src/stores/pages-store.ts:11](https://github.com/ancyloce/anvilkit-studio/blob/main/src/stores/pages-store.ts#L11)

Tracks which page in the IR is currently active in the editor. The IR's
`pages` array is the source of truth for the page list itself — this store
only owns the UI selection. Mutations to the IR's page list flow through
`historyStore.commit(...)` with page commands (`page.create`,
`page.delete`, `page.reorder`); helpers in `src/pages/page-actions.ts`
coordinate the two.

## Properties

| Property | Type | Defined in |
| ------ | ------ | ------ |
| &lt;a id="activepageid"&gt;&lt;/a&gt; `activePageId` | `string` | [packages/canvas/editor/src/stores/pages-store.ts:12](https://github.com/ancyloce/anvilkit-studio/blob/main/src/stores/pages-store.ts#L12) |
| &lt;a id="setactivepageid"&gt;&lt;/a&gt; `setActivePageId` | (`id`) =&gt; `void` | [packages/canvas/editor/src/stores/pages-store.ts:13](https://github.com/ancyloce/anvilkit-studio/blob/main/src/stores/pages-store.ts#L13) |
