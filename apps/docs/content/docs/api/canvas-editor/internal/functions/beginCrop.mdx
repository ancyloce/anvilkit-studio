---
title: "beginCrop"
description: "@anvilkit/canvas-editor · beginCrop"
---

```ts
function beginCrop(ctx, nodeId): boolean;
```

Defined in: [packages/capabilities/canvas/editor/src/selection/crop-actions.ts:96](https://github.com/ancyloce/anvilkit-studio/blob/main/src/selection/crop-actions.ts#L96)

Open the interactive crop editor for an image node. Returns false (no-op)
when the crop store is unavailable or the target is not an image node.

## Parameters

| Parameter | Type |
| ------ | ------ |
| `ctx` | [`BeginCropContext`](../interfaces/BeginCropContext) |
| `nodeId` | `string` |

## Returns

`boolean`
