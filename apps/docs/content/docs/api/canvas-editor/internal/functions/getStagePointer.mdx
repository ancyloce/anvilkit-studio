---
title: "getStagePointer"
description: "@anvilkit/canvas-editor · getStagePointer"
---

```ts
function getStagePointer(stage): StagePointer | null;
```

Defined in: [packages/canvas/editor/src/tools/get-stage-pointer.ts:15](https://github.com/ancyloce/anvilkit-studio/blob/main/src/tools/get-stage-pointer.ts#L15)

Resolve the current pointer position into both screen and world coordinates.
Returns null when Konva has no pointer position (e.g. before the first
pointer event), so callers must guard.

## Parameters

| Parameter | Type |
| ------ | ------ |
| `stage` | `Stage` |

## Returns

[`StagePointer`](../interfaces/StagePointer) \| `null`
