---
title: "penBounds"
description: "@anvilkit/canvas-editor · penBounds"
---

```ts
function penBounds(anchors): PenBounds;
```

Defined in: [packages/capabilities/canvas/editor/src/tools/pen-geometry.ts:25](https://github.com/ancyloce/anvilkit-studio/blob/main/src/tools/pen-geometry.ts#L25)

Axis-aligned bounds of a pen path, including each anchor's point plus its
outgoing and (mirrored) incoming control handles so bezier overshoot is
enclosed. Returns a zero box for an empty anchor list.

## Parameters

| Parameter | Type |
| ------ | ------ |
| `anchors` | readonly [`PenAnchor`](../interfaces/PenAnchor)[] |

## Returns

[`PenBounds`](../interfaces/PenBounds)
