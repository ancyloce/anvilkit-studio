---
title: "buildPathD"
description: "@anvilkit/canvas-editor · buildPathD"
---

```ts
function buildPathD(
   anchors, 
   closed, 
   ox?, 
   oy?): string;
```

Defined in: [packages/capabilities/canvas/editor/src/tools/pen-geometry.ts:52](https://github.com/ancyloce/anvilkit-studio/blob/main/src/tools/pen-geometry.ts#L52)

Build SVG path data from pen anchors, translated by `(-ox, -oy)` so the path
is local to its bounds. A segment whose adjoining handles sit on their anchors
is emitted as a straight `L`; otherwise as a cubic `C` (prev outgoing handle,
next mirrored incoming handle). When `closed`, a final segment returns to the
first anchor followed by `Z`.

## Parameters

| Parameter | Type | Default value |
| ------ | ------ | ------ |
| `anchors` | readonly [`PenAnchor`](../interfaces/PenAnchor)[] | `undefined` |
| `closed` | `boolean` | `undefined` |
| `ox` | `number` | `0` |
| `oy` | `number` | `0` |

## Returns

`string`
