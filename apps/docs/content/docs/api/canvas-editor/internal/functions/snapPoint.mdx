---
title: "snapPoint"
description: "@anvilkit/canvas-editor · snapPoint"
---

```ts
function snapPoint(
   ctx, 
   point, 
   excludeIds?): PointSnapResult;
```

Defined in: [packages/capabilities/canvas/editor/src/tools/draw-snap.ts:20](https://github.com/ancyloce/anvilkit-studio/blob/main/src/tools/draw-snap.ts#L20)

Snap a single point (used by draw tools — width/height = 0 collapses every
candidate edge to the same coord). Honors viewportStore's snap-to-grid +
object-snap toggles and its snap threshold; grid snap is INDEPENDENT of
grid visibility (`gridEnabled`) per FR-112. Pass `excludeIds` to ignore
specific nodes (e.g. the node being dragged in select mode).

## Parameters

| Parameter | Type |
| ------ | ------ |
| `ctx` | [`ToolContext`](../interfaces/ToolContext) |
| `point` | \{ `x`: `number`; `y`: `number`; \} |
| `point.x` | `number` |
| `point.y` | `number` |
| `excludeIds` | `ReadonlySet`\&lt;`string`\&gt; |

## Returns

`PointSnapResult`
