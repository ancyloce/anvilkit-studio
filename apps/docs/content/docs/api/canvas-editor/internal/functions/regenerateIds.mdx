---
title: "regenerateIds"
description: "@anvilkit/canvas-editor · regenerateIds"
---

```ts
function regenerateIds(node): CanvasNode;
```

Defined in: [packages/canvas/editor/src/pages/clone-page.ts:24](https://github.com/ancyloce/anvilkit-studio/blob/main/src/pages/clone-page.ts#L24)

Walk a structurally-cloned (JSON-roundtripped) node tree and rewrite every
`id` field with a fresh uuid. Recurses into `group.children`. Exported
separately so tests can verify id regeneration in isolation.

## Parameters

| Parameter | Type |
| ------ | ------ |
| `node` | `CanvasNode` |

## Returns

`CanvasNode`
