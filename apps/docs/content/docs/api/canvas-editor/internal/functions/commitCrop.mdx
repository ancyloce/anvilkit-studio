---
title: "commitCrop"
description: "@anvilkit/canvas-editor · commitCrop"
---

```ts
function commitCrop(ctx): void;
```

Defined in: [packages/canvas/editor/src/selection/crop-actions.ts:92](https://github.com/ancyloce/anvilkit-studio/blob/main/src/selection/crop-actions.ts#L92)

Commit the in-progress crop draft as a single `node.update` and close the
editor. A zero-area draft is discarded (treated as a cancel) so an empty drag
never writes a degenerate crop.

## Parameters

| Parameter | Type |
| ------ | ------ |
| `ctx` | [`CanvasStudioContextValue`](../../index/interfaces/CanvasStudioContextValue) |

## Returns

`void`
