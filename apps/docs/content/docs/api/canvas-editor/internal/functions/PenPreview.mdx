---
title: "PenPreview"
description: "@anvilkit/canvas-editor · PenPreview"
---

```ts
function PenPreview(): Element | null;
```

Defined in: [packages/canvas/editor/src/tools/PenPreview.tsx:17](https://github.com/ancyloce/anvilkit-studio/blob/main/src/tools/PenPreview.tsx#L17)

Live on-stage preview of the in-progress pen path (I3-2): the open-path curve
plus a dot at each placed anchor. Rendered in the selection layer; purely
visual (`listening={false}`), so it never intercepts pointer events.

## Returns

`Element` \| `null`
