---
title: "validatePageRootProps"
description: "@anvilkit/validator · validatePageRootProps"
---

```ts
function validatePageRootProps(rootProps): ValidationResult;
```

Defined in: [validator/src/validate-page-payload.ts:54](https://github.com/ancyloce/anvilkit-studio/blob/main/packages/foundation/validator/src/validate-page-payload.ts#L54)

Pure, isomorphic gateway that validates a page's `root.props` against the
canonical PageRootSchema from `@anvilkit/schema`. This is the
narrow, metadata-only check used by draft saves and settings edits.

Carries no React import; runs in Node / Edge / browser.

## Parameters

| Parameter | Type |
| ------ | ------ |
| `rootProps` | `unknown` |

## Returns

[`ValidationResult`](../interfaces/ValidationResult)
