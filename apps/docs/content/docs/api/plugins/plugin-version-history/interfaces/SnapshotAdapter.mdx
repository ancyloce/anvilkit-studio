---
title: "SnapshotAdapter"
description: "@anvilkit/plugin-version-history · SnapshotAdapter"
---

Defined in: [types/types.ts:255](https://github.com/ancyloce/anvilkit-studio/blob/main/src/types/types.ts#L255)

Host-provided storage backend for snapshots — the single integration seam
between this plugin and a persistence layer (in-memory, localStorage, Yjs,
a REST API, …). `save`/`list`/`load` are the required core; everything else
is optional and must be feature-detected by callers, so older adapters keep
working unchanged. Methods may be sync or async.

## Properties

| Property | Modifier | Type | Description | Defined in |
| ------ | ------ | ------ | ------ | ------ |
| &lt;a id="delete"&gt;&lt;/a&gt; `delete?` | `readonly` | (`id`) =&gt; [`MaybePromise`](../type-aliases/MaybePromise)\&lt;`void`\&gt; | Optional single delete. Removes the snapshot `id` while keeping the rest loadable (adapters re-root any deltas that chained onto it). Additive and backward compatible — feature-detect it. | [types/types.ts:285](https://github.com/ancyloce/anvilkit-studio/blob/main/src/types/types.ts#L285) |
| &lt;a id="deletemany"&gt;&lt;/a&gt; `deleteMany?` | `readonly` | (`ids`) =&gt; [`MaybePromise`](../type-aliases/MaybePromise)\&lt;`void`\&gt; | Optional batch delete for administrative cleanup. Removes every id in `ids` (unknown ids are ignored) while leaving the remaining snapshots loadable. Additive and fully backward compatible — adapters that omit it are unaffected and callers must feature-detect it. Equivalent in effect to calling `delete` per id, but adapters can implement it more efficiently (the reference localStorage adapter does a single index write). | [types/types.ts:295](https://github.com/ancyloce/anvilkit-studio/blob/main/src/types/types.ts#L295) |
| &lt;a id="exportall"&gt;&lt;/a&gt; `exportAll?` | `readonly` | () =&gt; [`MaybePromise`](../type-aliases/MaybePromise)\&lt;[`VersionHistoryExport`](VersionHistoryExport.md)\&gt; | Optional portability export. Materializes every snapshot's full `PageIR` (reconstructing the internal delta chain via `load`) into a self-contained [VersionHistoryExport](VersionHistoryExport.md) archive in deterministic save order. Additive/optional — feature-detect it. Pairs with [importAll](#importall). | [types/types.ts:303](https://github.com/ancyloce/anvilkit-studio/blob/main/src/types/types.ts#L303) |
| &lt;a id="importall"&gt;&lt;/a&gt; `importAll?` | `readonly` | (`data`, `options?`) =&gt; [`MaybePromise`](../type-aliases/MaybePromise)\&lt;`void`\&gt; | Optional portability import. Restores snapshots from a [VersionHistoryExport](VersionHistoryExport.md) produced by [exportAll](#exportall). The default `"merge"` mode adds/overwrites by id; `"replace"` clears the store first. Imported snapshots preserve their original ids/metadata and are stored as standalone keyframes, so each is immediately loadable. Additive/optional — feature-detect it. Throws a `STORAGE_CORRUPT` [VersionHistoryError](../classes/VersionHistoryError) (via `normalizeVersionHistoryExport`) when the archive is malformed. | [types/types.ts:313](https://github.com/ancyloce/anvilkit-studio/blob/main/src/types/types.ts#L313) |
| &lt;a id="list"&gt;&lt;/a&gt; `list` | `readonly` | () =&gt; [`MaybePromise`](../type-aliases/MaybePromise)\&lt;readonly [`SnapshotMeta`](SnapshotMeta.md)[]\&gt; | Return every snapshot's metadata in canonical save order. | [types/types.ts:266](https://github.com/ancyloce/anvilkit-studio/blob/main/src/types/types.ts#L266) |
| &lt;a id="load"&gt;&lt;/a&gt; `load` | `readonly` | (`id`) =&gt; [`MaybePromise`](../type-aliases/MaybePromise)\&lt;`PageIR`\&gt; | Materialize and return a snapshot's full `PageIR` by id, reconstructing it from the internal delta chain if needed. Throws a `SNAPSHOT_NOT_FOUND` [VersionHistoryError](../classes/VersionHistoryError) for an unknown id. | [types/types.ts:279](https://github.com/ancyloce/anvilkit-studio/blob/main/src/types/types.ts#L279) |
| &lt;a id="presence"&gt;&lt;/a&gt; `presence?` | `readonly` | [`SnapshotAdapterPresence`](SnapshotAdapterPresence.md) | Optional presence channel for live cursors / selections, **owned by the host / a collaborative adapter — not by the bundled reference adapters**. The in-memory and localStorage reference adapters have no multiplayer awareness and intentionally do not implement it; a real collaboration transport (e.g. a Yjs adapter such as `@anvilkit/plugin-collab-yjs`) supplies it. Callers must feature-detect it. See [SnapshotAdapterPresence](SnapshotAdapterPresence.md) for the channel contract. | [types/types.ts:357](https://github.com/ancyloce/anvilkit-studio/blob/main/src/types/types.ts#L357) |
| &lt;a id="query"&gt;&lt;/a&gt; `query?` | `readonly` | (`options`) =&gt; [`MaybePromise`](../type-aliases/MaybePromise)\&lt;[`SnapshotPage`](SnapshotPage.md)\&gt; | Optional paginated/filtered/sorted view over the same metadata `list()` returns. Additive and fully backward compatible — adapters that omit it are unaffected, and callers must feature-detect it. Reference adapters implement it by delegating to `querySnapshots` from `./utils/query.js`. | [types/types.ts:273](https://github.com/ancyloce/anvilkit-studio/blob/main/src/types/types.ts#L273) |
| &lt;a id="save"&gt;&lt;/a&gt; `save` | `readonly` | (`ir`, `meta`) =&gt; [`MaybePromise`](../type-aliases/MaybePromise)\&lt;`string`\&gt; | Persist `ir` as a new snapshot and resolve to its newly assigned id. The adapter owns `id`/`savedAt` (hence they are omitted from the meta argument) and may store the IR as a delta against earlier snapshots. | [types/types.ts:261](https://github.com/ancyloce/anvilkit-studio/blob/main/src/types/types.ts#L261) |
| &lt;a id="subscribe"&gt;&lt;/a&gt; `subscribe?` | `readonly` | (`onUpdate`) =&gt; [`Unsubscribe`](../type-aliases/Unsubscribe) | Optional change subscription, **owned by the host / a collaborative adapter — not by the bundled reference adapters**. The in-memory and localStorage reference adapters are single-user and intentionally do not implement it; a real collaboration transport (e.g. a Yjs adapter such as `@anvilkit/plugin-collab-yjs`) supplies it. Callers must feature-detect it. When implemented, `onUpdate` fires whenever a remote peer mutates the shared document (with the new `ir` and the originating `peer` when known); the panel drops its load cache and re-lists so the diff view never shows a stale snapshot. Returns an [Unsubscribe](../type-aliases/Unsubscribe); call it to stop receiving updates (idempotent — calling it again after the first time is a no-op). | [types/types.ts:345](https://github.com/ancyloce/anvilkit-studio/blob/main/src/types/types.ts#L345) |
| &lt;a id="updatemeta"&gt;&lt;/a&gt; `updateMeta?` | `readonly` | (`id`, `patch`) =&gt; [`MaybePromise`](../type-aliases/MaybePromise)\&lt;`void`\&gt; | Optional metadata update. Patches the mutable, host-curated fields of a stored snapshot's metadata (the [SnapshotMetaPatch](SnapshotMetaPatch.md) subset — label/tags/milestone/protected/author/notes) in place, leaving its `PageIR`/delta-chain record and identity fields (`id`/`savedAt`/ `pageIRHash`/`delta`) untouched. Omitted patch fields are left unchanged. Additive and fully backward compatible — adapters that omit it are unaffected and callers must feature-detect it. Throws a `SNAPSHOT_NOT_FOUND` [VersionHistoryError](../classes/VersionHistoryError) for an unknown id. | [types/types.ts:328](https://github.com/ancyloce/anvilkit-studio/blob/main/src/types/types.ts#L328) |
