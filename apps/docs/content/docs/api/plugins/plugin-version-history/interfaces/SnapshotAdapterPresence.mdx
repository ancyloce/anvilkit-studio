---
title: "SnapshotAdapterPresence"
description: "@anvilkit/plugin-version-history · SnapshotAdapterPresence"
---

Defined in: [types/types.ts:236](https://github.com/ancyloce/anvilkit-studio/blob/main/src/types/types.ts#L236)

Presence channel a collaborative adapter exposes for live cursors /
selections.

Optional and **host / collaboration-adapter owned**: the bundled reference
adapters (in-memory, localStorage) are single-user and intentionally do not
implement it. Only a real multiplayer transport — e.g. a Yjs adapter such as
`@anvilkit/plugin-collab-yjs` (`YjsSnapshotAdapter`) — provides it, so this
interface lives here as the shared contract that collaborative adapters
implement, not something the reference adapters supply.

## Methods

### onPeerChange()

```ts
onPeerChange(callback): Unsubscribe;
```

Defined in: [types/types.ts:243](https://github.com/ancyloce/anvilkit-studio/blob/main/src/types/types.ts#L243)

Subscribe to the roster of remote peers' presence; `callback` fires with
the full set whenever it changes. Returns an [Unsubscribe](../type-aliases/Unsubscribe).

#### Parameters

| Parameter | Type |
| ------ | ------ |
| `callback` | (`peers`) =&gt; `void` |

#### Returns

[`Unsubscribe`](../type-aliases/Unsubscribe)

***

### update()

```ts
update(state): void;
```

Defined in: [types/types.ts:238](https://github.com/ancyloce/anvilkit-studio/blob/main/src/types/types.ts#L238)

Publish the local peer's presence to other collaborators.

#### Parameters

| Parameter | Type |
| ------ | ------ |
| `state` | [`PresenceState`](PresenceState.md) |

#### Returns

`void`
