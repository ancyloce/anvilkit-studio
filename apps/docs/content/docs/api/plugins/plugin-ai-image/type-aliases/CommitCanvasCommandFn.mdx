---
title: "CommitCanvasCommandFn"
description: "@anvilkit/plugin-ai-image · CommitCanvasCommandFn"
---

```ts
type CommitCanvasCommandFn<T> = (cmd) => T;
```

Defined in: [extensions/plugins/plugin-ai-image/src/commit/commit-image-replace.ts:21](https://github.com/ancyloce/anvilkit-studio/blob/main/src/commit/commit-image-replace.ts#L21)

Applies a canvas command, returning whatever the host's history store yields
(typically the next `CanvasIR`, or a `CommandApplyResult`). Injected so this
package stays free of `@anvilkit/canvas-editor`.

## Type Parameters

| Type Parameter | Default type |
| ------ | ------ |
| `T` | `unknown` |

## Parameters

| Parameter | Type |
| ------ | ------ |
| `cmd` | `CanvasImageReplaceCommand` |

## Returns

`T`
