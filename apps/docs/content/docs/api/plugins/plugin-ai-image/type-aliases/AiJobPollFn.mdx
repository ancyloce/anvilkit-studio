---
title: "AiJobPollFn"
description: "@anvilkit/plugin-ai-image · AiJobPollFn"
---

```ts
type AiJobPollFn<TResult> = (jobId, options?) => Promise<TResult>;
```

Defined in: [extensions/plugins/plugin-ai-image/src/job/ai-job-client.ts:44](https://github.com/ancyloce/anvilkit-studio/blob/main/src/job/ai-job-client.ts#L44)

Polls a previously-submitted job for its latest status. Supplied by
hosts that run AI work as an async background job; omitted for the
synchronous (and mock) path, where the first provider result is
already terminal.

## Type Parameters

| Type Parameter | Default type |
| ------ | ------ |
| `TResult` *extends* [`AiJobResultLike`](../interfaces/AiJobResultLike) | [`AiImageJobResult`](AiImageJobResult.md) |

## Parameters

| Parameter | Type |
| ------ | ------ |
| `jobId` | `string` |
| `options?` | \{ `signal?`: `AbortSignal`; \} |
| `options.signal?` | `AbortSignal` |

## Returns

`Promise`\<`TResult`\>
