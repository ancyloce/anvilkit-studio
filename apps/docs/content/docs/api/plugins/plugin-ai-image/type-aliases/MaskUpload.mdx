---
title: "MaskUpload"
description: "@anvilkit/plugin-ai-image · MaskUpload"
---

```ts
type MaskUpload = (file) => Promise<MaskUploadResult>;
```

Defined in: [extensions/plugins/plugin-ai-image/src/mask/exporter.ts:130](https://github.com/ancyloce/anvilkit-studio/blob/main/src/mask/exporter.ts#L130)

Injected uploader. The host wraps `plugin-asset-manager`'s
`uploadAsset(ctx, file)` (whose `UploadResult` satisfies `MaskUploadResult`).

## Parameters

| Parameter | Type |
| ------ | ------ |
| `file` | `File` |

## Returns

`Promise`\<[`MaskUploadResult`](../interfaces/MaskUploadResult)\>
