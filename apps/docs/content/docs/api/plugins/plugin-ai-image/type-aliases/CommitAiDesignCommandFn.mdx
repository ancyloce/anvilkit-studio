---
title: "CommitAiDesignCommandFn"
description: "@anvilkit/plugin-ai-image · CommitAiDesignCommandFn"
---

```ts
type CommitAiDesignCommandFn<T> = (cmd) => T;
```

Defined in: extensions/plugins/plugin-ai-image/src/commit/commit-ai-design-result.ts:25

Commits a canvas command, returning whatever the host's history store
yields (typically the next `CanvasIR`, or a `CommandApplyResult`).
Injected so this package stays free of `@anvilkit/canvas-editor`.

## Type Parameters

| Type Parameter | Default type |
| ------ | ------ |
| `T` | `unknown` |

## Parameters

| Parameter | Type |
| ------ | ------ |
| `cmd` | `CanvasBatchCommand` |

## Returns

`T`
