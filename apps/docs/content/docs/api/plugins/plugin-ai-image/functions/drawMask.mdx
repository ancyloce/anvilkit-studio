---
title: "drawMask"
description: "@anvilkit/plugin-ai-image · drawMask"
---

```ts
function drawMask(
   ctx, 
   strokes, 
   opts?): void;
```

Defined in: [extensions/plugins/plugin-ai-image/src/mask/exporter.ts:26](https://github.com/ancyloce/anvilkit-studio/blob/main/src/mask/exporter.ts#L26)

Paint `strokes` onto a 2D context with round caps/joins. The canvas is
assumed transparent on entry, so the result is white strokes over alpha — a
standard inpaint mask. Pure aside from the `ctx` mutations.

## Parameters

| Parameter | Type |
| ------ | ------ |
| `ctx` | `CanvasRenderingContext2D` |
| `strokes` | readonly [`MaskStroke`](../interfaces/MaskStroke)[] |
| `opts` | [`DrawMaskOptions`](../interfaces/DrawMaskOptions) |

## Returns

`void`
