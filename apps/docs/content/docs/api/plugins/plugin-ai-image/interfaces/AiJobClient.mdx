---
title: "AiJobClient"
description: "@anvilkit/plugin-ai-image · AiJobClient"
---

Defined in: [extensions/plugins/plugin-ai-image/src/job/ai-job-client.ts:107](https://github.com/ancyloce/anvilkit-studio/blob/main/src/job/ai-job-client.ts#L107)

## Type Parameters

| Type Parameter | Default type |
| ------ | ------ |
| `TRequest` | [`AiImageJobRequest`](../type-aliases/AiImageJobRequest) |
| `TResult` *extends* [`AiJobResultLike`](AiJobResultLike.md) | [`AiImageJobResult`](../type-aliases/AiImageJobResult) |
| `TContext` | [`AiLayerContext`](AiLayerContext.md) |

## Methods

### run()

```ts
run(
   request, 
   context, 
options?): Promise<TResult>;
```

Defined in: [extensions/plugins/plugin-ai-image/src/job/ai-job-client.ts:121](https://github.com/ancyloce/anvilkit-studio/blob/main/src/job/ai-job-client.ts#L121)

Submit a job and resolve to its terminal result.

- Transient failures (provider throws `RetryableError`) are retried
  with full-jitter backoff.
- Aborting `options.signal` resolves to a `cancelled` result.
- A non-terminal result is polled to terminal when `poll` is set.
- Non-retryable thrown errors propagate (reject).

#### Parameters

| Parameter | Type |
| ------ | ------ |
| `request` | `TRequest` |
| `context` | `TContext` |
| `options?` | [`AiJobRunOptions`](AiJobRunOptions.md) |

#### Returns

`Promise`\<`TResult`\>
