---
title: "AiJobClient"
description: "@anvilkit/plugin-ai-image · AiJobClient"
---

Defined in: [extensions/plugins/plugin-ai-image/src/job/ai-job-client.ts:79](https://github.com/ancyloce/anvilkit-studio/blob/main/src/job/ai-job-client.ts#L79)

## Methods

### run()

```ts
run(
   request, 
   context, 
options?): Promise<AiImageJobResult>;
```

Defined in: [extensions/plugins/plugin-ai-image/src/job/ai-job-client.ts:89](https://github.com/ancyloce/anvilkit-studio/blob/main/src/job/ai-job-client.ts#L89)

Submit a job and resolve to its terminal [AiImageJobResult](AiImageJobResult.md).

- Transient failures (provider throws `RetryableError`) are retried
  with full-jitter backoff.
- Aborting `options.signal` resolves to a `cancelled` result.
- A non-terminal result is polled to terminal when `poll` is set.
- Non-retryable thrown errors propagate (reject).

#### Parameters

| Parameter | Type |
| ------ | ------ |
| `request` | [`AiImageJobRequest`](../type-aliases/AiImageJobRequest) |
| `context` | [`AiLayerContext`](AiLayerContext.md) |
| `options?` | [`AiJobRunOptions`](AiJobRunOptions.md) |

#### Returns

`Promise`\<[`AiImageJobResult`](AiImageJobResult.md)\>
