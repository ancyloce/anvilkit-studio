---
title: "hashIR"
description: "@anvilkit/plugin-collab-yjs · hashIR"
---

```ts
function hashIR(raw): string;
```

Defined in: [plugin-collab-yjs/src/utils/encode.ts:56](https://github.com/ancyloce/anvilkit-studio/blob/main/src/utils/encode.ts#L56)

Lightweight deterministic hash for SnapshotMeta.pageIRHash.
Not cryptographic — just stable across replicas given the same input.

## Parameters

| Parameter | Type |
| ------ | ------ |
| `raw` | `string` |

## Returns

`string`
