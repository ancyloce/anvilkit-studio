---
title: "YjsSnapshotAdapter"
description: "@anvilkit/plugin-collab-yjs · YjsSnapshotAdapter"
---

Defined in: [plugin-collab-yjs/src/types/types.ts:692](https://github.com/ancyloce/anvilkit-studio/blob/main/src/types/types.ts#L692)

Yjs-specific extension of `SnapshotAdapter` that exposes the
conflict-diagnostics event surface, the connection-state contract,
and the force-resync action. `createYjsAdapter` returns this narrow
type so hosts can subscribe to overlap events and render sync UI
without dropping into Yjs internals.

## Extends

- `SnapshotAdapter`.[`UndoController`](UndoController.md)

## Properties

| Property | Modifier | Type | Description | Overrides | Inherited from | Defined in |
| ------ | ------ | ------ | ------ | ------ | ------ | ------ |
| &lt;a id="canredo"&gt;&lt;/a&gt; `canRedo` | `readonly` | () =&gt; `boolean` | `true` when there is at least one undone change to redo. | - | [`UndoController`](UndoController.md).[`canRedo`](UndoController.md#canredo) | [plugin-collab-yjs/src/types/types.ts:271](https://github.com/ancyloce/anvilkit-studio/blob/main/src/types/types.ts#L271) |
| &lt;a id="canundo"&gt;&lt;/a&gt; `canUndo` | `readonly` | () =&gt; `boolean` | `true` when there is at least one local change to undo. | - | [`UndoController`](UndoController.md).[`canUndo`](UndoController.md#canundo) | [plugin-collab-yjs/src/types/types.ts:269](https://github.com/ancyloce/anvilkit-studio/blob/main/src/types/types.ts#L269) |
| &lt;a id="clearundo"&gt;&lt;/a&gt; `clearUndo` | `readonly` | () =&gt; `void` | Drop every tracked undo/redo step. Hosts call this when switching the live document (e.g. loading a different page) so a later `undo()` cannot bleed the previous document's edits into the new one. | - | [`UndoController`](UndoController.md).[`clearUndo`](UndoController.md#clearundo) | [plugin-collab-yjs/src/types/types.ts:278](https://github.com/ancyloce/anvilkit-studio/blob/main/src/types/types.ts#L278) |
| &lt;a id="delete"&gt;&lt;/a&gt; `delete?` | `readonly` | (`id`) =&gt; `MaybePromise`\&lt;`void`\&gt; | Optional single delete. Removes the snapshot `id` while keeping the rest loadable (adapters re-root any deltas that chained onto it). Additive and backward compatible — feature-detect it. | - | `SnapshotAdapter.delete` | plugin-version-history/dist/types/types.d.ts:263 |
| &lt;a id="deletemany"&gt;&lt;/a&gt; `deleteMany?` | `readonly` | (`ids`) =&gt; `MaybePromise`\&lt;`void`\&gt; | Optional batch delete for administrative cleanup. Removes every id in `ids` (unknown ids are ignored) while leaving the remaining snapshots loadable. Additive and fully backward compatible — adapters that omit it are unaffected and callers must feature-detect it. Equivalent in effect to calling `delete` per id, but adapters can implement it more efficiently (the reference localStorage adapter does a single index write). | - | `SnapshotAdapter.deleteMany` | plugin-version-history/dist/types/types.d.ts:273 |
| &lt;a id="destroy"&gt;&lt;/a&gt; `destroy` | `readonly` | () =&gt; `void` | Release internal subscriptions (the optional `connectionSource` tear-down, status/conflict/subscribe listener sets). Hosts using `createCollabPlugin` do not need to call this directly — the Studio plugin's `onDestroy` invokes it automatically. | - | - | [plugin-collab-yjs/src/types/types.ts:767](https://github.com/ancyloce/anvilkit-studio/blob/main/src/types/types.ts#L767) |
| &lt;a id="exportall"&gt;&lt;/a&gt; `exportAll?` | `readonly` | () =&gt; `MaybePromise`\&lt;`VersionHistoryExport`\&gt; | Optional portability export. Materializes every snapshot's full `PageIR` (reconstructing the internal delta chain via `load`) into a self-contained VersionHistoryExport archive in deterministic save order. Additive/optional — feature-detect it. Pairs with [importAll](#importall). | - | `SnapshotAdapter.exportAll` | plugin-version-history/dist/types/types.d.ts:281 |
| &lt;a id="forceresync"&gt;&lt;/a&gt; `forceResync` | `readonly` | () =&gt; `Promise`\&lt;`PageIR` \| `null`\&gt; | Discard unsaved local edits and re-emit the latest authoritative snapshot through `subscribe`. Wired into `<ForceResyncDialog />` when the host is using the `@anvilkit/collab-ui` package. If no snapshot exists, resolves to `null` and the live IR is left untouched — hosts should disable the action in that case. | - | - | [plugin-collab-yjs/src/types/types.ts:729](https://github.com/ancyloce/anvilkit-studio/blob/main/src/types/types.ts#L729) |
| &lt;a id="getstatus"&gt;&lt;/a&gt; `getStatus` | `readonly` | () =&gt; [`ConnectionStatus`](../type-aliases/ConnectionStatus) | Read the current `ConnectionStatus` synchronously. Useful for non-React hosts; React hosts should prefer `onStatusChange` inside a `useSyncExternalStore` to avoid tearing. | - | - | [plugin-collab-yjs/src/types/types.ts:719](https://github.com/ancyloce/anvilkit-studio/blob/main/src/types/types.ts#L719) |
| &lt;a id="importall"&gt;&lt;/a&gt; `importAll?` | `readonly` | (`data`, `options?`) =&gt; `MaybePromise`\&lt;`void`\&gt; | Optional portability import. Restores snapshots from a VersionHistoryExport produced by [exportAll](#exportall). The default `"merge"` mode adds/overwrites by id; `"replace"` clears the store first. Imported snapshots preserve their original ids/metadata and are stored as standalone keyframes, so each is immediately loadable. Additive/optional — feature-detect it. Throws a `STORAGE_CORRUPT` VersionHistoryError (via `normalizeVersionHistoryExport`) when the archive is malformed. | - | `SnapshotAdapter.importAll` | plugin-version-history/dist/types/types.d.ts:291 |
| &lt;a id="incinboundcoalesced"&gt;&lt;/a&gt; `incInboundCoalesced?` | `readonly` | (`n`) =&gt; `void` | Internal-but-stable counter sink (H1). The Studio plugin reports the number of inbound remote IRs the coalescing scheduler dropped here so it surfaces in `metrics().inboundCoalesced`. | - | - | [plugin-collab-yjs/src/types/types.ts:760](https://github.com/ancyloce/anvilkit-studio/blob/main/src/types/types.ts#L760) |
| &lt;a id="list"&gt;&lt;/a&gt; `list` | `readonly` | () =&gt; `MaybePromise`\&lt;readonly `SnapshotMeta`[]\&gt; | Return every snapshot's metadata in canonical save order. | - | `SnapshotAdapter.list` | plugin-version-history/dist/types/types.d.ts:244 |
| &lt;a id="load"&gt;&lt;/a&gt; `load` | `readonly` | (`id`) =&gt; `MaybePromise`\&lt;`PageIR`\&gt; | Materialize and return a snapshot's full `PageIR` by id, reconstructing it from the internal delta chain if needed. Throws a `SNAPSHOT_NOT_FOUND` VersionHistoryError for an unknown id. | - | `SnapshotAdapter.load` | plugin-version-history/dist/types/types.d.ts:257 |
| &lt;a id="loadpersistedsnapshot"&gt;&lt;/a&gt; `loadPersistedSnapshot` | `readonly` | (`id`) =&gt; `Promise`\&lt;`PageIR` \| `undefined`\&gt; | §4.2.2 — hydrate a snapshot's `PageIR` from the optional [CreateYjsAdapterOptions.snapshotPersistence](CreateYjsAdapterOptions.md#snapshotpersistence) backend, applying the configured `decode` transform. Resolves to `undefined` when no `snapshotPersistence` adapter was supplied or the backend has no record of `id`. Lets a host bootstrap from the durable server-grade store independently of the bounded in-`Y.Doc` snapshot window (which `load()` reads). | - | - | [plugin-collab-yjs/src/types/types.ts:739](https://github.com/ancyloce/anvilkit-studio/blob/main/src/types/types.ts#L739) |
| &lt;a id="metrics"&gt;&lt;/a&gt; `metrics` | `readonly` | () =&gt; [`MetricsSnapshot`](MetricsSnapshot.md) | Phase 3 (D10) observability snapshot. Cheap to call — it copies the latency window into a sorted scratch array on each invocation, which is fine at the host's polling cadence (seconds, not frames). Hosts stream the snapshot to their telemetry sink. | - | - | [plugin-collab-yjs/src/types/types.ts:746](https://github.com/ancyloce/anvilkit-studio/blob/main/src/types/types.ts#L746) |
| &lt;a id="onconflict"&gt;&lt;/a&gt; `onConflict` | `readonly` | (`callback`) =&gt; `Unsubscribe` | - | - | - | [plugin-collab-yjs/src/types/types.ts:702](https://github.com/ancyloce/anvilkit-studio/blob/main/src/types/types.ts#L702) |
| &lt;a id="onstatuschange"&gt;&lt;/a&gt; `onStatusChange` | `readonly` | (`callback`) =&gt; `Unsubscribe` | Subscribe to `ConnectionStatus` transitions. The callback is invoked synchronously with the current status on registration so hosts can paint an initial state without waiting for the next transport event. | - | - | [plugin-collab-yjs/src/types/types.ts:711](https://github.com/ancyloce/anvilkit-studio/blob/main/src/types/types.ts#L711) |
| &lt;a id="onundostackchange"&gt;&lt;/a&gt; `onUndoStackChange` | `readonly` | (`callback`) =&gt; `Unsubscribe` | Subscribe to undo/redo stack mutations (item added on a tracked local edit, item popped on undo/redo, stack cleared). The callback takes no arguments — hosts re-read `canUndo()`/`canRedo()` to drive toolbar enabled-state, typically via `useSyncExternalStore`. Returns an unsubscribe. | - | [`UndoController`](UndoController.md).[`onUndoStackChange`](UndoController.md#onundostackchange) | [plugin-collab-yjs/src/types/types.ts:286](https://github.com/ancyloce/anvilkit-studio/blob/main/src/types/types.ts#L286) |
| &lt;a id="presence"&gt;&lt;/a&gt; `presence?` | `readonly` | [`RichSnapshotAdapterPresence`](RichSnapshotAdapterPresence.md) | §4.2.5 — narrowed override of SnapshotAdapter.presence. The runtime object is the same awareness bridge, but typed as [RichSnapshotAdapterPresence](RichSnapshotAdapterPresence.md) so hosts can `update` and read (`onPeerChange`) the versioned multi-select / viewport / focused-block / typing fields with full type-checking, not just the base cursor + selection contract. | `SnapshotAdapter.presence` | - | [plugin-collab-yjs/src/types/types.ts:701](https://github.com/ancyloce/anvilkit-studio/blob/main/src/types/types.ts#L701) |
| &lt;a id="query"&gt;&lt;/a&gt; `query?` | `readonly` | (`options`) =&gt; `MaybePromise`\&lt;`SnapshotPage`\&gt; | Optional paginated/filtered/sorted view over the same metadata `list()` returns. Additive and fully backward compatible — adapters that omit it are unaffected, and callers must feature-detect it. Reference adapters implement it by delegating to `querySnapshots` from `./utils/query.js`. | - | `SnapshotAdapter.query` | plugin-version-history/dist/types/types.d.ts:251 |
| &lt;a id="recordtiming"&gt;&lt;/a&gt; `recordTiming?` | `readonly` | (`kind`, `ms`) =&gt; `void` | Internal-but-stable hot-path timing sink (P1). The Studio plugin records inbound-queue-delay, IR&lt;-&gt;Puck conversion, and Puck dispatch durations here so they surface in `metrics()` alongside the adapter-side encode/apply/read timings. Feature-detected by the plugin — a non-Yjs adapter without it simply skips timings. | - | - | [plugin-collab-yjs/src/types/types.ts:754](https://github.com/ancyloce/anvilkit-studio/blob/main/src/types/types.ts#L754) |
| &lt;a id="redo"&gt;&lt;/a&gt; `redo` | `readonly` | () =&gt; `void` | Redo the most recently undone change. No-op when the redo stack is empty. | - | [`UndoController`](UndoController.md).[`redo`](UndoController.md#redo) | [plugin-collab-yjs/src/types/types.ts:267](https://github.com/ancyloce/anvilkit-studio/blob/main/src/types/types.ts#L267) |
| &lt;a id="save"&gt;&lt;/a&gt; `save` | `readonly` | (`ir`, `meta`) =&gt; `MaybePromise`\&lt;`string`\&gt; | Persist `ir` as a new snapshot and resolve to its newly assigned id. The adapter owns `id`/`savedAt` (hence they are omitted from the meta argument) and may store the IR as a delta against earlier snapshots. | - | `SnapshotAdapter.save` | plugin-version-history/dist/types/types.d.ts:242 |
| &lt;a id="subscribe"&gt;&lt;/a&gt; `subscribe?` | `readonly` | (`onUpdate`) =&gt; `Unsubscribe` | Optional change subscription, **owned by the host / a collaborative adapter — not by the bundled reference adapters**. The in-memory and localStorage reference adapters are single-user and intentionally do not implement it; a real collaboration transport (e.g. a Yjs adapter such as `@anvilkit/plugin-collab-yjs`) supplies it. Callers must feature-detect it. When implemented, `onUpdate` fires whenever a remote peer mutates the shared document (with the new `ir` and the originating `peer` when known); the panel drops its load cache and re-lists so the diff view never shows a stale snapshot. Returns an Unsubscribe; call it to stop receiving updates (idempotent — calling it again after the first time is a no-op). | - | `SnapshotAdapter.subscribe` | plugin-version-history/dist/types/types.d.ts:317 |
| &lt;a id="undo"&gt;&lt;/a&gt; `undo` | `readonly` | () =&gt; `void` | Undo the most recent locally-tracked change. No-op when nothing is tracked. | - | [`UndoController`](UndoController.md).[`undo`](UndoController.md#undo) | [plugin-collab-yjs/src/types/types.ts:265](https://github.com/ancyloce/anvilkit-studio/blob/main/src/types/types.ts#L265) |
| &lt;a id="updatemeta"&gt;&lt;/a&gt; `updateMeta?` | `readonly` | (`id`, `patch`) =&gt; `MaybePromise`\&lt;`void`\&gt; | Optional metadata update. Patches the mutable, host-curated fields of a stored snapshot's metadata (the SnapshotMetaPatch subset — label/tags/milestone/protected/author/notes) in place, leaving its `PageIR`/delta-chain record and identity fields (`id`/`savedAt`/ `pageIRHash`/`delta`) untouched. Omitted patch fields are left unchanged. Additive and fully backward compatible — adapters that omit it are unaffected and callers must feature-detect it. Throws a `SNAPSHOT_NOT_FOUND` VersionHistoryError for an unknown id. | - | `SnapshotAdapter.updateMeta` | plugin-version-history/dist/types/types.d.ts:303 |
