---
title: "CreateCollabPluginOptions"
description: "@anvilkit/plugin-collab-yjs · CreateCollabPluginOptions"
---

Defined in: [plugin-collab-yjs/src/types/types.ts:891](https://github.com/ancyloce/anvilkit-studio/blob/main/src/types/types.ts#L891)

## Properties

| Property | Modifier | Type | Description | Defined in |
| ------ | ------ | ------ | ------ | ------ |
| &lt;a id="adapter"&gt;&lt;/a&gt; `adapter` | `readonly` | `SnapshotAdapter` | - | [plugin-collab-yjs/src/types/types.ts:892](https://github.com/ancyloce/anvilkit-studio/blob/main/src/types/types.ts#L892) |
| &lt;a id="inboundbudgetms"&gt;&lt;/a&gt; `inboundBudgetMs?` | `readonly` | `number` | H1 — fallback flush cadence in milliseconds when no `requestAnimationFrame` is available. Default 16. | [plugin-collab-yjs/src/types/types.ts:949](https://github.com/ancyloce/anvilkit-studio/blob/main/src/types/types.ts#L949) |
| &lt;a id="inboundscheduler"&gt;&lt;/a&gt; `inboundScheduler?` | `readonly` | `FrameScheduler` | H1 — override the inbound coalescing scheduler. Defaults to `requestAnimationFrame` (browser editor) or `setTimeout` (SSR/Node). Tests inject a manual scheduler to pump flushes deterministically; do not set this in production. | [plugin-collab-yjs/src/types/types.ts:944](https://github.com/ancyloce/anvilkit-studio/blob/main/src/types/types.ts#L944) |
| &lt;a id="localpeer"&gt;&lt;/a&gt; `localPeer?` | `readonly` | `PeerInfo` | Local peer identity used when checking outbound saves and as a fallback for inbound checks when the remote peer is unknown. If omitted, the plugin mints a per-instance ephemeral id (`local-<uuid>`) and logs a warning — it does NOT fall back to a shared `{ id: "local" }`, which used to collapse multi-peer sessions into one fake user. Provide a stable id in production. | [plugin-collab-yjs/src/types/types.ts:925](https://github.com/ancyloce/anvilkit-studio/blob/main/src/types/types.ts#L925) |
| &lt;a id="logger"&gt;&lt;/a&gt; `logger?` | `readonly` | [`CollabLogger`](../type-aliases/CollabLogger) | Optional logging hook. Routes the deprecated-alias deprecation warning through the host's logger instead of `console.warn`. See [CollabLogger](../type-aliases/CollabLogger). | [plugin-collab-yjs/src/types/types.ts:899](https://github.com/ancyloce/anvilkit-studio/blob/main/src/types/types.ts#L899) |
| &lt;a id="onpolicyviolation"&gt;&lt;/a&gt; `onPolicyViolation?` | `readonly` | (`violation`) =&gt; `void` | Optional callback fired whenever `policy.canEdit` rejects an outbound save or an inbound dispatch. | [plugin-collab-yjs/src/types/types.ts:930](https://github.com/ancyloce/anvilkit-studio/blob/main/src/types/types.ts#L930) |
| &lt;a id="onsaveerror"&gt;&lt;/a&gt; `onSaveError?` | `readonly` | (`error`) =&gt; `void` | Optional callback fired when an outbound `adapter.save(...)` rejects or throws. Without this hook, transport failures surface as `unhandledRejection` warnings with no host visibility — wire it to a toast, telemetry sink, or retry queue. | [plugin-collab-yjs/src/types/types.ts:937](https://github.com/ancyloce/anvilkit-studio/blob/main/src/types/types.ts#L937) |
| &lt;a id="onvalidationfailure"&gt;&lt;/a&gt; `onValidationFailure?` | `readonly` | (`failure`) =&gt; `void` | Optional callback invoked when a remote IR is rejected. Useful for surfacing toasts in host UI. Fires after the warning is logged. | [plugin-collab-yjs/src/types/types.ts:911](https://github.com/ancyloce/anvilkit-studio/blob/main/src/types/types.ts#L911) |
| &lt;a id="policy"&gt;&lt;/a&gt; `policy?` | `readonly` | [`CollabPolicy`](CollabPolicy.md) | Phase 3 (D6) policy bridge. Applied symmetrically inbound and outbound — see [CollabPolicy](CollabPolicy.md) for semantics. | [plugin-collab-yjs/src/types/types.ts:916](https://github.com/ancyloce/anvilkit-studio/blob/main/src/types/types.ts#L916) |
| &lt;a id="puckconfig"&gt;&lt;/a&gt; `puckConfig?` | `readonly` | `Config` | - | [plugin-collab-yjs/src/types/types.ts:893](https://github.com/ancyloce/anvilkit-studio/blob/main/src/types/types.ts#L893) |
| &lt;a id="replacebatchthreshold"&gt;&lt;/a&gt; `replaceBatchThreshold?` | `readonly` | `number` | P1 — at or below this many per-node `replace` actions the plugin dispatches them individually (only the touched components re-render); above it a single `setData` is cheaper than N dispatches. Default `50` (REPLACE\_BATCH\_THRESHOLD). Tune for hosts with very large bulk-edit bursts without a release. | [plugin-collab-yjs/src/types/types.ts:957](https://github.com/ancyloce/anvilkit-studio/blob/main/src/types/types.ts#L957) |
| &lt;a id="validateremoteir"&gt;&lt;/a&gt; `validateRemoteIR?` | `readonly` | [`ValidateRemoteIR`](../type-aliases/ValidateRemoteIR) | Optional hook applied to every remote `PageIR` before dispatch into Puck. Returning `null` rejects the update and emits a warning via the Studio plugin context. Synchronous throws are also treated as a rejection. | [plugin-collab-yjs/src/types/types.ts:906](https://github.com/ancyloce/anvilkit-studio/blob/main/src/types/types.ts#L906) |
