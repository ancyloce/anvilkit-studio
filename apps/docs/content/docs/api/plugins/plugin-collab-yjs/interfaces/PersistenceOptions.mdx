---
title: "PersistenceOptions"
description: "@anvilkit/plugin-collab-yjs · PersistenceOptions"
---

Defined in: [plugin-collab-yjs/src/types/types.ts:142](https://github.com/ancyloce/anvilkit-studio/blob/main/src/types/types.ts#L142)

Opt-in cross-tab persistence (L5). The default `createYjsAdapter`
call uses no persistence — outbound Y.js updates flow through the
host transport (websocket, hocuspocus, etc.) only. Enabling
`indexedDb` adds a durable queue so edits made while offline replay
on reconnect. Enabling `broadcastChannel` adds same-origin cross-tab
sync so two tabs of the same app see each other's edits without
round-tripping through the transport.

Each backend feature-detects at construction time and degrades
silently to a no-op if its API is unavailable (SSR, older browsers,
certain test runners).

## Properties

| Property | Modifier | Type | Description | Defined in |
| ------ | ------ | ------ | ------ | ------ |
| &lt;a id="broadcastchannel"&gt;&lt;/a&gt; `broadcastChannel?` | `readonly` | `boolean` | Enable same-origin cross-tab sync via `BroadcastChannel`. Default: `false`. | [plugin-collab-yjs/src/types/types.ts:151](https://github.com/ancyloce/anvilkit-studio/blob/main/src/types/types.ts#L151) |
| &lt;a id="channelname"&gt;&lt;/a&gt; `channelName?` | `readonly` | `string` | BroadcastChannel name. Defaults to `"<dbName>:<mapName>"` so multiple rooms in the same origin do not collide. | [plugin-collab-yjs/src/types/types.ts:162](https://github.com/ancyloce/anvilkit-studio/blob/main/src/types/types.ts#L162) |
| &lt;a id="dbname"&gt;&lt;/a&gt; `dbName?` | `readonly` | `string` | IndexedDB database name. The adapter combines this with the `mapName` so multiple rooms in the same origin do not collide. Default: `"anvilkit-collab-yjs"`. | [plugin-collab-yjs/src/types/types.ts:157](https://github.com/ancyloce/anvilkit-studio/blob/main/src/types/types.ts#L157) |
| &lt;a id="indexeddb"&gt;&lt;/a&gt; `indexedDb?` | `readonly` | `boolean` | Enable the IndexedDB-backed offline queue. Default: `false`. | [plugin-collab-yjs/src/types/types.ts:146](https://github.com/ancyloce/anvilkit-studio/blob/main/src/types/types.ts#L146) |
| &lt;a id="onfault"&gt;&lt;/a&gt; `onFault?` | `readonly` | (`reason`) =&gt; `void` | Notified once when either backend fails to open / falls back to a no-op. Useful for telemetry — the adapter never throws out of persistence into the Y.Doc observer chain, so this is the only way to learn about quota or schema faults. | [plugin-collab-yjs/src/types/types.ts:175](https://github.com/ancyloce/anvilkit-studio/blob/main/src/types/types.ts#L175) |
| &lt;a id="schemaversion"&gt;&lt;/a&gt; `schemaVersion?` | `readonly` | `number` | IndexedDB schema version. Bumped only when the queue's stored shape changes; rolling forward never downgrades the on-disk data. Default: `1`. | [plugin-collab-yjs/src/types/types.ts:168](https://github.com/ancyloce/anvilkit-studio/blob/main/src/types/types.ts#L168) |
