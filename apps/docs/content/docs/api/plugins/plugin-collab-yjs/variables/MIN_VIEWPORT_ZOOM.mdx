---
title: "MIN_VIEWPORT_ZOOM"
description: "@anvilkit/plugin-collab-yjs · MIN_VIEWPORT_ZOOM"
---

```ts
const MIN_VIEWPORT_ZOOM: 0.01 = 0.01;
```

Defined in: [plugin-collab-yjs/src/utils/presence-schema.ts:183](https://github.com/ancyloce/anvilkit-studio/blob/main/src/utils/presence-schema.ts#L183)

Clamp range for `viewport.zoom`. Values outside the range are clamped
(not rejected) so a peer mid-gesture with a transient out-of-range
zoom still produces a usable viewport.
