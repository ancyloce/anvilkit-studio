---
title: "Canvas Export — API Reference"
description: "@anvilkit/plugin-export-canvas public API reference, generated by TypeDoc."
---

## Interfaces

| Interface | Description |
| ------ | ------ |
| [CanvasExportOptions](interfaces/CanvasExportOptions.md) | Option bag for the canvas export formats. The Studio export contract hands `run()` a Puck `PageIR`, which is meaningless for a canvas design — so the canvas document (and any pre-rendered rasters) arrive HERE instead, supplied by the canvas-mode host when it invokes the format. |
| [CanvasRaster](interfaces/CanvasRaster.md) | A pre-rendered raster of one canvas page. The canvas renderer is React/Konva and cannot run headlessly, so this plugin never rasterizes itself — the host (e.g. `plugin-canvas-studio`'s `CanvasExportBridge`) renders each page (via `stage.toDataURL()` / `rasterizePage`) and passes the bytes/data-URL in. |

## Type Aliases

| Type Alias | Description |
| ------ | ------ |
| [CanvasExportPluginOptions](type-aliases/CanvasExportPluginOptions.md) | Options for [createCanvasExportPlugin](functions/createCanvasExportPlugin.md). Reserved for future use. |

## Variables

| Variable | Description |
| ------ | ------ |
| [canvasExportFormats](variables/canvasExportFormats.md) | All four canvas export formats, in menu order. |
| [jsonFormat](variables/jsonFormat.md) | - |
| [pdfFormat](variables/pdfFormat.md) | - |
| [pngFormat](variables/pngFormat.md) | - |
| [svgFormat](variables/svgFormat.md) | - |

## Functions

| Function | Description |
| ------ | ------ |
| [canvasToJson](functions/canvasToJson.md) | Serialize the whole canvas document to a JSON snapshot string. |
| [canvasToPdf](functions/canvasToPdf.md) | Serialize the canvas document to a multi-page PDF (wraps canvas-core). The caller supplies one pre-rendered raster per page — this stays headless. |
| [canvasToPng](functions/canvasToPng.md) | Produce PNG bytes from a pre-rendered raster (data URL or bytes). |
| [canvasToSvg](functions/canvasToSvg.md) | Serialize one canvas page to an SVG string (wraps canvas-core). |
| [createCanvasExportPlugin](functions/createCanvasExportPlugin.md) | I2-3: a Studio plugin that registers the four canvas export formats (PNG / JSON / SVG / PDF) via `StudioPluginRegistration.exportFormats`. |
| [rasterToBytes](functions/rasterToBytes.md) | Decode a `data:` URL (or pass through raw bytes) to a `Uint8Array`. |
