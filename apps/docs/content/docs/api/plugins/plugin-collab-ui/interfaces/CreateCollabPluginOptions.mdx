---
title: "CreateCollabPluginOptions"
description: "@anvilkit/collab-ui · CreateCollabPluginOptions"
---

Defined in: [plugin.tsx:99](https://github.com/ancyloce/anvilkit-studio/blob/main/src/plugin.tsx#L99)

Options for [createCollabPlugin](../functions/createCollabPlugin).

Host owns identity (`self`) and transport (`doc` / `awareness`); the
factory composes the adapter, the data plugin, and the UI
contributions on top of those primitives.

## Properties

| Property | Modifier | Type | Description | Defined in |
| ------ | ------ | ------ | ------ | ------ |
| &lt;a id="awareness"&gt;&lt;/a&gt; `awareness?` | `readonly` | `Awareness` | `Awareness` instance for presence + cursor sync. In BYO mode this is the host's; in managed mode it is an optional override the transport reuses instead of minting its own. | [plugin.tsx:167](https://github.com/ancyloce/anvilkit-studio/blob/main/src/plugin.tsx#L167) |
| &lt;a id="awarenessratelimit"&gt;&lt;/a&gt; `awarenessRateLimit?` | `readonly` | `AwarenessRateLimitOptions` | Awareness outbound rate limit. Forwarded to `createYjsAdapter`. | [plugin.tsx:195](https://github.com/ancyloce/anvilkit-studio/blob/main/src/plugin.tsx#L195) |
| &lt;a id="computedelta"&gt;&lt;/a&gt; `computeDelta?` | `readonly` | `boolean` | Compute structural deltas on every save (cost / index tradeoff). Forwarded to `createYjsAdapter`. | [plugin.tsx:191](https://github.com/ancyloce/anvilkit-studio/blob/main/src/plugin.tsx#L191) |
| &lt;a id="connectionsource"&gt;&lt;/a&gt; `connectionSource?` | `readonly` | `ConnectionSource` | Connection-state event source. Forwarded to `createYjsAdapter`. | [plugin.tsx:186](https://github.com/ancyloce/anvilkit-studio/blob/main/src/plugin.tsx#L186) |
| &lt;a id="doc"&gt;&lt;/a&gt; `doc?` | `readonly` | `Doc` | Host-owned `Y.Doc`. Provide it to opt into **BYO transport mode** — the factory neither creates nor destroys it and honors your `awareness`/`connectionSource`. When omitted, the plugin builds and owns the transport (see `websocketUrl`). Takes precedence over `websocketUrl` when both are set. | [plugin.tsx:161](https://github.com/ancyloce/anvilkit-studio/blob/main/src/plugin.tsx#L161) |
| &lt;a id="mapname"&gt;&lt;/a&gt; `mapName?` | `readonly` | `string` | Map name on the Y.Doc that backs the page IR. Defaults to the `@anvilkit/plugin-collab-yjs` default. | [plugin.tsx:172](https://github.com/ancyloce/anvilkit-studio/blob/main/src/plugin.tsx#L172) |
| &lt;a id="notifications"&gt;&lt;/a&gt; `notifications?` | `readonly` | [`ConflictNoticeCenterProps`](ConflictNoticeCenterProps.md) & \{ `enabled?`: `boolean`; \} | Props forwarded to `<ConflictNoticeCenter>`. Pass `enabled: false` to skip the toaster. | [plugin.tsx:267](https://github.com/ancyloce/anvilkit-studio/blob/main/src/plugin.tsx#L267) |
| &lt;a id="onconnectionerror"&gt;&lt;/a&gt; `onConnectionError?` | `readonly` | (`err`) =&gt; `void` | Called when the managed transport fails (provider lib not installed, bad URL, auth failure). Defaults to a single `console.error`. Never throws from the factory. | [plugin.tsx:151](https://github.com/ancyloce/anvilkit-studio/blob/main/src/plugin.tsx#L151) |
| &lt;a id="onidentitychange"&gt;&lt;/a&gt; `onIdentityChange?` | `readonly` | (`next`) =&gt; `void` | Optional callback fired whenever the local peer's identity changes via `useCollabContext().updateSelf(...)`. The host's source of truth (auth profile, settings store, …) can mirror the new value back here. Does **not** fire on initial mount — the host already knows the value they passed in via `self`. Subsequent same-value updates are deduplicated by shallow content equality on `{ id, displayName, color }`. | [plugin.tsx:122](https://github.com/ancyloce/anvilkit-studio/blob/main/src/plugin.tsx#L122) |
| &lt;a id="onpolicyviolation"&gt;&lt;/a&gt; `onPolicyViolation?` | `readonly` | (`violation`) =&gt; `void` | Fires when `policy.canEdit` rejects an edit (either direction). | [plugin.tsx:237](https://github.com/ancyloce/anvilkit-studio/blob/main/src/plugin.tsx#L237) |
| &lt;a id="onsaveerror"&gt;&lt;/a&gt; `onSaveError?` | `readonly` | (`error`) =&gt; `void` | Fires when an outbound `adapter.save(...)` rejects or throws. Without this hook, transport failures surface as `unhandledRejection` warnings. | [plugin.tsx:243](https://github.com/ancyloce/anvilkit-studio/blob/main/src/plugin.tsx#L243) |
| &lt;a id="onvalidationfailure"&gt;&lt;/a&gt; `onValidationFailure?` | `readonly` | (`failure`) =&gt; `void` | Fires after a remote IR is rejected by `validateRemoteIR`. | [plugin.tsx:229](https://github.com/ancyloce/anvilkit-studio/blob/main/src/plugin.tsx#L229) |
| &lt;a id="persistence"&gt;&lt;/a&gt; `persistence?` | `readonly` | `PersistenceOptions` | Cross-tab persistence options (IndexedDB / BroadcastChannel). | [plugin.tsx:199](https://github.com/ancyloce/anvilkit-studio/blob/main/src/plugin.tsx#L199) |
| &lt;a id="policy"&gt;&lt;/a&gt; `policy?` | `readonly` | `CollabPolicy` | RBAC / lock policy applied symmetrically on inbound and outbound. | [plugin.tsx:233](https://github.com/ancyloce/anvilkit-studio/blob/main/src/plugin.tsx#L233) |
| &lt;a id="presence"&gt;&lt;/a&gt; `presence?` | `readonly` | [`CollabPresenceLayerProps`](CollabPresenceLayerProps.md) & \{ `broadcastCursor?`: `boolean`; `enabled?`: `boolean`; \} | Props forwarded verbatim to `<PresenceLayer>`. The layer reads peers from `<CollabUIProvider>` context, so only visual / cursor options need to be specified here. Pass `enabled: false` to skip mounting the layer entirely (e.g. read-only embeds). `broadcastCursor` (default `true`) additionally mounts a turnkey local-cursor publisher so the zero-config one-liner shows peers' cursors out of the box — no host pointer plumbing required. Set it `false` if you run your own presence writer (e.g. one that also broadcasts the Puck selection): awareness *replaces* the local frame on every update, so two writers would clobber each other. | [plugin.tsx:259](https://github.com/ancyloce/anvilkit-studio/blob/main/src/plugin.tsx#L259) |
| &lt;a id="provider"&gt;&lt;/a&gt; `provider?` | `readonly` | `ManagedTransportProvider` | Managed-mode backend. Default `"hocuspocus"`. Ignored in BYO mode. | [plugin.tsx:141](https://github.com/ancyloce/anvilkit-studio/blob/main/src/plugin.tsx#L141) |
| &lt;a id="puckconfig"&gt;&lt;/a&gt; `puckConfig?` | `readonly` | `Config` | The host's Puck `Config`. Required for **outgoing** sync (Puck data → IR → adapter `save()`). If omitted, the plugin still applies incoming remote updates but does not write local edits back to the adapter — useful for read-only viewers. | [plugin.tsx:220](https://github.com/ancyloce/anvilkit-studio/blob/main/src/plugin.tsx#L220) |
| &lt;a id="room"&gt;&lt;/a&gt; `room?` | `readonly` | `string` | Shared room/document name for managed mode. Default `"anvilkit-default-room"` so two tabs collaborate out of the box. | [plugin.tsx:137](https://github.com/ancyloce/anvilkit-studio/blob/main/src/plugin.tsx#L137) |
| &lt;a id="savedebouncems"&gt;&lt;/a&gt; `saveDebounceMs?` | `readonly` | `number` | P2 — coalesce keystroke-rate local saves before they hit the Y.Doc/transport. This consolidated factory wraps the adapter in `createDebouncedAdapter` BY DEFAULT (the headless `createCollabDataPlugin` accepts any adapter, so a bare `createYjsAdapter` would otherwise save on every keystroke and flood the Y.Doc). Default `150` ms. Set `0` to opt out (e.g. a host that already debounces upstream). Only the save path is debounced — presence, status, and conflict reads still use the live adapter. | [plugin.tsx:213](https://github.com/ancyloce/anvilkit-studio/blob/main/src/plugin.tsx#L213) |
| &lt;a id="self"&gt;&lt;/a&gt; `self?` | `readonly` | `PeerInfo` | The local peer's identity. Mirrored into awareness on mount and whenever a downstream consumer calls `updateSelf` on the context. Optional: when omitted, an anonymous identity is generated (`anon-<uuid>` id + a stable hashed color) so "just give me collaboration" needs neither a `doc` nor a `self`. | [plugin.tsx:109](https://github.com/ancyloce/anvilkit-studio/blob/main/src/plugin.tsx#L109) |
| &lt;a id="staleafterms"&gt;&lt;/a&gt; `staleAfterMs?` | `readonly` | `number` | Overlap window for `onConflict` (ms). Forwarded to `createYjsAdapter`. | [plugin.tsx:182](https://github.com/ancyloce/anvilkit-studio/blob/main/src/plugin.tsx#L182) |
| &lt;a id="token"&gt;&lt;/a&gt; `token?` | `readonly` | `string` | Auth token forwarded to the relay (managed mode). Default `""`. | [plugin.tsx:145](https://github.com/ancyloce/anvilkit-studio/blob/main/src/plugin.tsx#L145) |
| &lt;a id="usenativetree"&gt;&lt;/a&gt; `useNativeTree?` | `readonly` | `boolean` | Opt out of native-tree per-node CRDT merge. See `@anvilkit/plugin-collab-yjs`'s `CreateYjsAdapterOptions.useNativeTree`. | [plugin.tsx:177](https://github.com/ancyloce/anvilkit-studio/blob/main/src/plugin.tsx#L177) |
| &lt;a id="validateremoteir"&gt;&lt;/a&gt; `validateRemoteIR?` | `readonly` | `ValidateRemoteIR` | Validate remote IR before dispatching it into Puck. Forwarded to `createCollabDataPlugin`. | [plugin.tsx:225](https://github.com/ancyloce/anvilkit-studio/blob/main/src/plugin.tsx#L225) |
| &lt;a id="websocketurl"&gt;&lt;/a&gt; `websocketUrl?` | `readonly` | `string` | WebSocket relay URL, e.g. `ws://localhost:1234`. The **one field most hosts set.** Provide it (and omit `doc`) to let the plugin own the whole transport lifecycle — doc, awareness, provider, status bridge, and teardown. Omit both `websocketUrl` and `doc` for single-tab in-memory mode (a one-time dev warning fires). | [plugin.tsx:132](https://github.com/ancyloce/anvilkit-studio/blob/main/src/plugin.tsx#L132) |
