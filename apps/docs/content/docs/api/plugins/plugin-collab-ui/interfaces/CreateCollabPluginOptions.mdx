---
title: "CreateCollabPluginOptions"
description: "@anvilkit/collab-ui · CreateCollabPluginOptions"
---

Defined in: [plugin.tsx:110](https://github.com/ancyloce/anvilkit-studio/blob/main/src/plugin.tsx#L110)

Options for [createCollabPlugin](../functions/createCollabPlugin).

Host owns identity (`self`) and transport (`doc` / `awareness`); the
factory composes the adapter, the data plugin, and the UI
contributions on top of those primitives.

## Properties

| Property | Modifier | Type | Description | Defined in |
| ------ | ------ | ------ | ------ | ------ |
| &lt;a id="awareness"&gt;&lt;/a&gt; `awareness?` | `readonly` | `Awareness` | `Awareness` instance for presence + cursor sync. In BYO mode this is the host's; in managed mode it is an optional override the transport reuses instead of minting its own. | [plugin.tsx:178](https://github.com/ancyloce/anvilkit-studio/blob/main/src/plugin.tsx#L178) |
| &lt;a id="awarenessratelimit"&gt;&lt;/a&gt; `awarenessRateLimit?` | `readonly` | `AwarenessRateLimitOptions` | Awareness outbound rate limit. Forwarded to `createYjsAdapter`. | [plugin.tsx:206](https://github.com/ancyloce/anvilkit-studio/blob/main/src/plugin.tsx#L206) |
| &lt;a id="computedelta"&gt;&lt;/a&gt; `computeDelta?` | `readonly` | `boolean` | Compute structural deltas on every save (cost / index tradeoff). Forwarded to `createYjsAdapter`. | [plugin.tsx:202](https://github.com/ancyloce/anvilkit-studio/blob/main/src/plugin.tsx#L202) |
| &lt;a id="connectionsource"&gt;&lt;/a&gt; `connectionSource?` | `readonly` | `ConnectionSource` | Connection-state event source. Forwarded to `createYjsAdapter`. | [plugin.tsx:197](https://github.com/ancyloce/anvilkit-studio/blob/main/src/plugin.tsx#L197) |
| &lt;a id="doc"&gt;&lt;/a&gt; `doc?` | `readonly` | `Doc` | Host-owned `Y.Doc`. Provide it to opt into **BYO transport mode** — the factory neither creates nor destroys it and honors your `awareness`/`connectionSource`. When omitted, the plugin builds and owns the transport (see `websocketUrl`). Takes precedence over `websocketUrl` when both are set. | [plugin.tsx:172](https://github.com/ancyloce/anvilkit-studio/blob/main/src/plugin.tsx#L172) |
| &lt;a id="initialshowremotecursors"&gt;&lt;/a&gt; `initialShowRemoteCursors?` | `readonly` | `boolean` | Seed the initial remote-cursor visibility on the bundled `<CollabUIProvider>` (review §4.2.1). Forwarded verbatim so a host can restore a persisted preference on mount. Defaults to `true` (cursors visible). Kept host-driven — no storage is baked into the plugin. | [plugin.tsx:342](https://github.com/ancyloce/anvilkit-studio/blob/main/src/plugin.tsx#L342) |
| &lt;a id="mapname"&gt;&lt;/a&gt; `mapName?` | `readonly` | `string` | Map name on the Y.Doc that backs the page IR. Defaults to the `@anvilkit/plugin-collab-yjs` default. | [plugin.tsx:183](https://github.com/ancyloce/anvilkit-studio/blob/main/src/plugin.tsx#L183) |
| &lt;a id="notifications"&gt;&lt;/a&gt; `notifications?` | `readonly` | [`ConflictNoticeCenterProps`](ConflictNoticeCenterProps.md) & \{ `enabled?`: `boolean`; \} | Props forwarded to `<ConflictNoticeCenter>`. Pass `enabled: false` to skip the toaster. | [plugin.tsx:353](https://github.com/ancyloce/anvilkit-studio/blob/main/src/plugin.tsx#L353) |
| &lt;a id="onconnectionerror"&gt;&lt;/a&gt; `onConnectionError?` | `readonly` | (`err`) =&gt; `void` | Called when the managed transport fails (provider lib not installed, bad URL, auth failure). Defaults to a single `console.error`. Never throws from the factory. | [plugin.tsx:162](https://github.com/ancyloce/anvilkit-studio/blob/main/src/plugin.tsx#L162) |
| &lt;a id="onidentitychange"&gt;&lt;/a&gt; `onIdentityChange?` | `readonly` | (`next`) =&gt; `void` | Optional callback fired whenever the local peer's identity changes via `useCollabContext().updateSelf(...)`. The host's source of truth (auth profile, settings store, …) can mirror the new value back here. Does **not** fire on initial mount — the host already knows the value they passed in via `self`. Subsequent same-value updates are deduplicated by shallow content equality on `{ id, displayName, color }`. | [plugin.tsx:133](https://github.com/ancyloce/anvilkit-studio/blob/main/src/plugin.tsx#L133) |
| &lt;a id="onpolicyviolation"&gt;&lt;/a&gt; `onPolicyViolation?` | `readonly` | (`violation`) =&gt; `void` | Fires when `policy.canEdit` rejects an edit (either direction). | [plugin.tsx:282](https://github.com/ancyloce/anvilkit-studio/blob/main/src/plugin.tsx#L282) |
| &lt;a id="onsaveerror"&gt;&lt;/a&gt; `onSaveError?` | `readonly` | (`error`) =&gt; `void` | Fires when an outbound `adapter.save(...)` rejects or throws. Without this hook, transport failures surface as `unhandledRejection` warnings. | [plugin.tsx:288](https://github.com/ancyloce/anvilkit-studio/blob/main/src/plugin.tsx#L288) |
| &lt;a id="onshowremotecursorschange"&gt;&lt;/a&gt; `onShowRemoteCursorsChange?` | `readonly` | (`next`) =&gt; `void` | Fired whenever remote-cursor visibility changes (settings toggle or a programmatic `setShowRemoteCursors`), so a host can persist the preference (review §4.2.1). Forwarded to `<CollabUIProvider>`. | [plugin.tsx:348](https://github.com/ancyloce/anvilkit-studio/blob/main/src/plugin.tsx#L348) |
| &lt;a id="onvalidationfailure"&gt;&lt;/a&gt; `onValidationFailure?` | `readonly` | (`failure`) =&gt; `void` | Fires after a remote IR is rejected by `validateRemoteIR`. | [plugin.tsx:274](https://github.com/ancyloce/anvilkit-studio/blob/main/src/plugin.tsx#L274) |
| &lt;a id="persistence"&gt;&lt;/a&gt; `persistence?` | `readonly` | `PersistenceOptions` | Cross-tab persistence options (IndexedDB / BroadcastChannel). | [plugin.tsx:210](https://github.com/ancyloce/anvilkit-studio/blob/main/src/plugin.tsx#L210) |
| &lt;a id="policy"&gt;&lt;/a&gt; `policy?` | `readonly` | `CollabPolicy` | RBAC / lock policy applied symmetrically on inbound and outbound. | [plugin.tsx:278](https://github.com/ancyloce/anvilkit-studio/blob/main/src/plugin.tsx#L278) |
| &lt;a id="presence"&gt;&lt;/a&gt; `presence?` | `readonly` | [`CollabPresenceLayerProps`](CollabPresenceLayerProps.md) & \{ `broadcastCursor?`: `boolean`; `enabled?`: `boolean`; \} | Props forwarded verbatim to `<PresenceLayer>`. The layer reads peers from `<CollabUIProvider>` context, so only visual / cursor options need to be specified here. Pass `enabled: false` to skip mounting the layer entirely (e.g. read-only embeds). `broadcastCursor` (default `true`) additionally mounts a turnkey local-cursor publisher so the zero-config one-liner shows peers' cursors out of the box — no host pointer plumbing required. Set it `false` if you run your own presence writer (e.g. one that also broadcasts the Puck selection): awareness *replaces* the local frame on every update, so two writers would clobber each other. | [plugin.tsx:304](https://github.com/ancyloce/anvilkit-studio/blob/main/src/plugin.tsx#L304) |
| &lt;a id="presencepublishing"&gt;&lt;/a&gt; `presencePublishing?` | `readonly` | [`PresencePublishingMode`](../type-aliases/PresencePublishingMode) | Which local-presence frames the bundled writer publishes. Default `"cursor"` (byte-for-byte today's behavior): the Puck-free `<PresenceCursorBroadcaster>` is mounted and publishes only pointer coordinates. Set `"cursor+selection"` to instead mount the Puck-aware `<CollabPresencePublisher>`, which publishes pointer coordinates AND the current Puck selection ring (via `usePuckSelection()`) in a single awareness frame. The combined publisher is mounted INSTEAD OF the cursor-only broadcaster — never alongside it — because awareness *replaces* the local frame on every update, so two writers would clobber each other. **Single Puck-module-context requirement.** `"cursor+selection"` reads the selection through `createUsePuck()`, which binds to whichever `@puckeditor/core` instance THIS package resolved. The host MUST dedupe `@puckeditor/core` to a single copy so the plugin's selection hook subscribes to the same `<Puck>` the host renders; a duplicated core throws "usePuck must be used inside &lt;Puck&gt;". Hosts that cannot guarantee a single core copy should keep the default `"cursor"` mode and broadcast selection from their own writer (`broadcastCursor: false`). Honored only while the bundled writer is mounted — i.e. when presence is enabled and `presence.broadcastCursor` is not `false`. | [plugin.tsx:334](https://github.com/ancyloce/anvilkit-studio/blob/main/src/plugin.tsx#L334) |
| &lt;a id="propguards"&gt;&lt;/a&gt; `propGuards?` | `readonly` | `PropGuardOptions` | Per-prop decode bounds applied at the native-tree trust boundary (Y3/§4.1.3). Forwarded to `createYjsAdapter`. A prop value exceeding any bound is dropped from the decoded node. Omit for the permissive defaults. See `@anvilkit/plugin-collab-yjs`'s `CreateYjsAdapterOptions.propGuards`. | [plugin.tsx:235](https://github.com/ancyloce/anvilkit-studio/blob/main/src/plugin.tsx#L235) |
| &lt;a id="provider"&gt;&lt;/a&gt; `provider?` | `readonly` | `ManagedTransportProvider` | Managed-mode backend. Default `"hocuspocus"`. Ignored in BYO mode. | [plugin.tsx:152](https://github.com/ancyloce/anvilkit-studio/blob/main/src/plugin.tsx#L152) |
| &lt;a id="puckconfig"&gt;&lt;/a&gt; `puckConfig?` | `readonly` | `Config` | The host's Puck `Config`. Required for **outgoing** sync (Puck data → IR → adapter `save()`). If omitted, the plugin still applies incoming remote updates but does not write local edits back to the adapter — useful for read-only viewers. | [plugin.tsx:265](https://github.com/ancyloce/anvilkit-studio/blob/main/src/plugin.tsx#L265) |
| &lt;a id="resolveconflict"&gt;&lt;/a&gt; `resolveConflict?` | `readonly` | `ResolveConflict` | Optional semantic merge-strategy hook for same-node, same-field prop conflicts (§4.2.3). Forwarded to `createYjsAdapter`. Returning `"local"` / `{ fields }` writes a resolution back into the shared `Y.Doc`; `"remote"` / `undefined` keeps last-write-wins. Omit to keep pure last-write-wins. See `@anvilkit/plugin-collab-yjs`'s `CreateYjsAdapterOptions.resolveConflict`. | [plugin.tsx:244](https://github.com/ancyloce/anvilkit-studio/blob/main/src/plugin.tsx#L244) |
| &lt;a id="room"&gt;&lt;/a&gt; `room?` | `readonly` | `string` | Shared room/document name for managed mode. Default `"anvilkit-default-room"` so two tabs collaborate out of the box. | [plugin.tsx:148](https://github.com/ancyloce/anvilkit-studio/blob/main/src/plugin.tsx#L148) |
| &lt;a id="savedebouncems"&gt;&lt;/a&gt; `saveDebounceMs?` | `readonly` | `number` | P2 — coalesce keystroke-rate local saves before they hit the Y.Doc/transport. This consolidated factory wraps the adapter in `createDebouncedAdapter` BY DEFAULT (the headless `createCollabDataPlugin` accepts any adapter, so a bare `createYjsAdapter` would otherwise save on every keystroke and flood the Y.Doc). Default `150` ms. Set `0` to opt out (e.g. a host that already debounces upstream). Only the save path is debounced — presence, status, and conflict reads still use the live adapter. | [plugin.tsx:258](https://github.com/ancyloce/anvilkit-studio/blob/main/src/plugin.tsx#L258) |
| &lt;a id="self"&gt;&lt;/a&gt; `self?` | `readonly` | `PeerInfo` | The local peer's identity. Mirrored into awareness on mount and whenever a downstream consumer calls `updateSelf` on the context. Optional: when omitted, an anonymous identity is generated (`anon-<uuid>` id + a stable hashed color) so "just give me collaboration" needs neither a `doc` nor a `self`. | [plugin.tsx:120](https://github.com/ancyloce/anvilkit-studio/blob/main/src/plugin.tsx#L120) |
| &lt;a id="snapshotpersistence"&gt;&lt;/a&gt; `snapshotPersistence?` | `readonly` | `SnapshotPersistenceOptions` | Opt-in server-grade snapshot persistence (§4.2.2). Forwarded to `createYjsAdapter` — every `save()` best-effort mirrors the snapshot to the supplied backend sink. Omit to keep the in-`Y.Doc` snapshot store as the only sink. See `@anvilkit/plugin-collab-yjs`'s `CreateYjsAdapterOptions.snapshotPersistence`. | [plugin.tsx:227](https://github.com/ancyloce/anvilkit-studio/blob/main/src/plugin.tsx#L227) |
| &lt;a id="staleafterms"&gt;&lt;/a&gt; `staleAfterMs?` | `readonly` | `number` | Overlap window for `onConflict` (ms). Forwarded to `createYjsAdapter`. | [plugin.tsx:193](https://github.com/ancyloce/anvilkit-studio/blob/main/src/plugin.tsx#L193) |
| &lt;a id="token"&gt;&lt;/a&gt; `token?` | `readonly` | `string` | Auth token forwarded to the relay (managed mode). Default `""`. | [plugin.tsx:156](https://github.com/ancyloce/anvilkit-studio/blob/main/src/plugin.tsx#L156) |
| &lt;a id="undo"&gt;&lt;/a&gt; `undo?` | `readonly` | `UndoOptions` | Opt-in local undo/redo (§4.1.1). Forwarded to `createYjsAdapter`, which wraps a `Y.UndoManager` over the shared type backing the live `PageIR` and exposes the `UndoController`. Omit to keep the pre-undo behavior (inert controller). See `@anvilkit/plugin-collab-yjs`'s `CreateYjsAdapterOptions.undo`. | [plugin.tsx:218](https://github.com/ancyloce/anvilkit-studio/blob/main/src/plugin.tsx#L218) |
| &lt;a id="usenativetree"&gt;&lt;/a&gt; `useNativeTree?` | `readonly` | `boolean` | Opt out of native-tree per-node CRDT merge. See `@anvilkit/plugin-collab-yjs`'s `CreateYjsAdapterOptions.useNativeTree`. | [plugin.tsx:188](https://github.com/ancyloce/anvilkit-studio/blob/main/src/plugin.tsx#L188) |
| &lt;a id="validateremoteir"&gt;&lt;/a&gt; `validateRemoteIR?` | `readonly` | `ValidateRemoteIR` | Validate remote IR before dispatching it into Puck. Forwarded to `createCollabDataPlugin`. | [plugin.tsx:270](https://github.com/ancyloce/anvilkit-studio/blob/main/src/plugin.tsx#L270) |
| &lt;a id="websocketurl"&gt;&lt;/a&gt; `websocketUrl?` | `readonly` | `string` | WebSocket relay URL, e.g. `ws://localhost:1234`. The **one field most hosts set.** Provide it (and omit `doc`) to let the plugin own the whole transport lifecycle — doc, awareness, provider, status bridge, and teardown. Omit both `websocketUrl` and `doc` for single-tab in-memory mode (a one-time dev warning fires). | [plugin.tsx:143](https://github.com/ancyloce/anvilkit-studio/blob/main/src/plugin.tsx#L143) |
