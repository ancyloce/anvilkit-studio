---
title: "CollabPresenceLayerProps"
description: "@anvilkit/collab-ui · CollabPresenceLayerProps"
---

Defined in: [components/presence-layer.tsx:40](https://github.com/ancyloce/anvilkit-studio/blob/main/src/components/presence-layer.tsx#L40)

Props for [PresenceLayer](../functions/PresenceLayer) — the overlay that renders remote peer cursors and selection rings.

## Properties

| Property | Modifier | Type | Description | Defined in |
| ------ | ------ | ------ | ------ | ------ |
| &lt;a id="classname"&gt;&lt;/a&gt; `className?` | `readonly` | `string` | - | [components/presence-layer.tsx:51](https://github.com/ancyloce/anvilkit-studio/blob/main/src/components/presence-layer.tsx#L51) |
| &lt;a id="maxcursors"&gt;&lt;/a&gt; `maxCursors?` | `readonly` | `number` | Maximum number of remote cursors animated at once (review U1). Defaults to DEFAULT\_MAX\_CURSORS. Rooms under the cap render every peer's cursor; past it, only the most-recently-moved cursors animate — each cursor mounts two `motion` springs, so an uncapped large room would otherwise spin up ~2N rAF loops. Selection rings are unaffected. Pass `0` to render no cursors. | [components/presence-layer.tsx:60](https://github.com/ancyloce/anvilkit-studio/blob/main/src/components/presence-layer.tsx#L60) |
| &lt;a id="maxselectionrings"&gt;&lt;/a&gt; `maxSelectionRings?` | `readonly` | `number` | Maximum number of remote selection rings rendered across ALL peers at once (review M4 / report 4.2.4). Defaults to DEFAULT\_MAX\_SELECTION\_RINGS. Each ring is a positioned overlay whose rect comes from a synchronous layout read, so a pathological room (many peers each multi-selecting many nodes) would otherwise mount `peers × nodes` overlay DOM nodes without bound. The cap counts the TOTAL, not per peer; over the cap the first rings in deterministic peer-then-node order survive. Pass `0` to render no selection rings. Independent of [maxCursors](#maxcursors). | [components/presence-layer.tsx:72](https://github.com/ancyloce/anvilkit-studio/blob/main/src/components/presence-layer.tsx#L72) |
| &lt;a id="resolveselectionrect"&gt;&lt;/a&gt; `resolveSelectionRect?` | `readonly` | (`nodeId`) =&gt; `PresenceSelectionRingRect` \| `null` | - | [components/presence-layer.tsx:48](https://github.com/ancyloce/anvilkit-studio/blob/main/src/components/presence-layer.tsx#L48) |
| &lt;a id="showcursors"&gt;&lt;/a&gt; `showCursors?` | `readonly` | `boolean` | Per-instance override for remote-cursor visibility. When omitted, the layer follows the shared `useCollabCursorVisibility()` state that the bundled `<CollabSettingsPopover>` toggle writes (review §C3 / §4.3). | [components/presence-layer.tsx:47](https://github.com/ancyloce/anvilkit-studio/blob/main/src/components/presence-layer.tsx#L47) |
