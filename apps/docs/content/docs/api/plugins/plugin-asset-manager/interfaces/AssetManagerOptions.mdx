---
title: "AssetManagerOptions"
description: "@anvilkit/plugin-asset-manager · AssetManagerOptions"
---

Defined in: [types/options.ts:21](https://github.com/ancyloce/anvilkit-studio/blob/main/src/types/options.ts#L21)

Configuration accepted by `createAssetManagerPlugin()`.

## Properties

| Property | Modifier | Type | Description | Defined in |
| ------ | ------ | ------ | ------ | ------ |
| &lt;a id="acceptedfileextensions"&gt;&lt;/a&gt; `acceptedFileExtensions?` | `readonly` | readonly `string`[] | Optional extension allowlist, matched case-insensitively against `File.name`. Entries may be supplied with or without the leading dot (`".png"` or `"png"`). When both MIME and extension allowlists are configured, both known MIME types and extensions must be accepted; an empty browser MIME type can still pass via a matching extension. | [types/options.ts:90](https://github.com/ancyloce/anvilkit-studio/blob/main/src/types/options.ts#L90) |
| &lt;a id="acceptedmimetypes"&gt;&lt;/a&gt; `acceptedMimeTypes?` | `readonly` | readonly `string`[] | - | [types/options.ts:82](https://github.com/ancyloce/anvilkit-studio/blob/main/src/types/options.ts#L82) |
| &lt;a id="allowmixedscripthostnames"&gt;&lt;/a&gt; `allowMixedScriptHostnames?` | `readonly` | `boolean` | Permit `http(s)` URLs whose hostname mixes Unicode scripts. Defaults to `false` (homoglyph-attack guard). Single-script IDN hosts are always allowed regardless of this flag. | [types/options.ts:102](https://github.com/ancyloce/anvilkit-studio/blob/main/src/types/options.ts#L102) |
| &lt;a id="categories"&gt;&lt;/a&gt; `categories?` | `readonly` | readonly [`AssetCategory`](AssetCategory.md)[] | Host saved-view chips shown beside the kind chips. | [types/options.ts:76](https://github.com/ancyloce/anvilkit-studio/blob/main/src/types/options.ts#L76) |
| &lt;a id="datasource"&gt;&lt;/a&gt; `dataSource?` | `readonly` | [`AssetDataSource`](AssetDataSource.md) | Unified local-library data plane (list + asset/folder CRUD). Omitted ⇒ a full in-memory default over the built-in registry + a folder side-index. | [types/options.ts:61](https://github.com/ancyloce/anvilkit-studio/blob/main/src/types/options.ts#L61) |
| &lt;a id="dataurlallowlistoptin"&gt;&lt;/a&gt; `dataUrlAllowlistOptIn?` | `readonly` | `boolean` | Permit `data:` URLs through the trust boundary. Defaults to `false` — `http`, `https`, and `blob` are always allowed; every other scheme is rejected. Replaced the previous `urlAllowlist: ["data"]` pattern in v1.0. | [types/options.ts:96](https://github.com/ancyloce/anvilkit-studio/blob/main/src/types/options.ts#L96) |
| &lt;a id="dedupe"&gt;&lt;/a&gt; `dedupe?` | `readonly` | `boolean` | Opt-in content deduplication. When `true`, each upload's bytes are hashed (SHA-256) before the adapter runs; if an asset with the same hash already exists in the registry, that existing asset is reused (its reference is dispatched) instead of re-uploading. The computed digest is stored on `AssetMeta.hash`. Off by default — hashing reads the whole file, and distinct-but-identical uploads are kept separate unless enabled. | [types/options.ts:46](https://github.com/ancyloce/anvilkit-studio/blob/main/src/types/options.ts#L46) |
| &lt;a id="facets"&gt;&lt;/a&gt; `facets?` | `readonly` | readonly [`AssetFacetDefinition`](AssetFacetDefinition.md)[] | Host-registered custom facets, merged onto the built-ins. | [types/options.ts:78](https://github.com/ancyloce/anvilkit-studio/blob/main/src/types/options.ts#L78) |
| &lt;a id="folders"&gt;&lt;/a&gt; `folders?` | `readonly` | `boolean` \| [`FolderOptions`](FolderOptions.md) | Enable folder management. `true` (default) ⇒ in-memory folder store; `false` ⇒ flat library (today's UX); an object tunes depth / move. | [types/options.ts:66](https://github.com/ancyloce/anvilkit-studio/blob/main/src/types/options.ts#L66) |
| &lt;a id="getthumbnail"&gt;&lt;/a&gt; `getThumbnail?` | `readonly` | (`entry`) =&gt; `string` \| `undefined` | Optional thumbnail derivation. Returning a string sets `StudioAsset.thumbnailUrl`; returning `undefined` suppresses the thumbnail (overriding the default-for-images behavior). | [types/options.ts:108](https://github.com/ancyloce/anvilkit-studio/blob/main/src/types/options.ts#L108) |
| &lt;a id="maxfilesize"&gt;&lt;/a&gt; `maxFileSize?` | `readonly` | `number` | - | [types/options.ts:81](https://github.com/ancyloce/anvilkit-studio/blob/main/src/types/options.ts#L81) |
| &lt;a id="onassetdeleted"&gt;&lt;/a&gt; `onAssetDeleted?` | `readonly` | [`AssetDeletedHook`](../type-aliases/AssetDeletedHook) | Called when an asset is deleted through the source (default in-memory data plane / lightweight registry source), with the record at deletion time. Use it to release backend objects tied to the upload. `blob:` URLs minted by the built-in `inMemoryUploader` are revoked automatically regardless of this hook. Not invoked when a host-owned `dataSource` owns the asset plane — that backend owns its own deletion. | [types/options.ts:126](https://github.com/ancyloce/anvilkit-studio/blob/main/src/types/options.ts#L126) |
| &lt;a id="providers"&gt;&lt;/a&gt; `providers?` | `readonly` | readonly [`AssetSourceProvider`](AssetSourceProvider.md)[] | Additional external read-only sources (federated tabs, e.g. Unsplash). | [types/options.ts:68](https://github.com/ancyloce/anvilkit-studio/blob/main/src/types/options.ts#L68) |
| &lt;a id="resumable"&gt;&lt;/a&gt; `resumable?` | `readonly` | [`ResumableUploadConfig`](ResumableUploadConfig.md) | Opt-in resumable / multipart upload for large media. When set, files at or above `resumable.threshold` upload through the session-based [ResumableUploadConfig.adapter](ResumableUploadConfig.md#adapter) (chunked, per-part retry, resumable across interruptions); smaller files keep using the single-shot `uploader`. Omitted ⇒ every upload is single-shot (unchanged default). | [types/options.ts:37](https://github.com/ancyloce/anvilkit-studio/blob/main/src/types/options.ts#L37) |
| &lt;a id="sniffcontent"&gt;&lt;/a&gt; `sniffContent?` | `readonly` | `boolean` | Opt-in magic-byte content sniffing. When `true`, each upload's leading bytes are inspected and rejected (`CONTENT_TYPE_MISMATCH`) if the detected type contradicts a declared, specific `file.type` — defense-in-depth against spoofed/empty browser MIME beyond the `acceptedMimeTypes` / `acceptedFileExtensions` checks. Off by default; unsignable types and generic declarations (`application/octet-stream`) pass. The backend should still validate independently. | [types/options.ts:56](https://github.com/ancyloce/anvilkit-studio/blob/main/src/types/options.ts#L56) |
| &lt;a id="transformresolver"&gt;&lt;/a&gt; `transformResolver?` | `readonly` | [`TransformResolver`](../type-aliases/TransformResolver) | Maps an asset + [AssetTransform](AssetTransform.md) to a derivative URL (resize / crop / format / quality), produced by the host's image CDN or service. Wired into the IR resolver so an `asset://<id>?w=…&fm=…` reference resolves to the derivative URL at export / render time. Omitted ⇒ transforms are ignored and the original URL is used. See `createQueryParamTransformResolver` for a built-in query-param CDN mapping. | [types/options.ts:117](https://github.com/ancyloce/anvilkit-studio/blob/main/src/types/options.ts#L117) |
| &lt;a id="unsplash"&gt;&lt;/a&gt; `unsplash?` | `readonly` | [`UnsplashSourceOptions`](UnsplashSourceOptions.md) | Built-in Unsplash source config — sugar that builds + registers an Unsplash provider. Enabled when a proxy endpoint or access key is present; the package never bundles a key. | [types/options.ts:74](https://github.com/ancyloce/anvilkit-studio/blob/main/src/types/options.ts#L74) |
| &lt;a id="uploader"&gt;&lt;/a&gt; `uploader?` | `readonly` | [`UploadAdapter`](../type-aliases/UploadAdapter) | Binary ingest: `File → UploadResult`. WIDENED to optional in PRD 0002 — omitted ⇒ the plugin resolves the built-in `inMemoryUploader()`, so `createAssetManagerPlugin()` works with zero config. The resolved value is always non-optional internally; every existing `{ uploader }` caller still assigns (pure widening). | [types/options.ts:29](https://github.com/ancyloce/anvilkit-studio/blob/main/src/types/options.ts#L29) |
