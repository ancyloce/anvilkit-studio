---
title: "AssetDeletedHook"
description: "@anvilkit/plugin-asset-manager · AssetDeletedHook"
---

```ts
type AssetDeletedHook = (asset) => void | Promise<void>;
```

Defined in: [types/types.ts:93](https://github.com/ancyloce/anvilkit-studio/blob/main/src/types/types.ts#L93)

Lifecycle hook fired when an asset is deleted or its URL is superseded, with
the record as it existed before removal/replacement. Lets a host release its
backing object (e.g. delete the stored S3 object or revoke a `blob:` URL).
Awaited; throwing rejects the mutation.

## Parameters

| Parameter | Type |
| ------ | ------ |
| `asset` | [`UploadResult`](../interfaces/UploadResult) |

## Returns

`void` \| `Promise`\&lt;`void`\&gt;
