---
title: "ReplacePayload"
description: "@anvilkit/plugin-asset-manager · ReplacePayload"
---

```ts
type ReplacePayload = File;
```

Defined in: [types/data-source.ts:24](https://github.com/ancyloce/anvilkit-studio/blob/main/src/types/data-source.ts#L24)

Replacement payload for [AssetDataSource.replace](../interfaces/AssetDataSource#replace). Kept `File`-only for
parity with core's `StudioAssetSource.replace` boundary; programmatic
`UploadResult` replacement stays on the registry.
