---
title: "CanvasSnapshotAdapter"
description: "@anvilkit/plugin-canvas-studio · CanvasSnapshotAdapter"
---

Defined in: [types/types.ts:73](https://github.com/ancyloce/anvilkit-studio/blob/main/src/types/types.ts#L73)

Persistence layer for canvas snapshots — the `canvas:` keyspace
analogue of `plugin-version-history`'s `SnapshotAdapter`. Implements
the same shape but typed for `CanvasIR` so a single host can wire
both Puck PageIR history and canvas design history into compatible
stores. Hosts that don't need durable canvas history can omit the
option entirely — the plugin falls back to an in-memory adapter.

## Properties

| Property | Modifier | Type | Defined in |
| ------ | ------ | ------ | ------ |
| &lt;a id="delete"&gt;&lt;/a&gt; `delete?` | `readonly` | (`designId`, `snapshotId`) =&gt; [`MaybePromise`](../type-aliases/MaybePromise)\&lt;`void`\&gt; | [types/types.ts:86](https://github.com/ancyloce/anvilkit-studio/blob/main/src/types/types.ts#L86) |
| &lt;a id="list"&gt;&lt;/a&gt; `list` | `readonly` | (`designId`) =&gt; [`MaybePromise`](../type-aliases/MaybePromise)\&lt;readonly [`CanvasSnapshotMeta`](CanvasSnapshotMeta.md)[]\&gt; | [types/types.ts:79](https://github.com/ancyloce/anvilkit-studio/blob/main/src/types/types.ts#L79) |
| &lt;a id="load"&gt;&lt;/a&gt; `load` | `readonly` | (`designId`, `snapshotId`) =&gt; [`MaybePromise`](../type-aliases/MaybePromise)\&lt;`CanvasIR` \| `null`\&gt; | [types/types.ts:82](https://github.com/ancyloce/anvilkit-studio/blob/main/src/types/types.ts#L82) |
| &lt;a id="save"&gt;&lt;/a&gt; `save` | `readonly` | (`designId`, `ir`, `meta?`) =&gt; [`MaybePromise`](../type-aliases/MaybePromise)\&lt;`string`\&gt; | [types/types.ts:74](https://github.com/ancyloce/anvilkit-studio/blob/main/src/types/types.ts#L74) |
