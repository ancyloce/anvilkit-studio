---
title: "CreateCanvasModeOverlayOptions"
description: "@anvilkit/plugin-canvas-studio · CreateCanvasModeOverlayOptions"
---

Defined in: [overlays/CanvasModeOverlay.tsx:62](https://github.com/ancyloce/anvilkit-studio/blob/main/src/overlays/CanvasModeOverlay.tsx#L62)

## Properties

| Property | Modifier | Type | Description | Defined in |
| ------ | ------ | ------ | ------ | ------ |
| &lt;a id="adapter"&gt;&lt;/a&gt; `adapter` | `readonly` | [`CanvasPersistenceAdapter`](CanvasPersistenceAdapter.md) | - | [overlays/CanvasModeOverlay.tsx:64](https://github.com/ancyloce/anvilkit-studio/blob/main/src/overlays/CanvasModeOverlay.tsx#L64) |
| &lt;a id="assetuploader"&gt;&lt;/a&gt; `assetUploader?` | `readonly` | `CanvasAssetUploader` | FR-091: upload transport forwarded to `<CanvasWorkspace assetUploader>`. | [overlays/CanvasModeOverlay.tsx:121](https://github.com/ancyloce/anvilkit-studio/blob/main/src/overlays/CanvasModeOverlay.tsx#L121) |
| &lt;a id="autosave"&gt;&lt;/a&gt; `autoSave?` | `readonly` | `boolean` \| `CanvasAutoSaveOptions` | FR-162: auto-save tuning forwarded to `<CanvasWorkspace autoSave>`. | [overlays/CanvasModeOverlay.tsx:133](https://github.com/ancyloce/anvilkit-studio/blob/main/src/overlays/CanvasModeOverlay.tsx#L133) |
| &lt;a id="brandkit"&gt;&lt;/a&gt; `brandKit?` | `readonly` | `BrandKitDefinition` | The full canonical Brand Kit (FR-031, canvas-m2-005). Takes precedence over `StudioConfig.brandKit`/`branding.primaryColor` (`studioConfigToBrandKit`) when present — `StudioConfig`'s own brand-kit block stays colors/fonts-only by design (`@anvilkit/core` must not depend on `@anvilkit/canvas-core`), so a host that wants logos, typography, tone of voice, or brand rules surfaced in the canvas editor passes a full `BrandKitDefinition` here instead. | [overlays/CanvasModeOverlay.tsx:119](https://github.com/ancyloce/anvilkit-studio/blob/main/src/overlays/CanvasModeOverlay.tsx#L119) |
| &lt;a id="headerplugins"&gt;&lt;/a&gt; `headerPlugins?` | `readonly` | readonly `CanvasHeaderPlugin`[] | FR-150/§15.16: workspace header plugins. Defaults to the editor's built-in export plugin so the overlay gets the export dialog. | [overlays/CanvasModeOverlay.tsx:131](https://github.com/ancyloce/anvilkit-studio/blob/main/src/overlays/CanvasModeOverlay.tsx#L131) |
| &lt;a id="modestore"&gt;&lt;/a&gt; `modeStore` | `readonly` | [`CanvasModeStoreApi`](CanvasModeStoreApi.md) | - | [overlays/CanvasModeOverlay.tsx:63](https://github.com/ancyloce/anvilkit-studio/blob/main/src/overlays/CanvasModeOverlay.tsx#L63) |
| &lt;a id="oncommitandclose"&gt;&lt;/a&gt; `onCommitAndClose?` | `readonly` | (`input`) =&gt; `void` \| `Promise`\&lt;`void`\&gt; | Optional callback fired when the user clicks "Back to Page". The factory `createCanvasStudioPlugin` injects a real implementation (T6: `CanvasExportBridge` — exports the stage, saves the IR, and patches the Puck DesignBlock). When omitted, "Back to Page" only closes the overlay. Receives the live Konva stage so the bridge can call `stage.toDataURL()` without re-mounting the editor. | [overlays/CanvasModeOverlay.tsx:75](https://github.com/ancyloce/anvilkit-studio/blob/main/src/overlays/CanvasModeOverlay.tsx#L75) |
| &lt;a id="onirchange"&gt;&lt;/a&gt; `onIRChange?` | `readonly` | (`designId`, `pages`) =&gt; `void` | Optional callback fired whenever the overlay learns about the IR's page list — once after the initial IR load, and again on every `<CanvasStudio>` `onChange`. The plugin uses this to keep a synchronous `designId → artboards[]` mirror up to date so the Puck DesignBlock inspector's `artboardId` field can render as a populated select. | [overlays/CanvasModeOverlay.tsx:90](https://github.com/ancyloce/anvilkit-studio/blob/main/src/overlays/CanvasModeOverlay.tsx#L90) |
| &lt;a id="onpickasset"&gt;&lt;/a&gt; `onPickAsset?` | `readonly` | () =&gt; `Promise`\&lt;`string`\&gt; | Host image picker for the editor's `image` tool. The tool calls this to resolve the asset id to place; resolve with an id present in the design's `assets` (see [seedAssets](#seedassets)), or reject / resolve `""` to cancel. Omit it to leave the image tool inert (it throws if used). | [overlays/CanvasModeOverlay.tsx:100](https://github.com/ancyloce/anvilkit-studio/blob/main/src/overlays/CanvasModeOverlay.tsx#L100) |
| &lt;a id="recoveryadapter"&gt;&lt;/a&gt; `recoveryAdapter?` | `readonly` | `false` \| `CanvasRecoveryAdapter` | FR-164: local-recovery store. Defaults to the editor's IndexedDB adapter when IndexedDB is available; `false` disables recovery. | [overlays/CanvasModeOverlay.tsx:126](https://github.com/ancyloce/anvilkit-studio/blob/main/src/overlays/CanvasModeOverlay.tsx#L126) |
| &lt;a id="seedassets"&gt;&lt;/a&gt; `seedAssets?` | `readonly` | `Readonly`\&lt;`Record`\&lt;`string`, `CanvasAssetRef`\&gt;\&gt; | Host asset-library entries merged into every opened design's `assets` (the design's own entries win on id collision). Canvas commands cannot add an asset to a live scene, so a placed image only renders if its asset is present at mount — this is how a host makes its library placeable through [onPickAsset](#onpickasset) in the overlay. | [overlays/CanvasModeOverlay.tsx:108](https://github.com/ancyloce/anvilkit-studio/blob/main/src/overlays/CanvasModeOverlay.tsx#L108) |
| &lt;a id="templates"&gt;&lt;/a&gt; `templates?` | `readonly` | readonly `CanvasTemplateEntry`[] | Template catalog for the editor's Templates dock panel (canvas-m0-009). | [overlays/CanvasModeOverlay.tsx:110](https://github.com/ancyloce/anvilkit-studio/blob/main/src/overlays/CanvasModeOverlay.tsx#L110) |
