---
title: "CreateCanvasStudioPluginOptions"
description: "@anvilkit/plugin-canvas-studio · CreateCanvasStudioPluginOptions"
---

Defined in: [plugin.ts:38](https://github.com/ancyloce/anvilkit-studio/blob/main/src/plugin.ts#L38)

## Properties

| Property | Modifier | Type | Description | Defined in |
| ------ | ------ | ------ | ------ | ------ |
| &lt;a id="adapter"&gt;&lt;/a&gt; `adapter` | `readonly` | [`CanvasPersistenceAdapter`](CanvasPersistenceAdapter.md) | - | [plugin.ts:39](https://github.com/ancyloce/anvilkit-studio/blob/main/src/plugin.ts#L39) |
| &lt;a id="assetuploader"&gt;&lt;/a&gt; `assetUploader?` | `readonly` | `CanvasAssetUploader` | Host upload transport for the editor's drag-and-drop / Uploads panel (PRD 0012 FR-091, §15.16). Passed straight through to `<CanvasWorkspace assetUploader>`; omit to leave upload surfaces in their empty state. | [plugin.ts:84](https://github.com/ancyloce/anvilkit-studio/blob/main/src/plugin.ts#L84) |
| &lt;a id="autosave"&gt;&lt;/a&gt; `autoSave?` | `readonly` | `boolean` \| `CanvasAutoSaveOptions` | Auto-save tuning forwarded to `<CanvasWorkspace autoSave>` (FR-162). The editor's default (on, debounced) applies when omitted. | [plugin.ts:101](https://github.com/ancyloce/anvilkit-studio/blob/main/src/plugin.ts#L101) |
| &lt;a id="brandkit"&gt;&lt;/a&gt; `brandKit?` | `readonly` | `BrandKitDefinition` | The full canonical Brand Kit (FR-031, canvas-m2-005) — logos, typography, tone of voice, and compliance rules, not just colors/fonts. Takes precedence over `StudioConfig.brandKit`/`branding.primaryColor` when present; see `CreateCanvasModeOverlayOptions.brandKit` for why `StudioConfig` itself stays colors/fonts-only. | [plugin.ts:77](https://github.com/ancyloce/anvilkit-studio/blob/main/src/plugin.ts#L77) |
| &lt;a id="canvassnapshotadapter"&gt;&lt;/a&gt; `canvasSnapshotAdapter?` | `readonly` | [`CanvasSnapshotAdapter`](CanvasSnapshotAdapter.md) | Optional canvas-snapshot store used by the version-history bridge. When omitted, the plugin falls back to an in-process adapter (`inMemoryCanvasSnapshotAdapter`). Hosts that want durable history — Postgres, IndexedDB, etc. — implement `CanvasSnapshotAdapter` and pass it here. | [plugin.ts:49](https://github.com/ancyloce/anvilkit-studio/blob/main/src/plugin.ts#L49) |
| &lt;a id="designblockcomponenttype"&gt;&lt;/a&gt; `designBlockComponentType?` | `readonly` | `string` | Optional override for the Puck component type id. Defaults to `"DesignBlock"`. | [plugin.ts:41](https://github.com/ancyloce/anvilkit-studio/blob/main/src/plugin.ts#L41) |
| &lt;a id="headerplugins"&gt;&lt;/a&gt; `headerPlugins?` | `readonly` | readonly `CanvasHeaderPlugin`[] | Workspace header plugins (FR-150/§15.16). Defaults to the editor's built-in export plugin so every overlay mount gets the export dialog; pass `[]` to strip it or a custom list to replace it. | [plugin.ts:96](https://github.com/ancyloce/anvilkit-studio/blob/main/src/plugin.ts#L96) |
| &lt;a id="onpickasset"&gt;&lt;/a&gt; `onPickAsset?` | `readonly` | () =&gt; `Promise`\&lt;`string`\&gt; | Host image picker for the canvas editor's `image` tool, surfaced through the overlay's `<CanvasWorkspace>`. Resolve with an asset id present in the design (seed library entries via [seedAssets](#seedassets)); reject or resolve `""` to cancel. Omit it to leave the image tool inert. | [plugin.ts:56](https://github.com/ancyloce/anvilkit-studio/blob/main/src/plugin.ts#L56) |
| &lt;a id="recoveryadapter"&gt;&lt;/a&gt; `recoveryAdapter?` | `readonly` | `false` \| `CanvasRecoveryAdapter` | Local-recovery store for unsaved drafts (FR-164). Defaults to the editor's IndexedDB adapter when IndexedDB is available; pass `false` to disable recovery entirely. | [plugin.ts:90](https://github.com/ancyloce/anvilkit-studio/blob/main/src/plugin.ts#L90) |
| &lt;a id="seedassets"&gt;&lt;/a&gt; `seedAssets?` | `readonly` | `Readonly`\&lt;`Record`\&lt;`string`, `CanvasAssetRef`\&gt;\&gt; | Host asset-library entries merged into every design the overlay opens, so ids returned by [onPickAsset](#onpickasset) resolve to renderable bytes (canvas commands cannot add assets to a live scene). The design's own assets win on id collision. | [plugin.ts:63](https://github.com/ancyloce/anvilkit-studio/blob/main/src/plugin.ts#L63) |
| &lt;a id="templates"&gt;&lt;/a&gt; `templates?` | `readonly` | readonly `CanvasTemplateEntry`[] | Template catalog surfaced in the editor's Templates dock panel (canvas-m0-009). Plain data — pass e.g. `Object.values(canvasTemplates)` from `@anvilkit/canvas-templates`. Omit to show the panel's empty state. | [plugin.ts:69](https://github.com/ancyloce/anvilkit-studio/blob/main/src/plugin.ts#L69) |
