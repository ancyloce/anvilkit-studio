---
title: "exportAllArtboards"
description: "@anvilkit/plugin-canvas-studio · exportAllArtboards"
---

```ts
function exportAllArtboards(input): Promise<ExportAllArtboardsResult>;
```

Defined in: [export/CanvasExportBridge.ts:152](https://github.com/ancyloce/anvilkit-studio/blob/main/src/export/CanvasExportBridge.ts#L152)

Export a preview for every artboard in `ir.pages` and seed the
`previewCache` per artboard.

The active artboard reuses the live stage (synchronous and accurate),
serialized through `exportStageContentDataURL` so the editor-only chrome
layers (selection, presence) are hidden — the preview is content-only,
matching the off-screen `rasterizePage` output for the other pages.
Non-active artboards are rendered through `rasterizePage` from
`@anvilkit/canvas-editor`, which mounts each page off-screen in a
detached `Konva.Stage` and serializes it. Non-active rasterizations
run with a bounded concurrency so a 20-page deck does not spawn 20
stages at once.

Per-page rasterization errors are isolated: one failure does not
abort the others and the live-stage export always runs first. The
caller can inspect `errors[]` to log or retry individual artboards.

## Parameters

| Parameter | Type |
| ------ | ------ |
| `input` | [`ExportAllArtboardsInput`](../interfaces/ExportAllArtboardsInput) |

## Returns

`Promise`\<[`ExportAllArtboardsResult`](../interfaces/ExportAllArtboardsResult)\>
