---
title: "AiGenerationContext"
description: "@anvilkit/contracts · AiGenerationContext"
---

Defined in: [ai.ts:206](https://github.com/ancyloce/anvilkit-studio/blob/main/packages/contracts/src/ai.ts#L206)

The full input to a single LLM generation call.

Constructed by the AI copilot plugin at the moment the user asks
for generation. [availableComponents](#availablecomponents) is derived from the
Puck `Config` once per session and cached; the remaining fields
are per-call hints.

## Properties

| Property | Modifier | Type | Description | Defined in |
| ------ | ------ | ------ | ------ | ------ |
| &lt;a id="availablecomponents"&gt;&lt;/a&gt; `availableComponents` | `readonly` | readonly [`AiComponentSchema`](AiComponentSchema.md)[] | Every component the LLM is allowed to emit, described as an [AiComponentSchema](AiComponentSchema.md). The LLM cannot use a component that is not present in this list. | [ai.ts:212](https://github.com/ancyloce/anvilkit-studio/blob/main/packages/contracts/src/ai.ts#L212) |
| &lt;a id="currentdata"&gt;&lt;/a&gt; `currentData?` | `readonly` | `Data` | Optional snapshot of the current Puck page data. Lets the LLM generate contextual edits (e.g. "add a CTA to the existing hero") instead of always producing a full page from scratch. | [ai.ts:218](https://github.com/ancyloce/anvilkit-studio/blob/main/packages/contracts/src/ai.ts#L218) |
| &lt;a id="locale"&gt;&lt;/a&gt; `locale?` | `readonly` | `string` | Optional BCP 47 language tag (e.g. `"en-US"`, `"fr-FR"`) so the LLM generates copy in the right language. | [ai.ts:229](https://github.com/ancyloce/anvilkit-studio/blob/main/packages/contracts/src/ai.ts#L229) |
| &lt;a id="theme"&gt;&lt;/a&gt; `theme?` | `readonly` | [`AiThemeHint`](../type-aliases/AiThemeHint) | Optional theme hint (`"light"` or `"dark"`) so the LLM can pick appropriate colors, imagery, and tone. Host apps typically forward the current editor theme. | [ai.ts:224](https://github.com/ancyloce/anvilkit-studio/blob/main/packages/contracts/src/ai.ts#L224) |
