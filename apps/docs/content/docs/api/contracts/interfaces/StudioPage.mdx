---
title: "StudioPage"
description: "@anvilkit/contracts · StudioPage"
---

Defined in: page.ts:21

A single page entry shown in the `layer/pages` sub-panel.

`route` is a tagged flag, not a route string: it tells the sidebar
to display the globe badge keyed `studio.module.layer.pages.routeBadge`.
The actual URL lives on `path` and is interpreted by the host app.

## Properties

| Property | Modifier | Type | Description | Defined in |
| ------ | ------ | ------ | ------ | ------ |
| &lt;a id="active"&gt;&lt;/a&gt; `active?` | `readonly` | `boolean` | `true` for the page currently loaded into the canvas. The host computes this from its routing state during [StudioPagesSource.list](StudioPagesSource.md#list) and re-emits via `subscribe()` when the active page changes. The sidebar renders the row with the `--ak-studio-accent` background (PRD §6.2). Optional — sources that omit it always render every row inactive. | page.ts:42 |
| &lt;a id="description"&gt;&lt;/a&gt; `description?` | `readonly` | `string` | Short meta/description text. Surfaced in the page settings dialog and (optionally) the SEO `<meta name="description">` fallback when [StudioPageSeo.metaDescription](StudioPageSeo.md#metadescription) is unset. | page.ts:48 |
| &lt;a id="id"&gt;&lt;/a&gt; `id` | `readonly` | `string` | Stable id, unique within the source. | page.ts:23 |
| &lt;a id="locked"&gt;&lt;/a&gt; `locked?` | `readonly` | `boolean` | `true` marks a page as non-renamable / non-deletable (e.g. `home`). Generalises the previous `isHome` heuristic — when set, rename and delete affordances are suppressed regardless of callback presence. | page.ts:64 |
| &lt;a id="order"&gt;&lt;/a&gt; `order?` | `readonly` | `number` | Advisory only — [StudioPagesSource.list](StudioPagesSource.md#list) order is authoritative for rendering. Hosts may use this for their own sorting/persistence. | page.ts:58 |
| &lt;a id="path"&gt;&lt;/a&gt; `path?` | `readonly` | `string` | Optional path (e.g. `"/about"`). Required for `route=true` rows. | page.ts:27 |
| &lt;a id="route"&gt;&lt;/a&gt; `route?` | `readonly` | `boolean` | `true` if this page corresponds to a public route (gets the globe badge); `false`/omitted for non-route content (templates, drafts, etc.). | page.ts:33 |
| &lt;a id="seo"&gt;&lt;/a&gt; `seo?` | `readonly` | [`StudioPageSeo`](StudioPageSeo.md) | Structured SEO block. When present, edited via the SEO section of the page settings dialog; when absent, the section is hidden. | page.ts:53 |
| &lt;a id="title"&gt;&lt;/a&gt; `title` | `readonly` | `string` | Human-readable title shown in the row. | page.ts:25 |
