---
title: "PageIRNode"
description: "@anvilkit/contracts · PageIRNode"
---

Defined in: ir.ts:153

A single node in the page IR tree.

Structurally recursive: every node may carry a `children` array of
further nodes, mirroring the component tree the author laid out in
the editor. Leaf nodes omit `children` entirely rather than setting
it to an empty array (the distinction is not semantically
meaningful, but omitting the key keeps serialized IR snapshots
smaller).

### `type` vs `id`

- [id](#id) is a stable per-node identifier, unique within a
  single [PageIR](PageIR.md) instance. Used by exporters for
  key/anchor/href generation.
- [type](#type) is the component name the node was produced from
  (e.g. `"Hero"`, `"Button"`). Exporters map this to their own
  render logic.

### Why `Readonly<Record<string, unknown>>` for props?

The IR has no awareness of specific component prop shapes — that's
the exporter's job. Keeping props opaque keeps the type surface
small and makes `PageIR` JSON-serializable without a discriminator
explosion.

## Properties

| Property | Modifier | Type | Description | Defined in |
| ------ | ------ | ------ | ------ | ------ |
| &lt;a id="assets"&gt;&lt;/a&gt; `assets?` | `readonly` | readonly [`PageIRAsset`](PageIRAsset.md)[] | Optional assets scoped to this node. Duplicates from [PageIR.assets](PageIR.md#assets) are allowed and expected — exporters deduplicate by [PageIRAsset.id](PageIRAsset.md#id). | ir.ts:196 |
| &lt;a id="children"&gt;&lt;/a&gt; `children?` | `readonly` | readonly `PageIRNode`[] | Optional child nodes. Absent on leaf nodes. | ir.ts:189 |
| &lt;a id="id"&gt;&lt;/a&gt; `id` | `readonly` | `string` | Stable per-node identifier, unique within its owning [PageIR](PageIR.md). Typically carried over from Puck's component data id so round-tripping is straightforward. | ir.ts:159 |
| &lt;a id="meta"&gt;&lt;/a&gt; `meta?` | `readonly` | [`PageIRNodeMeta`](PageIRNodeMeta.md) | Optional authoring-time metadata. See [PageIRNodeMeta](PageIRNodeMeta.md). Additive in `@anvilkit/ir@0.22` (Phase 6 / M10). Round-trip safe: when absent, every Phase 3/5 invariant (e.g. byte-equal `puckDataToIR` output across runs) is preserved. Exporters MUST NOT consume `meta`; it is an authoring contract only. | ir.ts:205 |
| &lt;a id="props"&gt;&lt;/a&gt; `props` | `readonly` | `Readonly`\&lt;`Record`\&lt;`string`, `unknown`\&gt;\&gt; | Serialized prop bag for this node. Opaque to the IR — exporters are responsible for interpreting the keys they care about and ignoring the rest. | ir.ts:172 |
| &lt;a id="slot"&gt;&lt;/a&gt; `slot?` | `readonly` | `string` | Optional parent slot/zone name this node belongs to. Top-level nodes in the root content omit this field. Nested nodes produced from a Puck slot field carry the slot field key here so `irToPuckData()` can rebuild the correct parent prop. | ir.ts:180 |
| &lt;a id="slotkind"&gt;&lt;/a&gt; `slotKind?` | `readonly` | `"slot"` \| `"zone"` | Distinguishes modern Puck slot fields from legacy `data.zones` entries when [slot](#slot) is present. Omitted means `"slot"`. | ir.ts:185 |
| &lt;a id="type"&gt;&lt;/a&gt; `type` | `readonly` | `string` | The component name this node was produced from (e.g. `"Hero"`, `"Button"`). Exporters dispatch on this value to select a renderer. | ir.ts:165 |
