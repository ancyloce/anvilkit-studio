---
title: "Utils — API Reference"
description: "@anvilkit/utils public API reference, generated by TypeDoc."
---

## Type Aliases

| Type Alias | Description |
| ------ | ------ |
| [DebouncedFunction](type-aliases/DebouncedFunction.md) | A debounced wrapper around a function, augmented with a `.cancel()` method that discards any pending invocation. |
| [DeepPartial](type-aliases/DeepPartial.md) | Recursive `Partial<T>` that descends into nested objects while leaving array element types alone. Used by `deepMerge` to accept layered config patches. |

## Functions

| Function | Description |
| ------ | ------ |
| [debounce](functions/debounce.md) | Returns a debounced version of `fn` that delays invocation until `wait` milliseconds have elapsed since the last call. If the returned function is called again within the window, the timer resets. Calling `.cancel()` drops any pending invocation without firing `fn`. |
| [deepMerge](functions/deepMerge.md) | Deeply merges one or more `sources` onto `target`, returning a **new** object. The original `target` is never mutated — callers can treat the result as an immutable snapshot. |
| [generateId](functions/generateId.md) | Generates a collision-resistant identifier. |
| [getStrictContext](functions/getStrictContext.md) | Creates a React context paired with a hook that throws a descriptive error when used outside its provider. The returned tuple is designed to be destructured into a single module, producing the common `<FooProvider>` + `useFoo()` pattern with no repeated boilerplate. |
| [invariant](functions/invariant.md) | Asserts that `condition` is truthy, throwing an `Error` with the given `message` otherwise. Because the return type is `asserts condition`, TypeScript narrows `condition` for the caller on the success path — no `if`/`throw` boilerplate required. |
