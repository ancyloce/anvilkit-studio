---
title: "DeepPartial"
description: "@anvilkit/utils · DeepPartial"
---

```ts
type DeepPartial<T> = T extends ReadonlyArray<infer _U> ? T : T extends object ? { [K in keyof T]?: DeepPartial<T[K]> } : T;
```

Defined in: [deep-merge.ts:6](https://github.com/ancyloce/anvilkit-studio/blob/main/packages/utils/src/deep-merge.ts#L6)

Recursive `Partial<T>` that descends into nested objects while
leaving array element types alone. Used by `deepMerge` to accept
layered config patches.

## Type Parameters

| Type Parameter |
| ------ |
| `T` |
