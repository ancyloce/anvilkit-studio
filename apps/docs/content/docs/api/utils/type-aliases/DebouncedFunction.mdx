---
title: "DebouncedFunction"
description: "@anvilkit/utils · DebouncedFunction"
---

```ts
type DebouncedFunction<T> = T & {
  cancel: () => void;
};
```

Defined in: [debounce.ts:8](https://github.com/ancyloce/anvilkit-studio/blob/main/packages/foundation/utils/src/debounce.ts#L8)

A debounced wrapper around a function, augmented with a `.cancel()`
method that discards any pending invocation.

## Type Declaration

| Name | Type | Description | Defined in |
| ------ | ------ | ------ | ------ |
| `cancel()` | () =&gt; `void` | Cancels any pending invocation. Calling `cancel()` on an idle debounced function is a no-op. | [debounce.ts:13](https://github.com/ancyloce/anvilkit-studio/blob/main/packages/foundation/utils/src/debounce.ts#L13) |

## Type Parameters

| Type Parameter |
| ------ |
| `T` *extends* `AnyFunction` |
