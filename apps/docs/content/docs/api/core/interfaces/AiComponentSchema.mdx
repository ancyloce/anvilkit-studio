---
title: "AiComponentSchema"
description: "@anvilkit/core · AiComponentSchema"
---

Defined in: foundation/contracts/dist/ai.d.ts:157

Description of a single component in a shape an LLM can reason
about — name, purpose, props, and an optional concrete example.

An array of these is passed to the LLM so it knows what components
are available and how to use them. Produced by
`@anvilkit/schema`'s `configToAiContext()` (Phase 3) from a Puck
`Config`; this package only owns the destination shape.

## Properties

| Property | Modifier | Type | Description | Defined in |
| ------ | ------ | ------ | ------ | ------ |
| &lt;a id="componentname"&gt;&lt;/a&gt; `componentName` | `readonly` | `string` | The component name as registered in the Puck config (e.g. `"Hero"`, `"Button"`, `"Card"`). Matches the `type` used in Puck's component data. | foundation/contracts/dist/ai.d.ts:163 |
| &lt;a id="description"&gt;&lt;/a&gt; `description` | `readonly` | `string` | Human-readable description of what the component does and when to use it. Forwarded verbatim into the LLM prompt — write it as guidance to a collaborator. | foundation/contracts/dist/ai.d.ts:169 |
| &lt;a id="example"&gt;&lt;/a&gt; `example?` | `readonly` | `Readonly`\&lt;`Record`\&lt;`string`, `unknown`\&gt;\&gt; | Optional concrete example of a valid prop bag. The LLM uses this as a one-shot example of how the fields should look when populated; leaving it off falls back to zero-shot from the field descriptions alone. | foundation/contracts/dist/ai.d.ts:180 |
| &lt;a id="fields"&gt;&lt;/a&gt; `fields` | `readonly` | readonly [`AiFieldSchema`](AiFieldSchema.md)[] | The component's props, described field-by-field. | foundation/contracts/dist/ai.d.ts:173 |
