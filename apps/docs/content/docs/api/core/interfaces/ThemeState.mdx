---
title: "ThemeState"
description: "@anvilkit/core · ThemeState"
---

Defined in: [runtime/core/src/state/slices/theme-store.ts:71](https://github.com/ancyloce/anvilkit-studio/blob/main/packages/runtime/core/src/state/slices/theme-store.ts#L71)

Full shape of the theme store — two read fields, two setters.
No `reset()` is required by the spec, but we add one for
parity with the other two stores and so tests have a uniform
isolation hook.

## Properties

| Property | Modifier | Type | Description | Defined in |
| ------ | ------ | ------ | ------ | ------ |
| &lt;a id="mode"&gt;&lt;/a&gt; `mode` | `readonly` | [`ThemeMode`](../type-aliases/ThemeMode) | The user's declared preference. Persisted. | [runtime/core/src/state/slices/theme-store.ts:75](https://github.com/ancyloce/anvilkit-studio/blob/main/packages/runtime/core/src/state/slices/theme-store.ts#L75) |
| &lt;a id="resolved"&gt;&lt;/a&gt; `resolved` | `readonly` | [`ThemeResolved`](../type-aliases/ThemeResolved) | The theme currently rendered on screen. **Never persisted** — recomputed at mount from `matchMedia` and updated on every OS-level preference change. See file header for rationale. | [runtime/core/src/state/slices/theme-store.ts:81](https://github.com/ancyloce/anvilkit-studio/blob/main/packages/runtime/core/src/state/slices/theme-store.ts#L81) |

## Methods

### reset()

```ts
reset(): void;
```

Defined in: [runtime/core/src/state/slices/theme-store.ts:98](https://github.com/ancyloce/anvilkit-studio/blob/main/packages/runtime/core/src/state/slices/theme-store.ts#L98)

Restore every field to its initial state. Present for
symmetry with the other stores in this directory — see
[ExportState.reset](ExportState.md#reset).

#### Returns

`void`

***

### setMode()

```ts
setMode(mode): void;
```

Defined in: [runtime/core/src/state/slices/theme-store.ts:86](https://github.com/ancyloce/anvilkit-studio/blob/main/packages/runtime/core/src/state/slices/theme-store.ts#L86)

Update the user's preference. Typically called from the
header's theme-toggle button.

#### Parameters

| Parameter | Type |
| ------ | ------ |
| `mode` | [`ThemeMode`](../type-aliases/ThemeMode) |

#### Returns

`void`

***

### setResolved()

```ts
setResolved(resolved): void;
```

Defined in: [runtime/core/src/state/slices/theme-store.ts:92](https://github.com/ancyloce/anvilkit-studio/blob/main/packages/runtime/core/src/state/slices/theme-store.ts#L92)

Update the resolved on-screen value. Called once at mount
with `matchMedia("(prefers-color-scheme: dark)").matches`
and again from a `change` listener inside `<Studio>`.

#### Parameters

| Parameter | Type |
| ------ | ------ |
| `resolved` | [`ThemeResolved`](../type-aliases/ThemeResolved) |

#### Returns

`void`
