---
title: "StudioRuntime"
description: "@anvilkit/core · StudioRuntime"
---

Defined in: [runtime/core/src/runtime/compile-plugins.ts:87](https://github.com/ancyloce/anvilkit-studio/blob/main/packages/runtime/core/src/runtime/compile-plugins.ts#L87)

The compiled, ready-to-mount output of [compilePlugins](../functions/compilePlugins).

Fields are documented on the return statement of `compilePlugins`
— see inline JSDoc there for the lifecycle / merge semantics of
each one.

## Properties

| Property | Modifier | Type | Description | Defined in |
| ------ | ------ | ------ | ------ | ------ |
| &lt;a id="assetresolvers"&gt;&lt;/a&gt; `assetResolvers` | `readonly` | readonly [`IRAssetResolver`](../type-aliases/IRAssetResolver)[] | Runtime-registered asset resolvers, in registration order. Exporters that support asset URL rewriting consult this list and stop at the first resolver that returns a non-null result. | [runtime/core/src/runtime/compile-plugins.ts:121](https://github.com/ancyloce/anvilkit-studio/blob/main/packages/runtime/core/src/runtime/compile-plugins.ts#L121) |
| &lt;a id="collabcapabilities"&gt;&lt;/a&gt; `collabCapabilities` | `readonly` | readonly \{ `capability`: [`StudioPluginCollabCapability`](StudioPluginCollabCapability.md); `pluginName`: `string`; \}[] | Every registered plugin's declared collaboration capability (CORE-P0-020 freeze §2): a pure, additive projection of METAs — identical inputs produce a deep-equal list; no gating happens at compile time. Consumed by the Phase 1A authoring gate. | [runtime/core/src/runtime/compile-plugins.ts:197](https://github.com/ancyloce/anvilkit-studio/blob/main/packages/runtime/core/src/runtime/compile-plugins.ts#L197) |
| &lt;a id="exportformats"&gt;&lt;/a&gt; `exportFormats` | `readonly` | `ReadonlyMap`\&lt;`string`, [`ExportFormatDefinition`](ExportFormatDefinition.md)\&lt;`Record`\&lt;`string`, `unknown`\&gt;\&gt;\&gt; | Export formats indexed by [ExportFormatDefinition.id](ExportFormatDefinition.md#id), preserving plugin-declared insertion order. | [runtime/core/src/runtime/compile-plugins.ts:114](https://github.com/ancyloce/anvilkit-studio/blob/main/packages/runtime/core/src/runtime/compile-plugins.ts#L114) |
| &lt;a id="headeractions"&gt;&lt;/a&gt; `headerActions` | `readonly` | readonly [`StudioHeaderAction`](StudioHeaderAction.md)[] | Header action descriptors in the order they were contributed. Duplicate ids pass through here — `core-009`'s `composeHeaderActions()` is responsible for dedupe/ordering. | [runtime/core/src/runtime/compile-plugins.ts:127](https://github.com/ancyloce/anvilkit-studio/blob/main/packages/runtime/core/src/runtime/compile-plugins.ts#L127) |
| &lt;a id="i18n"&gt;&lt;/a&gt; `i18n` | `readonly` | \{ `entries`: readonly `RegistryEntry`[]; \} | Plugin-contributed i18n message bundles, in registration order, collected React-free during compile via `ctx.registerMessages`. The React `EditorI18nProvider` prepends the core `studio.*` entry and merges these so plugin namespaces join the catalog `useMsg` resolves. | [runtime/core/src/runtime/compile-plugins.ts:188](https://github.com/ancyloce/anvilkit-studio/blob/main/packages/runtime/core/src/runtime/compile-plugins.ts#L188) |
| `i18n.entries` | `readonly` | readonly `RegistryEntry`[] | - | [runtime/core/src/runtime/compile-plugins.ts:189](https://github.com/ancyloce/anvilkit-studio/blob/main/packages/runtime/core/src/runtime/compile-plugins.ts#L189) |
| &lt;a id="lifecycle"&gt;&lt;/a&gt; `lifecycle` | `readonly` | [`LifecycleManager`](LifecycleManager.md) | The lifecycle dispatcher. See [createLifecycleManager](../functions/createLifecycleManager). | [runtime/core/src/runtime/compile-plugins.ts:109](https://github.com/ancyloce/anvilkit-studio/blob/main/packages/runtime/core/src/runtime/compile-plugins.ts#L109) |
| &lt;a id="overlays"&gt;&lt;/a&gt; `overlays` | `readonly` | readonly [`StudioPluginOverlay`](StudioPluginOverlay.md)[] | Plugin-contributed top-level overlay components, sorted within each placement bucket by `(order ?? 100, registrationIndex)`. `<Studio>` dispatches them by `placement` (canvas / viewport / notifications). | [runtime/core/src/runtime/compile-plugins.ts:159](https://github.com/ancyloce/anvilkit-studio/blob/main/packages/runtime/core/src/runtime/compile-plugins.ts#L159) |
| &lt;a id="overrides"&gt;&lt;/a&gt; `overrides` | `readonly` | readonly `Partial`\&lt;[`Overrides`](https://puckeditor.com/docs/api-reference/configuration/overrides)\&gt;[] | Plugin-contributed Puck override slices, in registration order. This is the **raw** per-plugin array — `<Studio>` (`core-014`) runs it through `mergeOverrides([...runtime.overrides, consumer])` to produce the curried-per-key merge Puck actually receives. Keeping the array un-composed here means the `runtime/` layer stays React-free (architecture §17) and the `react/` layer owns the composition step. Entries appear in plugin registration order, first-registered first — so when [mergeOverrides](../functions/mergeOverrides) folds them, the first plugin becomes the innermost wrapper (closest to the default render) and later plugins wrap it. | [runtime/core/src/runtime/compile-plugins.ts:143](https://github.com/ancyloce/anvilkit-studio/blob/main/packages/runtime/core/src/runtime/compile-plugins.ts#L143) |
| &lt;a id="pluginmeta"&gt;&lt;/a&gt; `pluginMeta` | `readonly` | readonly [`StudioPluginMeta`](StudioPluginMeta.md)[] | The `meta` block of every Studio plugin that successfully registered, in compile order (declaration order unless a plugin sets `order` / `dependsOn`). Diagnostic surface only — the runtime does not re-read fields from here after compile. | [runtime/core/src/runtime/compile-plugins.ts:94](https://github.com/ancyloce/anvilkit-studio/blob/main/packages/runtime/core/src/runtime/compile-plugins.ts#L94) |
| &lt;a id="providers"&gt;&lt;/a&gt; `providers` | `readonly` | readonly [`StudioPluginProvider`](StudioPluginProvider.md)[] | Plugin-contributed React providers that wrap the Studio tree. Sorted ascending by `(order ?? 100, registrationIndex)`. The provider at index 0 is composed **outermost** by `<Studio>`. The runtime layer treats the `component` field as opaque — `<Studio>` is the React boundary that instantiates each provider. | [runtime/core/src/runtime/compile-plugins.ts:152](https://github.com/ancyloce/anvilkit-studio/blob/main/packages/runtime/core/src/runtime/compile-plugins.ts#L152) |
| &lt;a id="puckplugins"&gt;&lt;/a&gt; `puckPlugins` | `readonly` | readonly [`Plugin`](https://puckeditor.com/docs/extending-puck/plugins)[] | Raw Puck plugin objects, passed through to `<Puck plugins={…}>` verbatim. These do not participate in the Studio lifecycle. | [runtime/core/src/runtime/compile-plugins.ts:170](https://github.com/ancyloce/anvilkit-studio/blob/main/packages/runtime/core/src/runtime/compile-plugins.ts#L170) |
| &lt;a id="registrations"&gt;&lt;/a&gt; `registrations` | `readonly` | readonly [`StudioPluginRegistration`](StudioPluginRegistration.md)\&lt;`Config`\&gt;[] | The raw plugin registrations in compile order (declaration order unless a plugin sets `order` / `dependsOn`), as returned by each plugin's `register()`. Primarily consumed by the `@anvilkit/core/testing` harness so unit tests can drive a single plugin's lifecycle hooks without re-implementing the compile-time invariant checks (structural guard, coreVersion range, duplicate-id detection). | [runtime/core/src/runtime/compile-plugins.ts:104](https://github.com/ancyloce/anvilkit-studio/blob/main/packages/runtime/core/src/runtime/compile-plugins.ts#L104) |
| &lt;a id="sidebar"&gt;&lt;/a&gt; `sidebar` | `readonly` | [`StudioSidebarContributions`](StudioSidebarContributions.md) | Plugin-contributed sidebar surfaces (insert sections, layer quick-adds, asset source/actions, copy packs, copilot/history/ design-system panels), collected React-free during compile (review finding **AR-b**). Mirrors [assetResolvers](#assetresolvers): the runtime owns the collection so headless consumers (CLI exporter, `@anvilkit/core/testing`) read contributions directly, while the React `sidebar-registry-store` remains the live view the chrome renders from (both are written in lock-step during compile). | [runtime/core/src/runtime/compile-plugins.ts:181](https://github.com/ancyloce/anvilkit-studio/blob/main/packages/runtime/core/src/runtime/compile-plugins.ts#L181) |
| &lt;a id="slots"&gt;&lt;/a&gt; `slots` | `readonly` | `ReadonlyMap`\&lt;`string`, [`StudioPluginSlotContribution`](StudioPluginSlotContribution.md)\&gt; | Plugin-contributed named chrome slot contributions. Single- occupancy: if two plugins contribute the same slot id the first registration wins (a warn is logged via `ctx.log` on duplicate). | [runtime/core/src/runtime/compile-plugins.ts:165](https://github.com/ancyloce/anvilkit-studio/blob/main/packages/runtime/core/src/runtime/compile-plugins.ts#L165) |
