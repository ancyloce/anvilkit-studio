---
title: "StudioRuntime"
description: "@anvilkit/core · StudioRuntime"
---

Defined in: [core/src/runtime/compile-plugins.ts:86](https://github.com/ancyloce/anvilkit-studio/blob/main/packages/core/src/runtime/compile-plugins.ts#L86)

The compiled, ready-to-mount output of [compilePlugins](../functions/compilePlugins).

Fields are documented on the return statement of `compilePlugins`
— see inline JSDoc there for the lifecycle / merge semantics of
each one.

## Properties

| Property | Modifier | Type | Description | Defined in |
| ------ | ------ | ------ | ------ | ------ |
| &lt;a id="assetresolvers"&gt;&lt;/a&gt; `assetResolvers` | `readonly` | readonly [`IRAssetResolver`](../type-aliases/IRAssetResolver)[] | Runtime-registered asset resolvers, in registration order. Exporters that support asset URL rewriting consult this list and stop at the first resolver that returns a non-null result. | [core/src/runtime/compile-plugins.ts:120](https://github.com/ancyloce/anvilkit-studio/blob/main/packages/core/src/runtime/compile-plugins.ts#L120) |
| &lt;a id="exportformats"&gt;&lt;/a&gt; `exportFormats` | `readonly` | `ReadonlyMap`\&lt;`string`, [`ExportFormatDefinition`](ExportFormatDefinition.md)\&lt;`Record`\&lt;`string`, `unknown`\&gt;\&gt;\&gt; | Export formats indexed by [ExportFormatDefinition.id](ExportFormatDefinition.md#id), preserving plugin-declared insertion order. | [core/src/runtime/compile-plugins.ts:113](https://github.com/ancyloce/anvilkit-studio/blob/main/packages/core/src/runtime/compile-plugins.ts#L113) |
| &lt;a id="headeractions"&gt;&lt;/a&gt; `headerActions` | `readonly` | readonly [`StudioHeaderAction`](StudioHeaderAction.md)[] | Header action descriptors in the order they were contributed. Duplicate ids pass through here — `core-009`'s `composeHeaderActions()` is responsible for dedupe/ordering. | [core/src/runtime/compile-plugins.ts:126](https://github.com/ancyloce/anvilkit-studio/blob/main/packages/core/src/runtime/compile-plugins.ts#L126) |
| &lt;a id="i18n"&gt;&lt;/a&gt; `i18n` | `readonly` | \{ `entries`: readonly `RegistryEntry`[]; \} | Plugin-contributed i18n message bundles, in registration order, collected React-free during compile via `ctx.registerMessages`. The React `EditorI18nProvider` prepends the core `studio.*` entry and merges these so plugin namespaces join the catalog `useMsg` resolves. | [core/src/runtime/compile-plugins.ts:187](https://github.com/ancyloce/anvilkit-studio/blob/main/packages/core/src/runtime/compile-plugins.ts#L187) |
| `i18n.entries` | `readonly` | readonly `RegistryEntry`[] | - | [core/src/runtime/compile-plugins.ts:188](https://github.com/ancyloce/anvilkit-studio/blob/main/packages/core/src/runtime/compile-plugins.ts#L188) |
| &lt;a id="lifecycle"&gt;&lt;/a&gt; `lifecycle` | `readonly` | [`LifecycleManager`](LifecycleManager.md) | The lifecycle dispatcher. See [createLifecycleManager](../functions/createLifecycleManager). | [core/src/runtime/compile-plugins.ts:108](https://github.com/ancyloce/anvilkit-studio/blob/main/packages/core/src/runtime/compile-plugins.ts#L108) |
| &lt;a id="overlays"&gt;&lt;/a&gt; `overlays` | `readonly` | readonly [`StudioPluginOverlay`](StudioPluginOverlay.md)[] | Plugin-contributed top-level overlay components, sorted within each placement bucket by `(order ?? 100, registrationIndex)`. `<Studio>` dispatches them by `placement` (canvas / viewport / notifications). | [core/src/runtime/compile-plugins.ts:158](https://github.com/ancyloce/anvilkit-studio/blob/main/packages/core/src/runtime/compile-plugins.ts#L158) |
| &lt;a id="overrides"&gt;&lt;/a&gt; `overrides` | `readonly` | readonly `Partial`\&lt;[`Overrides`](https://puckeditor.com/docs/api-reference/configuration/overrides)\&gt;[] | Plugin-contributed Puck override slices, in registration order. This is the **raw** per-plugin array — `<Studio>` (`core-014`) runs it through `mergeOverrides([...runtime.overrides, consumer])` to produce the curried-per-key merge Puck actually receives. Keeping the array un-composed here means the `runtime/` layer stays React-free (architecture §17) and the `react/` layer owns the composition step. Entries appear in plugin registration order, first-registered first — so when [mergeOverrides](../functions/mergeOverrides) folds them, the first plugin becomes the innermost wrapper (closest to the default render) and later plugins wrap it. | [core/src/runtime/compile-plugins.ts:142](https://github.com/ancyloce/anvilkit-studio/blob/main/packages/core/src/runtime/compile-plugins.ts#L142) |
| &lt;a id="pluginmeta"&gt;&lt;/a&gt; `pluginMeta` | `readonly` | readonly [`StudioPluginMeta`](StudioPluginMeta.md)[] | The `meta` block of every Studio plugin that successfully registered, in compile order (declaration order unless a plugin sets `order` / `dependsOn`). Diagnostic surface only — the runtime does not re-read fields from here after compile. | [core/src/runtime/compile-plugins.ts:93](https://github.com/ancyloce/anvilkit-studio/blob/main/packages/core/src/runtime/compile-plugins.ts#L93) |
| &lt;a id="providers"&gt;&lt;/a&gt; `providers` | `readonly` | readonly [`StudioPluginProvider`](StudioPluginProvider.md)[] | Plugin-contributed React providers that wrap the Studio tree. Sorted ascending by `(order ?? 100, registrationIndex)`. The provider at index 0 is composed **outermost** by `<Studio>`. The runtime layer treats the `component` field as opaque — `<Studio>` is the React boundary that instantiates each provider. | [core/src/runtime/compile-plugins.ts:151](https://github.com/ancyloce/anvilkit-studio/blob/main/packages/core/src/runtime/compile-plugins.ts#L151) |
| &lt;a id="puckplugins"&gt;&lt;/a&gt; `puckPlugins` | `readonly` | readonly [`Plugin`](https://puckeditor.com/docs/extending-puck/plugins)[] | Raw Puck plugin objects, passed through to `<Puck plugins={…}>` verbatim. These do not participate in the Studio lifecycle. | [core/src/runtime/compile-plugins.ts:169](https://github.com/ancyloce/anvilkit-studio/blob/main/packages/core/src/runtime/compile-plugins.ts#L169) |
| &lt;a id="registrations"&gt;&lt;/a&gt; `registrations` | `readonly` | readonly [`StudioPluginRegistration`](StudioPluginRegistration.md)\&lt;`Config`\&gt;[] | The raw plugin registrations in compile order (declaration order unless a plugin sets `order` / `dependsOn`), as returned by each plugin's `register()`. Primarily consumed by the `@anvilkit/core/testing` harness so unit tests can drive a single plugin's lifecycle hooks without re-implementing the compile-time invariant checks (structural guard, coreVersion range, duplicate-id detection). | [core/src/runtime/compile-plugins.ts:103](https://github.com/ancyloce/anvilkit-studio/blob/main/packages/core/src/runtime/compile-plugins.ts#L103) |
| &lt;a id="sidebar"&gt;&lt;/a&gt; `sidebar` | `readonly` | [`StudioSidebarContributions`](StudioSidebarContributions.md) | Plugin-contributed sidebar surfaces (insert sections, layer quick-adds, asset source/actions, copy packs, copilot/history/ design-system panels), collected React-free during compile (review finding **AR-b**). Mirrors [assetResolvers](#assetresolvers): the runtime owns the collection so headless consumers (CLI exporter, `@anvilkit/core/testing`) read contributions directly, while the React `sidebar-registry-store` remains the live view the chrome renders from (both are written in lock-step during compile). | [core/src/runtime/compile-plugins.ts:180](https://github.com/ancyloce/anvilkit-studio/blob/main/packages/core/src/runtime/compile-plugins.ts#L180) |
| &lt;a id="slots"&gt;&lt;/a&gt; `slots` | `readonly` | `ReadonlyMap`\&lt;`string`, [`StudioPluginSlotContribution`](StudioPluginSlotContribution.md)\&gt; | Plugin-contributed named chrome slot contributions. Single- occupancy: if two plugins contribute the same slot id the first registration wins (a warn is logged via `ctx.log` on duplicate). | [core/src/runtime/compile-plugins.ts:164](https://github.com/ancyloce/anvilkit-studio/blob/main/packages/core/src/runtime/compile-plugins.ts#L164) |
