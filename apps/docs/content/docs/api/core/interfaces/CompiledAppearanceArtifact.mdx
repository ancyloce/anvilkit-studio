---
title: "CompiledAppearanceArtifact"
description: "@anvilkit/core · CompiledAppearanceArtifact"
---

Defined in: foundation/contracts/dist/export.d.ts:122

The unified appearance compiler's output as exporters consume it
(PLAN-0025 §9.3, P4-05/P4-06). A structural mirror of
`@anvilkit/core`'s `CompiledAppearance` (contracts cannot depend on
core): the export runner compiles the EXACT document being exported
and hands formats this artifact, so exporters never re-derive
appearance CSS with a second algorithm (§7.5 — dual resolvers are
prohibited).

## Properties

| Property | Modifier | Type | Description | Defined in |
| ------ | ------ | ------ | ------ | ------ |
| &lt;a id="css"&gt;&lt;/a&gt; `css` | `readonly` | `string` | Deterministic document CSS (base rules + media queries). | foundation/contracts/dist/export.d.ts:124 |
| &lt;a id="fingerprint"&gt;&lt;/a&gt; `fingerprint` | `readonly` | `string` | Content fingerprint — the §14.4 cross-surface parity key. | foundation/contracts/dist/export.d.ts:126 |
| &lt;a id="stylednodeids"&gt;&lt;/a&gt; `styledNodeIds` | `readonly` | readonly `string`[] | Ids of nodes the stylesheet actually targets. | foundation/contracts/dist/export.d.ts:128 |
| &lt;a id="targetmanifest"&gt;&lt;/a&gt; `targetManifest` | `readonly` | `Readonly`\&lt;`Record`\&lt;`string`, readonly `string`[]\&gt;\&gt; | Styled target ids per node id (selector attribute pairs). | foundation/contracts/dist/export.d.ts:130 |
