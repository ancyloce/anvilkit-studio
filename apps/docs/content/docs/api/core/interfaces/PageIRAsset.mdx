---
title: "PageIRAsset"
description: "@anvilkit/core · PageIRAsset"
---

Defined in: foundation/contracts/dist/ir.d.ts:49

An out-of-band resource referenced by the page — images, videos,
fonts, stylesheets, inline scripts, etc.

Assets are listed both at the root of [PageIR](PageIR.md) (so exporters
can emit a manifest or preload block in one pass) and, optionally,
on individual [PageIRNode](PageIRNode.md)s (so a node-scoped exporter can
resolve its own dependencies without walking the whole tree).

The same asset may appear in both places — exporters are expected
to deduplicate by [PageIRAsset.id](#id).

## Properties

| Property | Modifier | Type | Description | Defined in |
| ------ | ------ | ------ | ------ | ------ |
| &lt;a id="id"&gt;&lt;/a&gt; `id` | `readonly` | `string` | Stable, unique identifier for this asset within the IR. Typically a hash of the URL or a caller-provided slug. Exporters use this for deduplication when the same asset is referenced by multiple nodes. | foundation/contracts/dist/ir.d.ts:57 |
| &lt;a id="kind"&gt;&lt;/a&gt; `kind` | `readonly` | `"other"` \| `"image"` \| `"video"` \| `"font"` \| `"script"` \| `"style"` | The asset's category. Closed union — extending this list is a breaking change to the IR contract. Use `"other"` for anything that doesn't fit the named categories rather than introducing an ad-hoc string. | foundation/contracts/dist/ir.d.ts:65 |
| &lt;a id="meta"&gt;&lt;/a&gt; `meta?` | `readonly` | `Readonly`\&lt;`Record`\&lt;`string`, `unknown`\&gt;\&gt; | Free-form metadata the producer wants to pass through to consumers (e.g. `{ width: 1600, height: 900 }` for an image). Exporters should treat unknown keys as forward-compatible — a missing key must not cause a hard failure. | foundation/contracts/dist/ir.d.ts:81 |
| &lt;a id="url"&gt;&lt;/a&gt; `url` | `readonly` | `string` | Absolute or root-relative URL pointing at the asset. Exporters may rewrite this (e.g. to a CDN or a hashed filename) during serialization — the IR shape does not constrain the scheme. | foundation/contracts/dist/ir.d.ts:73 |
