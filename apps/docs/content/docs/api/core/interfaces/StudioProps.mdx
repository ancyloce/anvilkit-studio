---
title: "StudioProps"
description: "@anvilkit/core · StudioProps"
---

Defined in: [runtime/core/src/react/components/studio-controller-types.ts:97](https://github.com/ancyloce/anvilkit-studio/blob/main/packages/runtime/core/src/react/components/studio-controller-types.ts#L97)

Props accepted by `<Studio>` and consumed by the controller. Lives
here (not in `Studio.tsx`) so there is **no import cycle** between
the view and its controller — `Studio.tsx` re-exports it to keep the
public `@anvilkit/core/react` path (`{ StudioProps }`) unchanged.

Mirrors the subset of `<Puck>`'s props meaningful at the Studio
shell level, plus the Studio-specific `plugins`, `config`, and
legacy `aiHost` slots. Kept a plain interface (not
`PropsWithChildren`) because `<Studio>` delegates its entire UI to
`<Puck>`.

## Type Parameters

| Type Parameter | Default type |
| ------ | ------ |
| `UserConfig` *extends* `PuckConfig` | `PuckConfig` |

## Properties

| Property | Modifier | Type | Description | Defined in |
| ------ | ------ | ------ | ------ | ------ |
| &lt;a id="aihost"&gt;&lt;/a&gt; ~~`aiHost?`~~ | `readonly` | `string` | **Deprecated** Legacy `aiHost` string. When provided, `<Studio>` dynamically imports `@anvilkit/core/compat/ai-host-adapter` and prepends the adapter to the plugin list. Migrate to `createAiGenerationPlugin()` from `@anvilkit/plugins/ai-generation`. | [runtime/core/src/react/components/studio-controller-types.ts:162](https://github.com/ancyloce/anvilkit-studio/blob/main/packages/runtime/core/src/react/components/studio-controller-types.ts#L162) |
| &lt;a id="analytics"&gt;&lt;/a&gt; `analytics?` | `readonly` | [`StudioAnalyticsPort`](StudioAnalyticsPort.md) | Optional analytics sink. Typed as the runtime-owned [StudioAnalyticsPort](StudioAnalyticsPort.md) — every `@anvilkit/analytics-core` adapter satisfies it structurally. When set, `<Studio>` emits the system events `draft_saved` / `page_published` / `component_dropped` with lightweight props only. Omitting it is a complete no-op — `<Studio>` behaves identically. | [runtime/core/src/react/components/studio-controller-types.ts:304](https://github.com/ancyloce/anvilkit-studio/blob/main/packages/runtime/core/src/react/components/studio-controller-types.ts#L304) |
| &lt;a id="chrome"&gt;&lt;/a&gt; `chrome?` | `readonly` | `StudioChromeMode` | Which chrome to render. `"anvilkit"` (default) mounts the AnvilKit Studio shell; `"puck"` ships the raw `@puckeditor/core` UI (bit-for-bit pre-Phase-5 output). | [runtime/core/src/react/components/studio-controller-types.ts:168](https://github.com/ancyloce/anvilkit-studio/blob/main/packages/runtime/core/src/react/components/studio-controller-types.ts#L168) |
| &lt;a id="config"&gt;&lt;/a&gt; `config?` | `readonly` | \{ `ai?`: \{ `defaultModel?`: `string`; `maxRetries?`: `number`; \}; `branding?`: \{ `appName?`: `string`; `logoUrl?`: `string`; `primaryColor?`: `string`; \}; `brandKit?`: \{ `colors?`: \{ `name`: `string`; `value`: `string`; \}[]; `fonts?`: `string`[]; \}; `experimental?`: \{ \[`key`: `string`\]: `unknown`; \}; `export?`: \{ `defaultFormat?`: `string`; `filenamePrefix?`: `string`; \}; `features?`: \{ `enableAi?`: `boolean`; `enableCollaboration?`: `boolean`; `enableExport?`: `boolean`; \}; `i18n?`: \{ `fallbackLocale?`: `string`; `locale?`: `string`; `messages?`: \{ \[`key`: `string`\]: \| \{ \[`key`: `string`\]: `string` \| `undefined`; \} \| `undefined`; \}; `showLocaleSwitch?`: `boolean`; \}; `theme?`: \{ `allowToggle?`: `boolean`; `defaultMode?`: `"light"` \| `"dark"` \| `"system"`; \}; \} | Layer 3 host overrides forwarded to `createStudioConfig`. `<Studio>` owns the config factory call so descendants get a single `StudioConfigProvider`-scoped instance. | [runtime/core/src/react/components/studio-controller-types.ts:138](https://github.com/ancyloce/anvilkit-studio/blob/main/packages/runtime/core/src/react/components/studio-controller-types.ts#L138) |
| `config.ai?` | `public` | \{ `defaultModel?`: `string`; `maxRetries?`: `number`; \} | AI block. Intentionally carries **no credentials** — API keys, endpoint URLs, and auth tokens belong to the host backend, and the AI copilot plugin factory receives a `generatePage()` callback that wraps whatever network path the host has set up. Architecture §9 is explicit about this boundary. | [runtime/core/src/config/schema.ts:294](https://github.com/ancyloce/anvilkit-studio/blob/main/packages/runtime/core/src/config/schema.ts#L294) |
| `config.ai.defaultModel?` | `public` | `string` | Optional hint for the AI plugin about which model to request by default (e.g. `"claude-opus-4-6"`). Plugins are free to ignore this if their host-side implementation is locked to a specific model. | [runtime/core/src/config/schema.ts:302](https://github.com/ancyloce/anvilkit-studio/blob/main/packages/runtime/core/src/config/schema.ts#L302) |
| `config.ai.maxRetries?` | `public` | `number` | Maximum number of retries for a failed AI generation call. Clamped to `[0, 10]` so hosts cannot accidentally enable unbounded retry storms. Defaults to `3`. | [runtime/core/src/config/schema.ts:309](https://github.com/ancyloce/anvilkit-studio/blob/main/packages/runtime/core/src/config/schema.ts#L309) |
| `config.branding?` | `public` | \{ `appName?`: `string`; `logoUrl?`: `string`; `primaryColor?`: `string`; \} | Branding block for the editor chrome — app name, logo, and accent color surfaced in the Studio header. | [runtime/core/src/config/schema.ts:110](https://github.com/ancyloce/anvilkit-studio/blob/main/packages/runtime/core/src/config/schema.ts#L110) |
| `config.branding.appName?` | `public` | `string` | Display name shown in the editor header and the browser tab. Defaults to `"AnvilKit Studio"` so an un-configured host still looks intentional. | [runtime/core/src/config/schema.ts:117](https://github.com/ancyloce/anvilkit-studio/blob/main/packages/runtime/core/src/config/schema.ts#L117) |
| `config.branding.logoUrl?` | `public` | `string` | Optional URL of a logo rendered in the header. Validated as a URL via `z.url()` (top-level format in Zod v4). Omit to fall back to the default wordmark. | [runtime/core/src/config/schema.ts:124](https://github.com/ancyloce/anvilkit-studio/blob/main/packages/runtime/core/src/config/schema.ts#L124) |
| `config.branding.primaryColor?` | `public` | `string` | Optional primary accent color for the editor chrome. Free-form string so hosts can pass a CSS variable reference (`"var(--brand)"`) or any CSS color value. | [runtime/core/src/config/schema.ts:131](https://github.com/ancyloce/anvilkit-studio/blob/main/packages/runtime/core/src/config/schema.ts#L131) |
| `config.brandKit?` | `public` | \{ `colors?`: \{ `name`: `string`; `value`: `string`; \}[]; `fonts?`: `string`[]; \} | Brand-kit block (I3-4) — shared colors + font families the Canvas Studio editor surfaces (color pickers, font menus) so designs stay on-brand. Distinct from `branding`, which styles the editor *chrome*: `brandKit` is design content the canvas reads via its `useBrandKit()` hook. `plugin-canvas-studio` maps these entries to the editor's `BrandKit` shape. Deliberately stays colors/fonts-only (canvas-m2-005, FR-031): the canonical `BrandKitDefinition` (logos, typography, tone of voice, compliance rules) lives in `@anvilkit/canvas-core`, and `@anvilkit/core` must not take a dependency on a canvas-specific package to widen this schema. A host that wants the fuller kit surfaced passes a `BrandKitDefinition` directly to `createCanvasStudioPlugin({ brandKit })`, which takes precedence over this block. | [runtime/core/src/config/schema.ts:238](https://github.com/ancyloce/anvilkit-studio/blob/main/packages/runtime/core/src/config/schema.ts#L238) |
| `config.brandKit.colors?` | `public` | \{ `name`: `string`; `value`: `string`; \}[] | Named brand color swatches. Each `value` is any CSS color (`"#2563eb"`, `"var(--brand)"`). Empty by default. | [runtime/core/src/config/schema.ts:244](https://github.com/ancyloce/anvilkit-studio/blob/main/packages/runtime/core/src/config/schema.ts#L244) |
| `config.brandKit.fonts?` | `public` | `string`[] | Brand font families, e.g. `["Inter", "Poppins"]`. The first entry is treated as the brand default. Empty by default. | [runtime/core/src/config/schema.ts:259](https://github.com/ancyloce/anvilkit-studio/blob/main/packages/runtime/core/src/config/schema.ts#L259) |
| `config.experimental?` | `readonly` | \{ \[`key`: `string`\]: `unknown`; \} | - | [runtime/core/src/types/config.ts:82](https://github.com/ancyloce/anvilkit-studio/blob/main/packages/runtime/core/src/types/config.ts#L82) |
| `config.export?` | `public` | \{ `defaultFormat?`: `string`; `filenamePrefix?`: `string`; \} | Export pipeline block — configuration for the export registry and download flow. | [runtime/core/src/config/schema.ts:266](https://github.com/ancyloce/anvilkit-studio/blob/main/packages/runtime/core/src/config/schema.ts#L266) |
| `config.export.defaultFormat?` | `public` | `string` | Optional id of the format to pre-select in export menus (e.g. `"html"`, `"react"`). Resolved at runtime against the registered format registry — an id that does not match any registered format falls back to the first registered format. | [runtime/core/src/config/schema.ts:275](https://github.com/ancyloce/anvilkit-studio/blob/main/packages/runtime/core/src/config/schema.ts#L275) |
| `config.export.filenamePrefix?` | `public` | `string` | Filename prefix for downloads. The export pipeline appends the format's extension (e.g. `"page.html"`). Defaults to `"page"` — hosts typically override with a slug derived from the current page title. | [runtime/core/src/config/schema.ts:283](https://github.com/ancyloce/anvilkit-studio/blob/main/packages/runtime/core/src/config/schema.ts#L283) |
| `config.features?` | `public` | \{ `enableAi?`: `boolean`; `enableCollaboration?`: `boolean`; `enableExport?`: `boolean`; \} | Feature flag block. Every flag defaults to `false` so a fresh install has no surprise behavior enabled — hosts opt in explicitly. | [runtime/core/src/config/schema.ts:76](https://github.com/ancyloce/anvilkit-studio/blob/main/packages/runtime/core/src/config/schema.ts#L76) |
| `config.features.enableAi?` | `public` | `boolean` | When `true`, the AI copilot plugin (if installed) mounts its panel and the `generatePage()` pipeline is active. No-op when the AI plugin is not in the host's plugin array. | [runtime/core/src/config/schema.ts:91](https://github.com/ancyloce/anvilkit-studio/blob/main/packages/runtime/core/src/config/schema.ts#L91) |
| `config.features.enableCollaboration?` | `public` | `boolean` | **Experimental / reserved.** `@anvilkit/core` does **not** act on this flag — there is no collaboration implementation in core. Real-time collaboration (presence / cursors) is provided by the separate, experimental `@anvilkit/plugin-collab-*` packages a host wires in; this is a reserved config seam those packages (or host code) may consult to gate the presence layer. Defaulted to `false` and kept stable so turning collaboration on later is not a breaking config change. No core behavior keys off it today. | [runtime/core/src/config/schema.ts:103](https://github.com/ancyloce/anvilkit-studio/blob/main/packages/runtime/core/src/config/schema.ts#L103) |
| `config.features.enableExport?` | `public` | `boolean` | When `true`, the export registry is compiled and the host may call `exportAs(formatId)`. When `false`, exporter plugins register but their header actions are hidden and `exportAs` throws `StudioExportError`. | [runtime/core/src/config/schema.ts:84](https://github.com/ancyloce/anvilkit-studio/blob/main/packages/runtime/core/src/config/schema.ts#L84) |
| `config.i18n?` | `public` | \{ `fallbackLocale?`: `string`; `locale?`: `string`; `messages?`: \{ \[`key`: `string`\]: \| \{ \[`key`: `string`\]: `string` \| `undefined`; \} \| `undefined`; \}; `showLocaleSwitch?`: `boolean`; \} | Internationalization block — the active locale, the fallback locale, optional per-locale message overrides, and the built-in language-switcher flag. The whole block is **reactive**: it is excluded from the plugin compile fingerprint (`stripReactiveConfig`), so changing any `i18n.*` value at runtime updates the editor chrome in place — no plugin recompile, no editor teardown. The env override nests: `ANVILKIT_I18N__LOCALE=zh` maps to `i18n.locale` (the `__` separator). A bare `ANVILKIT_LOCALE` is rejected — the root schema is `strictObject`, so a top-level `locale` key is unknown. | [runtime/core/src/config/schema.ts:171](https://github.com/ancyloce/anvilkit-studio/blob/main/packages/runtime/core/src/config/schema.ts#L171) |
| `config.i18n.fallbackLocale?` | `public` | `string` | Locale whose [messages](#config) entries back-fill keys missing from the active locale's overrides. Defaults to `"en"`. | [runtime/core/src/config/schema.ts:197](https://github.com/ancyloce/anvilkit-studio/blob/main/packages/runtime/core/src/config/schema.ts#L197) |
| `config.i18n.locale?` | `public` | `string` | Active locale (BCP-47-ish tag, e.g. `"en"`, `"zh"`). When the host **explicitly sets** this on the raw `<Studio config>` prop, the instance is locale-**controlled**: this value is authoritative and reactive (changing it switches the language live), the built-in `localStorage` persistence is bypassed, and internal switch requests (the built-in `LanguageSwitcher`) only fire `onLocaleChange` — the host applies them by re-rendering with the new value. Controlled-ness is latched at mount (remount with a new React `key` to switch modes). When absent, the instance is uncontrolled: the locale store defaults to `"en"`, persists per `storeId`, and a non-`"en"` value resolved from env (`ANVILKIT_I18N__LOCALE`) seeds the store once when no persisted choice exists. Env values never trigger controlled mode. | [runtime/core/src/config/schema.ts:192](https://github.com/ancyloce/anvilkit-studio/blob/main/packages/runtime/core/src/config/schema.ts#L192) |
| `config.i18n.messages?` | `public` | \{ \[`key`: `string`\]: \| \{ \[`key`: `string`\]: `string` \| `undefined`; \} \| `undefined`; \} | Optional per-locale message overrides, keyed `locale → (messageKey → string)`. Applied to the rendered chrome catalog for the active locale (with [fallbackLocale](#config) back-fill) and to `ctx.t`. Beats the built-in catalog and plugin packs; the deprecated flat `<Studio messages>` prop still wins over these during its migration window. Omit to use the built-in English catalog plus any plugin packs. | [runtime/core/src/config/schema.ts:208](https://github.com/ancyloce/anvilkit-studio/blob/main/packages/runtime/core/src/config/schema.ts#L208) |
| `config.i18n.showLocaleSwitch?` | `public` | `boolean` | When `true`, the built-in `<LanguageSwitcher>` renders in the header between the plugin header actions and the host `headerEnd` node. Defaults to `false` (opt-in, like `features.*`) so existing mounts — including hosts already rendering their own switcher via `headerEnd` — see no change. | [runtime/core/src/config/schema.ts:218](https://github.com/ancyloce/anvilkit-studio/blob/main/packages/runtime/core/src/config/schema.ts#L218) |
| `config.theme?` | `public` | \{ `allowToggle?`: `boolean`; `defaultMode?`: `"light"` \| `"dark"` \| `"system"`; \} | Theme block — the initial color mode and whether the built-in theme toggle is shown. | [runtime/core/src/config/schema.ts:138](https://github.com/ancyloce/anvilkit-studio/blob/main/packages/runtime/core/src/config/schema.ts#L138) |
| `config.theme.allowToggle?` | `public` | `boolean` | When `true`, the built-in theme toggle button is rendered in the header. Hosts that manage theme externally can set this to `false` and drive the theme via their own controls. | [runtime/core/src/config/schema.ts:153](https://github.com/ancyloce/anvilkit-studio/blob/main/packages/runtime/core/src/config/schema.ts#L153) |
| `config.theme.defaultMode?` | `public` | `"light"` \| `"dark"` \| `"system"` | Initial color mode. `"system"` respects the user's OS preference via `prefers-color-scheme` and is the default. `"light"` and `"dark"` force the editor into that mode regardless of OS setting. | [runtime/core/src/config/schema.ts:146](https://github.com/ancyloce/anvilkit-studio/blob/main/packages/runtime/core/src/config/schema.ts#L146) |
| &lt;a id="data"&gt;&lt;/a&gt; `data?` | `readonly` | `Data`\&lt;`ExtractConfigParams`\&lt;`UserConfig`\&gt;\[`"props"`\], `ExtractConfigParams`\&lt;`UserConfig`\&gt;\[`"rootProps"`\]\&gt; | Initial Puck page data. Forwarded to `<Puck>` verbatim and also captured into a ref so [StudioPluginContext.getData](StudioPluginContext.md#getdata) returns the latest snapshot. | [runtime/core/src/react/components/studio-controller-types.ts:116](https://github.com/ancyloce/anvilkit-studio/blob/main/packages/runtime/core/src/react/components/studio-controller-types.ts#L116) |
| &lt;a id="errorfallback"&gt;&lt;/a&gt; `errorFallback?` | `readonly` | `ReactNode` \| ((`error`) =&gt; `ReactNode`) | Rendered in place of the editor when the plugin runtime fails to compile, instead of the component hanging on the loading fallback forever. Either a static node or a render function receiving the thrown value. When omitted, `<Studio>` renders a built-in recoverable error screen (with a Retry action). A successful retry/recompile clears the error and mounts the editor. | [runtime/core/src/react/components/studio-controller-types.ts:322](https://github.com/ancyloce/anvilkit-studio/blob/main/packages/runtime/core/src/react/components/studio-controller-types.ts#L322) |
| &lt;a id="headerend"&gt;&lt;/a&gt; `headerEnd?` | `readonly` | `ReactNode` | Optional host node rendered in the chrome header's right-hand action cluster (between the plugin header actions and the Preview / Publish controls). The supported seam for arbitrary host header content — e.g. `<LanguageSwitcher />` from `@anvilkit/core/i18n`. Ignored when `chrome="puck"`. Memoize it host-side so the chrome-props context value stays stable across `<Studio>` re-renders. | [runtime/core/src/react/components/studio-controller-types.ts:237](https://github.com/ancyloce/anvilkit-studio/blob/main/packages/runtime/core/src/react/components/studio-controller-types.ts#L237) |
| &lt;a id="ispublishing"&gt;&lt;/a&gt; `isPublishing?` | `readonly` | `boolean` | Drives the "Publish" button's loading state. | [runtime/core/src/react/components/studio-controller-types.ts:201](https://github.com/ancyloce/anvilkit-studio/blob/main/packages/runtime/core/src/react/components/studio-controller-types.ts#L201) |
| &lt;a id="issavingdraft"&gt;&lt;/a&gt; `isSavingDraft?` | `readonly` | `boolean` | Drives the "save draft" button's loading state. | [runtime/core/src/react/components/studio-controller-types.ts:197](https://github.com/ancyloce/anvilkit-studio/blob/main/packages/runtime/core/src/react/components/studio-controller-types.ts#L197) |
| &lt;a id="lastsavedat"&gt;&lt;/a&gt; `lastSavedAt?` | `readonly` | `Date` \| `null` | Timestamp of the last successful save (relative-time hint). | [runtime/core/src/react/components/studio-controller-types.ts:199](https://github.com/ancyloce/anvilkit-studio/blob/main/packages/runtime/core/src/react/components/studio-controller-types.ts#L199) |
| &lt;a id="loading"&gt;&lt;/a&gt; `loading?` | `readonly` | `ReactNode` | Optional node rendered while the runtime compiles (and, for anvilkit chrome, while the lazily-loaded chrome assets resolve). Lets a host supply its own branded skeleton without wrapping `<Studio>` in a loading-state machine. A supplied node always wins over the built-in default. Default when omitted: - `chrome="anvilkit"` (the default): the built-in `StudioLoadingScreen` skeleton (skeleton rail / panel / header + a spinner-and-text canvas), so the pre-compile window shows the editor's shape instead of a blank frame. - `chrome="puck"`: bare `null`, exactly as before — the legacy path stays byte-for-byte identical to pre-Phase-5 Puck. Kept a plain `ReactNode` (not a render-prop) so it stays trivially passthrough and does not couple the host skeleton to runtime internals — a host that wants to paint deferred-plugin toolbar placeholders can compute them from its own plugin metas' `staticHeaderActions` (3.3) and render them inside this node. | [runtime/core/src/react/components/studio-controller-types.ts:295](https://github.com/ancyloce/anvilkit-studio/blob/main/packages/runtime/core/src/react/components/studio-controller-types.ts#L295) |
| &lt;a id="logger"&gt;&lt;/a&gt; `logger?` | `readonly` | [`StudioLogger`](../type-aliases/StudioLogger) | Optional diagnostics sink for plugin log records and Studio setup failures. Metadata is shallow-redacted before delivery. | [runtime/core/src/react/components/studio-controller-types.ts:242](https://github.com/ancyloce/anvilkit-studio/blob/main/packages/runtime/core/src/react/components/studio-controller-types.ts#L242) |
| &lt;a id="messages"&gt;&lt;/a&gt; ~~`messages?`~~ | `readonly` | `Readonly`\&lt;`Record`\&lt;`string`, `string`\&gt;\&gt; | Per-instance i18n overrides forwarded to `EditorI18nProvider`. Keys are message ids (`studio.module.*`). Ignored when `chrome="puck"`. This flat map is **locale-agnostic** — it applies to every locale — and still wins over `config.i18n.messages` during its migration window (do not use both). **Deprecated** Use `config.i18n.messages` (a per-locale `locale → (messageKey → string)` map) instead. Removal in 0.2.0. | [runtime/core/src/react/components/studio-controller-types.ts:260](https://github.com/ancyloce/anvilkit-studio/blob/main/packages/runtime/core/src/react/components/studio-controller-types.ts#L260) |
| &lt;a id="onaction"&gt;&lt;/a&gt; `onAction?` | `readonly` | `OnAction`\&lt;`Data`\&lt;`ExtractConfigParams`\&lt;`UserConfig`\&gt;\[`"props"`\], `ExtractConfigParams`\&lt;`UserConfig`\&gt;\[`"rootProps"`\]\&gt;\&gt; | Forwarded to `<Puck onAction>` — every Puck action dispatch. | [runtime/core/src/react/components/studio-controller-types.ts:176](https://github.com/ancyloce/anvilkit-studio/blob/main/packages/runtime/core/src/react/components/studio-controller-types.ts#L176) |
| &lt;a id="onback"&gt;&lt;/a&gt; `onBack?` | `readonly` | () =&gt; `void` | Optional "back" handler for the chrome header. Hidden when absent. | [runtime/core/src/react/components/studio-controller-types.ts:193](https://github.com/ancyloce/anvilkit-studio/blob/main/packages/runtime/core/src/react/components/studio-controller-types.ts#L193) |
| &lt;a id="onchange"&gt;&lt;/a&gt; `onChange?` | `readonly` | (`data`) =&gt; `void` | Forwarded to `<Puck onChange>` after the `onDataChange` lifecycle has fired. | [runtime/core/src/react/components/studio-controller-types.ts:149](https://github.com/ancyloce/anvilkit-studio/blob/main/packages/runtime/core/src/react/components/studio-controller-types.ts#L149) |
| &lt;a id="onerror"&gt;&lt;/a&gt; `onError?` | `readonly` | (`error`) =&gt; `void` | Called when the plugin runtime fails to compile (a plugin's `register` throws or rejects). Receives the thrown value. Fires once per failed compile; a later successful recompile does not re-notify. Use it for host-side error reporting — it does not replace the on-screen error UI (see [errorFallback](#errorfallback)). Read through a ref, so an inline handler never triggers a recompile. | [runtime/core/src/react/components/studio-controller-types.ts:313](https://github.com/ancyloce/anvilkit-studio/blob/main/packages/runtime/core/src/react/components/studio-controller-types.ts#L313) |
| &lt;a id="onexport"&gt;&lt;/a&gt; `onExport?` | `readonly` | (`formatId`) =&gt; `void` \| `Promise`\&lt;`void`\&gt; | Optional handler invoked from the publish panel's Export submenu with the format id. The host normalizes Puck data to `PageIR`, calls `runtime.exportFormats.get(formatId).run(ir, options)`, and triggers the download. The submenu disables itself when omitted. | [runtime/core/src/react/components/studio-controller-types.ts:228](https://github.com/ancyloce/anvilkit-studio/blob/main/packages/runtime/core/src/react/components/studio-controller-types.ts#L228) |
| &lt;a id="onlocalechange"&gt;&lt;/a&gt; `onLocaleChange?` | `readonly` | (`locale`) =&gt; `void` | Locale-change notification (config-centric i18n). - **Controlled** (`config.i18n.locale` explicitly set by the host): fired when something inside requests a switch (the built-in `LanguageSwitcher` when `config.i18n.showLocaleSwitch` is on); the store does NOT change until the host re-renders with the new `config.i18n.locale` — exactly like a controlled `<input>`. - **Uncontrolled**: fired *after* the store applied (and persisted) the switch — a sync tap for routers/cookies/analytics; ignoring it changes nothing. | [runtime/core/src/react/components/studio-controller-types.ts:273](https://github.com/ancyloce/anvilkit-studio/blob/main/packages/runtime/core/src/react/components/studio-controller-types.ts#L273) |
| &lt;a id="onpreview"&gt;&lt;/a&gt; `onPreview?` | `readonly` | (`data`) =&gt; `void` | Optional click handler for the chrome header's Preview action (the ▶ button). Disabled when omitted. Receives the **live** editor document (read from the Puck API at click time), so the host previews current edits rather than a stale snapshot — e.g. opening a render route in a new tab. A `() => void` handler stays assignable (the data arg is ignored). | [runtime/core/src/react/components/studio-controller-types.ts:221](https://github.com/ancyloce/anvilkit-studio/blob/main/packages/runtime/core/src/react/components/studio-controller-types.ts#L221) |
| &lt;a id="onpublish"&gt;&lt;/a&gt; `onPublish?` | `readonly` | (`data`) =&gt; `void` \| `Promise`\&lt;`void`\&gt; | Forwarded to `<Puck onPublish>` **only if** the `onBeforePublish` lifecycle resolves without throwing. | [runtime/core/src/react/components/studio-controller-types.ts:154](https://github.com/ancyloce/anvilkit-studio/blob/main/packages/runtime/core/src/react/components/studio-controller-types.ts#L154) |
| &lt;a id="onpublishclick"&gt;&lt;/a&gt; `onPublishClick?` | `readonly` | (`data`) =&gt; `void` | Optional click handler for the panel's "Publish to live" action. Disabled when omitted. The Puck `onPublish` pipeline still fires through Puck's own publish UI; this exists so the AnvilKit chrome can offer a header button wired to the host's publish flow. Receives the **live** editor document (read from the Puck API at click time), so the host publishes current edits rather than a stale snapshot. A `() => void` handler stays assignable (the data arg is simply ignored). | [runtime/core/src/react/components/studio-controller-types.ts:213](https://github.com/ancyloce/anvilkit-studio/blob/main/packages/runtime/core/src/react/components/studio-controller-types.ts#L213) |
| &lt;a id="onsavedraft"&gt;&lt;/a&gt; `onSaveDraft?` | `readonly` | () =&gt; `void` \| `Promise`\&lt;`void`\&gt; | Optional "save draft" handler; header surfaces a button when set. | [runtime/core/src/react/components/studio-controller-types.ts:195](https://github.com/ancyloce/anvilkit-studio/blob/main/packages/runtime/core/src/react/components/studio-controller-types.ts#L195) |
| &lt;a id="overrides"&gt;&lt;/a&gt; `overrides?` | `readonly` | `Partial`\&lt;[`Overrides`](https://puckeditor.com/docs/api-reference/configuration/overrides)\&lt;`UserConfig`\&gt;\&gt; | Consumer-supplied Puck overrides. Composed **outermost** — after the plugins' overrides — so the consumer always has the final word on any given slot. | [runtime/core/src/react/components/studio-controller-types.ts:144](https://github.com/ancyloce/anvilkit-studio/blob/main/packages/runtime/core/src/react/components/studio-controller-types.ts#L144) |
| &lt;a id="pages"&gt;&lt;/a&gt; `pages?` | `readonly` | [`StudioPagesSource`](StudioPagesSource.md) | Optional pages source for the sidebar's `layer` module. Ignored when `chrome="puck"`. | [runtime/core/src/react/components/studio-controller-types.ts:247](https://github.com/ancyloce/anvilkit-studio/blob/main/packages/runtime/core/src/react/components/studio-controller-types.ts#L247) |
| &lt;a id="plugins"&gt;&lt;/a&gt; `plugins?` | `readonly` | readonly ( \| [`StudioPlugin`](StudioPlugin.md)\&lt;`UserConfig`\&gt; \| [`Plugin`](https://puckeditor.com/docs/extending-puck/plugins)\&lt;`UserConfig`\&gt;)[] | StudioPlugins (and/or raw `@puckeditor/core` plugins) to mount. Order matters for override composition: the first plugin's overrides become the innermost wrapper, later plugins wrap it. Consumers that want to recover the contributed capability types from this set can declare the array `as const` and apply [InferPluginContributions](../type-aliases/InferPluginContributions)`<typeof plugins>` — the tuple preserves each plugin's `StudioPlugin<_, Contributes>` parameter, which the helper distributes into a union. The runtime erases at the `compilePlugins` boundary regardless. | [runtime/core/src/react/components/studio-controller-types.ts:129](https://github.com/ancyloce/anvilkit-studio/blob/main/packages/runtime/core/src/react/components/studio-controller-types.ts#L129) |
| &lt;a id="puckconfig"&gt;&lt;/a&gt; `puckConfig` | `readonly` | `UserConfig` | The Puck component config the editor operates on. Typically imported from a host-side module like `apps/demo/lib/puck-demo.ts` that aggregates every `@anvilkit/*` component package's `componentConfig` into a single `PuckConfig`. The inferred `UserConfig` flows into `data`, `onChange`/`onPublish`, `plugins`, and `overrides`, so an externally-typed `StudioPlugin<UserConfig>` keeps its type contribution through `<Studio>` (the typing is realized via [StudioPlugin](StudioPlugin.md), not a generic runtime — the runtime erases to the default at the `compilePlugins` boundary). | [runtime/core/src/react/components/studio-controller-types.ts:110](https://github.com/ancyloce/anvilkit-studio/blob/main/packages/runtime/core/src/react/components/studio-controller-types.ts#L110) |
| &lt;a id="storeid"&gt;&lt;/a&gt; `storeId?` | `readonly` | `string` | Per-instance store id. Persisted `EditorUiState` keys live under `anvilkit-ui-${storeId}`, so two `<Studio>` instances on one page should pass distinct ids. **Keep this stable for a given mount** (review finding Z-b/Z-2): the injected theme/export/ai stores freeze their resolved store id via `useState` at create time and do **not** re-key on a live `storeId` change. To re-target persistence (e.g. switch documents), remount `<Studio>` with a new React `key` rather than changing `storeId` in place. | [runtime/core/src/react/components/studio-controller-types.ts:191](https://github.com/ancyloce/anvilkit-studio/blob/main/packages/runtime/core/src/react/components/studio-controller-types.ts#L191) |
| &lt;a id="ui"&gt;&lt;/a&gt; `ui?` | `readonly` | `Partial`\&lt;`UiState`\&gt; | Initial Puck `UiState` partial. When `chrome="anvilkit"` it is merged with the chrome's full-width-viewport defaults via `mergeStudioUi()`. | [runtime/core/src/react/components/studio-controller-types.ts:174](https://github.com/ancyloce/anvilkit-studio/blob/main/packages/runtime/core/src/react/components/studio-controller-types.ts#L174) |
| &lt;a id="viewports"&gt;&lt;/a&gt; `viewports?` | `readonly` | `Viewports` | Forwarded to `<Puck viewports>`. | [runtime/core/src/react/components/studio-controller-types.ts:178](https://github.com/ancyloce/anvilkit-studio/blob/main/packages/runtime/core/src/react/components/studio-controller-types.ts#L178) |
