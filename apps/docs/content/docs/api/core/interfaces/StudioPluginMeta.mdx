---
title: "StudioPluginMeta"
description: "@anvilkit/core · StudioPluginMeta"
---

Defined in: [runtime/core/src/types/plugin.ts:78](https://github.com/ancyloce/anvilkit-studio/blob/main/packages/runtime/core/src/types/plugin.ts#L78)

Identifying metadata every Studio plugin must declare.

Authored once per plugin and carried on the `StudioPlugin` object.
The runtime consumes this block for both diagnostics and
[StudioPluginMeta.coreVersion](#coreversion) compatibility checks at
`compilePlugins()` time.

## Properties

| Property | Modifier | Type | Description | Defined in |
| ------ | ------ | ------ | ------ | ------ |
| &lt;a id="capabilities"&gt;&lt;/a&gt; `capabilities?` | `readonly` | [`StudioPluginCapabilities`](../type-aliases/StudioPluginCapabilities) | Optional declarative capability hints. Used by host tooling (settings UIs, plugin discovery) to query what surfaces a plugin contributes without compiling it. Purely advisory: the runtime continues to drive UI visibility from actual `register*()` calls into the sidebar registry, not from this flag. | [runtime/core/src/types/plugin.ts:138](https://github.com/ancyloce/anvilkit-studio/blob/main/packages/runtime/core/src/types/plugin.ts#L138) |
| &lt;a id="coreversion"&gt;&lt;/a&gt; `coreVersion` | `readonly` | `string` | Semver range of `@anvilkit/core` this plugin targets (e.g. `"^0.1.0"`). `compilePlugins()` validates this against the installed Core version. A mismatch throws `StudioPluginError` at compile time so host apps fail loud rather than rendering with a misaligned plugin. | [runtime/core/src/types/plugin.ts:113](https://github.com/ancyloce/anvilkit-studio/blob/main/packages/runtime/core/src/types/plugin.ts#L113) |
| &lt;a id="dependson"&gt;&lt;/a&gt; `dependsOn?` | `readonly` | readonly `string`[] | Optional ids of other plugins this one must compile **after**. `compilePlugins()` topologically orders the array so every declared dependency registers first; independent plugins keep their [order](#order) / declaration order. A `dependsOn` id that is absent from the plugin array, or a dependency cycle, throws a `StudioPluginError` at compile time. Use it when a plugin's contribution must observe another's (e.g. a plugin that decorates a surface a base plugin registers). | [runtime/core/src/types/plugin.ts:200](https://github.com/ancyloce/anvilkit-studio/blob/main/packages/runtime/core/src/types/plugin.ts#L200) |
| &lt;a id="description"&gt;&lt;/a&gt; `description?` | `readonly` | `string` | Optional one-line description of what the plugin does. | [runtime/core/src/types/plugin.ts:118](https://github.com/ancyloce/anvilkit-studio/blob/main/packages/runtime/core/src/types/plugin.ts#L118) |
| &lt;a id="icon"&gt;&lt;/a&gt; `icon?` | `readonly` | `ReactNode` | Optional display icon for the plugin, surfaced by host tooling (plugin lists, settings UIs). A `ReactNode` so callers can pass a rendered icon element (e.g. a `lucide-react` icon). Populated in a plugin's TypeScript meta, not in `meta/config.json` (JSON cannot hold a React node). | [runtime/core/src/types/plugin.ts:127](https://github.com/ancyloce/anvilkit-studio/blob/main/packages/runtime/core/src/types/plugin.ts#L127) |
| &lt;a id="id"&gt;&lt;/a&gt; `id` | `readonly` | `string` | Stable, globally-unique plugin identifier. Convention: reverse-dns or namespaced slug, e.g. `"@anvilkit/plugin-export-html"` or `"com.example.ai-copilot"`. The runtime rejects duplicate ids during `compilePlugins()`. | [runtime/core/src/types/plugin.ts:86](https://github.com/ancyloce/anvilkit-studio/blob/main/packages/runtime/core/src/types/plugin.ts#L86) |
| &lt;a id="name"&gt;&lt;/a&gt; `name` | `readonly` | `string` | Human-readable display name for the plugin. Surfaced in logs, error messages, and (optionally) in the Studio UI's plugin list. | [runtime/core/src/types/plugin.ts:94](https://github.com/ancyloce/anvilkit-studio/blob/main/packages/runtime/core/src/types/plugin.ts#L94) |
| &lt;a id="order"&gt;&lt;/a&gt; `order?` | `readonly` | `number` | Optional compile-ordering hint. Plugins compile — and their first-registration-wins / single-occupancy contributions resolve — in ascending `order`; ties keep declaration order. Lower = earlier. A plugin that omits it sorts at the default (`100`), after any explicitly lower-ordered plugin. [dependsOn](#dependson) constraints always take precedence over `order`. | [runtime/core/src/types/plugin.ts:188](https://github.com/ancyloce/anvilkit-studio/blob/main/packages/runtime/core/src/types/plugin.ts#L188) |
| &lt;a id="prefetch"&gt;&lt;/a&gt; `prefetch?` | `readonly` | [`StudioPluginPrefetch`](../type-aliases/StudioPluginPrefetch) | Optional hint for *when* a deferred (lazy) plugin's chunk should be fetched. Advisory only — like [capabilities](#capabilities), the runtime does not gate behavior on it; hosts that wrap a plugin with `lazyPlugin()` may read it to schedule the loader. See [StudioPluginPrefetch](../type-aliases/StudioPluginPrefetch) for the per-value semantics. The default (`"mount"`) is today's behavior: `compilePlugins()` awaits every `register()`, so the chunk is fetched as soon as `<Studio>` compiles its plugin array. | [runtime/core/src/types/plugin.ts:178](https://github.com/ancyloce/anvilkit-studio/blob/main/packages/runtime/core/src/types/plugin.ts#L178) |
| &lt;a id="staticheaderactions"&gt;&lt;/a&gt; `staticHeaderActions?` | `readonly` | readonly [`StaticHeaderActionPlaceholder`](../type-aliases/StaticHeaderActionPlaceholder)[] | Optional **static** header-action declarations — the toolbar buttons a plugin will contribute, declared up-front on `meta` so the chrome can reserve their slots *before* the plugin's chunk is fetched and `register()` runs. The live [StudioHeaderAction](StudioHeaderAction.md)s returned from `register()` carry the interactive `onClick` / `disabled` closures, which only exist once the plugin is loaded. A [StaticHeaderActionPlaceholder](../type-aliases/StaticHeaderActionPlaceholder) is that same descriptor **minus** those two closures — everything left (`id`, `label`, the string-name `icon`, `group`, `order`) is serializable, so a lazy plugin can advertise its buttons while the runtime stays React-free. A placeholder and the live action that share the **same `id`** are one slot: render the placeholder (disabled) while the chunk loads, then swap in the live action — positionally stable because both sort by the same `(group, order, id)` key. See `resolveHeaderActionSlots()` in `@anvilkit/core/runtime`. Advisory + additive: a plugin that omits this is unchanged, and a placeholder whose `id` never gets a live action simply renders as a permanently-disabled button (the host's cue that something failed to register). | [runtime/core/src/types/plugin.ts:165](https://github.com/ancyloce/anvilkit-studio/blob/main/packages/runtime/core/src/types/plugin.ts#L165) |
| &lt;a id="version"&gt;&lt;/a&gt; `version` | `readonly` | `string` | The plugin's own semver version (e.g. `"1.2.3"`). Distinct from [coreVersion](#coreversion). Used for diagnostics only — the runtime does not gate behavior on this field. | [runtime/core/src/types/plugin.ts:102](https://github.com/ancyloce/anvilkit-studio/blob/main/packages/runtime/core/src/types/plugin.ts#L102) |
