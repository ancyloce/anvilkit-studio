---
title: "ExportResult"
description: "@anvilkit/core · ExportResult"
---

Defined in: [types/export.ts:145](https://github.com/ancyloce/anvilkit-studio/blob/main/packages/core/src/types/export.ts#L145)

The return value of a successful [ExportFormatDefinition.run](ExportFormatDefinition.md#run)
call.

Text formats (HTML, JSON, Markdown, React source) should set
[content](#content) to a `string`. Binary formats (PDF, ZIP, images)
should set it to a `Uint8Array`. Host apps dispatch on the type at
download time — a `string` becomes a `Blob` with the format's
`mimeType`, a `Uint8Array` becomes a `Blob` wrapping the raw bytes.

## Properties

| Property | Modifier | Type | Description | Defined in |
| ------ | ------ | ------ | ------ | ------ |
| &lt;a id="content"&gt;&lt;/a&gt; `content` | `readonly` | `string` \| `Uint8Array`\&lt;`ArrayBufferLike`\&gt; | The serialized output. Use `string` for text formats and `Uint8Array` for binary formats. Do not base64-encode binary content into a string; host apps handle the two cases differently. | [types/export.ts:153](https://github.com/ancyloce/anvilkit-studio/blob/main/packages/core/src/types/export.ts#L153) |
| &lt;a id="filename"&gt;&lt;/a&gt; `filename` | `readonly` | `string` | The suggested filename for the download, including the extension (e.g. `"page.html"`, `"page.pdf"`). Host apps may override this at the download step, but the exporter is expected to provide a sensible default. | [types/export.ts:161](https://github.com/ancyloce/anvilkit-studio/blob/main/packages/core/src/types/export.ts#L161) |
| &lt;a id="warnings"&gt;&lt;/a&gt; `warnings?` | `readonly` | readonly [`ExportWarning`](ExportWarning.md)[] | Optional non-fatal diagnostics emitted during the export run. See [ExportWarning](ExportWarning.md). Host apps may surface these in a dev console or toast; the export itself is still considered successful. | [types/export.ts:169](https://github.com/ancyloce/anvilkit-studio/blob/main/packages/core/src/types/export.ts#L169) |
