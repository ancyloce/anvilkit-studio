---
title: "StudioCopilotPanel"
description: "@anvilkit/core · StudioCopilotPanel"
---

Defined in: [core/src/types/sidebar.ts:423](https://github.com/ancyloce/anvilkit-studio/blob/main/packages/core/src/types/sidebar.ts#L423)

Host-supplied panel body for the sidebar's `copilot` module.
`@anvilkit/core` stays agnostic about any specific AI plugin — the
host (or an integration plugin paired with `@anvilkit/plugin-ai-copilot`)
registers a single panel via `ctx.registerCopilotPanel(panel)` and
owns its own React state, plugin reference, and dispatch wiring.

Mirrors the [StudioAssetSource](StudioAssetSource.md) shape: v1 supports a single
panel, last-write-wins; the module shows `studio.module.copilot.empty`
until a panel is registered.

## Properties

| Property | Modifier | Type | Description | Defined in |
| ------ | ------ | ------ | ------ | ------ |
| &lt;a id="render"&gt;&lt;/a&gt; `render` | `readonly` | () =&gt; `ReactNode` | Render the panel body. Called from the `copilot` module on every render; the returned tree is rendered directly inside the sidebar panel's body slot. The function is the React component itself — implementations may close over plugin references and own their internal state via standard hooks. | [core/src/types/sidebar.ts:431](https://github.com/ancyloce/anvilkit-studio/blob/main/packages/core/src/types/sidebar.ts#L431) |
