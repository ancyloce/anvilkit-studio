---
title: "CreateStudioConfigOptions"
description: "@anvilkit/core · CreateStudioConfigOptions"
---

Defined in: [core/src/config/create-config.ts:71](https://github.com/ancyloce/anvilkit-studio/blob/main/packages/core/src/config/create-config.ts#L71)

Options bag for [createStudioConfig](../functions/createStudioConfig).

A nested options object (instead of a second positional arg for
the env bag directly) leaves room for future knobs —
deprecation handling, plugin discovery overrides, strict mode
toggles — without breaking the public signature.

## Properties

| Property | Modifier | Type | Description | Defined in |
| ------ | ------ | ------ | ------ | ------ |
| &lt;a id="env"&gt;&lt;/a&gt; `env?` | `readonly` | `Record`\&lt;`string`, `string` \| `undefined`\&gt; | Environment variable bag to consume for Layer 2. Defaults to `process.env` when that global is available. Tests and non-Node runtimes can inject a synthetic bag so the factory stays deterministic and never touches the real process env. | [core/src/config/create-config.ts:79](https://github.com/ancyloce/anvilkit-studio/blob/main/packages/core/src/config/create-config.ts#L79) |
