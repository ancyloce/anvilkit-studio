---
title: "RunExportOptions"
description: "@anvilkit/core · RunExportOptions"
---

Defined in: [runtime/core/src/export/run-export.ts:45](https://github.com/ancyloce/anvilkit-studio/blob/main/packages/runtime/core/src/export/run-export.ts#L45)

Options for [runExport](../functions/runExport).

## Extended by

- [`ExportAndDownloadOptions`](ExportAndDownloadOptions.md)

## Type Parameters

| Type Parameter | Default type |
| ------ | ------ |
| `Opts` *extends* `Record`\&lt;`string`, `unknown`\&gt; | `Record`\&lt;`string`, `unknown`\&gt; |

## Properties

| Property | Modifier | Type | Description | Defined in |
| ------ | ------ | ------ | ------ | ------ |
| &lt;a id="assetresolvers"&gt;&lt;/a&gt; `assetResolvers?` | `readonly` | readonly [`IRAssetResolver`](../type-aliases/IRAssetResolver)[] | Registration-ordered asset resolvers (e.g. `runtime.assetResolvers`) passed to the format's run context for URL rewriting. Omit for "no asset rewriting". | [runtime/core/src/export/run-export.ts:84](https://github.com/ancyloce/anvilkit-studio/blob/main/packages/runtime/core/src/export/run-export.ts#L84) |
| &lt;a id="config"&gt;&lt;/a&gt; `config?` | `readonly` | `Config` | The Puck `Config` for [data](#data) (PLAN-0025 §9.3, P4-05). When provided, the runner compiles the EXACT exported document through the unified appearance compiler and hands formats the result via the run context's `compiledAppearance` — so an exporter never re-derives appearance CSS with a second algorithm. Compiler diagnostics fan out through [onWarning](#onwarning) (§9.2 step 6). | [runtime/core/src/export/run-export.ts:63](https://github.com/ancyloce/anvilkit-studio/blob/main/packages/runtime/core/src/export/run-export.ts#L63) |
| &lt;a id="data"&gt;&lt;/a&gt; `data` | `readonly` | `Data` | The Puck page data to export. Normalized to IR via [toIR](#toir). | [runtime/core/src/export/run-export.ts:54](https://github.com/ancyloce/anvilkit-studio/blob/main/packages/runtime/core/src/export/run-export.ts#L54) |
| &lt;a id="exportstore"&gt;&lt;/a&gt; `exportStore?` | `readonly` | [`ExportStoreApi`](../type-aliases/ExportStoreApi) | Optional export store to drive: `setIsExporting(true)` before the run, `recordExport(id, ok)` after, `setIsExporting(false)` in a `finally`. Omit for headless/CLI callers with no UI state. | [runtime/core/src/export/run-export.ts:90](https://github.com/ancyloce/anvilkit-studio/blob/main/packages/runtime/core/src/export/run-export.ts#L90) |
| &lt;a id="format"&gt;&lt;/a&gt; `format` | `readonly` | [`ExportFormatDefinition`](ExportFormatDefinition.md)\&lt;`Opts`\&gt; | The resolved export format to run. Typically obtained from the compiled runtime, e.g. `runtime.exportFormats.get(formatId)`. | [runtime/core/src/export/run-export.ts:52](https://github.com/ancyloce/anvilkit-studio/blob/main/packages/runtime/core/src/export/run-export.ts#L52) |
| &lt;a id="onvalidation"&gt;&lt;/a&gt; `onValidation?` | `readonly` | (`event`) =&gt; `void` | Content-free `export.validation` sink (DD-0019 §22.4, §32.1; PLAN-0020 CORE-P3-009 Val, wired in CORE-P4-004). `runExportPreflight` already *builds* this event, but Core ships no export UI of its own, so without a sink the payload was constructed and dropped — an operational event nobody could observe. Hosts pass `bridge.diagnostics.emit`. Fires exactly once per export attempt, **before** the blocked throw, so a blocked export is still reported. A throwing sink is a host reporting bug and must not be mistaken for an export failure, so it is caught and ignored here. | [runtime/core/src/export/run-export.ts:128](https://github.com/ancyloce/anvilkit-studio/blob/main/packages/runtime/core/src/export/run-export.ts#L128) |
| &lt;a id="onwarning"&gt;&lt;/a&gt; `onWarning?` | `readonly` | (`warning`) =&gt; `void` | Called once per warning emitted by the format (the warnings are also returned on [ExportResult.warnings](ExportResult.md#warnings)). Route to a logger or toast. A throwing `onWarning` is a host-side reporting bug — it propagates *after* the export is recorded successful, so it is never misattributed as an export failure. | [runtime/core/src/export/run-export.ts:98](https://github.com/ancyloce/anvilkit-studio/blob/main/packages/runtime/core/src/export/run-export.ts#L98) |
| &lt;a id="options"&gt;&lt;/a&gt; `options?` | `readonly` | `Opts` | Format-specific option bag forwarded to `format.run`. **Defaults to `{}`** when omitted — so a format whose `Opts` has required fields must pass them here explicitly (the empty default would otherwise reach `run` missing those fields). | [runtime/core/src/export/run-export.ts:78](https://github.com/ancyloce/anvilkit-studio/blob/main/packages/runtime/core/src/export/run-export.ts#L78) |
| &lt;a id="preflight"&gt;&lt;/a&gt; `preflight?` | `readonly` | `ExportPreflightResult` | Editor-feature preflight verdict (DD-0019 §23.2; PLAN-0020 CORE-P3-009). Computed by the editor layer — see `react/editor/export-preflight.ts` — and merely *enforced* here. The split is a layering requirement, not a preference: the verdict needs `@anvilkit/ir`'s `listUsedAuthoringFeatures`, which `src/export/` may not import (`check:no-headless-import`). Passing the result in keeps this module free of that dependency; the import above is type-only and erases at build. Omitted = no editor features in play (the pre-editor path), which must keep exporting through any format. | [runtime/core/src/export/run-export.ts:113](https://github.com/ancyloce/anvilkit-studio/blob/main/packages/runtime/core/src/export/run-export.ts#L113) |
| &lt;a id="toir"&gt;&lt;/a&gt; `toIR` | `readonly` | (`data`) =&gt; [`PageIR`](PageIR.md) \| `Promise`\&lt;[`PageIR`](PageIR.md)\&gt; | Normalizes Puck `Data` to a [PageIR](PageIR.md). Inject `@anvilkit/ir`'s `puckDataToIR` here (bound to the host's `Config`); core does not depend on `@anvilkit/ir`. May be async. | [runtime/core/src/export/run-export.ts:71](https://github.com/ancyloce/anvilkit-studio/blob/main/packages/runtime/core/src/export/run-export.ts#L71) |
| &lt;a id="tokenmode"&gt;&lt;/a&gt; `tokenMode?` | `readonly` | `string` | Token mode for the appearance compile; design-system default when omitted. | [runtime/core/src/export/run-export.ts:65](https://github.com/ancyloce/anvilkit-studio/blob/main/packages/runtime/core/src/export/run-export.ts#L65) |
