---
title: "RunExportOptions"
description: "@anvilkit/core · RunExportOptions"
---

Defined in: [export/run-export.ts:41](https://github.com/ancyloce/anvilkit-studio/blob/main/packages/core/src/export/run-export.ts#L41)

Options for [runExport](../functions/runExport).

## Extended by

- [`ExportAndDownloadOptions`](ExportAndDownloadOptions.md)

## Type Parameters

| Type Parameter | Default type |
| ------ | ------ |
| `Opts` *extends* `Record`\&lt;`string`, `unknown`\&gt; | `Record`\&lt;`string`, `unknown`\&gt; |

## Properties

| Property | Modifier | Type | Description | Defined in |
| ------ | ------ | ------ | ------ | ------ |
| &lt;a id="assetresolvers"&gt;&lt;/a&gt; `assetResolvers?` | `readonly` | readonly [`IRAssetResolver`](../type-aliases/IRAssetResolver)[] | Registration-ordered asset resolvers (e.g. `runtime.assetResolvers`) passed to the format's run context for URL rewriting. Omit for "no asset rewriting". | [export/run-export.ts:69](https://github.com/ancyloce/anvilkit-studio/blob/main/packages/core/src/export/run-export.ts#L69) |
| &lt;a id="data"&gt;&lt;/a&gt; `data` | `readonly` | `Data` | The Puck page data to export. Normalized to IR via [toIR](#toir). | [export/run-export.ts:50](https://github.com/ancyloce/anvilkit-studio/blob/main/packages/core/src/export/run-export.ts#L50) |
| &lt;a id="exportstore"&gt;&lt;/a&gt; `exportStore?` | `readonly` | [`ExportStoreApi`](../type-aliases/ExportStoreApi) | Optional export store to drive: `setIsExporting(true)` before the run, `recordExport(id, ok)` after, `setIsExporting(false)` in a `finally`. Omit for headless/CLI callers with no UI state. | [export/run-export.ts:75](https://github.com/ancyloce/anvilkit-studio/blob/main/packages/core/src/export/run-export.ts#L75) |
| &lt;a id="format"&gt;&lt;/a&gt; `format` | `readonly` | [`ExportFormatDefinition`](ExportFormatDefinition.md)\&lt;`Opts`\&gt; | The resolved export format to run. Typically obtained from the compiled runtime, e.g. `runtime.exportFormats.get(formatId)`. | [export/run-export.ts:48](https://github.com/ancyloce/anvilkit-studio/blob/main/packages/core/src/export/run-export.ts#L48) |
| &lt;a id="onwarning"&gt;&lt;/a&gt; `onWarning?` | `readonly` | (`warning`) =&gt; `void` | Called once per warning emitted by the format (the warnings are also returned on [ExportResult.warnings](ExportResult.md#warnings)). Route to a logger or toast. A throwing `onWarning` is a host-side reporting bug — it propagates *after* the export is recorded successful, so it is never misattributed as an export failure. | [export/run-export.ts:83](https://github.com/ancyloce/anvilkit-studio/blob/main/packages/core/src/export/run-export.ts#L83) |
| &lt;a id="options"&gt;&lt;/a&gt; `options?` | `readonly` | `Opts` | Format-specific option bag forwarded to `format.run`. **Defaults to `{}`** when omitted — so a format whose `Opts` has required fields must pass them here explicitly (the empty default would otherwise reach `run` missing those fields). | [export/run-export.ts:63](https://github.com/ancyloce/anvilkit-studio/blob/main/packages/core/src/export/run-export.ts#L63) |
| &lt;a id="toir"&gt;&lt;/a&gt; `toIR` | `readonly` | (`data`) =&gt; [`PageIR`](PageIR.md) \| `Promise`\&lt;[`PageIR`](PageIR.md)\&gt; | Normalizes Puck `Data` to a [PageIR](PageIR.md). Inject `@anvilkit/ir`'s `puckDataToIR` here (bound to the host's `Config`); core does not depend on `@anvilkit/ir`. May be async. | [export/run-export.ts:56](https://github.com/ancyloce/anvilkit-studio/blob/main/packages/core/src/export/run-export.ts#L56) |
