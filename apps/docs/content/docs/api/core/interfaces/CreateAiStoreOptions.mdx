---
title: "CreateAiStoreOptions"
description: "@anvilkit/core · CreateAiStoreOptions"
---

Defined in: [runtime/core/src/state/slices/ai-store.ts:236](https://github.com/ancyloce/anvilkit-studio/blob/main/packages/runtime/core/src/state/slices/ai-store.ts#L236)

Zustand store for AI copilot generation state.

## Example

```ts
// Inside a plugin-agnostic Studio panel:
const isGenerating = useAiStore((s) => s.isGenerating);
const history = useAiStore((s) => s.history);

// Inside the generation lifecycle (typically <Studio>):
useAiStore.getState().startGeneration(prompt);
try {
  await generatePage(prompt);
  useAiStore.getState().finishGeneration(true);
} catch (err) {
  useAiStore.getState().finishGeneration(false, String(err));
}
```

## Properties

| Property | Modifier | Type | Defined in |
| ------ | ------ | ------ | ------ |
| &lt;a id="storeid"&gt;&lt;/a&gt; `storeId` | `readonly` | `string` | [runtime/core/src/state/slices/ai-store.ts:237](https://github.com/ancyloce/anvilkit-studio/blob/main/packages/runtime/core/src/state/slices/ai-store.ts#L237) |
