---
title: "Core — API Reference"
description: "@anvilkit/core public API reference, generated by TypeDoc."
---

## Classes

| Class | Description |
| ------ | ------ |
| [StudioConfigError](classes/StudioConfigError.md) | Thrown when Studio configuration validation fails. |
| [StudioError](classes/StudioError.md) | Abstract base class for every `@anvilkit/core` runtime error. |
| [StudioExportError](classes/StudioExportError.md) | Thrown when a registered export format's `run()` implementation fails. |
| [StudioPluginError](classes/StudioPluginError.md) | Thrown when plugin compilation or lifecycle invocation fails. |

## Interfaces

| Interface | Description |
| ------ | ------ |
| [AiComponentSchema](interfaces/AiComponentSchema.md) | Description of a single component in a shape an LLM can reason about — name, purpose, props, and an optional concrete example. |
| [AiFieldSchema](interfaces/AiFieldSchema.md) | Description of a single prop on a component, in a shape an LLM can reason about. |
| [AiGenerationContext](interfaces/AiGenerationContext.md) | The full input to a single LLM generation call. |
| [AiHistoryEntry](interfaces/AiHistoryEntry.md) | A single prompt in [AiState.history](interfaces/AiState.md#history). The entry is intentionally minimal — prompt text and a timestamp, no result payload, no error record. Failed runs are recorded the same way as successful ones because the UI typically wants to re-run or tweak a prompt regardless of how it ended. |
| [AiSectionContext](interfaces/AiSectionContext.md) | The LLM-shaped view of a section-level regeneration request. |
| [AiSectionPatch](interfaces/AiSectionPatch.md) | The LLM's response to a section-level regeneration request. |
| [AiSectionSelection](interfaces/AiSectionSelection.md) | The editor's description of which nodes the author selected before asking for a section-level regeneration. |
| [AiState](interfaces/AiState.md) | Full shape of the AI store — read fields plus the three write actions plus [AiState.reset](interfaces/AiState.md#reset). |
| [AiStoreProviderProps](interfaces/AiStoreProviderProps.md) | Props for [AiStoreProvider](functions/AiStoreProvider.md). |
| [AiValidationIssue](interfaces/AiValidationIssue.md) | A single issue produced by validating an LLM's response against the expected component/field shape. |
| [AiValidationResult](interfaces/AiValidationResult.md) | The outcome of validating an LLM response. |
| [AssetResolution](interfaces/AssetResolution.md) | The outcome of resolving an indirect asset reference (e.g. `asset://…`) to a concrete, directly-usable location. |
| [ComponentPackageManifest](interfaces/ComponentPackageManifest.md) | Runtime-discoverable metadata for an `@anvilkit/<slug>` component package. |
| [ConfigToAiSectionContextOptions](interfaces/ConfigToAiSectionContextOptions.md) | Optional environment hints for `configToAiSectionContext()`. |
| [CreateAiStoreOptions](interfaces/CreateAiStoreOptions.md) | Zustand store for AI copilot generation state. |
| [CreateExportStoreOptions](interfaces/CreateExportStoreOptions.md) | Zustand store for the export pipeline's view-level state. |
| [CreateStudioConfigOptions](interfaces/CreateStudioConfigOptions.md) | Options bag for [createStudioConfig](functions/createStudioConfig.md). |
| [CreateThemeStoreOptions](interfaces/CreateThemeStoreOptions.md) | - |
| [ExportAndDownloadOptions](interfaces/ExportAndDownloadOptions.md) | Options for [exportAndDownload](functions/exportAndDownload.md): [runExport](functions/runExport.md)'s plus a filename override. |
| [ExportFormatDefinition](interfaces/ExportFormatDefinition.md) | The descriptor every exporter plugin contributes to Studio. |
| [ExportFormatRunContext](interfaces/ExportFormatRunContext.md) | Per-run context handed to [ExportFormatDefinition.run](interfaces/ExportFormatDefinition.md#run) for formats that opt into asset-URL rewriting (review finding TS-c). |
| [ExportRegistry](interfaces/ExportRegistry.md) | Read-only lookup interface returned by [createExportRegistry](functions/createExportRegistry.md). |
| [ExportResult](interfaces/ExportResult.md) | The return value of a successful [ExportFormatDefinition.run](interfaces/ExportFormatDefinition.md#run) call. |
| [ExportState](interfaces/ExportState.md) | Full shape of the export store. Split into read fields (the state snapshot) and setter actions (the write surface). |
| [ExportStoreProviderProps](interfaces/ExportStoreProviderProps.md) | Props for [ExportStoreProvider](functions/ExportStoreProvider.md). |
| [ExportWarning](interfaces/ExportWarning.md) | A single diagnostic emitted by an exporter. |
| [LastExportRecord](interfaces/LastExportRecord.md) | A single entry in [ExportState.lastExport](interfaces/ExportState.md#lastexport) — the record of the most recently completed export attempt. |
| [LifecycleManager](interfaces/LifecycleManager.md) | The public shape returned by [createLifecycleManager](functions/createLifecycleManager.md). |
| [PageIR](interfaces/PageIR.md) | The root IR document — the single argument passed to every [ExportFormatDefinition.run](interfaces/ExportFormatDefinition.md#run) call. |
| [PageIRAsset](interfaces/PageIRAsset.md) | An out-of-band resource referenced by the page — images, videos, fonts, stylesheets, inline scripts, etc. |
| [PageIRMetadata](interfaces/PageIRMetadata.md) | Page-level metadata — title, description, and timestamps the exporter may surface in `<head>`, a feed, or a manifest. |
| [PageIRNode](interfaces/PageIRNode.md) | A single node in the page IR tree. |
| [PageIRNodeMeta](interfaces/PageIRNodeMeta.md) | Authoring-time metadata attached to a [PageIRNode](interfaces/PageIRNode.md). |
| [PageRootInput](interfaces/PageRootInput.md) | The title + seo slice of a page's `root.props` this module projects. |
| [PageRootSeoInput](interfaces/PageRootSeoInput.md) | SEO sub-shape of a page's `root.props.seo` (mirrors `@anvilkit/schema` `PageSeo`). |
| [RunExportOptions](interfaces/RunExportOptions.md) | Options for [runExport](functions/runExport.md). |
| [SortableHeaderAction](interfaces/SortableHeaderAction.md) | The minimal shape [composeHeaderActions](functions/composeHeaderActions.md) needs to order an action: only the three sort keys. Both [StudioHeaderAction](interfaces/StudioHeaderAction.md) (live) and [StaticHeaderActionPlaceholder](type-aliases/StaticHeaderActionPlaceholder.md) (declared) satisfy it, so the exact same ordering applies whether or not the live `onClick` / `disabled` closures exist yet — that positional parity is what lets a placeholder swap to its live action with no layout shift. |
| [StudioAsset](interfaces/StudioAsset.md) | A single asset entry returned by a [StudioAssetSource](interfaces/StudioAssetSource.md). |
| [StudioAssetAction](interfaces/StudioAssetAction.md) | Plugin-contributed entry in the per-asset overflow `…` menu. Built-ins (Rename, Replace, Copy URL, Delete) come from the source itself; this surface lets plugins add commands like "Open in CDN". |
| [StudioAssetFolder](interfaces/StudioAssetFolder.md) | A folder returned by a folder-aware [StudioAssetSource](interfaces/StudioAssetSource.md) (PRD 0002 §7). `parentId: null` is the root convention. Counts/timestamps are optional so a source can supply a lightweight folder for the tree without them. |
| [StudioAssetListPage](interfaces/StudioAssetListPage.md) | Pagination envelope returned by [StudioAssetSource.listPaginated](interfaces/StudioAssetSource.md#listpaginated). |
| [StudioAssetListQuery](interfaces/StudioAssetListQuery.md) | Search / filter / pagination query passed to [StudioAssetSource.listPaginated](interfaces/StudioAssetSource.md#listpaginated). Filters compose with AND semantics: an asset must satisfy every supplied filter to land in the page. |
| [StudioAssetSort](interfaces/StudioAssetSort.md) | Sort axis for [StudioAssetListQuery](interfaces/StudioAssetListQuery.md). |
| [StudioAssetSource](interfaces/StudioAssetSource.md) | Asset listing + mutation contract registered by an asset-manager plugin via `ctx.registerAssetSource(source)`. The `image` sidebar module reads from this surface; when no source is registered it shows the `studio.module.image.pluginMissing` empty state. |
| [StudioAssetTheme](interfaces/StudioAssetTheme.md) | A browse theme (e.g. an Unsplash topic) exposed by a themed source. `label` is an i18n key. |
| [StudioConfigProviderProps](interfaces/StudioConfigProviderProps.md) | Props for [StudioConfigProvider](functions/StudioConfigProvider.md). |
| [StudioCopilotPanel](interfaces/StudioCopilotPanel.md) | Host-supplied panel body for the sidebar's `copilot` module. `@anvilkit/core` stays agnostic about any specific AI plugin — the host (or an integration plugin paired with `@anvilkit/plugin-ai-copilot`) registers a single panel via `ctx.registerCopilotPanel(panel)` and owns its own React state, plugin reference, and dispatch wiring. |
| [StudioCopySnippet](interfaces/StudioCopySnippet.md) | - |
| [StudioCopySnippetPack](interfaces/StudioCopySnippetPack.md) | A bundle of snippets registered as a unit via `ctx.registerCopySnippetPack(pack)`. Optional `locale` lets the host register multiple packs and switch between them based on the active locale (the v1 sidebar does not auto-switch — the host decides). |
| [StudioDesignSystemPanel](interfaces/StudioDesignSystemPanel.md) | Host-supplied panel body for the sidebar's `design-system` module. `@anvilkit/core` stays agnostic about any specific token vocabulary — the host (or an integration plugin paired with `@anvilkit/plugin-design-system`) registers a single panel via `ctx.registerDesignSystemPanel(panel)` and owns its own React state, token tree, and theme-toggle wiring. |
| [StudioErrorScreenProps](interfaces/StudioErrorScreenProps.md) | - |
| [StudioExperimentalConfig](interfaces/StudioExperimentalConfig.md) | Module-augmentation slot for plugin-specific `experimental` config. |
| [StudioHeaderAction](interfaces/StudioHeaderAction.md) | Header action descriptor, contributed by plugins via [StudioPluginRegistration.headerActions](interfaces/StudioPluginRegistration.md#headeractions). |
| [StudioHistoryPanel](interfaces/StudioHistoryPanel.md) | Host-supplied panel body for the sidebar's `history` module. `@anvilkit/core` stays agnostic about any specific snapshot store — the host (or an integration plugin paired with `@anvilkit/plugin-version-history`) registers a single panel via `ctx.registerHistoryPanel(panel)` and owns its own React state, adapter reference, and restore-dispatch wiring. |
| [StudioInsertSection](interfaces/StudioInsertSection.md) | A collapsible section in the `insert` module. Default sections (`recommended`, `navigation`, `top`, `team`) are seeded by `@anvilkit/core` from each component's metadata category; plugins can register additional sections via `registerInsertSection()`. |
| [StudioLayerQuickAdd](interfaces/StudioLayerQuickAdd.md) | A primitive entry in the `layer` module's "+" quick-add popover. Built-ins (`Layout`, `Row`, `Column`, `Text`) are seeded by `@anvilkit/core`; plugins can append their own. |
| [StudioLoadingScreenProps](interfaces/StudioLoadingScreenProps.md) | - |
| [StudioPage](interfaces/StudioPage.md) | A single page entry shown in the `layer/pages` sub-panel. |
| [StudioPageCreateInput](interfaces/StudioPageCreateInput.md) | Input shape for [StudioPagesSource.onCreate](interfaces/StudioPagesSource.md#oncreate). |
| [StudioPageRenameInput](interfaces/StudioPageRenameInput.md) | Input shape for [StudioPagesSource.onRename](interfaces/StudioPagesSource.md#onrename). |
| [StudioPageReorderInput](interfaces/StudioPageReorderInput.md) | Input shape for [StudioPagesSource.onReorder](interfaces/StudioPagesSource.md#onreorder). |
| [StudioPageSeo](interfaces/StudioPageSeo.md) | Structured SEO metadata for a page. Every field is optional so the host can surface only what it persists; the settings dialog shows the full set when the source implements [StudioPagesSource.onUpdateSettings](interfaces/StudioPagesSource.md#onupdatesettings). |
| [StudioPageSettingsInput](interfaces/StudioPageSettingsInput.md) | Input shape for [StudioPagesSource.onUpdateSettings](interfaces/StudioPagesSource.md#onupdatesettings). |
| [StudioPageSettingsSeoFields](interfaces/StudioPageSettingsSeoFields.md) | Host-registered SEO field group for the `layer` module's page-settings dialog. Unlike [StudioSeoPanel](interfaces/StudioSeoPanel.md) — which edits the *active* Puck doc's `root.props.seo` via `usePuck()` — this seam edits *any* page row's stored SEO through core's controlled `value`/`onChange` props, so the SEO-specific field UI lives in `@anvilkit/plugin-page-seo` rather than core chrome. Single-occupancy, last-write-wins; when no plugin is registered the dialog renders no SEO section at all. |
| [StudioPageSettingsSeoFieldsProps](interfaces/StudioPageSettingsSeoFieldsProps.md) | Props core's page-settings dialog passes to [StudioPageSettingsSeoFields.render](interfaces/StudioPageSettingsSeoFields.md#render). The dialog owns the form state, the change diff, and the `onUpdateSettings` submission; the plugin owns only the field UI. `value` is the page row's current SEO sub-block (controlled); `onChange` replaces it wholesale with the next snapshot. |
| [StudioPagesSource](interfaces/StudioPagesSource.md) | Pages source contract. The host application supplies one of these via the `<Studio>` `pages` prop. When omitted, the `layer/pages` sub-panel renders the empty state keyed `studio.module.layer.pages.empty`. |
| [StudioPlugin](interfaces/StudioPlugin.md) | The authored plugin object. |
| [StudioPluginContext](interfaces/StudioPluginContext.md) | Read / write / observe handle passed to every plugin lifecycle call. |
| [StudioPluginContributing](interfaces/StudioPluginContributing.md) | A `StudioPlugin` that advertises a `Contributes` capability surface. |
| [StudioPluginLifecycleHooks](interfaces/StudioPluginLifecycleHooks.md) | Optional lifecycle method bag returned by a plugin's `register()`. |
| [StudioPluginMeta](interfaces/StudioPluginMeta.md) | Identifying metadata every Studio plugin must declare. |
| [StudioPluginOverlay](interfaces/StudioPluginOverlay.md) | Top-level overlay contributed by a plugin via [StudioPluginRegistration.overlays](interfaces/StudioPluginRegistration.md#overlays). Overlays render as siblings of the Puck editor at the placement specified by [placement](interfaces/StudioPluginOverlay.md#placement) (see [StudioOverlayPlacement](type-aliases/StudioOverlayPlacement.md)). |
| [StudioPluginProvider](interfaces/StudioPluginProvider.md) | React provider contributed by a plugin via [StudioPluginRegistration.providers](interfaces/StudioPluginRegistration.md#providers). The Studio shell composes every contributed provider around the editor tree (inside `StudioRuntimeProvider`, so each provider's component may call `useStudio()`). |
| [StudioPluginRegistration](interfaces/StudioPluginRegistration.md) | Return shape from a plugin's `register()` method. |
| [StudioPluginSlotContribution](interfaces/StudioPluginSlotContribution.md) | Named chrome slot contribution — a plugin claims a specific anchor and supplies the component Studio should render there. |
| [StudioProps](interfaces/StudioProps.md) | Props accepted by `<Studio>` and consumed by the controller. Lives here (not in `Studio.tsx`) so there is **no import cycle** between the view and its controller — `Studio.tsx` re-exports it to keep the public `@anvilkit/core/react` path (`{ StudioProps }`) unchanged. |
| [StudioRuntime](interfaces/StudioRuntime.md) | The compiled, ready-to-mount output of [compilePlugins](functions/compilePlugins.md). |
| [StudioSeoPanel](interfaces/StudioSeoPanel.md) | Host-registered SEO panel (PRD 0004 F5). Mirrors [StudioDesignSystemPanel](interfaces/StudioDesignSystemPanel.md): a single panel, last-write-wins; the `seo` module shows `studio.module.seo.empty` until a panel is registered. The plugin's panel edits the page's `root.props.seo` via an immutable Puck dispatch. |
| [StudioSidebarContributions](interfaces/StudioSidebarContributions.md) | Immutable snapshot of every sidebar contribution collected during a single [compilePlugins](functions/compilePlugins.md) pass, exposed on [StudioRuntime.sidebar](interfaces/StudioRuntime.md#sidebar). Map-keyed surfaces are last-write-wins by id; the single-occupancy panels/source are last-write-wins by registration order. |
| [ThemeState](interfaces/ThemeState.md) | Full shape of the theme store — two read fields, two setters. No `reset()` is required by the spec, but we add one for parity with the other two stores and so tests have a uniform isolation hook. |
| [ThemeStoreProviderProps](interfaces/ThemeStoreProviderProps.md) | Props for [ThemeStoreProvider](functions/ThemeStoreProvider.md). |
| [UseStudioResult](interfaces/UseStudioResult.md) | Diagnostic projection of the compiled runtime, exposed to host apps and plugin UIs. |

## Type Aliases

| Type Alias | Description |
| ------ | ------ |
| [AiFieldType](type-aliases/AiFieldType.md) | The finite set of field types an AI schema can describe. |
| [AiStoreApi](type-aliases/AiStoreApi.md) | - |
| [AiThemeHint](type-aliases/AiThemeHint.md) | Theme hint forwarded into AI prompts so the model can pick appropriate colors, imagery, and tone (review finding TS-4). Named once here rather than inlining `"light" | "dark"` at each AI context site. (Kept local to the type-only `ai.ts` layer rather than reusing the react-layer `ThemeResolved`, which would cross the engine boundary.) |
| [ExportOptions](type-aliases/ExportOptions.md) | Generic bag of format-specific options passed to [ExportFormatDefinition.run](interfaces/ExportFormatDefinition.md#run). |
| [ExportStoreApi](type-aliases/ExportStoreApi.md) | - |
| [ExportWarningLevel](type-aliases/ExportWarningLevel.md) | Severity of a warning produced during an export run. |
| [HeaderActionSlot](type-aliases/HeaderActionSlot.md) | One resolved toolbar slot: either a fully-loaded live action or a static placeholder still awaiting its plugin's chunk. |
| [InferPluginContributions](type-aliases/InferPluginContributions.md) | Infer the union of contribution types from a plugins tuple. |
| [IRAssetResolver](type-aliases/IRAssetResolver.md) | Rewrites an asset reference to its resolved location, or returns `null` when this resolver does not handle the reference. |
| [LifecycleEventName](type-aliases/LifecycleEventName.md) | The closed union of lifecycle event names the runtime fires. |
| [LifecyclePhase](type-aliases/LifecyclePhase.md) | Engine-owned lifecycle phase (architecture §6 A2). The shell drives transitions with [LifecycleManager.advanceTo](interfaces/LifecycleManager.md#advanceto); the engine owns the ordering and dedupe that used to live as three cooperating refs in the React shell (`onInitPromise` / `readyFiredFor` / the `queueMicrotask` + `.then(fireReady, fireReady)` chain). |
| [LifecycleSubscriber](type-aliases/LifecycleSubscriber.md) | Handler signature registered via [LifecycleManager.subscribe](interfaces/LifecycleManager.md#subscribe). |
| [RegistrationTransform](type-aliases/RegistrationTransform.md) | Pure transform applied to a plugin's [StudioPluginRegistration](interfaces/StudioPluginRegistration.md) — e.g. to drop a registration field before the runtime aggregates it. |
| [StaticHeaderActionPlaceholder](type-aliases/StaticHeaderActionPlaceholder.md) | The **serializable** subset of [StudioHeaderAction](interfaces/StudioHeaderAction.md) a plugin can declare on [StudioPluginMeta.staticHeaderActions](interfaces/StudioPluginMeta.md#staticheaderactions) before its chunk loads — the descriptor **minus** its two live closures (`onClick`, `disabled`). |
| [StudioAnyPlugin](type-aliases/StudioAnyPlugin.md) | Either a Studio plugin or a raw `@puckeditor/core` plugin. |
| [StudioAssetKind](type-aliases/StudioAssetKind.md) | The kind of asset surfaced by a [StudioAssetSource](interfaces/StudioAssetSource.md). Drives which sub-panel layout (image grid / video card / audio row) the sidebar uses for the tile. |
| [StudioAssetSourceStatus](type-aliases/StudioAssetSourceStatus.md) | Loading / mutation status streamed by a source via [StudioAssetSource.subscribeStatus](interfaces/StudioAssetSource.md#subscribestatus). The error variant carries a structural error (no class dependency on a plugin). |
| [StudioAssetUploadEvent](type-aliases/StudioAssetUploadEvent.md) | Progress envelope emitted while an upload is in flight. |
| [StudioAssetUploadListener](type-aliases/StudioAssetUploadListener.md) | - |
| [StudioConfig](type-aliases/StudioConfig.md) | Runtime configuration surface for `<Studio>`. Identical to the Zod-inferred shape except `experimental` also carries any keys a plugin/host declared on [StudioExperimentalConfig](interfaces/StudioExperimentalConfig.md) — the runtime/type single-source-of-truth is unchanged (the schema still accepts any record); this only *adds* optional typed keys. |
| [StudioCopySnippetCategory](type-aliases/StudioCopySnippetCategory.md) | Built-in snippet categories. Plugins may extend by passing arbitrary strings; the i18n catalog ships keys for `basic` / `brand` only — unknown categories render under the snippet's literal category name. |
| [StudioInsertSectionPredicate](type-aliases/StudioInsertSectionPredicate.md) | Predicate used by `registerInsertSection()` to decide which components belong in this section. Receives the component name (a Puck Config key) and may inspect the component's metadata category via the second argument. |
| [StudioLayerQuickAddInserter](type-aliases/StudioLayerQuickAddInserter.md) | Inserter callback handed to a `registerLayerQuickAdd` item when the user clicks it in the `+` quick-add popover. Plugins use the `puckApi` to dispatch a `setData`/`insert` action that drops the primitive into the canvas. |
| [StudioLogger](type-aliases/StudioLogger.md) | Host-supplied structured logger. Re-exported from `Studio.tsx` so the public `@anvilkit/core/react` surface is unchanged. |
| [StudioLogLevel](type-aliases/StudioLogLevel.md) | Severity level for [StudioPluginContext.log](interfaces/StudioPluginContext.md#log) and [StudioAssetAction.run](interfaces/StudioAssetAction.md#run)'s `log`. |
| [StudioOverlayPlacement](type-aliases/StudioOverlayPlacement.md) | Placement targets for plugin-contributed overlays. Each placement corresponds to a distinct slot inside `<Studio>`: |
| [StudioPluginCapabilities](type-aliases/StudioPluginCapabilities.md) | Declarative capability hint carried on [StudioPluginMeta](interfaces/StudioPluginMeta.md). |
| [StudioPluginLoader](type-aliases/StudioPluginLoader.md) | Loader function returning either a plugin module's default export or a named export that is itself a `StudioPlugin`. Most ergonomic shape for callers writing `() => import("@anvilkit/plugin-x").then(m => m.plugin)`. |
| [StudioPluginPrefetch](type-aliases/StudioPluginPrefetch.md) | When a deferred plugin's chunk should be fetched. Advisory hint on [StudioPluginMeta.prefetch](interfaces/StudioPluginMeta.md#prefetch). |
| [StudioSidebarUnregister](type-aliases/StudioSidebarUnregister.md) | Handle returned by every `register*` extension method. Calling it removes the contributed entry from the registry. Plugins should collect these in their `register()` body and call them in `onDestroy` so a remount does not duplicate registrations. |
| [StudioSlotId](type-aliases/StudioSlotId.md) | Identifier of a named chrome slot a plugin can fill. |
| [ThemeMode](type-aliases/ThemeMode.md) | The user-facing theme preference. Matches the enum in `StudioConfigSchema.theme.defaultMode` exactly — if one changes, update both. |
| [ThemeResolved](type-aliases/ThemeResolved.md) | The resolved on-screen theme. Only two values because `"system"` is a preference, not a concrete style — it always resolves to `"light"` or `"dark"` at render time. |
| [ThemeStoreApi](type-aliases/ThemeStoreApi.md) | The vanilla theme store handle. Inferred from [createThemeStore](functions/createThemeStore.md) so it carries the `persist` middleware surface (`hasHydrated`, `onFinishHydration`, `rehydrate`, …) with full fidelity — consumers reach `store.persist` directly instead of casting through `unknown`. zustand does not export `StorePersist`, so deriving the type is the only way to keep it statically guaranteed rather than asserted. |

## Variables

| Variable | Description |
| ------ | ------ |
| [CORE\_VERSION](variables/CORE_VERSION.md) | - |
| [jsonFormat](variables/jsonFormat.md) | The built-in JSON snapshot exporter — the one export format `@anvilkit/core` ships itself (every other format comes from a plugin). |
| [StudioConfigSchema](variables/StudioConfigSchema.md) | The root schema for `StudioConfig`. |
| [withoutHeaderActions](variables/withoutHeaderActions.md) | Ready-made [RegistrationTransform](type-aliases/RegistrationTransform.md) that drops a plugin's `headerActions` from its registration while leaving every other contribution (overrides, sidebar registrations, export formats, providers, overlays, slots) intact. |

## Functions

| Function | Description |
| ------ | ------ |
| [AiStoreProvider](functions/AiStoreProvider.md) | - |
| [compilePlugins](functions/compilePlugins.md) | - |
| [composeHeaderActions](functions/composeHeaderActions.md) | Sort and validate the flat list of plugin-contributed header actions into the canonical render order. |
| [createAiStore](functions/createAiStore.md) | Build a fresh per-`<Studio>` AI store. The persistence key is namespaced by `storeId` so two concurrent editors never bleed in-flight generation state or prompt history into each other. Consume via [AiStoreProvider](functions/AiStoreProvider.md) + the `useAiStore` selector hook. |
| [createExportRegistry](functions/createExportRegistry.md) | Build an [ExportRegistry](interfaces/ExportRegistry.md) from a flat array of [ExportFormatDefinition](interfaces/ExportFormatDefinition.md) objects. |
| [createExportStore](functions/createExportStore.md) | Build a fresh per-`<Studio>` export store. The persistence key is namespaced by `storeId` so two concurrent editors never share in-flight export state or available-format lists. Consume via [ExportStoreProvider](functions/ExportStoreProvider.md) + the `useExportStore` selector hook. |
| [createLifecycleManager](functions/createLifecycleManager.md) | Create a lifecycle manager bound to a frozen array of plugin registrations. |
| [createStudioConfig](functions/createStudioConfig.md) | - |
| [createThemeStore](functions/createThemeStore.md) | Build a fresh per-`<Studio>` theme store. The persistence key is namespaced by `storeId` so two editors on the same page never collide on `localStorage` or bleed preference state into each other. Consume via [ThemeStoreProvider](functions/ThemeStoreProvider.md) + the `useThemeStore` selector hook (re-exported from the provider module). |
| [defineStudioPlugin](functions/defineStudioPlugin.md) | Brand a plain `StudioPlugin` literal as [StudioPluginContributing](interfaces/StudioPluginContributing.md)`<Contributes>`. |
| [downloadExportResult](functions/downloadExportResult.md) | Trigger a browser download of exported content via a transient object-URL + anchor click. |
| [exportAndDownload](functions/exportAndDownload.md) | Browser convenience: [runExport](functions/runExport.md) then download the result. The one-liner a `<Studio onExport>` handler can delegate to. |
| [ExportStoreProvider](functions/ExportStoreProvider.md) | - |
| [isPuckPlugin](functions/isPuckPlugin.md) | Type guard that returns `true` if `value` conforms to the `@puckeditor/core` [PuckPlugin](https://puckeditor.com/docs/extending-puck/plugins) shape. |
| [isStudioPlugin](functions/isStudioPlugin.md) | Type guard that returns `true` if `value` conforms to the [StudioPlugin](interfaces/StudioPlugin.md) shape. |
| [lazyPlugin](functions/lazyPlugin.md) | Wrap an async plugin loader as a `StudioPlugin` with the supplied synchronous `meta`. The actual plugin module is fetched on first `register()` invocation — i.e. once `<Studio>` mounts and `compilePlugins()` reaches this entry of the `plugins` prop. |
| [lazyPluginWith](functions/lazyPluginWith.md) | Like [lazyPlugin](functions/lazyPlugin.md), but applies `transform` to the loaded plugin's registration **inside the lazy boundary** — after the chunk is fetched at `register()` time, never before. |
| [mergeOverrides](functions/mergeOverrides.md) | Compose a list of plugin-contributed Puck override slices into a single [PuckOverrides](https://puckeditor.com/docs/api-reference/configuration/overrides) dictionary, with per-key currying so multiple plugins touching the same key all run. |
| [pageRootSeoToStudioPageSeo](functions/pageRootSeoToStudioPageSeo.md) | Project `root.props.seo` (schema field names) onto the core [StudioPageSeo](interfaces/StudioPageSeo.md) shape. `canonical` is carried through unchanged so the projection round-trips losslessly (review finding M3). Returns `undefined` when no SEO field is set so callers can omit the block entirely. |
| [pageRootToStudioPageFields](functions/pageRootToStudioPageFields.md) | Project a page's `root.props` (title + seo) onto the [StudioPage](interfaces/StudioPage.md) fields the rail/breadcrumb read, so the sidecar no longer holds a parallel copy. Returns only the derived slice; callers merge it with the routing metadata they own (`id`/`path`/`route`/`locked`). |
| [parseStudioEnv](functions/parseStudioEnv.md) | - |
| [resolveHeaderActionSlots](functions/resolveHeaderActionSlots.md) | Coalesce declared placeholders and live header actions into one ordered slot list, keyed by `id`: |
| [runExport](functions/runExport.md) | Normalize → run → record an export, returning the [ExportResult](interfaces/ExportResult.md) (no download — see [exportAndDownload](functions/exportAndDownload.md)). |
| [Studio](functions/Studio.md) | The public Studio shell. Thin view over useStudioController: select the loading state, then render the provider stack around `<Puck>`. |
| [StudioConfigProvider](functions/StudioConfigProvider.md) | - |
| [StudioErrorScreen](functions/StudioErrorScreen.md) | Default recoverable error panel rendered when `<Studio>` fails to compile its plugin runtime. Exported from `@anvilkit/core/react` so hosts can also render it inside their own `errorFallback`. |
| [StudioLoadingScreen](functions/StudioLoadingScreen.md) | Default skeleton chrome rendered while `<Studio>` compiles its plugin runtime. Exported from `@anvilkit/core/react` so hosts can also pass it explicitly: `<Studio loading={<StudioLoadingScreen />} />`. |
| [studioPageSeoToPageRootSeo](functions/studioPageSeoToPageRootSeo.md) | Inverse of [pageRootSeoToStudioPageSeo](functions/pageRootSeoToStudioPageSeo.md) for write-back: map an edited [StudioPageSeo](interfaces/StudioPageSeo.md) (e.g. from the page-settings dialog) onto `root.props.seo` field names. `canonical` round-trips through both directions, so write-back no longer drops it (review finding M3). |
| [ThemeStoreProvider](functions/ThemeStoreProvider.md) | - |
| [useAiStore](functions/useAiStore.md) | Reactive AI-store selector. Same call shape as the former module-singleton hook: `useAiStore((s) => s.isGenerating)`. |
| [useAiStoreApi](functions/useAiStoreApi.md) | - |
| [useExportStore](functions/useExportStore.md) | Reactive export-store selector. Same call shape as the former module-singleton hook: `useExportStore((s) => s.currentFormat)`. |
| [useExportStoreApi](functions/useExportStoreApi.md) | - |
| [useStudio](functions/useStudio.md) | Read the compiled [StudioRuntime](interfaces/StudioRuntime.md)'s diagnostic projection from inside a `<Studio>` subtree. |
| [useStudioConfig](functions/useStudioConfig.md) | - |
| [useThemeStore](functions/useThemeStore.md) | Reactive theme-store selector. Same call shape as the former module-singleton hook: `useThemeStore((s) => s.mode)`. |
| [useThemeStoreApi](functions/useThemeStoreApi.md) | The active per-instance theme store. Throws outside a `ThemeStoreProvider` so missing wiring fails loudly. |
