---
title: "ThemeMode"
description: "@anvilkit/core · ThemeMode"
---

```ts
type ThemeMode = "light" | "dark" | "system";
```

Defined in: [state/slices/theme-store.ts:56](https://github.com/ancyloce/anvilkit-studio/blob/main/packages/core/src/state/slices/theme-store.ts#L56)

The user-facing theme preference. Matches the enum in
`StudioConfigSchema.theme.defaultMode` exactly — if one changes,
update both.
