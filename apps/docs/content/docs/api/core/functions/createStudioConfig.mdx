---
title: "createStudioConfig"
description: "@anvilkit/core · createStudioConfig"
---

```ts
function createStudioConfig(overrides?, opts?): StudioConfig;
```

Defined in: [runtime/core/src/config/create-config.ts:132](https://github.com/ancyloce/anvilkit-studio/blob/main/packages/runtime/core/src/config/create-config.ts#L132)

Build a fully validated [StudioConfig](../type-aliases/StudioConfig) by layering schema
defaults, env vars, and explicit host overrides — in that order.

### Layering

1. **Layer 1 — defaults.** `StudioConfigSchema.parse({})` produces
   the fully-populated default shape. This is the base target for
   [deepMerge](https://github.com/ancyloce/anvilkit-studio/blob/main/packages/foundation/utils/src/deep-merge.ts), so missing keys in higher layers fall back
   to real values rather than `undefined`.
2. **Layer 2 — env.** [parseStudioEnv](parseStudioEnv.md) walks the env bag
   for `ANVILKIT_*` entries and produces a `DeepPartial` patch.
3. **Layer 3 — host overrides.** The caller's `overrides` argument
   is treated as an opaque `DeepPartial` and layered last, so it
   beats both defaults and env.

After merging, the result is re-parsed through the schema. This
re-parse is load-bearing: it re-runs every validator (URL format,
integer bounds, enum membership) against the merged shape and
catches env-induced type mismatches that layers 1 and 3 would
have passed cleanly on their own.

### Errors

Any `ZodError` raised during the final parse is wrapped in a
[StudioConfigError](../classes/StudioConfigError) whose message lists every failing field
in `path: reason` form. The underlying `ZodError` is attached via
`cause` so host apps that need structured access can drill in
with `err.cause as z.ZodError`.

## Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `overrides?` | \{ `ai?`: \{ `defaultModel?`: `string`; `maxRetries?`: `number`; \}; `branding?`: \{ `appName?`: `string`; `logoUrl?`: `string`; `primaryColor?`: `string`; \}; `brandKit?`: \{ `colors?`: \{ `name`: `string`; `value`: `string`; \}[]; `fonts?`: `string`[]; \}; `experimental?`: \{ \[`key`: `string`\]: `unknown`; \}; `export?`: \{ `defaultFormat?`: `string`; `filenamePrefix?`: `string`; \}; `features?`: \{ `enableAi?`: `boolean`; `enableCollaboration?`: `boolean`; `enableExport?`: `boolean`; \}; `i18n?`: \{ `fallbackLocale?`: `string`; `locale?`: `string`; `messages?`: \{ \[`key`: `string`\]: \| \{ \[`key`: `string`\]: `string` \| `undefined`; \} \| `undefined`; \}; `showLocaleSwitch?`: `boolean`; \}; `theme?`: \{ `allowToggle?`: `boolean`; `defaultMode?`: `"light"` \| `"dark"` \| `"system"`; \}; \} | Layer 3 host overrides. |
| `overrides.ai?` | \{ `defaultModel?`: `string`; `maxRetries?`: `number`; \} | AI block. Intentionally carries **no credentials** — API keys, endpoint URLs, and auth tokens belong to the host backend, and the AI copilot plugin factory receives a `generatePage()` callback that wraps whatever network path the host has set up. Architecture §9 is explicit about this boundary. |
| `overrides.ai.defaultModel?` | `string` | Optional hint for the AI plugin about which model to request by default (e.g. `"claude-opus-4-6"`). Plugins are free to ignore this if their host-side implementation is locked to a specific model. |
| `overrides.ai.maxRetries?` | `number` | Maximum number of retries for a failed AI generation call. Clamped to `[0, 10]` so hosts cannot accidentally enable unbounded retry storms. Defaults to `3`. |
| `overrides.branding?` | \{ `appName?`: `string`; `logoUrl?`: `string`; `primaryColor?`: `string`; \} | Branding block for the editor chrome — app name, logo, and accent color surfaced in the Studio header. |
| `overrides.branding.appName?` | `string` | Display name shown in the editor header and the browser tab. Defaults to `"AnvilKit Studio"` so an un-configured host still looks intentional. |
| `overrides.branding.logoUrl?` | `string` | Optional URL of a logo rendered in the header. Validated as a URL via `z.url()` (top-level format in Zod v4). Omit to fall back to the default wordmark. |
| `overrides.branding.primaryColor?` | `string` | Optional primary accent color for the editor chrome. Free-form string so hosts can pass a CSS variable reference (`"var(--brand)"`) or any CSS color value. |
| `overrides.brandKit?` | \{ `colors?`: \{ `name`: `string`; `value`: `string`; \}[]; `fonts?`: `string`[]; \} | Brand-kit block (I3-4) — shared colors + font families the Canvas Studio editor surfaces (color pickers, font menus) so designs stay on-brand. Distinct from `branding`, which styles the editor *chrome*: `brandKit` is design content the canvas reads via its `useBrandKit()` hook. `plugin-canvas-studio` maps these entries to the editor's `BrandKit` shape. |
| `overrides.brandKit.colors?` | \{ `name`: `string`; `value`: `string`; \}[] | Named brand color swatches. Each `value` is any CSS color (`"#2563eb"`, `"var(--brand)"`). Empty by default. |
| `overrides.brandKit.fonts?` | `string`[] | Brand font families, e.g. `["Inter", "Poppins"]`. The first entry is treated as the brand default. Empty by default. |
| `overrides.experimental?` | \{ \[`key`: `string`\]: `unknown`; \} | - |
| `overrides.export?` | \{ `defaultFormat?`: `string`; `filenamePrefix?`: `string`; \} | Export pipeline block — configuration for the export registry and download flow. |
| `overrides.export.defaultFormat?` | `string` | Optional id of the format to pre-select in export menus (e.g. `"html"`, `"react"`). Resolved at runtime against the registered format registry — an id that does not match any registered format falls back to the first registered format. |
| `overrides.export.filenamePrefix?` | `string` | Filename prefix for downloads. The export pipeline appends the format's extension (e.g. `"page.html"`). Defaults to `"page"` — hosts typically override with a slug derived from the current page title. |
| `overrides.features?` | \{ `enableAi?`: `boolean`; `enableCollaboration?`: `boolean`; `enableExport?`: `boolean`; \} | Feature flag block. Every flag defaults to `false` so a fresh install has no surprise behavior enabled — hosts opt in explicitly. |
| `overrides.features.enableAi?` | `boolean` | When `true`, the AI copilot plugin (if installed) mounts its panel and the `generatePage()` pipeline is active. No-op when the AI plugin is not in the host's plugin array. |
| `overrides.features.enableCollaboration?` | `boolean` | **Experimental / reserved.** `@anvilkit/core` does **not** act on this flag — there is no collaboration implementation in core. Real-time collaboration (presence / cursors) is provided by the separate, experimental `@anvilkit/plugin-collab-*` packages a host wires in; this is a reserved config seam those packages (or host code) may consult to gate the presence layer. Defaulted to `false` and kept stable so turning collaboration on later is not a breaking config change. No core behavior keys off it today. |
| `overrides.features.enableExport?` | `boolean` | When `true`, the export registry is compiled and the host may call `exportAs(formatId)`. When `false`, exporter plugins register but their header actions are hidden and `exportAs` throws `StudioExportError`. |
| `overrides.i18n?` | \{ `fallbackLocale?`: `string`; `locale?`: `string`; `messages?`: \{ \[`key`: `string`\]: \| \{ \[`key`: `string`\]: `string` \| `undefined`; \} \| `undefined`; \}; `showLocaleSwitch?`: `boolean`; \} | Internationalization block — the active locale, the fallback locale, optional per-locale message overrides, and the built-in language-switcher flag. The whole block is **reactive**: it is excluded from the plugin compile fingerprint (`stripReactiveConfig`), so changing any `i18n.*` value at runtime updates the editor chrome in place — no plugin recompile, no editor teardown. The env override nests: `ANVILKIT_I18N__LOCALE=zh` maps to `i18n.locale` (the `__` separator). A bare `ANVILKIT_LOCALE` is rejected — the root schema is `strictObject`, so a top-level `locale` key is unknown. |
| `overrides.i18n.fallbackLocale?` | `string` | Locale whose [messages](#createstudioconfig) entries back-fill keys missing from the active locale's overrides. Defaults to `"en"`. |
| `overrides.i18n.locale?` | `string` | Active locale (BCP-47-ish tag, e.g. `"en"`, `"zh"`). When the host **explicitly sets** this on the raw `<Studio config>` prop, the instance is locale-**controlled**: this value is authoritative and reactive (changing it switches the language live), the built-in `localStorage` persistence is bypassed, and internal switch requests (the built-in `LanguageSwitcher`) only fire `onLocaleChange` — the host applies them by re-rendering with the new value. Controlled-ness is latched at mount (remount with a new React `key` to switch modes). When absent, the instance is uncontrolled: the locale store defaults to `"en"`, persists per `storeId`, and a non-`"en"` value resolved from env (`ANVILKIT_I18N__LOCALE`) seeds the store once when no persisted choice exists. Env values never trigger controlled mode. |
| `overrides.i18n.messages?` | \{ \[`key`: `string`\]: \| \{ \[`key`: `string`\]: `string` \| `undefined`; \} \| `undefined`; \} | Optional per-locale message overrides, keyed `locale → (messageKey → string)`. Applied to the rendered chrome catalog for the active locale (with [fallbackLocale](#createstudioconfig) back-fill) and to `ctx.t`. Beats the built-in catalog and plugin packs; the deprecated flat `<Studio messages>` prop still wins over these during its migration window. Omit to use the built-in English catalog plus any plugin packs. |
| `overrides.i18n.showLocaleSwitch?` | `boolean` | When `true`, the built-in `<LanguageSwitcher>` renders in the header between the plugin header actions and the host `headerEnd` node. Defaults to `false` (opt-in, like `features.*`) so existing mounts — including hosts already rendering their own switcher via `headerEnd` — see no change. |
| `overrides.theme?` | \{ `allowToggle?`: `boolean`; `defaultMode?`: `"light"` \| `"dark"` \| `"system"`; \} | Theme block — the initial color mode and whether the built-in theme toggle is shown. |
| `overrides.theme.allowToggle?` | `boolean` | When `true`, the built-in theme toggle button is rendered in the header. Hosts that manage theme externally can set this to `false` and drive the theme via their own controls. |
| `overrides.theme.defaultMode?` | `"light"` \| `"dark"` \| `"system"` | Initial color mode. `"system"` respects the user's OS preference via `prefers-color-scheme` and is the default. `"light"` and `"dark"` force the editor into that mode regardless of OS setting. |
| `opts?` | [`CreateStudioConfigOptions`](../interfaces/CreateStudioConfigOptions) | Optional [CreateStudioConfigOptions](../interfaces/CreateStudioConfigOptions) bag. |

## Returns

[`StudioConfig`](../type-aliases/StudioConfig)

The merged, validated [StudioConfig](../type-aliases/StudioConfig).

## Examples

```ts
const config = createStudioConfig({
  features: { enableExport: true },
  theme: { defaultMode: "dark" },
});
```

```ts
// Test-friendly: inject a synthetic env bag.
const config = createStudioConfig(undefined, {
  env: { ANVILKIT_THEME__DEFAULT_MODE: "dark" },
});
```
