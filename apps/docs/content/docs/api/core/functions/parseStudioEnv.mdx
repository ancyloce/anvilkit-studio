---
title: "parseStudioEnv"
description: "@anvilkit/core · parseStudioEnv"
---

```ts
function parseStudioEnv(env?): {
  ai?: {
     defaultModel?: string;
     maxRetries?: number;
  };
  branding?: {
     appName?: string;
     logoUrl?: string;
     primaryColor?: string;
  };
  brandKit?: {
     colors?: {
        name: string;
        value: string;
     }[];
     fonts?: string[];
  };
  experimental?: {
   [key: string]: unknown;
  };
  export?: {
     defaultFormat?: string;
     filenamePrefix?: string;
  };
  features?: {
     enableAi?: boolean;
     enableCollaboration?: boolean;
     enableExport?: boolean;
  };
  i18n?: {
     fallbackLocale?: string;
     locale?: string;
     messages?: {
      [key: string]: 
        | {
      [key: string]: string | undefined;
      }
        | undefined;
     };
     showLocaleSwitch?: boolean;
  };
  theme?: {
     allowToggle?: boolean;
     defaultMode?: "light" | "dark" | "system";
  };
};
```

Defined in: [runtime/core/src/config/env-parser.ts:285](https://github.com/ancyloce/anvilkit-studio/blob/main/packages/runtime/core/src/config/env-parser.ts#L285)

Parse an environment-variable bag into a [DeepPartial](https://github.com/ancyloce/anvilkit-studio/blob/main/packages/utils/src/types.ts) of
[StudioConfig](../type-aliases/StudioConfig).

Pure function — accepts the env bag as a parameter so tests can
pass synthetic inputs without mutating `process.env`.

## Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `env` | `Record`\&lt;`string`, `string` \| `undefined`\&gt; | The env bag to walk. Defaults to `process.env` when that global exists, otherwise an empty object. |

## Returns

A partial config shape. Keys that do not match the
`ANVILKIT_` prefix and keys whose value is `undefined` are skipped.

| Name | Type | Description | Defined in |
| ------ | ------ | ------ | ------ |
| `ai?` | \{ `defaultModel?`: `string`; `maxRetries?`: `number`; \} | AI block. Intentionally carries **no credentials** — API keys, endpoint URLs, and auth tokens belong to the host backend, and the AI copilot plugin factory receives a `generatePage()` callback that wraps whatever network path the host has set up. Architecture §9 is explicit about this boundary. | [runtime/core/src/config/schema.ts:285](https://github.com/ancyloce/anvilkit-studio/blob/main/packages/runtime/core/src/config/schema.ts#L285) |
| `ai.defaultModel?` | `string` | Optional hint for the AI plugin about which model to request by default (e.g. `"claude-opus-4-6"`). Plugins are free to ignore this if their host-side implementation is locked to a specific model. | [runtime/core/src/config/schema.ts:293](https://github.com/ancyloce/anvilkit-studio/blob/main/packages/runtime/core/src/config/schema.ts#L293) |
| `ai.maxRetries?` | `number` | Maximum number of retries for a failed AI generation call. Clamped to `[0, 10]` so hosts cannot accidentally enable unbounded retry storms. Defaults to `3`. | [runtime/core/src/config/schema.ts:300](https://github.com/ancyloce/anvilkit-studio/blob/main/packages/runtime/core/src/config/schema.ts#L300) |
| `branding?` | \{ `appName?`: `string`; `logoUrl?`: `string`; `primaryColor?`: `string`; \} | Branding block for the editor chrome — app name, logo, and accent color surfaced in the Studio header. | [runtime/core/src/config/schema.ts:110](https://github.com/ancyloce/anvilkit-studio/blob/main/packages/runtime/core/src/config/schema.ts#L110) |
| `branding.appName?` | `string` | Display name shown in the editor header and the browser tab. Defaults to `"AnvilKit Studio"` so an un-configured host still looks intentional. | [runtime/core/src/config/schema.ts:117](https://github.com/ancyloce/anvilkit-studio/blob/main/packages/runtime/core/src/config/schema.ts#L117) |
| `branding.logoUrl?` | `string` | Optional URL of a logo rendered in the header. Validated as a URL via `z.url()` (top-level format in Zod v4). Omit to fall back to the default wordmark. | [runtime/core/src/config/schema.ts:124](https://github.com/ancyloce/anvilkit-studio/blob/main/packages/runtime/core/src/config/schema.ts#L124) |
| `branding.primaryColor?` | `string` | Optional primary accent color for the editor chrome. Free-form string so hosts can pass a CSS variable reference (`"var(--brand)"`) or any CSS color value. | [runtime/core/src/config/schema.ts:131](https://github.com/ancyloce/anvilkit-studio/blob/main/packages/runtime/core/src/config/schema.ts#L131) |
| `brandKit?` | \{ `colors?`: \{ `name`: `string`; `value`: `string`; \}[]; `fonts?`: `string`[]; \} | Brand-kit block (I3-4) — shared colors + font families the Canvas Studio editor surfaces (color pickers, font menus) so designs stay on-brand. Distinct from `branding`, which styles the editor *chrome*: `brandKit` is design content the canvas reads via its `useBrandKit()` hook. `plugin-canvas-studio` maps these entries to the editor's `BrandKit` shape. | [runtime/core/src/config/schema.ts:229](https://github.com/ancyloce/anvilkit-studio/blob/main/packages/runtime/core/src/config/schema.ts#L229) |
| `brandKit.colors?` | \{ `name`: `string`; `value`: `string`; \}[] | Named brand color swatches. Each `value` is any CSS color (`"#2563eb"`, `"var(--brand)"`). Empty by default. | [runtime/core/src/config/schema.ts:235](https://github.com/ancyloce/anvilkit-studio/blob/main/packages/runtime/core/src/config/schema.ts#L235) |
| `brandKit.fonts?` | `string`[] | Brand font families, e.g. `["Inter", "Poppins"]`. The first entry is treated as the brand default. Empty by default. | [runtime/core/src/config/schema.ts:250](https://github.com/ancyloce/anvilkit-studio/blob/main/packages/runtime/core/src/config/schema.ts#L250) |
| `experimental?` | \{ \[`key`: `string`\]: `unknown`; \} | - | [runtime/core/src/types/config.ts:82](https://github.com/ancyloce/anvilkit-studio/blob/main/packages/runtime/core/src/types/config.ts#L82) |
| `export?` | \{ `defaultFormat?`: `string`; `filenamePrefix?`: `string`; \} | Export pipeline block — configuration for the export registry and download flow. | [runtime/core/src/config/schema.ts:257](https://github.com/ancyloce/anvilkit-studio/blob/main/packages/runtime/core/src/config/schema.ts#L257) |
| `export.defaultFormat?` | `string` | Optional id of the format to pre-select in export menus (e.g. `"html"`, `"react"`). Resolved at runtime against the registered format registry — an id that does not match any registered format falls back to the first registered format. | [runtime/core/src/config/schema.ts:266](https://github.com/ancyloce/anvilkit-studio/blob/main/packages/runtime/core/src/config/schema.ts#L266) |
| `export.filenamePrefix?` | `string` | Filename prefix for downloads. The export pipeline appends the format's extension (e.g. `"page.html"`). Defaults to `"page"` — hosts typically override with a slug derived from the current page title. | [runtime/core/src/config/schema.ts:274](https://github.com/ancyloce/anvilkit-studio/blob/main/packages/runtime/core/src/config/schema.ts#L274) |
| `features?` | \{ `enableAi?`: `boolean`; `enableCollaboration?`: `boolean`; `enableExport?`: `boolean`; \} | Feature flag block. Every flag defaults to `false` so a fresh install has no surprise behavior enabled — hosts opt in explicitly. | [runtime/core/src/config/schema.ts:76](https://github.com/ancyloce/anvilkit-studio/blob/main/packages/runtime/core/src/config/schema.ts#L76) |
| `features.enableAi?` | `boolean` | When `true`, the AI copilot plugin (if installed) mounts its panel and the `generatePage()` pipeline is active. No-op when the AI plugin is not in the host's plugin array. | [runtime/core/src/config/schema.ts:91](https://github.com/ancyloce/anvilkit-studio/blob/main/packages/runtime/core/src/config/schema.ts#L91) |
| `features.enableCollaboration?` | `boolean` | **Experimental / reserved.** `@anvilkit/core` does **not** act on this flag — there is no collaboration implementation in core. Real-time collaboration (presence / cursors) is provided by the separate, experimental `@anvilkit/plugin-collab-*` packages a host wires in; this is a reserved config seam those packages (or host code) may consult to gate the presence layer. Defaulted to `false` and kept stable so turning collaboration on later is not a breaking config change. No core behavior keys off it today. | [runtime/core/src/config/schema.ts:103](https://github.com/ancyloce/anvilkit-studio/blob/main/packages/runtime/core/src/config/schema.ts#L103) |
| `features.enableExport?` | `boolean` | When `true`, the export registry is compiled and the host may call `exportAs(formatId)`. When `false`, exporter plugins register but their header actions are hidden and `exportAs` throws `StudioExportError`. | [runtime/core/src/config/schema.ts:84](https://github.com/ancyloce/anvilkit-studio/blob/main/packages/runtime/core/src/config/schema.ts#L84) |
| `i18n?` | \{ `fallbackLocale?`: `string`; `locale?`: `string`; `messages?`: \{ \[`key`: `string`\]: \| \{ \[`key`: `string`\]: `string` \| `undefined`; \} \| `undefined`; \}; `showLocaleSwitch?`: `boolean`; \} | Internationalization block — the active locale, the fallback locale, optional per-locale message overrides, and the built-in language-switcher flag. The whole block is **reactive**: it is excluded from the plugin compile fingerprint (`stripReactiveConfig`), so changing any `i18n.*` value at runtime updates the editor chrome in place — no plugin recompile, no editor teardown. The env override nests: `ANVILKIT_I18N__LOCALE=zh` maps to `i18n.locale` (the `__` separator). A bare `ANVILKIT_LOCALE` is rejected — the root schema is `strictObject`, so a top-level `locale` key is unknown. | [runtime/core/src/config/schema.ts:171](https://github.com/ancyloce/anvilkit-studio/blob/main/packages/runtime/core/src/config/schema.ts#L171) |
| `i18n.fallbackLocale?` | `string` | Locale whose [messages](#parsestudioenv) entries back-fill keys missing from the active locale's overrides. Defaults to `"en"`. | [runtime/core/src/config/schema.ts:197](https://github.com/ancyloce/anvilkit-studio/blob/main/packages/runtime/core/src/config/schema.ts#L197) |
| `i18n.locale?` | `string` | Active locale (BCP-47-ish tag, e.g. `"en"`, `"zh"`). When the host **explicitly sets** this on the raw `<Studio config>` prop, the instance is locale-**controlled**: this value is authoritative and reactive (changing it switches the language live), the built-in `localStorage` persistence is bypassed, and internal switch requests (the built-in `LanguageSwitcher`) only fire `onLocaleChange` — the host applies them by re-rendering with the new value. Controlled-ness is latched at mount (remount with a new React `key` to switch modes). When absent, the instance is uncontrolled: the locale store defaults to `"en"`, persists per `storeId`, and a non-`"en"` value resolved from env (`ANVILKIT_I18N__LOCALE`) seeds the store once when no persisted choice exists. Env values never trigger controlled mode. | [runtime/core/src/config/schema.ts:192](https://github.com/ancyloce/anvilkit-studio/blob/main/packages/runtime/core/src/config/schema.ts#L192) |
| `i18n.messages?` | \{ \[`key`: `string`\]: \| \{ \[`key`: `string`\]: `string` \| `undefined`; \} \| `undefined`; \} | Optional per-locale message overrides, keyed `locale → (messageKey → string)`. Applied to the rendered chrome catalog for the active locale (with [fallbackLocale](#parsestudioenv) back-fill) and to `ctx.t`. Beats the built-in catalog and plugin packs; the deprecated flat `<Studio messages>` prop still wins over these during its migration window. Omit to use the built-in English catalog plus any plugin packs. | [runtime/core/src/config/schema.ts:208](https://github.com/ancyloce/anvilkit-studio/blob/main/packages/runtime/core/src/config/schema.ts#L208) |
| `i18n.showLocaleSwitch?` | `boolean` | When `true`, the built-in `<LanguageSwitcher>` renders in the header between the plugin header actions and the host `headerEnd` node. Defaults to `false` (opt-in, like `features.*`) so existing mounts — including hosts already rendering their own switcher via `headerEnd` — see no change. | [runtime/core/src/config/schema.ts:218](https://github.com/ancyloce/anvilkit-studio/blob/main/packages/runtime/core/src/config/schema.ts#L218) |
| `theme?` | \{ `allowToggle?`: `boolean`; `defaultMode?`: `"light"` \| `"dark"` \| `"system"`; \} | Theme block — the initial color mode and whether the built-in theme toggle is shown. | [runtime/core/src/config/schema.ts:138](https://github.com/ancyloce/anvilkit-studio/blob/main/packages/runtime/core/src/config/schema.ts#L138) |
| `theme.allowToggle?` | `boolean` | When `true`, the built-in theme toggle button is rendered in the header. Hosts that manage theme externally can set this to `false` and drive the theme via their own controls. | [runtime/core/src/config/schema.ts:153](https://github.com/ancyloce/anvilkit-studio/blob/main/packages/runtime/core/src/config/schema.ts#L153) |
| `theme.defaultMode?` | `"light"` \| `"dark"` \| `"system"` | Initial color mode. `"system"` respects the user's OS preference via `prefers-color-scheme` and is the default. `"light"` and `"dark"` force the editor into that mode regardless of OS setting. | [runtime/core/src/config/schema.ts:146](https://github.com/ancyloce/anvilkit-studio/blob/main/packages/runtime/core/src/config/schema.ts#L146) |

## Throws

When two `ANVILKIT_*` env vars collide on the
same path — e.g. setting both `ANVILKIT_FEATURES=1` and
`ANVILKIT_FEATURES__ENABLE_EXPORT=1` (a scalar and a nested object
cannot coexist at the same key). Also thrown when an explicit
`num:` / `bool:` type prefix carries an uncoercible value, or when
the env var name contains an empty nested path segment. These cases
are operator-actionable: the message names the bad path or key so
the host process can be reconfigured.
