---
title: "RichTextTransform"
description: "@anvilkit/canvas-core · RichTextTransform"
---

```ts
type RichTextTransform = "none" | "uppercase" | "lowercase" | "capitalize";
```

Defined in: [ir/types.ts:503](https://github.com/ancyloce/anvilkit-studio/blob/main/src/ir/types.ts#L503)

How a span's glyphs are cased at render time. Purely presentational — the
span's `text` is never rewritten, so the original casing survives a round-trip.
