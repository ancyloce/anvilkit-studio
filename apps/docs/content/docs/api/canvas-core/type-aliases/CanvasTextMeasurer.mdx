---
title: "CanvasTextMeasurer"
description: "@anvilkit/canvas-core · CanvasTextMeasurer"
---

```ts
type CanvasTextMeasurer = (request) => MeasuredText;
```

Defined in: [text-contracts.ts:141](https://github.com/ancyloce/anvilkit-studio/blob/main/src/text-contracts.ts#L141)

Lay out rich text. Implementations MUST be pure: the same request has to give
the same result, or an export will not match what the editor showed.

## Parameters

| Parameter | Type |
| ------ | ------ |
| `request` | [`TextMeasureRequest`](../interfaces/TextMeasureRequest) |

## Returns

[`MeasuredText`](../interfaces/MeasuredText)
