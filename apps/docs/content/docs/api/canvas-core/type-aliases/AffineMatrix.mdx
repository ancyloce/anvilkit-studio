---
title: "AffineMatrix"
description: "@anvilkit/canvas-core · AffineMatrix"
---

```ts
type AffineMatrix = [number, number, number, number, number, number];
```

Defined in: [geometry/affine.ts:17](https://github.com/ancyloce/anvilkit-studio/blob/main/src/geometry/affine.ts#L17)

SVG `matrix(a b c d e f)` tuple: point (x,y) → (a·x + c·y + e, b·x + d·y + f).
