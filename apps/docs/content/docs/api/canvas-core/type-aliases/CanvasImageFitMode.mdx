---
title: "CanvasImageFitMode"
description: "@anvilkit/canvas-core · CanvasImageFitMode"
---

```ts
type CanvasImageFitMode = "fill" | "fit" | "stretch" | "original" | "center";
```

Defined in: [ir/types.ts:585](https://github.com/ancyloce/anvilkit-studio/blob/main/src/ir/types.ts#L585)

FR-094 image fit modes (B-02, PRD 0012 §9.5). Absent means `"stretch"` —
the pre-B-02 rendering — so existing documents need no migration.
