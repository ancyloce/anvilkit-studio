---
title: "alignRects"
description: "@anvilkit/canvas-core · alignRects"
---

```ts
function alignRects(rects, edge): number[];
```

Defined in: [geometry/snap.ts:170](https://github.com/ancyloce/anvilkit-studio/blob/main/src/geometry/snap.ts#L170)

Per-rect displacement (along the edge's axis) to align every rect to the
selection's bounding box: left/right/hcenter move on X, top/bottom/vcenter on
Y. Returned array is index-aligned with `rects`; empty input → empty output.

## Parameters

| Parameter | Type |
| ------ | ------ |
| `rects` | readonly [`SnapRect`](../interfaces/SnapRect)[] |
| `edge` | [`AlignEdge`](../type-aliases/AlignEdge) |

## Returns

`number`[]
