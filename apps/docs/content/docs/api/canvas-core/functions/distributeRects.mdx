---
title: "distributeRects"
description: "@anvilkit/canvas-core · distributeRects"
---

```ts
function distributeRects(rects, axis): number[];
```

Defined in: [geometry/snap.ts:211](https://github.com/ancyloce/anvilkit-studio/blob/main/src/geometry/snap.ts#L211)

Per-rect displacement (along `axis`) to even out the gaps between rects,
keeping the first and last (by position) fixed — like Figma's "distribute
spacing". Returned array is index-aligned with `rects`; needs ≥3 rects,
otherwise all zeros.

## Parameters

| Parameter | Type |
| ------ | ------ |
| `rects` | readonly [`SnapRect`](../interfaces/SnapRect)[] |
| `axis` | [`SnapAxis`](../type-aliases/SnapAxis) |

## Returns

`number`[]
