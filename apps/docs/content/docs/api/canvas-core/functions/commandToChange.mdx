---
title: "commandToChange"
description: "@anvilkit/canvas-core · commandToChange"
---

```ts
function commandToChange(cmd): CanvasChange | null;
```

Defined in: [commands/change-events.ts:31](https://github.com/ancyloce/anvilkit-studio/blob/main/src/commands/change-events.ts#L31)

Derive the change record for a single command, or `null` when there is no
meaningful granular record (a `batch` — its sub-commands are mapped
individually by `applyCommands`). Pure: reads only the command.

## Parameters

| Parameter | Type |
| ------ | ------ |
| `cmd` | [`CanvasCommand`](../type-aliases/CanvasCommand) |

## Returns

[`CanvasChange`](../type-aliases/CanvasChange) \| `null`
