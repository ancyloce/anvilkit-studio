---
title: "isNodeLocked"
description: "@anvilkit/canvas-core · isNodeLocked"
---

```ts
function isNodeLocked(definition, nodeId): boolean;
```

Defined in: templates/resolvers.ts:55

Whether `nodeId` is locked by `definition`. The single source of truth for
this check — canvas-m2-003's `instantiateTemplate` and any editor-side
"can I edit this node" gate must call this rather than re-reading
`lockedNodeIds` themselves.

## Parameters

| Parameter | Type |
| ------ | ------ |
| `definition` | [`CanvasTemplateDefinition`](../interfaces/CanvasTemplateDefinition) |
| `nodeId` | `string` |

## Returns

`boolean`
