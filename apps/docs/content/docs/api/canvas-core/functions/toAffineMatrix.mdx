---
title: "toAffineMatrix"
description: "@anvilkit/canvas-core · toAffineMatrix"
---

```ts
function toAffineMatrix(t): AffineMatrix;
```

Defined in: [geometry.ts:60](https://github.com/ancyloce/anvilkit-studio/blob/main/src/geometry.ts#L60)

Compose a transform into an affine matrix, replicating Konva's
`Node.getTransform` order exactly: translate → rotate → skew → scale.
(CanvasIR has no `offset`, so the trailing offset translate is always 0.)

## Parameters

| Parameter | Type |
| ------ | ------ |
| `t` | [`CanvasTransform`](../interfaces/CanvasTransform) |

## Returns

[`AffineMatrix`](../type-aliases/AffineMatrix)
