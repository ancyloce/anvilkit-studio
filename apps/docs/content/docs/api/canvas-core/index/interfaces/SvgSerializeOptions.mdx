---
title: "SvgSerializeOptions"
description: "@anvilkit/canvas-core · SvgSerializeOptions"
---

Defined in: [serialize/svg.ts:1494](https://github.com/ancyloce/anvilkit-studio/blob/main/src/serialize/svg.ts#L1494)

## Properties

| Property | Type | Description | Defined in |
| ------ | ------ | ------ | ------ |
| &lt;a id="fetchasset"&gt;&lt;/a&gt; `fetchAsset?` | [`SvgFetchAsset`](../type-aliases/SvgFetchAsset) | - | [serialize/svg.ts:1496](https://github.com/ancyloce/anvilkit-studio/blob/main/src/serialize/svg.ts#L1496) |
| &lt;a id="fonts"&gt;&lt;/a&gt; `fonts?` | [`SvgFontFaceDef`](SvgFontFaceDef.md)[] | - | [serialize/svg.ts:1497](https://github.com/ancyloce/anvilkit-studio/blob/main/src/serialize/svg.ts#L1497) |
| &lt;a id="images"&gt;&lt;/a&gt; `images?` | [`SvgImageMode`](../type-aliases/SvgImageMode) | - | [serialize/svg.ts:1495](https://github.com/ancyloce/anvilkit-studio/blob/main/src/serialize/svg.ts#L1495) |
| &lt;a id="nodekinds"&gt;&lt;/a&gt; `nodeKinds?` | [`CanvasNodeKindRegistry`](CanvasNodeKindRegistry.md) | Node-kind registry (typically `runtime.nodeKinds`). A node whose `type` is not a built-in kind is emitted via its registered `toSvg` hook; without one it is skipped with an `UNKNOWN_KIND_SKIPPED` warning. | [serialize/svg.ts:1515](https://github.com/ancyloce/anvilkit-studio/blob/main/src/serialize/svg.ts#L1515) |
| &lt;a id="pretty"&gt;&lt;/a&gt; `pretty?` | `boolean` | Newline-indent the output for readable/golden snapshots (default `false`). | [serialize/svg.ts:1501](https://github.com/ancyloce/anvilkit-studio/blob/main/src/serialize/svg.ts#L1501) |
| &lt;a id="resolvebrandtoken"&gt;&lt;/a&gt; `resolveBrandToken?` | [`SvgResolveBrandToken`](../type-aliases/SvgResolveBrandToken) | Resolve `BrandTokenRef` fills/fonts to concrete values. See [SvgResolveBrandToken](../type-aliases/SvgResolveBrandToken). | [serialize/svg.ts:1532](https://github.com/ancyloce/anvilkit-studio/blob/main/src/serialize/svg.ts#L1532) |
| &lt;a id="resolveddocument"&gt;&lt;/a&gt; `resolvedDocument?` | [`CanvasResolvedDocument`](CanvasResolvedDocument.md) | A resolution of `ir` produced by `resolveCanvasLayout` (T-M3-02). When present, every node with a resolved record emits the RESOLVED `localTransform`/`bounds` instead of its stored geometry; style and content always come from the source node. When omitted (or when it does not cover the serialized page) and the page carries Auto Layout intent, the output uses stored geometry and warns `LAYOUT_UNRESOLVED`. For LAYOUT the serializer never resolves on its own, even though its layer rank would permit it — the caller supplies the one shared resolved tree, and stored geometry is a legitimate (warned) fallback. COMPONENTS are the one exception, because there is no fallback: an unexpanded instance has nothing to paint at all. When a page holds instances and this option does not already carry their expansion, the serializer resolves through the same composed resolver every other consumer uses — `resolveCanvasDocument`, restricted to the serialized page (plan 0023 M6-02). Named in prose rather than `{@link}`ed: the helper that does it is module-private, and a link would put an internal symbol into the API snapshot and raise an unresolved-reference warning. | [serialize/svg.ts:1554](https://github.com/ancyloce/anvilkit-studio/blob/main/src/serialize/svg.ts#L1554) |
| &lt;a id="richtextdefaults"&gt;&lt;/a&gt; `richTextDefaults?` | `Partial`\&lt;[`RichTextStyleDefaults`](RichTextStyleDefaults.md)\&gt; | Fallbacks for span style fields a document leaves unset. Merged over the built-in defaults, which mirror `createText`'s. | [serialize/svg.ts:1530](https://github.com/ancyloce/anvilkit-studio/blob/main/src/serialize/svg.ts#L1530) |
| &lt;a id="skipinvisible"&gt;&lt;/a&gt; `skipInvisible?` | `boolean` | Skip nodes with `visible === false` (default `true`). | [serialize/svg.ts:1499](https://github.com/ancyloce/anvilkit-studio/blob/main/src/serialize/svg.ts#L1499) |
| &lt;a id="textmeasurer"&gt;&lt;/a&gt; `textMeasurer?` | [`CanvasTextMeasurer`](../type-aliases/CanvasTextMeasurer) | Lays out `rich-text` paragraphs. Core cannot measure glyphs (no DOM, no font metrics — see [CanvasTextMeasurer](../type-aliases/CanvasTextMeasurer)), so wrapping is only possible when the host injects its measurer here. The editor's measurer is the same one it renders with, which is what makes an export match what the user saw. Omit it and rich text still exports deterministically — one line per paragraph, flagged with `RICH_TEXT_WRAP_APPROXIMATE`. | [serialize/svg.ts:1525](https://github.com/ancyloce/anvilkit-studio/blob/main/src/serialize/svg.ts#L1525) |
| &lt;a id="validate"&gt;&lt;/a&gt; `validate?` | `boolean` | Run [CanvasIRSchema](../variables/CanvasIRSchema) over `ir` before emitting and throw on failure (default `false`). The serializer trusts its input and `fmt` coerces any non-finite number to `"0"` to keep the output well-formed; enable this to fail fast on a malformed/un-validated IR (NaN/Infinity, wrong types) instead of silently emitting zeros. | [serialize/svg.ts:1509](https://github.com/ancyloce/anvilkit-studio/blob/main/src/serialize/svg.ts#L1509) |
