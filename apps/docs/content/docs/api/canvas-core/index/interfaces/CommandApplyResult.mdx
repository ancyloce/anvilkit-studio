---
title: "CommandApplyResult"
description: "@anvilkit/canvas-core · CommandApplyResult"
---

Defined in: [commands/types.ts:630](https://github.com/ancyloce/anvilkit-studio/blob/main/src/commands/types.ts#L630)

`Inverse` defaults to the built-in [CanvasCommand](../type-aliases/CanvasCommand) union, so every
existing built-in-only call site (`applyCommand`, `applyCommands`, the
Editor's history store) needs no type argument and no change. A command
extension whose natural inverse is itself a custom command type supplies
`Inverse` explicitly (see `CanvasCommandHandler`) instead of casting
`inverse` to the built-in union (P0-4).

## Type Parameters

| Type Parameter | Default type |
| ------ | ------ |
| `Inverse` *extends* \{ `type`: `string`; \} | [`CanvasCommand`](../type-aliases/CanvasCommand) |

## Properties

| Property | Type | Defined in |
| ------ | ------ | ------ |
| &lt;a id="inverse-1"&gt;&lt;/a&gt; `inverse` | `Inverse` | [commands/types.ts:634](https://github.com/ancyloce/anvilkit-studio/blob/main/src/commands/types.ts#L634) |
| &lt;a id="ir"&gt;&lt;/a&gt; `ir` | [`CanvasIR`](CanvasIR.md) | [commands/types.ts:633](https://github.com/ancyloce/anvilkit-studio/blob/main/src/commands/types.ts#L633) |
