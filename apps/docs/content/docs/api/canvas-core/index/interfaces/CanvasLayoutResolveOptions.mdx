---
title: "CanvasLayoutResolveOptions"
description: "@anvilkit/canvas-core · CanvasLayoutResolveOptions"
---

Defined in: [layout/types.ts:192](https://github.com/ancyloce/anvilkit-studio/blob/main/src/layout/types.ts#L192)

Resolver options.

PRD §9.3 writes `resolveCanvasLayout(ir, options)` with `options` required
while TD §5.4 makes every field optional. The parameter is therefore
required with all-optional fields, which satisfies both readings (plan §10.1).

## Extended by

- [`CanvasLayoutFlattenOptions`](CanvasLayoutFlattenOptions.md)
- [`CanvasComponentResolveOptions`](CanvasComponentResolveOptions.md)

## Properties

| Property | Modifier | Type | Description | Defined in |
| ------ | ------ | ------ | ------ | ------ |
| &lt;a id="measurement"&gt;&lt;/a&gt; `measurement?` | `readonly` | [`CanvasLayoutMeasurementProvider`](CanvasLayoutMeasurementProvider.md) | Omit for a document with no text/asset-driven Hug; missing inputs are diagnosed, not thrown. | [layout/types.ts:194](https://github.com/ancyloce/anvilkit-studio/blob/main/src/layout/types.ts#L194) |
| &lt;a id="pageids"&gt;&lt;/a&gt; `pageIds?` | `readonly` | readonly `string`[] | Restrict resolution to these pages; omit to resolve the whole document. | [layout/types.ts:198](https://github.com/ancyloce/anvilkit-studio/blob/main/src/layout/types.ts#L198) |
| &lt;a id="previous"&gt;&lt;/a&gt; `previous?` | `readonly` | [`CanvasResolvedDocument`](CanvasResolvedDocument.md) | A previous resolution of the same document, used as the warm-path cache seed. Omit for a cold resolve. Passing it is what makes untouched records reference-identical across resolutions; the resolver validates it against the current IR rather than trusting it, so a stale or foreign document degrades to a cold pass instead of producing wrong geometry. | [layout/types.ts:208](https://github.com/ancyloce/anvilkit-studio/blob/main/src/layout/types.ts#L208) |
| &lt;a id="richtextdefaults"&gt;&lt;/a&gt; `richTextDefaults?` | `readonly` | `Partial`\&lt;[`RichTextStyleDefaults`](RichTextStyleDefaults.md)\&gt; | Same fallback semantics as `SvgSerializeOptions.richTextDefaults`. | [layout/types.ts:196](https://github.com/ancyloce/anvilkit-studio/blob/main/src/layout/types.ts#L196) |
