---
title: "extractNodeStyle"
description: "@anvilkit/canvas-core · extractNodeStyle"
---

```ts
function extractNodeStyle(node): CanvasNodeStyle;
```

Defined in: [commands/apply-style.ts:110](https://github.com/ancyloce/anvilkit-studio/blob/main/src/commands/apply-style.ts#L110)

FR-120: extract the portable style of a node — only keys its kind owns per
the matrix, only values actually present. A frame's `background` extracts
as `fill`; a legacy `shadow` extracts as its `effects` equivalent.

## Parameters

| Parameter | Type |
| ------ | ------ |
| `node` | [`CanvasNode`](../type-aliases/CanvasNode) |

## Returns

[`CanvasNodeStyle`](../interfaces/CanvasNodeStyle)
