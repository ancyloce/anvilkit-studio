---
title: "index"
description: "@anvilkit/canvas-core · index"
---

## Classes

| Class | Description |
| ------ | ------ |
| [CanvasClipboardError](classes/CanvasClipboardError.md) | - |
| [CanvasCommandError](classes/CanvasCommandError.md) | - |
| [CanvasComponentGraphError](classes/CanvasComponentGraphError.md) | Thrown by [assertComponentGraph](functions/assertComponentGraph.md); carries EVERY issue found, not just the first error. |
| [CanvasExtensionError](classes/CanvasExtensionError.md) | Structured error thrown when an extension registration is rejected. |
| [CanvasIRDepthError](classes/CanvasIRDepthError.md) | - |
| [CanvasIRInvariantError](classes/CanvasIRInvariantError.md) | Thrown by [assertCanvasIRInvariants](functions/assertCanvasIRInvariants.md); carries every issue found, not just the first. |
| [CanvasIRMutationError](classes/CanvasIRMutationError.md) | - |
| [CanvasLayoutInvariantError](classes/CanvasLayoutInvariantError.md) | Thrown by [assertLayoutInvariants](functions/assertLayoutInvariants.md); carries every issue found, not just the first. |

## Interfaces

| Interface | Description |
| ------ | ------ |
| [AiApplyBrandRequest](interfaces/AiApplyBrandRequest.md) | Applies a brand kit via an AI-inferred choice of transforms. The AI only decides *which* of canvas-m2-006's `applyBrandColors`/`replaceFonts`/etc. pure transforms to run and in what combination — the actual document mutation always goes through those already-reversible, already-tested functions, never a bespoke AI-authored edit. |
| [AiDesignJobError](interfaces/AiDesignJobError.md) | - |
| [AiDesignProviderOptions](interfaces/AiDesignProviderOptions.md) | - |
| [AiDesignQuarantineError](interfaces/AiDesignQuarantineError.md) | A structured, user-facing reason an `AiDesignJobResult` was quarantined rather than applied. |
| [AiGenerateHeadlineRequest](interfaces/AiGenerateHeadlineRequest.md) | - |
| [AiGenerateLayoutVariantsRequest](interfaces/AiGenerateLayoutVariantsRequest.md) | Generates N alternative layouts from one source page — each candidate is a full `CanvasPage`, inspectable/editable like any other page. |
| [AiGenerateSocialPackRequest](interfaces/AiGenerateSocialPackRequest.md) | Bundles a campaign resize with prompt-driven content adaptation per variant (e.g. shortening copy to fit a square crop). |
| [AiImageBackgroundReplaceRequest](interfaces/AiImageBackgroundReplaceRequest.md) | Replaces (not merely removes, unlike [AiImageBgRemoveRequest](interfaces/AiImageBgRemoveRequest.md)) the background with prompt-generated content. |
| [AiImageBgRemoveRequest](interfaces/AiImageBgRemoveRequest.md) | - |
| [AiImageGenerativeExpandRequest](interfaces/AiImageGenerativeExpandRequest.md) | Outpainting: extends the source image to a larger canvas (uncrop). |
| [AiImageGenerativeFillRequest](interfaces/AiImageGenerativeFillRequest.md) | Creative, prompt-driven fill of a masked region (Firefly-"Generative Fill" style) — distinct from [AiImageInpaintRequest](interfaces/AiImageInpaintRequest.md), which fills a masked region to match its surroundings rather than introduce new prompted content. Same field shape; the `kind` lets a provider route the two differently even where its backing model treats them alike. |
| [AiImageInpaintRequest](interfaces/AiImageInpaintRequest.md) | - |
| [AiImageJobError](interfaces/AiImageJobError.md) | - |
| [AiImageObjectEraseRequest](interfaces/AiImageObjectEraseRequest.md) | Removes the masked object and fills the gap to match its surroundings — no prompt, unlike [AiImageGenerativeFillRequest](interfaces/AiImageGenerativeFillRequest.md). |
| [AiImageProviderOptions](interfaces/AiImageProviderOptions.md) | - |
| [AiImageTextToImageRequest](interfaces/AiImageTextToImageRequest.md) | - |
| [AiImageUpscaleRequest](interfaces/AiImageUpscaleRequest.md) | - |
| [AiImageVariationRequest](interfaces/AiImageVariationRequest.md) | - |
| [AiLayerBounds](interfaces/AiLayerBounds.md) | - |
| [AiLayerContext](interfaces/AiLayerContext.md) | - |
| [AiPromptToDesignRequest](interfaces/AiPromptToDesignRequest.md) | Design-level AI job contracts (FR-050, canvas-m4-001) — a separate union from `AiImageJobRequest` (`ai-contracts.ts`) because these ops are not image-in/image-out: they read/produce whole pages, node content, or a `BrandKitDefinition`/`CanvasSizePreset` reference, none of which fit that union's shape. Ranked at 5 (alongside `serialize/`), not 2 like `ai-contracts.ts` — it needs `templates/`/`brand/`/`commands/` (rank 3-4) types, so it must outrank all three. |
| [AiPromptToTemplateRequest](interfaces/AiPromptToTemplateRequest.md) | Adapts an existing template to a prompt (e.g. "make me a fitness landing page") rather than generating from a blank canvas. |
| [AiProviderCapabilities](interfaces/AiProviderCapabilities.md) | Which ops a given provider supports (FR-051) — sibling metadata a host supplies alongside its `AiImageProvider`/`AiDesignProvider` functions, since a bare function has no introspectable capabilities of its own. The editor (canvas-m4-004) reads this to conditionally show/hide AI actions rather than surfacing an action that will always fail for a given provider. Omitting a list (vs. an empty array) means "unknown — assume everything is supported", so pre-M4 hosts that don't yet supply capabilities keep today's behavior. |
| [AiResizeCampaignRequest](interfaces/AiResizeCampaignRequest.md) | AI-initiated campaign resize — the design-level counterpart to canvas-m3-007's `resizeToVariants`, which a provider/adapter calls to fulfill this request. |
| [AiRewriteCopyRequest](interfaces/AiRewriteCopyRequest.md) | Rewrites one text/rich-text node's content in place. |
| [ApplyCommandsOptions](interfaces/ApplyCommandsOptions.md) | - |
| [BoundsExtent](interfaces/BoundsExtent.md) | - |
| [BrandApplyOptions](interfaces/BrandApplyOptions.md) | - |
| [BrandApplyReport](interfaces/BrandApplyReport.md) | What one `apply*` transform did to a document (FR-032). |
| [BrandApplyResult](interfaces/BrandApplyResult.md) | - |
| [BrandAsset](interfaces/BrandAsset.md) | A brand logo or reference image asset. |
| [BrandColorToken](interfaces/BrandColorToken.md) | A named brand color. Structurally identical to `@anvilkit/canvas-editor`'s pre-existing `BrandColor` (`{id?, name, value}`) by design, so the editor's mapping from this canonical shape is a straight pass-through, not a reshape. |
| [BrandComplianceIssue](interfaces/BrandComplianceIssue.md) | One compliance finding. |
| [BrandComplianceReport](interfaces/BrandComplianceReport.md) | - |
| [BrandFontToken](interfaces/BrandFontToken.md) | A named brand font family. |
| [BrandImageStylePreset](interfaces/BrandImageStylePreset.md) | A named image treatment (filter stack) brand-approved for photo/image content. |
| [BrandKitDefinition](interfaces/BrandKitDefinition.md) | The canonical Brand Kit contract (PRD FR-031, §12.6). |
| [BrandRule](interfaces/BrandRule.md) | - |
| [BrandTokenRef](interfaces/BrandTokenRef.md) | A reference to a value owned by an external brand kit (PRD §12.4) — a color, font, spacing value, asset, or logo — identified by `id` alone. Resolution is deliberately EXTERNAL to core: this is just the SHAPE of a reference, never a lookup table or a brand-kit type. A consumer (the SVG serializer's `resolveBrandToken` option, or a future editor equivalent) turns it into a concrete value; core never resolves one itself. |
| [BrandToneMetadata](interfaces/BrandToneMetadata.md) | Free-form brand voice/tone guidance, for AI copy generation (M4) to consume. |
| [BrandTypographyPreset](interfaces/BrandTypographyPreset.md) | A named typography preset (heading, body, caption, …) built from brand fonts. |
| [BuildDetachAllAndDeleteOptions](interfaces/BuildDetachAllAndDeleteOptions.md) | - |
| [BuildDetachCommandOptions](interfaces/BuildDetachCommandOptions.md) | - |
| [CampaignExportJobRequestOptions](interfaces/CampaignExportJobRequestOptions.md) | - |
| [CanvasAiPlaceholderNode](interfaces/CanvasAiPlaceholderNode.md) | - |
| [CanvasAiSourceMeta](interfaces/CanvasAiSourceMeta.md) | - |
| [CanvasAnimationBase](interfaces/CanvasAnimationBase.md) | Serializable animation metadata (FR-080, canvas-m6-001). Describes intent only — timing/kind data for a future editor preview or video/GIF export worker to interpret. Static exports (SVG/PNG/PDF) always render a node/page in its normal resting state (its own `transform`/`opacity`/etc., unaffected by this field) and merely surface a warning that motion exists but isn't represented — this metadata is never a second, divergent static appearance. |
| [CanvasAppliedOverrides](interfaces/CanvasAppliedOverrides.md) | - |
| [CanvasAssetPutCommand](interfaces/CanvasAssetPutCommand.md) | Upsert an entry in the document's asset table, keyed by `asset.id` (A-05/FR-021 paste, later FR-091 upload). Inverse restores the previous entry, or removes the key when it was new — so a paste batch of `asset.put` + `node.create` commands undoes cleanly in one step. |
| [CanvasAssetRef](interfaces/CanvasAssetRef.md) | - |
| [CanvasAssetRemoveCommand](interfaces/CanvasAssetRemoveCommand.md) | Remove an asset-table entry. The command layer does NOT check for nodes still referencing the asset (invariants are a trust-boundary tool, not auto-wired — see `ir/invariants.ts`); callers own that check. |
| [CanvasAudioNode](interfaces/CanvasAudioNode.md) | Fields shared by the video/audio asset-reference nodes (FR-081, canvas-m6-002). Deliberately holds ONLY an `assetId` — no inline media bytes ever enter Canvas IR, matching the `image`/`svg` asset-reference convention. No playback/rendering is implemented in canvas-core: this is a contract an export/render worker consumes later. |
| [CanvasAutoLayout](interfaces/CanvasAutoLayout.md) | Persisted Auto Layout intent on a frame (`layout.auto.v1`). |
| [CanvasBatchCommand](interfaces/CanvasBatchCommand.md) | A composite, reversible command: applies its `commands` in order as a single undoable unit. Its inverse (produced by `applyCommand`) is another `batch` whose sub-commands are the reversed inverses, so history replays it like any other command with no special-casing. Nestable. |
| [CanvasBlurEffect](interfaces/CanvasBlurEffect.md) | Gaussian blur of the node itself (C-03, §9.4). |
| [CanvasBounds](interfaces/CanvasBounds.md) | - |
| [CanvasBrandComponentPolicy](interfaces/CanvasBrandComponentPolicy.md) | What a component permits inside instances of itself. |
| [CanvasBrandTokenConstraint](interfaces/CanvasBrandTokenConstraint.md) | Which token values a property may take. |
| [CanvasChangeEmitter](interfaces/CanvasChangeEmitter.md) | A subscriber for change batches. Returns an unsubscribe function. |
| [CanvasChangeRecord](interfaces/CanvasChangeRecord.md) | An enriched, persistable, replayable record of one applied command (FR-070). Additive alongside [CanvasChange](type-aliases/CanvasChange.md): `change` carries the existing best-effort content diff, `command` carries the original command so a sequence of records can be replayed deterministically via [replayChanges](functions/replayChanges.md). |
| [CanvasClipboardBounds](interfaces/CanvasClipboardBounds.md) | - |
| [CanvasClipboardPayload](interfaces/CanvasClipboardPayload.md) | - |
| [CanvasCommandHandler](interfaces/CanvasCommandHandler.md) | A command extension. `apply` must be reversible like the built-in `applyCommand` — returning `{ ir, inverse }` so history undo/redo works. |
| [CanvasCommandLocationOptions](interfaces/CanvasCommandLocationOptions.md) | Targets a node command at one tree: a page's (the default when absent — every pre-component call site keeps its exact semantics) or a Component Source's (plan 0023 M3-02, LC-CMD/LC-CREATE-002). A command's inverse carries the same `location`, so undo edits the same tree. One successful command (or one `batch`/transaction) against a Source increments that definition's `revision` exactly once — see `applyCommand`. |
| [CanvasCommandRegistry](interfaces/CanvasCommandRegistry.md) | - |
| [CanvasCommentAnchorResolution](interfaces/CanvasCommentAnchorResolution.md) | - |
| [CanvasCommentCoordinateAnchor](interfaces/CanvasCommentCoordinateAnchor.md) | - |
| [CanvasCommentNodeAnchor](interfaces/CanvasCommentNodeAnchor.md) | - |
| [CanvasCommentPageAnchor](interfaces/CanvasCommentPageAnchor.md) | Headless comment anchor contract (FR-072). |
| [CanvasCommentSelectionAnchor](interfaces/CanvasCommentSelectionAnchor.md) | Anchors a comment to a set of nodes rather than a fixed rectangle, so the anchor survives every node in the set moving — the region is always whatever the current bounds of `nodeIds` are, recomputed by the consumer, never a stale snapshot core would need to keep in sync. |
| [CanvasComponentAddPropertyCommand](interfaces/CanvasComponentAddPropertyCommand.md) | Expose a component property (M3-06, LC-CREATE-003). Rejected on write when the Property ID already exists on this definition, the target node is not in the Source tree, or the target kind is incompatible (§10.1) — the same table `validateComponentGraph` applies on read. |
| [CanvasComponentColorProperty](interfaces/CanvasComponentColorProperty.md) | Binds one declared fill-bearing field on the target node. |
| [CanvasComponentDefinition](interfaces/CanvasComponentDefinition.md) | One document-local Component Source (LC-MODEL-001). |
| [CanvasComponentDeleteCommand](interfaces/CanvasComponentDeleteCommand.md) | Delete a definition with ZERO references (LC-DELETE). Any remaining page instance or dependent definition rejects the command — "detach all and delete" is a `component-ops` orchestration that builds one atomic batch. Deleting the last definition drops the `components` key entirely; the inverse (`component.create` restore) brings the key back (INV-10). |
| [CanvasComponentDuplicateCommand](interfaces/CanvasComponentDuplicateCommand.md) | Copy a definition under a new id. Source node ids are remapped via `regenerateNodeIds` (INV-2: unique across Pages AND definitions) and property bindings follow the same id map; Property IDs are kept — cross-definition reuse is explicitly permitted (TD §5.5). The copy starts at `revision: 1` with no dependents. |
| [CanvasComponentExpansionOptions](interfaces/CanvasComponentExpansionOptions.md) | - |
| [CanvasComponentGraph](interfaces/CanvasComponentGraph.md) | - |
| [CanvasComponentIdFactories](interfaces/CanvasComponentIdFactories.md) | - |
| [CanvasComponentImageProperty](interfaces/CanvasComponentImageProperty.md) | Binds an image-bearing node — an image node, or a frame with an image placeholder (the `TemplateImageSlot` precedent). |
| [CanvasComponentInstanceDetachCommand](interfaces/CanvasComponentInstanceDetachCommand.md) | Materialize an instance into plain nodes at the exact same tree position (M3-07, LC-INSTANCE-005). Resolution runs fully first — overrides applied, nested instances expanded recursively (never "outer only") — and fails atomically when it cannot (missing Source, cycle/depth/budget degradation). The materialized root keeps the instance's persistent id, placement, and `layoutItem` (a Flow child keeps its slot); descendants take ids from `nodeIds` (keyed by the resolver's deterministic virtual ids) or a fresh factory. Component metadata, virtual ids, and override maps are gone from the result (INV-12: world-space appearance is preserved exactly). Build the payload with `component-ops` `buildDetachCommand` to get the complete id map back. |
| [CanvasComponentInstanceInsertCommand](interfaces/CanvasComponentInstanceInsertCommand.md) | Insert a new instance node referencing `componentId` (LC-INSTANCE-001). A dedicated command (not bare `node.create`) so the Source reference is validated at the boundary — a typo never silently creates a broken reference. `pageId` is required without `location`, like `node.create`. |
| [CanvasComponentInstanceNode](interfaces/CanvasComponentInstanceNode.md) | A live reference to a Component Source in `ir.components` (plan 0023, PRD 0015 LC-MODEL-004). |
| [CanvasComponentInstanceResetAllOverridesCommand](interfaces/CanvasComponentInstanceResetAllOverridesCommand.md) | Remove every override on the instance. The inverse restores the exact prior map. |
| [CanvasComponentInstanceResetOverrideCommand](interfaces/CanvasComponentInstanceResetOverrideCommand.md) | Remove one override. Resetting an absent key is a validated no-op. |
| [CanvasComponentInstanceSetOverrideCommand](interfaces/CanvasComponentInstanceSetOverrideCommand.md) | Set one typed override, keyed by Property ID (LC-INSTANCE-003, INV-6). Writes the entry verbatim — target validation happens at resolve time, so an entry whose property has since changed is retained as an orphan rather than rejected here (§10.3). The inverse restores the prior entry, or resets the key when there was none. |
| [CanvasComponentIssue](interfaces/CanvasComponentIssue.md) | One component diagnostic (TD §19). `severity: "warning"` never throws anywhere — `assertComponentGraph` is the strict gate for `"error"` issues only, mirroring the shipped invariant trio. |
| [CanvasComponentPageInstanceRef](interfaces/CanvasComponentPageInstanceRef.md) | One page-tree instance referencing a component. |
| [CanvasComponentPropertyBase](interfaces/CanvasComponentPropertyBase.md) | - |
| [CanvasComponentReferenceIndex](interfaces/CanvasComponentReferenceIndex.md) | Where every component is referenced FROM (plan 0023 M3-08, LC-DELETE): page instances and Source-tree instances, keyed by the referenced component id. The read `component.delete` guards on and "detach all and delete" plans over. Entry order follows `walkDocument` — pages in document order, then definitions in sorted id order — so messages and plans built from it are byte-stable. |
| [CanvasComponentRemovePropertyCommand](interfaces/CanvasComponentRemovePropertyCommand.md) | Remove a property. Existing instance overrides for the id are NOT touched — they become orphans, retained verbatim, and re-apply if the same Property ID is later restored compatibly (§10.3). The Editor owns the "overrides exist — confirm?" prompt; the command itself is unconditional. |
| [CanvasComponentRenameCommand](interfaces/CanvasComponentRenameCommand.md) | - |
| [CanvasComponentResolutionCache](interfaces/CanvasComponentResolutionCache.md) | - |
| [CanvasComponentResolveOptions](interfaces/CanvasComponentResolveOptions.md) | Resolver options. |
| [CanvasComponentSourceInstanceRef](interfaces/CanvasComponentSourceInstanceRef.md) | One Source-tree instance referencing a component, inside `componentId`'s definition. |
| [CanvasComponentTextProperty](interfaces/CanvasComponentTextProperty.md) | Binds a text-bearing node's content. |
| [CanvasComponentUpdatePropertyCommand](interfaces/CanvasComponentUpdatePropertyCommand.md) | Replace one property wholesale (rename, retarget, re-kind). The Property ID is STABLE (INV-6): `to.id` must equal `propertyId` — renaming a property never changes its identity, so instance overrides keep applying. |
| [CanvasComponentVariantAxis](interfaces/CanvasComponentVariantAxis.md) | One axis of variation, e.g. `size` with values `sm`/`md`/`lg`. |
| [CanvasComponentVariantDefinition](interfaces/CanvasComponentVariantDefinition.md) | One stored variant — a selection plus the property mapping it implies. |
| [CanvasComponentVariantIssue](interfaces/CanvasComponentVariantIssue.md) | - |
| [CanvasComponentVariantResolution](interfaces/CanvasComponentVariantResolution.md) | - |
| [CanvasComponentVariantSet](interfaces/CanvasComponentVariantSet.md) | - |
| [CanvasComponentVariantValue](interfaces/CanvasComponentVariantValue.md) | - |
| [CanvasComponentVisibilityProperty](interfaces/CanvasComponentVisibilityProperty.md) | Binds the target node's `visible` flag. |
| [CanvasCornerRadii](interfaces/CanvasCornerRadii.md) | FR-076 per-corner radii (B-03b) for rect/frame. When present it takes precedence over the shared `radius`. Values clamp to half the box size at render time. |
| [CanvasDetachAllAndDeletePlan](interfaces/CanvasDetachAllAndDeletePlan.md) | - |
| [CanvasDetachPlan](interfaces/CanvasDetachPlan.md) | - |
| [CanvasDocumentCompatibility](interfaces/CanvasDocumentCompatibility.md) | What a reader needs in order to open this document correctly. |
| [CanvasDocumentLocation](interfaces/CanvasDocumentLocation.md) | Where a visited node lives: a page tree, or a Component Source tree. |
| [CanvasDropShadowEffect](interfaces/CanvasDropShadowEffect.md) | Drop shadow as an entry in the extensible effect list (C-03, PRD 0012 §9.4/FR-077). Supersedes the legacy per-node `shadow` field — see [resolveNodeEffects](functions/resolveNodeEffects.md) for the documented precedence — and adds `spread` over [CanvasShadow](interfaces/CanvasShadow.md). |
| [CanvasEllipseNode](interfaces/CanvasEllipseNode.md) | - |
| [CanvasExportFidelity](interfaces/CanvasExportFidelity.md) | - |
| [CanvasExportJobArtifact](interfaces/CanvasExportJobArtifact.md) | One produced file. `data` stays `string | Uint8Array` — no base64 round-tripping (matches `@anvilkit/contracts`' `ExportResult` convention). |
| [CanvasExportJobOptions](interfaces/CanvasExportJobOptions.md) | Format-specific knobs for one export job. Deliberately narrower than the plugin's `CanvasExportOptions`: `canvasIR`/`page` move to [CanvasExportJobRequest.source](interfaces/CanvasExportJobRequest.md#source)/`.pages` here, so they aren't duplicated. `rasters` is omitted too — a raster-embedding job contract is FR-043 (canvas-m3-004)'s concern, not this one's. |
| [CanvasExportJobRequest](interfaces/CanvasExportJobRequest.md) | - |
| [CanvasExportJobResponse](interfaces/CanvasExportJobResponse.md) | - |
| [CanvasExportJobVariant](interfaces/CanvasExportJobVariant.md) | One target variant within a batch/campaign export (wiring point for canvas-m3-006's `CanvasSizePreset` and canvas-m3-007's campaign resize — both land after this contract, additively). |
| [CanvasExportWarning](interfaces/CanvasExportWarning.md) | A unified, format-agnostic export warning (FR-041, canvas-m3-002). `code` is a plain string rather than a closed union: a single job may touch SVG-specific (`SvgWarningCode`) or PDF-specific (`PdfWarningCode`) codes depending on `format`, and this shape must not force every new per-format code to widen a shared enum. Adapters that serialize via `serializePageToSvg`/`serializeDocumentToPdf` map those functions' own typed warnings into this shape 1:1 (`code` carries the same string value). |
| [CanvasExtension](interfaces/CanvasExtension.md) | A bundle of domain extensions registered against a [CanvasRuntime](interfaces/CanvasRuntime.md). |
| [CanvasExternalComponentRef](interfaces/CanvasExternalComponentRef.md) | An **exact, immutable** reference to one version of one external component. |
| [CanvasExternalComponentSnapshot](interfaces/CanvasExternalComponentSnapshot.md) | One immutable, integrity-verified copy of an external component version, stored in the document (plan 0021 T-014, TD 0016 §5.3). |
| [CanvasExternalSnapshotIndex](interfaces/CanvasExternalSnapshotIndex.md) | O(1) lookup of an admitted snapshot by exact reference. |
| [CanvasFadeAnimation](interfaces/CanvasFadeAnimation.md) | Serializable animation metadata (FR-080, canvas-m6-001). Describes intent only — timing/kind data for a future editor preview or video/GIF export worker to interpret. Static exports (SVG/PNG/PDF) always render a node/page in its normal resting state (its own `transform`/`opacity`/etc., unaffected by this field) and merely surface a warning that motion exists but isn't represented — this metadata is never a second, divergent static appearance. |
| [CanvasForeignComponentRef](interfaces/CanvasForeignComponentRef.md) | One pasted-instance-to-be whose Source is absent from the target registry. |
| [CanvasFrameNode](interfaces/CanvasFrameNode.md) | A layout container. Unlike a group — which is a pure grouping of siblings and derives its bounds from them — a frame has its own bounds, can clip its children to them, and can paint a background. It is the unit a template stamps content into. |
| [CanvasFrameRemoveLayoutCommand](interfaces/CanvasFrameRemoveLayoutCommand.md) | Remove a frame's Auto Layout intent, baking the caller-computed resolved geometry into its descendants so the document keeps its appearance. |
| [CanvasFrameSetLayoutCommand](interfaces/CanvasFrameSetLayoutCommand.md) | Set or replace a frame's Auto Layout intent, optionally writing resolved geometry for its descendants in the same atomic step. |
| [CanvasGradientFill](interfaces/CanvasGradientFill.md) | A linear or radial gradient fill. `from`/`to` are normalized (0..1) points in the node's local box, so the gradient scales with the node. |
| [CanvasGradientStop](interfaces/CanvasGradientStop.md) | - |
| [CanvasGroupNode](interfaces/CanvasGroupNode.md) | - |
| [CanvasImageAdjustments](interfaces/CanvasImageAdjustments.md) | Non-destructive image adjustments (C-04, FR-100). All fields optional; absent = neutral. Ranges: `brightness`/`contrast`/`saturation`/`exposure`/ `temperature`/`tint` -1..1, `grayscale`/`sepia` 0..1, `blur` 0..100 (page units). Rendering math lives in `ir/image-adjustments.ts` — one color matrix shared by the editor canvas and the SVG serializer. |
| [CanvasImageCrop](interfaces/CanvasImageCrop.md) | - |
| [CanvasImageNode](interfaces/CanvasImageNode.md) | - |
| [CanvasImageReplaceCommand](interfaces/CanvasImageReplaceCommand.md) | Targets a node command at one tree: a page's (the default when absent — every pre-component call site keeps its exact semantics) or a Component Source's (plan 0023 M3-02, LC-CMD/LC-CREATE-002). A command's inverse carries the same `location`, so undo edits the same tree. One successful command (or one `batch`/transaction) against a Source increments that definition's `revision` exactly once — see `applyCommand`. |
| [CanvasInsets](interfaces/CanvasInsets.md) | Per-edge inset distances. The single shared insets shape (PRD 0012 §9.3): there is deliberately no second inset type in this package. |
| [CanvasInvariantIssue](interfaces/CanvasInvariantIssue.md) | - |
| [CanvasIR](interfaces/CanvasIR.md) | - |
| [CanvasIRMetadata](interfaces/CanvasIRMetadata.md) | - |
| [CanvasLayoutFlattenOptions](interfaces/CanvasLayoutFlattenOptions.md) | Resolver options. |
| [CanvasLayoutGeometryWrite](interfaces/CanvasLayoutGeometryWrite.md) | One node's caller-computed geometry, written atomically as part of a layout command. |
| [CanvasLayoutIssue](interfaces/CanvasLayoutIssue.md) | - |
| [CanvasLayoutItem](interfaces/CanvasLayoutItem.md) | Per-node participation in an ancestor frame's Auto Layout (`layout.auto.v1`). Every field is optional and every omission is the default (`flow` / `fixed`), so a node with no layout relationship carries no `layoutItem` at all. |
| [CanvasLayoutMaterialization](interfaces/CanvasLayoutMaterialization.md) | Freshness stamp for the resolved geometry written back into the document. |
| [CanvasLayoutMaterializeOptions](interfaces/CanvasLayoutMaterializeOptions.md) | - |
| [CanvasLayoutMeasurementProvider](interfaces/CanvasLayoutMeasurementProvider.md) | Host-supplied measurement (TD §8.1). |
| [CanvasLayoutResolveOptions](interfaces/CanvasLayoutResolveOptions.md) | Resolver options. |
| [CanvasLineNode](interfaces/CanvasLineNode.md) | - |
| [CanvasLocalComponentSourceRef](interfaces/CanvasLocalComponentSourceRef.md) | A Source defined inside this document's own `ir.components` registry. |
| [CanvasMediaNodeBase](interfaces/CanvasMediaNodeBase.md) | Fields shared by the video/audio asset-reference nodes (FR-081, canvas-m6-002). Deliberately holds ONLY an `assetId` — no inline media bytes ever enter Canvas IR, matching the `image`/`svg` asset-reference convention. No playback/rendering is implemented in canvas-core: this is a contract an export/render worker consumes later. |
| [CanvasMediaTrim](interfaces/CanvasMediaTrim.md) | A playback trim window, in seconds into the source media (FR-081, canvas-m6-002). |
| [CanvasMigration](interfaces/CanvasMigration.md) | One step in the IR version chain, e.g. "1" → "2". |
| [CanvasMigrationRegistry](interfaces/CanvasMigrationRegistry.md) | - |
| [CanvasMotionPathAnimation](interfaces/CanvasMotionPathAnimation.md) | Serializable animation metadata (FR-080, canvas-m6-001). Describes intent only — timing/kind data for a future editor preview or video/GIF export worker to interpret. Static exports (SVG/PNG/PDF) always render a node/page in its normal resting state (its own `transform`/`opacity`/etc., unaffected by this field) and merely surface a warning that motion exists but isn't represented — this metadata is never a second, divergent static appearance. |
| [CanvasNodeApplyStyleCommand](interfaces/CanvasNodeApplyStyleCommand.md) | Paste style onto one node (C-05, FR-121): the payload is intersected with the target kind's compatible keys via `computeStylePatch` — incompatible keys are ignored (callers report them), never a failure. The inverse is a `node.update` restoring the applied keys' prior values exactly. Multi-node paste is a `batch` of these (one per target — one undo entry). |
| [CanvasNodeBase](interfaces/CanvasNodeBase.md) | - |
| [CanvasNodeCreateCommand](interfaces/CanvasNodeCreateCommand.md) | Targets a node command at one tree: a page's (the default when absent — every pre-component call site keeps its exact semantics) or a Component Source's (plan 0023 M3-02, LC-CMD/LC-CREATE-002). A command's inverse carries the same `location`, so undo edits the same tree. One successful command (or one `batch`/transaction) against a Source increments that definition's `revision` exactly once — see `applyCommand`. |
| [CanvasNodeDeleteCommand](interfaces/CanvasNodeDeleteCommand.md) | Targets a node command at one tree: a page's (the default when absent — every pre-component call site keeps its exact semantics) or a Component Source's (plan 0023 M3-02, LC-CMD/LC-CREATE-002). A command's inverse carries the same `location`, so undo edits the same tree. One successful command (or one `batch`/transaction) against a Source increments that definition's `revision` exactly once — see `applyCommand`. |
| [CanvasNodeGroupCommand](interfaces/CanvasNodeGroupCommand.md) | Wrap one or more sibling nodes in a new group node. All `childIds` must share the same immediate parent group on `pageId`; they are wrapped in their current sibling z-order and the new group takes the slot of the topmost selected node. No child transforms are altered (the group is created with an identity transform), so grouping is visually a no-op and exactly reversible. |
| [CanvasNodeKindDefinition](interfaces/CanvasNodeKindDefinition.md) | A node kind contributed at runtime. `N` is the concrete node type. |
| [CanvasNodeKindRegistry](interfaces/CanvasNodeKindRegistry.md) | - |
| [CanvasNodeMeta](interfaces/CanvasNodeMeta.md) | - |
| [CanvasNodeMoveCommand](interfaces/CanvasNodeMoveCommand.md) | Targets a node command at one tree: a page's (the default when absent — every pre-component call site keeps its exact semantics) or a Component Source's (plan 0023 M3-02, LC-CMD/LC-CREATE-002). A command's inverse carries the same `location`, so undo edits the same tree. One successful command (or one `batch`/transaction) against a Source increments that definition's `revision` exactly once — see `applyCommand`. |
| [CanvasNodeReorderCommand](interfaces/CanvasNodeReorderCommand.md) | Move a node to a new index among its siblings (same parent). `toIndex` is clamped to the sibling range; the inverse restores the prior index. |
| [CanvasNodeReparentCommand](interfaces/CanvasNodeReparentCommand.md) | Move a node into a different container (group/frame/page-root group) at `toIndex` (A-01, PRD 0012 FR-052). Same-page only; `toIndex` is clamped to the target's child range; moving a node into itself or a descendant is rejected (`invariant-violated`); page roots cannot be reparented. The inverse reparents back to the original parent at the original index, so layer-panel drag-and-drop is fully undoable. |
| [CanvasNodeResizeCommand](interfaces/CanvasNodeResizeCommand.md) | Targets a node command at one tree: a page's (the default when absent — every pre-component call site keeps its exact semantics) or a Component Source's (plan 0023 M3-02, LC-CMD/LC-CREATE-002). A command's inverse carries the same `location`, so undo edits the same tree. One successful command (or one `batch`/transaction) against a Source increments that definition's `revision` exactly once — see `applyCommand`. |
| [CanvasNodeRotateCommand](interfaces/CanvasNodeRotateCommand.md) | Targets a node command at one tree: a page's (the default when absent — every pre-component call site keeps its exact semantics) or a Component Source's (plan 0023 M3-02, LC-CMD/LC-CREATE-002). A command's inverse carries the same `location`, so undo edits the same tree. One successful command (or one `batch`/transaction) against a Source increments that definition's `revision` exactly once — see `applyCommand`. |
| [CanvasNodeStyle](interfaces/CanvasNodeStyle.md) | Copy/paste style (C-05, FR-120/121). A `CanvasNodeStyle` is the portable style payload: appearance only — never id, position, size, rotation, children, asset ids, or text content. Shadows travel exclusively as `effects` (extraction lifts a legacy `shadow` through core's ONE resolver), so pasting always writes the C-03 model. |
| [CanvasNodeUngroupCommand](interfaces/CanvasNodeUngroupCommand.md) | Dissolve a group, lifting its children into the group's parent. By default the children spill out contiguously at the group's former slot. When `restore` is present (as produced by the inverse of `node.group`), each child is instead placed back at its recorded original index so the prior tree is reconstructed exactly even for non-contiguous selections. |
| [CanvasNodeUpdateCommand](interfaces/CanvasNodeUpdateCommand.md) | Targets a node command at one tree: a page's (the default when absent — every pre-component call site keeps its exact semantics) or a Component Source's (plan 0023 M3-02, LC-CMD/LC-CREATE-002). A command's inverse carries the same `location`, so undo edits the same tree. One successful command (or one `batch`/transaction) against a Source increments that definition's `revision` exactly once — see `applyCommand`. |
| [CanvasPage](interfaces/CanvasPage.md) | - |
| [CanvasPageBackground](interfaces/CanvasPageBackground.md) | Page background. `solid` (a CSS color string in `value`) is the only kind with first-class rendering across the live stage, thumbnails, and the SVG serializer. `image` and `gradient` are RESERVED: their `value` format is not yet defined, the SVG serializer emits a typed `BACKGROUND_UNSUPPORTED` warning for them, and the editor renders the neutral default page white rather than interpreting the raw string (FR-063 explicit contract narrowing — see PRD 0012 §7.7). Hosts should not persist non-solid backgrounds until a format is specified. |
| [CanvasPageCreateCommand](interfaces/CanvasPageCreateCommand.md) | - |
| [CanvasPageDeleteCommand](interfaces/CanvasPageDeleteCommand.md) | - |
| [CanvasPageDuplicateCommand](interfaces/CanvasPageDuplicateCommand.md) | Duplicate a page (§9.1/§23, PRD 0012): deep-clones `sourcePageId`'s node tree with fresh ids via `regenerateNodeIds` (M0-05), preserves the source page's `size`/`background`/`layoutAids`/other page-level fields, and inserts the duplicate immediately after the source. This is real command domain logic (ID regeneration + positioning), not something a consumer should assemble client-side from `clonePage` + `page.create`. |
| [CanvasPageGuides](interfaces/CanvasPageGuides.md) | Persistent ruler guides for one page (FR-111): axis-aligned positions in page coordinates (`size.unit`). `horizontal` guides are y-positions, `vertical` guides are x-positions. |
| [CanvasPageLayoutAids](interfaces/CanvasPageLayoutAids.md) | Page-level layout aids (PRD 0012 §9.3 / FR-113): persistent guides plus margin/bleed/safe-area insets. Purely advisory editor chrome — serializers never render these unless a caller explicitly opts in. Absent field = that aid is unset; absent object = no aids at all (so pre-existing documents need no migration). |
| [CanvasPageRenameCommand](interfaces/CanvasPageRenameCommand.md) | - |
| [CanvasPageReorderCommand](interfaces/CanvasPageReorderCommand.md) | - |
| [CanvasPageResizeCommand](interfaces/CanvasPageResizeCommand.md) | Resize a page (B-01). `mode` decides what happens to the page's top-level content: `canvas-only` leaves it untouched, `scale-content` scales each top-level child's transform uniformly by min(w-ratio, h-ratio), `recenter` keeps content size and shifts it by half the size delta. The root group's bounds stay synced to the page size. The inverse restores the prior size AND the exact prior child transforms (a composite batch for `scale-content`, where a reciprocal scale would drift in floating point). |
| [CanvasPageSetBackgroundCommand](interfaces/CanvasPageSetBackgroundCommand.md) | Set a page's background fill (B-11, FR-063/§9.1). The inverse restores the ACTUAL prior background, even when `from` is stale. |
| [CanvasPageSetLayoutAidsCommand](interfaces/CanvasPageSetLayoutAidsCommand.md) | Set (or clear, with `to: undefined`) a page's layout aids — persistent guides, margin, bleed, safe area (C-01, PRD 0012 §9.3/FR-111/FR-113). Whole-object replace: guide add/move/delete are expressed by writing the full next `layoutAids` value, keeping the inverse trivial and exact. The inverse restores the ACTUAL prior value, even when `from` is stale. |
| [CanvasPageSize](interfaces/CanvasPageSize.md) | - |
| [CanvasPageVariantSource](interfaces/CanvasPageVariantSource.md) | Traceback metadata stamped onto a page generated by [resizeToVariants](functions/resizeToVariants.md) (FR-061, canvas-m3-007) — absent on every other page. `presetId`/ `presetVersion` pin the exact catalog entry (`CANVAS_SIZE_PRESETS`) the page was sized from, since a preset's dimensions can change under a later `version` while this page keeps whatever size it had at generation time. |
| [CanvasPathNode](interfaces/CanvasPathNode.md) | - |
| [CanvasPoint](interfaces/CanvasPoint.md) | - |
| [CanvasPolygonNode](interfaces/CanvasPolygonNode.md) | - |
| [CanvasPopAnimation](interfaces/CanvasPopAnimation.md) | Serializable animation metadata (FR-080, canvas-m6-001). Describes intent only — timing/kind data for a future editor preview or video/GIF export worker to interpret. Static exports (SVG/PNG/PDF) always render a node/page in its normal resting state (its own `transform`/`opacity`/etc., unaffected by this field) and merely surface a warning that motion exists but isn't represented — this metadata is never a second, divergent static appearance. |
| [CanvasPrintPdfMetadata](interfaces/CanvasPrintPdfMetadata.md) | Print-oriented PDF metadata (FR-043, canvas-m3-004). Shared verbatim with `serialize/pdf.ts`'s `PdfSerializeOptions.print` (that domain is allowed to import this one — `export/` sits at a strictly lower layer rank) so the job-contract metadata and the actual print-safety check never drift apart. |
| [CanvasPrintSafeArea](interfaces/CanvasPrintSafeArea.md) | - |
| [CanvasRect](interfaces/CanvasRect.md) | - |
| [CanvasRectNode](interfaces/CanvasRectNode.md) | - |
| [CanvasResolvedComponentDocument](interfaces/CanvasResolvedComponentDocument.md) | `CanvasResolvedDocument` plus the component diagnostics of the expansion pass. Additive subtype: every existing consumer of the base type keeps working unchanged. |
| [CanvasResolvedComponentInstance](interfaces/CanvasResolvedComponentInstance.md) | - |
| [CanvasResolvedComponentOrigin](interfaces/CanvasResolvedComponentOrigin.md) | Component provenance of one resolved record (plan 0023 M2-03, TD §9.3) — the additive optional field `CanvasResolvedNodeRecord.component` carries. Additive by contract: every existing `CanvasResolvedDocument` consumer keeps working unchanged on component-free documents, where the field is absent on every record. |
| [CanvasResolvedDocument](interfaces/CanvasResolvedDocument.md) | One immutable resolution of one document. |
| [CanvasResolvedGeometry](interfaces/CanvasResolvedGeometry.md) | Where a node ended up, in every space a consumer needs. |
| [CanvasResolvedNodeRecord](interfaces/CanvasResolvedNodeRecord.md) | - |
| [CanvasResolvedView](interfaces/CanvasResolvedView.md) | The read adapter consumers use instead of touching `records` directly (TD §12.1). |
| [CanvasRichTextNode](interfaces/CanvasRichTextNode.md) | Multi-span, multi-paragraph text with real wrapping. |
| [CanvasRotateAnimation](interfaces/CanvasRotateAnimation.md) | Serializable animation metadata (FR-080, canvas-m6-001). Describes intent only — timing/kind data for a future editor preview or video/GIF export worker to interpret. Static exports (SVG/PNG/PDF) always render a node/page in its normal resting state (its own `transform`/`opacity`/etc., unaffected by this field) and merely surface a warning that motion exists but isn't represented — this metadata is never a second, divergent static appearance. |
| [CanvasRuntime](interfaces/CanvasRuntime.md) | The resolved runtime = built-ins + extensions. With NO extension node kinds, `nodeSchema`/`irSchema` are the exact static module schemas (identity-equal), so the zero-extension runtime is behaviourally identical to today. |
| [CanvasScaleAnimation](interfaces/CanvasScaleAnimation.md) | Serializable animation metadata (FR-080, canvas-m6-001). Describes intent only — timing/kind data for a future editor preview or video/GIF export worker to interpret. Static exports (SVG/PNG/PDF) always render a node/page in its normal resting state (its own `transform`/`opacity`/etc., unaffected by this field) and merely surface a warning that motion exists but isn't represented — this metadata is never a second, divergent static appearance. |
| [CanvasSelectionWrapInLayoutFrameCommand](interfaces/CanvasSelectionWrapInLayoutFrameCommand.md) | Wrap sibling nodes in a NEW Auto Layout frame. |
| [CanvasShadow](interfaces/CanvasShadow.md) | - |
| [CanvasSizePreset](interfaces/CanvasSizePreset.md) | A lightweight, versioned width/height/unit preset (PRD §12.8) — e.g. "Instagram Post" or "LinkedIn Banner". Deliberately NOT a full design. The initial catalog (Instagram/TikTok/YouTube/etc., `CANVAS_SIZE_PRESETS` in `./size-presets.js`) ships in canvas-m3-006 (FR-060); this shape is what both the catalog and [CanvasTemplateDefinition.supportedSizes](interfaces/CanvasTemplateDefinition.md#supportedsizes) validate against. |
| [CanvasSlideAnimation](interfaces/CanvasSlideAnimation.md) | Serializable animation metadata (FR-080, canvas-m6-001). Describes intent only — timing/kind data for a future editor preview or video/GIF export worker to interpret. Static exports (SVG/PNG/PDF) always render a node/page in its normal resting state (its own `transform`/`opacity`/etc., unaffected by this field) and merely surface a warning that motion exists but isn't represented — this metadata is never a second, divergent static appearance. |
| [CanvasStarNode](interfaces/CanvasStarNode.md) | - |
| [CanvasStrokeStyle](interfaces/CanvasStrokeStyle.md) | - |
| [CanvasSvgHookContext](interfaces/CanvasSvgHookContext.md) | Minimal, React/Konva-free SVG emit surface handed to a kind's `toSvg` hook so a custom kind can render to the same `<svg>` the built-in serializer produces. |
| [CanvasSvgNode](interfaces/CanvasSvgNode.md) | An asset-referencing SVG node (FR-016). Deliberately holds ONLY an `assetId` — raw SVG markup never enters Canvas IR. `canvas-core` performs no SVG parsing or sanitization; that is an ingest-time host responsibility (see the threat-model doc referenced from this task). Serializers render it via the same safe `<image>` asset-reference path `image` nodes use, with a structured `SVG_INLINE_UNSUPPORTED` warning — true inline vector embedding is deferred behind a future `inlineVectorSvg` capability flag. |
| [CanvasTemplateDefinition](interfaces/CanvasTemplateDefinition.md) | The canonical template definition contract (PRD FR-020, §12.5). |
| [CanvasTextNode](interfaces/CanvasTextNode.md) | - |
| [CanvasTransform](interfaces/CanvasTransform.md) | - |
| [CanvasTreeAccess](interfaces/CanvasTreeAccess.md) | Read/write access to ONE node tree — a page's or a Component Source's. |
| [CanvasTypewriterAnimation](interfaces/CanvasTypewriterAnimation.md) | Serializable animation metadata (FR-080, canvas-m6-001). Describes intent only — timing/kind data for a future editor preview or video/GIF export worker to interpret. Static exports (SVG/PNG/PDF) always render a node/page in its normal resting state (its own `transform`/`opacity`/etc., unaffected by this field) and merely surface a warning that motion exists but isn't represented — this metadata is never a second, divergent static appearance. |
| [CanvasVideoNode](interfaces/CanvasVideoNode.md) | Fields shared by the video/audio asset-reference nodes (FR-081, canvas-m6-002). Deliberately holds ONLY an `assetId` — no inline media bytes ever enter Canvas IR, matching the `image`/`svg` asset-reference convention. No playback/rendering is implemented in canvas-core: this is a contract an export/render worker consumes later. |
| [CanvasVirtualNodePath](interfaces/CanvasVirtualNodePath.md) | The conceptual identity path of a virtual node (TD §9.2): `[outerInstanceId, sourceNodeId, nestedInstanceId, nestedSourceNodeId, …]`. A plain node's path is the single-segment `[sourceNodeId]`. |
| [ChangeRecordOptions](interfaces/ChangeRecordOptions.md) | - |
| [CommandApplyOptions](interfaces/CommandApplyOptions.md) | - |
| [CommandApplyResult](interfaces/CommandApplyResult.md) | `Inverse` defaults to the built-in [CanvasCommand](type-aliases/CanvasCommand.md) union, so every existing built-in-only call site (`applyCommand`, `applyCommands`, the Editor's history store) needs no type argument and no change. A command extension whose natural inverse is itself a custom command type supplies `Inverse` explicitly (see `CanvasCommandHandler`) instead of casting `inverse` to the built-in union (P0-4). |
| [CreateCanvasIROptions](interfaces/CreateCanvasIROptions.md) | - |
| [CreateComponentInstanceOptions](interfaces/CreateComponentInstanceOptions.md) | - |
| [CreateEllipseOptions](interfaces/CreateEllipseOptions.md) | - |
| [CreateFrameOptions](interfaces/CreateFrameOptions.md) | - |
| [CreateGroupOptions](interfaces/CreateGroupOptions.md) | - |
| [CreateImageOptions](interfaces/CreateImageOptions.md) | - |
| [CreateLineOptions](interfaces/CreateLineOptions.md) | - |
| [CreatePageOptions](interfaces/CreatePageOptions.md) | - |
| [CreatePathOptions](interfaces/CreatePathOptions.md) | - |
| [CreatePolygonOptions](interfaces/CreatePolygonOptions.md) | - |
| [CreateRectOptions](interfaces/CreateRectOptions.md) | - |
| [CreateRichTextOptions](interfaces/CreateRichTextOptions.md) | - |
| [CreateStarOptions](interfaces/CreateStarOptions.md) | - |
| [CreateSvgOptions](interfaces/CreateSvgOptions.md) | - |
| [CreateTextOptions](interfaces/CreateTextOptions.md) | - |
| [CreateVideoOptions](interfaces/CreateVideoOptions.md) | - |
| [DecomposedTransform](interfaces/DecomposedTransform.md) | Components recovered from an affine matrix, compatible with `CanvasTransform`. The shear is reported entirely as `skewX` (the canonical 2D affine decomposition folds any `skewY` into rotation/scale/`skewX`), so `skewY` is always 0 and omitted. `rotation` is in degrees, in the range (-180, 180]. |
| [FindNodeInSubtreeResult](interfaces/FindNodeInSubtreeResult.md) | - |
| [FindNodeResult](interfaces/FindNodeResult.md) | - |
| [FramePlaceholder](interfaces/FramePlaceholder.md) | Marks a frame as an empty slot awaiting content — an image well, a logo well. Deliberately minimal: enough to say "this frame is an image placeholder". Template slot binding is a separate concern and does not live here. |
| [HitTestOptions](interfaces/HitTestOptions.md) | - |
| [ImageFilter](interfaces/ImageFilter.md) | - |
| [InsertNodeOptions](interfaces/InsertNodeOptions.md) | Restricts a mutation to one tree — a page's, or a Component Source's (plan 0023 M3-01, LC-CMD). Absent = every page, the pre-component behavior, so every existing call site keeps its exact semantics. |
| [InstantiateTemplateOptions](interfaces/InstantiateTemplateOptions.md) | - |
| [InstantiateTemplateResult](interfaces/InstantiateTemplateResult.md) | - |
| [InstantiateTemplateWarning](interfaces/InstantiateTemplateWarning.md) | - |
| [LayoutIssuesToExportWarningsOptions](interfaces/LayoutIssuesToExportWarningsOptions.md) | Options for [layoutIssuesToExportWarnings](functions/layoutIssuesToExportWarnings.md). |
| [MarqueeHitsOptions](interfaces/MarqueeHitsOptions.md) | - |
| [MaterializeClipboardOptions](interfaces/MaterializeClipboardOptions.md) | - |
| [MaterializedClipboard](interfaces/MaterializedClipboard.md) | - |
| [MaterializeExportVariantOptions](interfaces/MaterializeExportVariantOptions.md) | - |
| [MeasuredLine](interfaces/MeasuredLine.md) | - |
| [MeasuredRun](interfaces/MeasuredRun.md) | A contiguous piece of ONE span that landed on ONE line. |
| [MeasuredText](interfaces/MeasuredText.md) | - |
| [MoveNodeOptions](interfaces/MoveNodeOptions.md) | Restricts a mutation to one tree — a page's, or a Component Source's (plan 0023 M3-01, LC-CMD). Absent = every page, the pre-component behavior, so every existing call site keeps its exact semantics. |
| [NormalizeTypographyOptions](interfaces/NormalizeTypographyOptions.md) | - |
| [ParentOfResult](interfaces/ParentOfResult.md) | - |
| [PdfRasterPage](interfaces/PdfRasterPage.md) | - |
| [PdfSerializeOptions](interfaces/PdfSerializeOptions.md) | - |
| [PdfSerializeResult](interfaces/PdfSerializeResult.md) | - |
| [PdfSerializeWarning](interfaces/PdfSerializeWarning.md) | - |
| [PolygonVertex](interfaces/PolygonVertex.md) | Pure vertex geometry for `polygon` and `star` nodes (FR-014). |
| [RegenerateNodeIdsOptions](interfaces/RegenerateNodeIdsOptions.md) | - |
| [RegenerateNodeIdsResult](interfaces/RegenerateNodeIdsResult.md) | - |
| [RemoveNodeOptions](interfaces/RemoveNodeOptions.md) | Restricts a mutation to one tree — a page's, or a Component Source's (plan 0023 M3-01, LC-CMD). Absent = every page, the pre-component behavior, so every existing call site keeps its exact semantics. |
| [ReorderChildrenOptions](interfaces/ReorderChildrenOptions.md) | Restricts a mutation to one tree — a page's, or a Component Source's (plan 0023 M3-01, LC-CMD). Absent = every page, the pre-component behavior, so every existing call site keeps its exact semantics. |
| [ReplaceChildrenInParentOptions](interfaces/ReplaceChildrenInParentOptions.md) | Restricts a mutation to one tree — a page's, or a Component Source's (plan 0023 M3-01, LC-CMD). Absent = every page, the pre-component behavior, so every existing call site keeps its exact semantics. |
| [ReplaceNodeOptions](interfaces/ReplaceNodeOptions.md) | Restricts a mutation to one tree — a page's, or a Component Source's (plan 0023 M3-01, LC-CMD). Absent = every page, the pre-component behavior, so every existing call site keeps its exact semantics. |
| [ResizeToVariantsOptions](interfaces/ResizeToVariantsOptions.md) | - |
| [ResizeToVariantsResult](interfaces/ResizeToVariantsResult.md) | - |
| [ResolvedFrameClipShape](interfaces/ResolvedFrameClipShape.md) | A frame's clip, fully resolved: is it clipping, to what, and on whose authority. |
| [ResolvedHitTarget](interfaces/ResolvedHitTarget.md) | Minimal structural view of a resolved-layout record, for the resolved variants below. |
| [ResolvedSpanStyle](interfaces/ResolvedSpanStyle.md) | A span's style with every optional field resolved. |
| [ResolveTemplateVariablesResult](interfaces/ResolveTemplateVariablesResult.md) | - |
| [RichTextParagraph](interfaces/RichTextParagraph.md) | A paragraph: a horizontal run of spans separated from its siblings by a break. |
| [RichTextSpan](interfaces/RichTextSpan.md) | A styled run of text. Every style field is optional: an omitted field inherits from the host's defaults at measure/render time rather than being resolved here, which keeps the IR free of any font or layout knowledge. |
| [RichTextStyleDefaults](interfaces/RichTextStyleDefaults.md) | The host's fallbacks for span fields a document leaves unset. |
| [ScopedMutationOptions](interfaces/ScopedMutationOptions.md) | Restricts a mutation to one tree — a page's, or a Component Source's (plan 0023 M3-01, LC-CMD). Absent = every page, the pre-component behavior, so every existing call site keeps its exact semantics. |
| [SmartGuide](interfaces/SmartGuide.md) | - |
| [SnapInput](interfaces/SnapInput.md) | - |
| [SnapRect](interfaces/SnapRect.md) | - |
| [SnapResult](interfaces/SnapResult.md) | - |
| [StylePatchResult](interfaces/StylePatchResult.md) | - |
| [SvgFontFaceDef](interfaces/SvgFontFaceDef.md) | - |
| [SvgSerializeOptions](interfaces/SvgSerializeOptions.md) | - |
| [SvgSerializeResult](interfaces/SvgSerializeResult.md) | - |
| [SvgSerializeWarning](interfaces/SvgSerializeWarning.md) | - |
| [TemplateAssetAttribution](interfaces/TemplateAssetAttribution.md) | Attribution metadata for a single asset a template references (FR-082, canvas-m6-003) — who created/owns it, and the credit string required when using it. Keyed by asset id in [CanvasTemplateDefinition.assetAttributions](interfaces/CanvasTemplateDefinition.md#assetattributions); distinct from [TemplateSourceMeta](interfaces/TemplateSourceMeta.md), which describes the TEMPLATE's own provenance, not a referenced asset's. |
| [TemplateColorSlot](interfaces/TemplateColorSlot.md) | A slot bound to one fill-bearing property (fill/background/stroke) of a node. |
| [TemplateFontSlot](interfaces/TemplateFontSlot.md) | A slot bound to a text-bearing node's `fontFamily`. |
| [TemplateFrameSlot](interfaces/TemplateFrameSlot.md) | A slot bound to a [CanvasFrameNode](interfaces/CanvasFrameNode.md) as a whole (its placeholder, clip, or background). |
| [TemplateGovernancePolicy](interfaces/TemplateGovernancePolicy.md) | Enterprise governance metadata for a template — approval workflows and org-level restrictions (FR-082, canvas-m6-003). Deliberately NOT a field on [CanvasTemplateDefinition](interfaces/CanvasTemplateDefinition.md): a marketplace/enterprise platform stores this keyed by `templateId` in its OWN store, so a non-enterprise consumer never has to deal with it, and it can never leak into Canvas IR or a template's core validated shape. |
| [TemplateImageSlot](interfaces/TemplateImageSlot.md) | A slot bound to an image-bearing node (typically a [CanvasImageNode](interfaces/CanvasImageNode.md), or a [CanvasFrameNode](interfaces/CanvasFrameNode.md) with an image [FramePlaceholder](interfaces/FramePlaceholder.md)). |
| [TemplateLicense](interfaces/TemplateLicense.md) | License metadata for a template (FR-082, canvas-m6-003). `type` is a human-facing label (e.g. "CC-BY-4.0", "Marketplace Standard", "Enterprise Internal"); the rest is additive over the canvas-m2-001 placeholder shape so existing consumers keep working unchanged. |
| [TemplateLogoSlot](interfaces/TemplateLogoSlot.md) | A slot bound to a logo-bearing node — same shape as [TemplateImageSlot](interfaces/TemplateImageSlot.md), kept distinct so a Templates panel can present logo replacement separately from generic image replacement. |
| [TemplateReferenceIssue](interfaces/TemplateReferenceIssue.md) | - |
| [TemplateSlotBase](interfaces/TemplateSlotBase.md) | Fields every slot kind shares: an id and the stable ID of the node inside the template's [CanvasTemplateDefinition.document](interfaces/CanvasTemplateDefinition.md#document) it controls. |
| [TemplateSourceMeta](interfaces/TemplateSourceMeta.md) | Provenance metadata for a template. Extended in canvas-m6-003 (FR-082). |
| [TemplateTextSlot](interfaces/TemplateTextSlot.md) | A slot bound to a [CanvasTextNode](interfaces/CanvasTextNode.md) or [CanvasRichTextNode](interfaces/CanvasRichTextNode.md)'s text content. |
| [TemplateVariable](interfaces/TemplateVariable.md) | A user-facing control bound to a [TemplateSlot](type-aliases/TemplateSlot.md). `defaultValue` fills in when no value is supplied at instantiation; `required` (with no default) surfaces as a structured warning instead of failing silently — see `resolveTemplateVariables` in `./resolvers.js`. |
| [TemplateVariableWarning](interfaces/TemplateVariableWarning.md) | A `TemplateVariable` left with no supplied value, no default, and `required: true`. |
| [TextMeasureRequest](interfaces/TextMeasureRequest.md) | What to lay out, and the box to lay it out into. |
| [TidyUpDelta](interfaces/TidyUpDelta.md) | Per-rect displacement produced by [tidyUpRects](functions/tidyUpRects.md). |
| [TransactionApplyResult](interfaces/TransactionApplyResult.md) | - |
| [TreeAccessWriteOptions](interfaces/TreeAccessWriteOptions.md) | - |
| [UpdateNodeOptions](interfaces/UpdateNodeOptions.md) | Restricts a mutation to one tree — a page's, or a Component Source's (plan 0023 M3-01, LC-CMD). Absent = every page, the pre-component behavior, so every existing call site keeps its exact semantics. |
| [ViewportDescriptor](interfaces/ViewportDescriptor.md) | The pan/zoom state of a canvas viewport. |
| [WalkContext](interfaces/WalkContext.md) | - |
| [WalkDocumentContext](interfaces/WalkDocumentContext.md) | - |

## Type Aliases

| Type Alias | Description |
| ------ | ------ |
| [Aabb](type-aliases/Aabb.md) | Axis-aligned bounding box in world coordinates. Alias of geometry's BoundsExtent. |
| [AffineMatrix](type-aliases/AffineMatrix.md) | SVG `matrix(a b c d e f)` tuple: point (x,y) → (a·x + c·y + e, b·x + d·y + f). |
| [AiDesignJobKind](type-aliases/AiDesignJobKind.md) | - |
| [AiDesignJobPayload](type-aliases/AiDesignJobPayload.md) | What a completed design job produced — never a flattened raster. `command` covers in-place edits to the CURRENT document (rewrite-copy, generate-headline, apply-brand); `pages` covers ops that generate new candidate pages (prompt-to-design, prompt-to-template, generate-layout-variants, resize-campaign, generate-social-pack), inserted as same-document pages exactly like `resizeToVariants`/`instantiateTemplate` already do. Both must validate against the normal `CanvasIR`/command schemas before ever reaching a real document (canvas-m4-003). |
| [AiDesignJobRequest](type-aliases/AiDesignJobRequest.md) | - |
| [AiDesignJobResult](type-aliases/AiDesignJobResult.md) | Status-discriminated, matching `AiImageJobResult`'s FR-050 failed-job invariant: `payload` exists ONLY on a `"complete"` result and `error` ONLY on an `"error"` one — a failed or cancelled design job has no payload to accidentally apply, enforced at compile time rather than by convention. |
| [AiDesignJobStatus](type-aliases/AiDesignJobStatus.md) | - |
| [AiDesignProvider](type-aliases/AiDesignProvider.md) | Bare function type, deliberately mirroring `AiImageProvider` rather than generalizing to an `AssetSourceProvider`-style object interface (canvas-m4-002's recorded decision): `AiImageProvider` is already the established, widely-consumed shape (mock + real Replicate adapter + `AiJobClient`), and object-ifying it now would be a breaking change to that existing surface for no behavioral gain — capability discovery (below) is handled as sibling metadata instead of restructuring the function itself. |
| [AiImageJobKind](type-aliases/AiImageJobKind.md) | - |
| [AiImageJobRequest](type-aliases/AiImageJobRequest.md) | - |
| [AiImageJobResult](type-aliases/AiImageJobResult.md) | Status-discriminated (FR-050's failed-job invariant, canvas-m4-001): `resultAssetId` exists ONLY on a `"complete"` result and `error` ONLY on an `"error"` one, so it is a compile error — not just a documented convention — to read an asset id off a job that didn't succeed. This is additive over the pre-M4 flat shape: every existing construction site already paired `status: "complete"` with `resultAssetId` and `status: "error"` with `error`, so no real provider/consumer changes. |
| [AiImageJobStatus](type-aliases/AiImageJobStatus.md) | - |
| [AiImageProvider](type-aliases/AiImageProvider.md) | - |
| [AlignEdge](type-aliases/AlignEdge.md) | - |
| [BrandComplianceIssueCode](type-aliases/BrandComplianceIssueCode.md) | Stable compliance codes. |
| [BrandRuleKind](type-aliases/BrandRuleKind.md) | The four rule kinds a brand kit can declare for compliance checking (canvas-m2-006). |
| [BrandTokenType](type-aliases/BrandTokenType.md) | The kinds of value a brand kit owns. Referenced by `BrandTokenRef` (the shape of an unresolved reference) and by `CanvasBrandTokenConstraint` (which kinds a policy permits on a property). |
| [CanvasAiPlaceholderStatus](type-aliases/CanvasAiPlaceholderStatus.md) | - |
| [CanvasAnimation](type-aliases/CanvasAnimation.md) | Animation intent carried by a node (`meta.animation`) or a page (`page.animation`) — FR-080, canvas-m6-001. |
| [CanvasAnimationDirection](type-aliases/CanvasAnimationDirection.md) | - |
| [CanvasAnimationEasing](type-aliases/CanvasAnimationEasing.md) | - |
| [CanvasAnyNodeUpdateCommand](type-aliases/CanvasAnyNodeUpdateCommand.md) | - |
| [CanvasArrowHead](type-aliases/CanvasArrowHead.md) | Line/path end decorations (FR-075). Absent = "none". |
| [CanvasBackgroundKind](type-aliases/CanvasBackgroundKind.md) | - |
| [CanvasChange](type-aliases/CanvasChange.md) | - |
| [CanvasChangeSource](type-aliases/CanvasChangeSource.md) | Who produced a [CanvasChangeRecord](interfaces/CanvasChangeRecord.md): applied locally, or received from a remote peer/server. |
| [CanvasClipboardErrorCode](type-aliases/CanvasClipboardErrorCode.md) | - |
| [CanvasCommand](type-aliases/CanvasCommand.md) | - |
| [CanvasCommandErrorCode](type-aliases/CanvasCommandErrorCode.md) | Every reason a command refuses to apply. |
| [CanvasCommandKind](type-aliases/CanvasCommandKind.md) | - |
| [CanvasCommentAnchor](type-aliases/CanvasCommentAnchor.md) | - |
| [CanvasCommentAnchorStatus](type-aliases/CanvasCommentAnchorStatus.md) | - |
| [CanvasComponentChangeOp](type-aliases/CanvasComponentChangeOp.md) | What happened to a Component Source (plan 0023 M3-03). `source-edit` is any location-scoped node command inside the Source tree; the named ops map 1:1 onto the registry commands. Instance-side edits (inserting an instance, setting/resetting overrides) are ordinary NODE changes on the instance node (`updated` with `keys: ["overrides"]`, `added`, `removed`) and deliberately do NOT use this kind. Virtual (resolved) node ids never appear as change targets. |
| [CanvasComponentCreateCommand](type-aliases/CanvasComponentCreateCommand.md) | Add a Component Source to the Registry. |
| [CanvasComponentIssueCode](type-aliases/CanvasComponentIssueCode.md) | The 12 stable diagnostic codes (PRD §9.12). GROW-ONLY once shipped: hosts switch on these to phrase user-facing messages, so a removed or renamed member is a breaking change. |
| [CanvasComponentOverride](type-aliases/CanvasComponentOverride.md) | One typed per-instance override, discriminated to match its property kind. A value whose kind mismatches its property is an orphan: retained, never applied, never reassigned. |
| [CanvasComponentOverrideMap](type-aliases/CanvasComponentOverrideMap.md) | Overrides keyed by Property ID — never by name or array index (INV-6). |
| [CanvasComponentProperty](type-aliases/CanvasComponentProperty.md) | - |
| [CanvasComponentRegistry](type-aliases/CanvasComponentRegistry.md) | The document-local Source registry — a PLAIN persisted map keyed by component id, deliberately NOT an extension-style registry: there is no `register()` surface, no runtime behavior, just data. An empty registry is normalized to an absent `components` key (INV-10). |
| [CanvasComponentSourceRef](type-aliases/CanvasComponentSourceRef.md) | Where an instance's Source comes from — the same field for both kinds. |
| [CanvasComponentVariantIssueCode](type-aliases/CanvasComponentVariantIssueCode.md) | - |
| [CanvasComponentVariantResolutionCode](type-aliases/CanvasComponentVariantResolutionCode.md) | - |
| [CanvasComponentVariantSelection](type-aliases/CanvasComponentVariantSelection.md) | A concrete combination: axis id → value id. |
| [CanvasContainerNode](type-aliases/CanvasContainerNode.md) | A node that holds `children` and is therefore recursed into by every walker, mutation, and serializer. Group was the only container until frame landed; anything gated on "does this node have children" must test against this union rather than `type === "group"`. |
| [CanvasDefinitionLookup](type-aliases/CanvasDefinitionLookup.md) | The outcome of asking "what definition does this Source name?". |
| [CanvasDocumentKind](type-aliases/CanvasDocumentKind.md) | What a document is: an editable design (the default when absent), an instance stamped from a template, or a variant produced for export. |
| [CanvasEffect](type-aliases/CanvasEffect.md) | One visual effect on a node (§9.4). Serializable and renderer-independent; the list is ordered (shadows render bottom-up, a trailing blur applies to the composited result). |
| [CanvasExportFidelityGrade](type-aliases/CanvasExportFidelityGrade.md) | - |
| [CanvasExportFormat](type-aliases/CanvasExportFormat.md) | The headless export job contract (PRD FR-040, §12.7). |
| [CanvasExportJobSource](type-aliases/CanvasExportJobSource.md) | Where the document to export comes from. `document` carries the IR inline; `documentRef` is an opaque reference a host/worker resolves — `canvas-core` never attempts to resolve one itself (see [resolveInlineExportDocument](functions/resolveInlineExportDocument.md) in `./resolve.js`). |
| [CanvasExportJobStatus](type-aliases/CanvasExportJobStatus.md) | - |
| [CanvasExportWarningLevel](type-aliases/CanvasExportWarningLevel.md) | - |
| [CanvasExtensionErrorCode](type-aliases/CanvasExtensionErrorCode.md) | - |
| [CanvasExternalComponentSnapshotRegistry](type-aliases/CanvasExternalComponentSnapshotRegistry.md) | The document's external snapshot registry, keyed by `libraryId/componentId/version/integrity` (see `ir/snapshot-key.ts`). |
| [CanvasExternalComponentState](type-aliases/CanvasExternalComponentState.md) | What the document alone can say about an external Source (TD §17.1). |
| [CanvasFill](type-aliases/CanvasFill.md) | A node fill: a plain CSS color string (back-compat), a structured gradient, or a brand-token reference (unresolved until a consumer looks it up). |
| [CanvasFontFamily](type-aliases/CanvasFontFamily.md) | A font family: a literal name, or a brand-token reference to one. |
| [CanvasFrameShape](type-aliases/CanvasFrameShape.md) | What geometry a clipping frame clips to (ADR 0008 decision 2 — the shape mask lives on the container, never on the image node). |
| [CanvasImageAdjustmentPresetId](type-aliases/CanvasImageAdjustmentPresetId.md) | - |
| [CanvasImageFitMode](type-aliases/CanvasImageFitMode.md) | FR-094 image fit modes (B-02, PRD 0012 §9.5). Absent means `"stretch"` — the pre-B-02 rendering — so existing documents need no migration. |
| [CanvasInvariantIssueCode](type-aliases/CanvasInvariantIssueCode.md) | Semantic invariant validation (P0-6) — deliberately separate from `ir/validators.ts`'s Zod schemas. A schema checks SHAPE (is `id` a non-empty string, is `pages` an array with &gt;= 1 element); it cannot express whole-document facts like "every node id is unique" or "every `assetId` a node references exists in `ir.assets`" — those require walking the tree and cross-referencing, which is exactly what this module does. |
| [CanvasIRMutationCode](type-aliases/CanvasIRMutationCode.md) | - |
| [CanvasIRVersion](type-aliases/CanvasIRVersion.md) | Every CanvasIR schema version this build can read. Documents at older versions are forward-migrated on read (`migrateCanvasIR`/`runtime.migrate`); `CanvasIR["version"]` — the version this build writes — is always the newest member. |
| [CanvasKnownCapability](type-aliases/CanvasKnownCapability.md) | Capabilities THIS build knows how to honour. |
| [CanvasLayoutAlign](type-aliases/CanvasLayoutAlign.md) | Alignment along either Auto Layout axis. Space-between, stretch, baseline and per-child `alignSelf` are deliberately excluded from v1 (PRD 0014 §7); adding a member here is a `layout.auto.v2` change, because an older reader rejects unknown enum values (see `CanvasDocumentCompatibility`). |
| [CanvasLayoutDirection](type-aliases/CanvasLayoutDirection.md) | Auto Layout main-axis direction (`layout.auto.v1`). Exactly two members — wrapping, grid, rows and columns are out of scope for v1 and must not appear here (PRD 0014 §7). |
| [CanvasLayoutIssueCode](type-aliases/CanvasLayoutIssueCode.md) | - |
| [CanvasLayoutIssueFallback](type-aliases/CanvasLayoutIssueFallback.md) | The deterministic degradation a consumer applies when an issue fires. |
| [CanvasLayoutIssueSeverity](type-aliases/CanvasLayoutIssueSeverity.md) | - |
| [CanvasLayoutPositioning](type-aliases/CanvasLayoutPositioning.md) | Whether a child participates in its parent frame's flow. `absolute` children are excluded from flow order, gap and Hug measurement, and position from the frame's **border-box** origin (PRD 0014 §9.4). |
| [CanvasLayoutSizing](type-aliases/CanvasLayoutSizing.md) | Per-axis sizing mode for a node inside an Auto Layout frame. |
| [CanvasLeafNode](type-aliases/CanvasLeafNode.md) | - |
| [CanvasNode](type-aliases/CanvasNode.md) | - |
| [CanvasNodeByKind](type-aliases/CanvasNodeByKind.md) | - |
| [CanvasNodeKind](type-aliases/CanvasNodeKind.md) | - |
| [CanvasNodeStyleKey](type-aliases/CanvasNodeStyleKey.md) | - |
| [CanvasPageResizeMode](type-aliases/CanvasPageResizeMode.md) | FR-063 page-resize content handling (B-01, PRD 0012). |
| [CanvasPrintColorMode](type-aliases/CanvasPrintColorMode.md) | - |
| [CanvasResolvedNodeId](type-aliases/CanvasResolvedNodeId.md) | Identity of a node in the resolved tree. |
| [CanvasSafeArea](type-aliases/CanvasSafeArea.md) | A safe-area inset, in the same unit as its owning [CanvasSizePreset](interfaces/CanvasSizePreset.md). Alias of the shared [CanvasInsets](interfaces/CanvasInsets.md) shape (PRD 0012 §9.3) — kept as a named export for existing consumers. |
| [CanvasStrokeCap](type-aliases/CanvasStrokeCap.md) | FR-075 extended stroke styling (B-03a). All optional — absent = current rendering. |
| [CanvasStrokeJoin](type-aliases/CanvasStrokeJoin.md) | - |
| [CanvasTextAlign](type-aliases/CanvasTextAlign.md) | - |
| [CanvasTextMeasurer](type-aliases/CanvasTextMeasurer.md) | Lay out rich text. Implementations MUST be pure: the same request has to give the same result, or an export will not match what the editor showed. |
| [CanvasTextOverrideValue](type-aliases/CanvasTextOverrideValue.md) | Plain or rich replacement content for a text property (TD §5.4). |
| [CanvasUnit](type-aliases/CanvasUnit.md) | - |
| [CanvasUnknownNode](type-aliases/CanvasUnknownNode.md) | A node whose `type` discriminant is not necessarily a built-in kind. |
| [CanvasVerticalAlign](type-aliases/CanvasVerticalAlign.md) | FR-081 vertical text alignment within a fixed-height box. |
| [CreateAudioOptions](type-aliases/CreateAudioOptions.md) | - |
| [FrameClipDegradation](type-aliases/FrameClipDegradation.md) | Why a declared [CanvasFrameShape](type-aliases/CanvasFrameShape.md) could not be honoured. Both cases are reachable at runtime even though [CanvasFrameShapeSchema](variables/CanvasFrameShapeSchema.md) rejects them: nodes are routinely constructed in TypeScript without going through the schema, and the IR's `looseObject` posture means a newer peer's shape kind survives a round-trip through a build that has never heard of it. |
| [FrameClipShapeSource](type-aliases/FrameClipShapeSource.md) | Where the resolved geometry came from. |
| [FramePlaceholderKind](type-aliases/FramePlaceholderKind.md) | What a frame stands in for until real content is dropped into it. |
| [InstantiateTemplateWarningCode](type-aliases/InstantiateTemplateWarningCode.md) | - |
| [PdfRasterMimeType](type-aliases/PdfRasterMimeType.md) | - |
| [PdfWarningCode](type-aliases/PdfWarningCode.md) | - |
| [RichTextOverflow](type-aliases/RichTextOverflow.md) | How a rich-text block behaves when its content exceeds its box. |
| [RichTextTransform](type-aliases/RichTextTransform.md) | How a span's glyphs are cased at render time. Purely presentational — the span's `text` is never rewritten, so the original casing survives a round-trip. |
| [RichTextWrap](type-aliases/RichTextWrap.md) | Where lines may break. `"none"` lays every paragraph out on a single line. |
| [SnapAxis](type-aliases/SnapAxis.md) | - |
| [SvgFetchAsset](type-aliases/SvgFetchAsset.md) | Fetch raw bytes for a remote/relative asset URI (only needed to embed). |
| [SvgImageMode](type-aliases/SvgImageMode.md) | - `auto` (default): inline `data:` URIs, reference everything else. - `embed`: fetch + base64-embed remote URIs (requires `fetchAsset`). - `reference`: always emit `href` referencing the original URI. |
| [SvgResolveBrandToken](type-aliases/SvgResolveBrandToken.md) | Resolve a `BrandTokenRef` to a concrete value: a fill (color/gradient) for a `"color"` token, or a font-family string for a `"font"` token. Core never calls this itself with a default — it is the host's brand-kit lookup, injected. Return `undefined` (or omit the option) to leave a token unresolved; the serializer degrades deterministically and records `BRAND_TOKEN_UNRESOLVED`, never throws. |
| [SvgWarningCode](type-aliases/SvgWarningCode.md) | - |
| [TemplateColorSlotProperty](type-aliases/TemplateColorSlotProperty.md) | Which fill-bearing property on the target node a [TemplateColorSlot](interfaces/TemplateColorSlot.md) controls. |
| [TemplateReferenceIssueCode](type-aliases/TemplateReferenceIssueCode.md) | A cross-reference problem found while checking a [CanvasTemplateDefinition](interfaces/CanvasTemplateDefinition.md)'s `editableSlots`/`lockedNodeIds` against its own `document`. Structured (code + offending id), not a thrown error — schema validation only checks shape, not these referential invariants, so callers decide how to surface them (block instantiation, warn, etc.). |
| [TemplateSlot](type-aliases/TemplateSlot.md) | A named, editable point inside a template's [CanvasTemplateDefinition.document](interfaces/CanvasTemplateDefinition.md#document) (FR-021) — one of the six supported kinds, each referencing a stable node ID. Kind-specific metadata beyond the shared `id`/`nodeId` lives on the individual slot interfaces (e.g. [TemplateColorSlot.property](interfaces/TemplateColorSlot.md#property)). |
| [TemplateSlotKind](type-aliases/TemplateSlotKind.md) | The six slot kinds a template can expose for controlled editing (FR-021). |
| [ValidateAiDesignJobResultOutcome](type-aliases/ValidateAiDesignJobResultOutcome.md) | - |
| [WalkDocumentVisitor](type-aliases/WalkDocumentVisitor.md) | - |
| [WalkVisitor](type-aliases/WalkVisitor.md) | - |

## Variables

| Variable | Description |
| ------ | ------ |
| [BrandAssetSchema](variables/BrandAssetSchema.md) | - |
| [BrandColorTokenSchema](variables/BrandColorTokenSchema.md) | - |
| [BrandFontTokenSchema](variables/BrandFontTokenSchema.md) | - |
| [BrandImageStylePresetSchema](variables/BrandImageStylePresetSchema.md) | - |
| [BrandKitDefinitionSchema](variables/BrandKitDefinitionSchema.md) | - |
| [BrandRuleSchema](variables/BrandRuleSchema.md) | - |
| [BrandTokenRefSchema](variables/BrandTokenRefSchema.md) | - |
| [BrandToneMetadataSchema](variables/BrandToneMetadataSchema.md) | - |
| [BrandTypographyPresetSchema](variables/BrandTypographyPresetSchema.md) | - |
| [CANVAS\_BRAND\_GOVERNANCE\_CAPABILITY](variables/CANVAS_BRAND_GOVERNANCE_CAPABILITY.md) | A document carries a brand policy that a reader must honour (`brand.governance.v1`). Declared now, emitted from M4 (T-036…T-040). |
| [CANVAS\_CLIPBOARD\_MIME](variables/CANVAS_CLIPBOARD_MIME.md) | MIME type the editor writes alongside `text/plain` when the system clipboard is available — lets AnvilKit instances recognize each other's payloads (FR-021 cross-editor paste). |
| [CANVAS\_CLIPBOARD\_VERSION](variables/CANVAS_CLIPBOARD_VERSION.md) | - |
| [CANVAS\_COMPONENTS\_EXTERNAL\_CAPABILITY](variables/CANVAS_COMPONENTS_EXTERNAL_CAPABILITY.md) | A document uses at least one external library component (plan 0021, `components.external.v1`). |
| [CANVAS\_COMPONENTS\_LOCAL\_CAPABILITY](variables/CANVAS_COMPONENTS_LOCAL_CAPABILITY.md) | Capability ids for document-local components (plan 0023, LC-COMPAT-001). |
| [CANVAS\_COMPONENTS\_OVERRIDES\_CAPABILITY](variables/CANVAS_COMPONENTS_OVERRIDES_CAPABILITY.md) | - |
| [CANVAS\_COMPONENTS\_VARIANTS\_CAPABILITY](variables/CANVAS_COMPONENTS_VARIANTS_CAPABILITY.md) | A document persists a variant selection on an instance (`components.variants.v1`). |
| [CANVAS\_CORE\_VERSION](variables/CANVAS_CORE_VERSION.md) | - |
| [CANVAS\_IMAGE\_ADJUSTMENT\_PRESETS](variables/CANVAS_IMAGE_ADJUSTMENT_PRESETS.md) | FR-101 presets — plain adjustment values, never a separate model. Applying one REPLACES the node's adjustments with these values. |
| [CANVAS\_IR\_MIGRATIONS](variables/CANVAS_IR_MIGRATIONS.md) | Production migrations for the core CanvasIR version chain, seeded into every registry so a zero-extension runtime still reads old documents. |
| [CANVAS\_IR\_VERSION](variables/CANVAS_IR_VERSION.md) | The CanvasIR schema version this build emits and treats as current. |
| [CANVAS\_LAYOUT\_AUTO\_CAPABILITY](variables/CANVAS_LAYOUT_AUTO_CAPABILITY.md) | The capability string a document must declare once any of its content carries Auto Layout intent. |
| [CANVAS\_SIZE\_PRESETS](variables/CANVAS_SIZE_PRESETS.md) | The initial social size preset catalog (PRD FR-060, §12.8). Each entry's `width`/`height` is the platform's own published dimension for that surface as of this catalog's `version: "1"` — cited per-preset below since these numbers drift as platforms redesign their apps; bump a preset's own `version` (never mutate a shipped one in place) when a platform changes its spec, so documents pinned to the old preset keep their original size. |
| [CanvasAiPlaceholderNodeSchema](variables/CanvasAiPlaceholderNodeSchema.md) | - |
| [CanvasAnimationSchema](variables/CanvasAnimationSchema.md) | A discriminated union over the seven animation kinds (FR-080), dispatched on `kind`. |
| [CanvasAssetRefSchema](variables/CanvasAssetRefSchema.md) | - |
| [CanvasAudioNodeSchema](variables/CanvasAudioNodeSchema.md) | - |
| [CanvasAutoLayoutSchema](variables/CanvasAutoLayoutSchema.md) | - |
| [CanvasBlurEffectSchema](variables/CanvasBlurEffectSchema.md) | - |
| [CanvasBoundsSchema](variables/CanvasBoundsSchema.md) | - |
| [CanvasClipboardPayloadSchema](variables/CanvasClipboardPayloadSchema.md) | Loose (unknown-key-preserving) schema, matching the IR validators' convention so compatible unknown fields survive a copy/paste round-trip between mixed editor versions. |
| [CanvasCommentAnchorSchema](variables/CanvasCommentAnchorSchema.md) | A discriminated union over the four anchor kinds (FR-072), dispatched on `kind`. |
| [CanvasCommentCoordinateAnchorSchema](variables/CanvasCommentCoordinateAnchorSchema.md) | - |
| [CanvasCommentNodeAnchorSchema](variables/CanvasCommentNodeAnchorSchema.md) | - |
| [CanvasCommentPageAnchorSchema](variables/CanvasCommentPageAnchorSchema.md) | - |
| [CanvasCommentSelectionAnchorSchema](variables/CanvasCommentSelectionAnchorSchema.md) | - |
| [CanvasComponentDefinitionShape](variables/CanvasComponentDefinitionShape.md) | `CanvasComponentDefinition`'s non-recursive fields, split out for the same reason as `CanvasFrameNodeShape`: `root` must bind to whichever node union is being assembled — static or extension-aware — and every other field must not drift between the two paths. |
| [CanvasComponentInstanceNodeSchema](variables/CanvasComponentInstanceNodeSchema.md) | The `component-instance` member of BOTH node unions. |
| [CanvasComponentOverrideSchema](variables/CanvasComponentOverrideSchema.md) | - |
| [CanvasComponentPropertySchema](variables/CanvasComponentPropertySchema.md) | - |
| [CanvasComponentRegistrySchema](variables/CanvasComponentRegistrySchema.md) | - |
| [CanvasComponentVariantSetSchema](variables/CanvasComponentVariantSetSchema.md) | - |
| [CanvasDocumentKindSchema](variables/CanvasDocumentKindSchema.md) | - |
| [CanvasDropShadowEffectSchema](variables/CanvasDropShadowEffectSchema.md) | - |
| [CanvasEffectSchema](variables/CanvasEffectSchema.md) | - |
| [CanvasEllipseNodeSchema](variables/CanvasEllipseNodeSchema.md) | - |
| [CanvasExternalComponentSnapshotRegistrySchema](variables/CanvasExternalComponentSnapshotRegistrySchema.md) | - |
| [CanvasFillSchema](variables/CanvasFillSchema.md) | A fill is a plain color string (back-compat), a structured gradient, or an unresolved brand-token reference. |
| [CanvasFontFamilySchema](variables/CanvasFontFamilySchema.md) | A font family is a literal name, or a brand-token reference to one. |
| [CanvasFrameNodeSchema](variables/CanvasFrameNodeSchema.md) | - |
| [CanvasFrameNodeShape](variables/CanvasFrameNodeShape.md) | Frame's own (non-recursive) fields. Split out because `children` must be bound to whichever node union is being assembled — the static one below, or the extension-aware one `buildExtendedSchemas` builds — and the two must not drift. |
| [CanvasFrameShapeSchema](variables/CanvasFrameShapeSchema.md) | The clip geometry of a frame (ADR 0008 decision 2). Optional and additive on `CanvasFrameNode`, so every document written before it existed parses unchanged and needs no migration. |
| [CanvasGradientFillSchema](variables/CanvasGradientFillSchema.md) | - |
| [CanvasGradientStopSchema](variables/CanvasGradientStopSchema.md) | - |
| [CanvasGroupNodeSchema](variables/CanvasGroupNodeSchema.md) | - |
| [CanvasImageAdjustmentsSchema](variables/CanvasImageAdjustmentsSchema.md) | - |
| [CanvasImageCropSchema](variables/CanvasImageCropSchema.md) | - |
| [CanvasImageNodeSchema](variables/CanvasImageNodeSchema.md) | - |
| [CanvasInsetsSchema](variables/CanvasInsetsSchema.md) | - |
| [CanvasIRComponentSourceRefSchema](variables/CanvasIRComponentSourceRefSchema.md) | The persisted `source` field. |
| [CanvasIRMetadataSchema](variables/CanvasIRMetadataSchema.md) | - |
| [CanvasIRSchema](variables/CanvasIRSchema.md) | - |
| [CanvasIRShape](variables/CanvasIRShape.md) | - |
| [CanvasLayoutItemSchema](variables/CanvasLayoutItemSchema.md) | - |
| [CanvasLineNodeSchema](variables/CanvasLineNodeSchema.md) | - |
| [CanvasMediaTrimSchema](variables/CanvasMediaTrimSchema.md) | - |
| [CanvasNodeBaseSchema](variables/CanvasNodeBaseSchema.md) | - |
| [CanvasNodeBaseShape](variables/CanvasNodeBaseShape.md) | - |
| [CanvasNodeMetaSchema](variables/CanvasNodeMetaSchema.md) | - |
| [CanvasNodeSchema](variables/CanvasNodeSchema.md) | - |
| [CanvasPageBackgroundSchema](variables/CanvasPageBackgroundSchema.md) | - |
| [CanvasPageGuidesSchema](variables/CanvasPageGuidesSchema.md) | - |
| [CanvasPageLayoutAidsSchema](variables/CanvasPageLayoutAidsSchema.md) | - |
| [CanvasPageSchema](variables/CanvasPageSchema.md) | - |
| [CanvasPageShape](variables/CanvasPageShape.md) | `CanvasPage`'s own (non-recursive) fields, split out for the same reason as `CanvasFrameNodeShape`: `root` must bind to whichever node union is being assembled — the static one below, or the extension-aware one `buildExtendedSchemas` builds — and every OTHER page field (notably `variantSource`/`animation`) must not drift between the two. Both paths spread this shape rather than re-declaring it (P0-3). |
| [CanvasPageSizeSchema](variables/CanvasPageSizeSchema.md) | - |
| [CanvasPageVariantSourceSchema](variables/CanvasPageVariantSourceSchema.md) | - |
| [CanvasPathNodeSchema](variables/CanvasPathNodeSchema.md) | - |
| [CanvasPolygonNodeSchema](variables/CanvasPolygonNodeSchema.md) | - |
| [CanvasRectNodeSchema](variables/CanvasRectNodeSchema.md) | - |
| [CanvasRichTextNodeSchema](variables/CanvasRichTextNodeSchema.md) | - |
| [CanvasSafeAreaSchema](variables/CanvasSafeAreaSchema.md) | Alias of the shared insets schema (PRD 0012 §9.3) — kept as a named export for existing consumers. |
| [CanvasShadowSchema](variables/CanvasShadowSchema.md) | - |
| [CanvasSizePresetSchema](variables/CanvasSizePresetSchema.md) | - |
| [CanvasStarNodeSchema](variables/CanvasStarNodeSchema.md) | - |
| [CanvasSvgNodeSchema](variables/CanvasSvgNodeSchema.md) | FR-016: deliberately holds ONLY `assetId` — there is no `markup`/`content` field for inline SVG text to occupy, so raw markup has nowhere to go even under this schema's loose-object (unknown-key-preserving) posture. No renderer in this package ever reads an unknown key, so an attacker-supplied extra field survives as inert data, never as executed markup. |
| [CanvasTemplateDefinitionSchema](variables/CanvasTemplateDefinitionSchema.md) | Validates a [CanvasTemplateDefinition](interfaces/CanvasTemplateDefinition.md) independently of normal Canvas IR validation — template-only fields (`id`, `category`, `tags`, `variables`, `editableSlots`, `lockedNodeIds`, `license`, `source`) are never accepted on `document`, which is validated purely as a normal [CanvasIRSchema](variables/CanvasIRSchema.md). |
| [CanvasTextNodeSchema](variables/CanvasTextNodeSchema.md) | - |
| [CanvasTextOverrideValueSchema](variables/CanvasTextOverrideValueSchema.md) | - |
| [CanvasTransformSchema](variables/CanvasTransformSchema.md) | - |
| [CanvasVideoNodeSchema](variables/CanvasVideoNodeSchema.md) | - |
| [DEFAULT\_RICH\_TEXT\_STYLE](variables/DEFAULT_RICH_TEXT_STYLE.md) | The package-wide fallbacks for rich-text style fields a document leaves unset. Chosen to match `createText`'s defaults (`ir/builders.ts`) so a rich-text block with no explicit styling renders like a plain `text` node. |
| [DEFAULT\_SNAP\_THRESHOLD](variables/DEFAULT_SNAP_THRESHOLD.md) | - |
| [FramePlaceholderSchema](variables/FramePlaceholderSchema.md) | - |
| [ImageFilterSchema](variables/ImageFilterSchema.md) | - |
| [MAX\_CHILDREN\_PER\_CONTAINER](variables/MAX_CHILDREN_PER_CONTAINER.md) | Ceiling on direct children of a single container. |
| [MAX\_CLIPBOARD\_BYTES](variables/MAX_CLIPBOARD_BYTES.md) | Maximum serialized byte size accepted for a single clipboard payload. |
| [MAX\_CLIPBOARD\_NODES](variables/MAX_CLIPBOARD_NODES.md) | Maximum number of nodes accepted in a single clipboard payload. |
| [MAX\_COMPONENT\_DEFINITIONS\_PER\_DOCUMENT](variables/MAX_COMPONENT_DEFINITIONS_PER_DOCUMENT.md) | Ceiling on Component Sources in one document's `ir.components` Registry. A document-local library; generous against real marketing documents while keeping a hostile Registry parse bounded. Enforced by `ir/validators.ts`. |
| [MAX\_COMPONENT\_EXPANDED\_NODES\_PER\_RESOLUTION](variables/MAX_COMPONENT_EXPANDED_NODES_PER_RESOLUTION.md) | Ceiling on virtual nodes one resolution pass may expand. Anchored to `MAX_DOCUMENT_NODES` — expansion output IS a document-scale node set, and beyond that figure the measured budgets no longer apply. Enforced by the M2 resolver (placeholder + diagnostic past the cap, never a throw). |
| [MAX\_COMPONENT\_NESTED\_DEPTH](variables/MAX_COMPONENT_NESTED_DEPTH.md) | Ceiling on nested component depth (instance → Source → instance …). Well under `MAX_TREE_DEPTH` (64) so an expanded virtual tree can never approach the walker guard even atop a deep page subtree. Enforced by the M2 dependency graph (`validateComponentGraph`) and again at read time. |
| [MAX\_COMPONENT\_OVERRIDES\_PER\_INSTANCE](variables/MAX_COMPONENT_OVERRIDES_PER_INSTANCE.md) | Ceiling on override-map entries per instance — 2× the properties cap, because orphaned overrides are retained rather than dropped (TD §10.3). Enforced by `ir/validators.ts`. |
| [MAX\_COMPONENT\_PROPERTIES\_PER\_COMPONENT](variables/MAX_COMPONENT_PROPERTIES_PER_COMPONENT.md) | Ceiling on exposed properties per definition. Enforced by `ir/validators.ts`. |
| [MAX\_COMPONENT\_RICH\_PARAGRAPHS\_PER\_OVERRIDE](variables/MAX_COMPONENT_RICH_PARAGRAPHS_PER_OVERRIDE.md) | Ceiling on paragraphs in one rich text override. Enforced by `ir/validators.ts`. |
| [MAX\_COMPONENT\_RICH\_SPANS\_PER\_PARAGRAPH](variables/MAX_COMPONENT_RICH_SPANS_PER_PARAGRAPH.md) | Ceiling on spans per paragraph in a rich text override. Enforced by `ir/validators.ts`. |
| [MAX\_COMPONENT\_SOURCE\_NODES\_PER\_DEFINITION](variables/MAX_COMPONENT_SOURCE_NODES_PER_DEFINITION.md) | Ceiling on nodes in one definition's Source tree — ~1/6 of the measured 3,000-node baseline document, so even a page of maximum-size instances stays inside the profiled envelope. Enforced by `ir/validators.ts`. |
| [MAX\_COMPONENT\_TEXT\_OVERRIDE\_CHARS](variables/MAX_COMPONENT_TEXT_OVERRIDE_CHARS.md) | Ceiling on characters carried by ONE text override — plain string length, or the summed span text of a rich value. Bounds a hostile override without constraining real copy (the measurement port itself accepts `MAX_MEASUREMENT_TEXT_LENGTH` = 100k). Enforced by `ir/validators.ts`. |
| [MAX\_COMPONENT\_VARIANT\_AXES](variables/MAX_COMPONENT_VARIANT_AXES.md) | Ceiling on variant axes declared by one component (TD §11.1). |
| [MAX\_COMPONENT\_VARIANT\_VALUES\_PER\_AXIS](variables/MAX_COMPONENT_VARIANT_VALUES_PER_AXIS.md) | Ceiling on distinct values one variant axis may declare. |
| [MAX\_COMPONENT\_VARIANTS\_PER\_COMPONENT](variables/MAX_COMPONENT_VARIANTS_PER_COMPONENT.md) | Ceiling on **stored** variant definitions for one component. |
| [MAX\_COMPOSITE\_COMMAND\_DESCENDANTS](variables/MAX_COMPOSITE_COMMAND_DESCENDANTS.md) | Ceiling on the number of descendants one composite layout command may carry. |
| [MAX\_DOCUMENT\_NODES](variables/MAX_DOCUMENT_NODES.md) | Ceiling on nodes in one document, above which whole-document passes (validation, resolution, materialization) are not expected to meet their performance targets. |
| [MAX\_EXTERNAL\_DEPENDENCIES\_PER\_COMPONENT](variables/MAX_EXTERNAL_DEPENDENCIES_PER_COMPONENT.md) | Ceiling on **direct** dependency references declared by one external definition — the fan-out guard against a dependency bomb (TD §22.1). |
| [MAX\_EXTERNAL\_DEPENDENCY\_DEPTH](variables/MAX_EXTERNAL_DEPENDENCY_DEPTH.md) | Ceiling on dependency-closure depth (component → depends on → component …). |
| [MAX\_EXTERNAL\_DISPLAY\_STRING\_CHARS](variables/MAX_EXTERNAL_DISPLAY_STRING_CHARS.md) | Ceiling on characters in a non-authoritative catalog display string (name, description, publisher, deprecation notice). |
| [MAX\_EXTERNAL\_ENVELOPE\_BYTES](variables/MAX_EXTERNAL_ENVELOPE_BYTES.md) | Ceiling on the serialized byte size of one Provider envelope. |
| [MAX\_EXTERNAL\_REF\_FIELD\_CHARS](variables/MAX_EXTERNAL_REF_FIELD_CHARS.md) | Ceiling on characters in one field of an exact external reference (`libraryId`, `componentId`, `version`, `integrity`). |
| [MAX\_EXTERNAL\_SNAPSHOTS\_PER\_DOCUMENT](variables/MAX_EXTERNAL_SNAPSHOTS_PER_DOCUMENT.md) | Ceiling on entries in one document's `externalComponentSnapshots` registry. |
| [MAX\_EXTERNAL\_URL\_CHARS](variables/MAX_EXTERNAL_URL_CHARS.md) | Ceiling on characters in a Provider-supplied URL (release notes, thumbnail, deep link). |
| [MAX\_FINITE\_LAYOUT\_MAGNITUDE](variables/MAX_FINITE_LAYOUT_MAGNITUDE.md) | Largest finite coordinate, extent, padding, or gap magnitude that layout arithmetic is guaranteed to handle without precision loss. |
| [MAX\_MEASUREMENT\_REQUESTS](variables/MAX_MEASUREMENT_REQUESTS.md) | Ceiling on text-measurement requests issued for one layout resolution. |
| [MAX\_MEASUREMENT\_TEXT\_LENGTH](variables/MAX_MEASUREMENT_TEXT_LENGTH.md) | Ceiling on the character length of a single text-measurement request. |
| [MAX\_RETAINED\_DIAGNOSTICS](variables/MAX_RETAINED_DIAGNOSTICS.md) | Number of layout diagnostics retained before truncation. |
| [MAX\_TREE\_DEPTH](variables/MAX_TREE_DEPTH.md) | Maximum node nesting depth accepted by the IR walkers. |
| [NODE\_STYLE\_KEYS](variables/NODE_STYLE_KEYS.md) | The FR-121 compatible-property matrix: which style keys each node kind can receive. Kinds absent from a key's list IGNORE that key on paste (reported, never blocking). Rich text is deliberately base-only — its typography and fills live on spans, out of scope for node-level style transfer. |
| [RichTextParagraphSchema](variables/RichTextParagraphSchema.md) | - |
| [RichTextSpanSchema](variables/RichTextSpanSchema.md) | A rich-text span. Every style field is optional — an omitted field means "inherit from the host's default", which is resolved at measure/render time, not here. `text` may legitimately be empty (an empty span is how an editor represents a caret sitting in a freshly-split paragraph). |
| [TemplateAssetAttributionSchema](variables/TemplateAssetAttributionSchema.md) | - |
| [TemplateColorSlotSchema](variables/TemplateColorSlotSchema.md) | - |
| [TemplateFontSlotSchema](variables/TemplateFontSlotSchema.md) | - |
| [TemplateFrameSlotSchema](variables/TemplateFrameSlotSchema.md) | - |
| [TemplateGovernancePolicySchema](variables/TemplateGovernancePolicySchema.md) | Standalone — deliberately NOT nested under [CanvasTemplateDefinitionSchema](variables/CanvasTemplateDefinitionSchema.md). A marketplace/enterprise platform validates and stores this separately, keyed by `templateId`, so it can never affect normal template validation. |
| [TemplateImageSlotSchema](variables/TemplateImageSlotSchema.md) | - |
| [TemplateLicenseSchema](variables/TemplateLicenseSchema.md) | - |
| [TemplateLogoSlotSchema](variables/TemplateLogoSlotSchema.md) | - |
| [TemplateSlotSchema](variables/TemplateSlotSchema.md) | A discriminated union over the six slot kinds (FR-021), dispatched on `kind`. |
| [TemplateSourceMetaSchema](variables/TemplateSourceMetaSchema.md) | - |
| [TemplateTextSlotSchema](variables/TemplateTextSlotSchema.md) | - |
| [TemplateVariableSchema](variables/TemplateVariableSchema.md) | - |

## Functions

| Function | Description |
| ------ | ------ |
| [adjustmentBlurRadius](functions/adjustmentBlurRadius.md) | Blur radius contributed by adjustments (0 when unset). |
| [alignRects](functions/alignRects.md) | Per-rect displacement (along the edge's axis) to align every rect to the selection's bounding box: left/right/hcenter move on X, top/bottom/vcenter on Y. Returned array is index-aligned with `rects`; empty input → empty output. |
| [applyBrandColors](functions/applyBrandColors.md) | Links every literal (non-token) fill/background that matches a brand color BY VALUE (case-insensitive) to that color's `BrandTokenRef`, so future brand-color edits propagate to it. Colors with no matching brand swatch are left untouched — this transform never invents a color choice for a node, only formalizes an already-correct one into a token reference. `stroke` is deliberately NOT covered (C-17): it is `string`-typed on every node kind that has one, with no `BrandTokenRef` variant to link to — `generateBrandComplianceReport` still flags an off-brand/forbidden literal stroke color, it just can't be tokenized here. |
| [applyColorMatrixToPixels](functions/applyColorMatrixToPixels.md) | Apply a 3x4 color matrix (0..1 space) to interleaved RGBA pixel bytes in place — the editor's Konva filter delegates here so the canvas applies the exact matrix the SVG export embeds. Alpha is untouched. |
| [applyCommand](functions/applyCommand.md) | Apply one command (including a whole `batch`) and settle Source revisions once — the entry point everything user-facing goes through. |
| [applyCommandCore](functions/applyCommandCore.md) | The non-settling application primitive: applies `cmd` and returns the inverse WITHOUT the once-per-application Source-revision settle. Every composition wrapper (`batch` sub-commands here, `applyCommands` in `transaction.ts`) folds over THIS and settles once at its own boundary — calling the settling `applyCommand` per sub-command would bump a Source once per sub-command instead of once per undoable unit. Use `applyCommand` unless you are building such a wrapper. |
| [applyCommands](functions/applyCommands.md) | Apply many commands as one reversible transaction — all-or-nothing, in order, in a single fold over a local `working` IR. The caller's `ir` is never mutated; a throw from any sub-command propagates before `working`/the inverses/records are used, so nothing partial escapes. |
| [applyComponentOverrides](functions/applyComponentOverrides.md) | Apply an override map to runtime views of `definition`'s nodes (TD §10.2, in order): look up the property by ID → verify the target node still exists → verify type compatibility → patch a runtime view — or emit the orphan/type diagnostic and skip. |
| [applyMatrix](functions/applyMatrix.md) | Apply an affine matrix to a point. |
| [assertBrandPolicy](functions/assertBrandPolicy.md) | Consult brand policy through the rank-2 port (plan 0021 T-039). |
| [assertCanvasIRInvariants](functions/assertCanvasIRInvariants.md) | Throwing wrapper around [validateCanvasIRInvariants](functions/validateCanvasIRInvariants.md) for a hard trust-boundary check. |
| [assertComponentGraph](functions/assertComponentGraph.md) | Strict gate: throws [CanvasComponentGraphError](classes/CanvasComponentGraphError.md) when any ERROR-severity issue is present. Warning-only documents pass — warnings never throw (TD §19). |
| [assertLayoutInvariants](functions/assertLayoutInvariants.md) | Throwing wrapper around [validateLayoutInvariants](functions/validateLayoutInvariants.md). |
| [buildCampaignExportJobRequest](functions/buildCampaignExportJobRequest.md) | Builds one [CanvasExportJobRequest](interfaces/CanvasExportJobRequest.md) that exports every variant page from a [resizeToVariants](functions/resizeToVariants.md) call in a single batch (FR-061's "export job can export all variants in one batch"). `document` must already contain `variantPages` (apply the result's `command` first) — this only assembles the request; executing a job is a worker/host concern (canvas-m3-001), not `canvas-core`'s. |
| [buildCanvasComponentRegistrySchema](functions/buildCanvasComponentRegistrySchema.md) | - |
| [buildCanvasExternalSnapshotRegistrySchema](functions/buildCanvasExternalSnapshotRegistrySchema.md) | Build the `ir.externalComponentSnapshots` schema against a node union (plan 0021 T-014), for the same reason the Registry builder exists: a custom node kind inside an external Source tree must validate exactly like one inside a local Source tree. |
| [buildComponentGraph](functions/buildComponentGraph.md) | - |
| [buildComponentReferenceIndex](functions/buildComponentReferenceIndex.md) | - |
| [buildDetachAllAndDeleteCommand](functions/buildDetachAllAndDeleteCommand.md) | - |
| [buildDetachCommand](functions/buildDetachCommand.md) | Resolve the instance fully and allocate a persistent id for every node the detach will materialize. Throws (nothing applied) when the expansion is unsafe — missing Source or a degraded boundary — mirroring the command's own guard so a plan that builds is a plan that applies. |
| [buildExternalSnapshotIndex](functions/buildExternalSnapshotIndex.md) | - |
| [canonicalVariantKey](functions/canonicalVariantKey.md) | Encode one selection as a stable string. |
| [childrenBoundsFromExtents](functions/childrenBoundsFromExtents.md) | Width/height of the box covering every extent AND the local origin (0,0) — the shared children-bounds math for container creation (`node.group`, the editor's wrap flows). Anchoring to the origin keeps positive-coordinate content measuring from 0 while rotated/scaled content that spills into negative coordinates is still fully covered. |
| [collectNestedComponentIds](functions/collectNestedComponentIds.md) | Direct nested component references inside one Source tree (sorted, deduped). |
| [commandToChange](functions/commandToChange.md) | Derive the change record for a single command, or `null` when there is no meaningful granular record (a `batch` — its sub-commands are mapped individually by `applyCommands`). Pure: reads only the command. |
| [commandToChangeRecord](functions/commandToChangeRecord.md) | Enrich a single command into a full [CanvasChangeRecord](interfaces/CanvasChangeRecord.md), or `null` for a `batch` (mirrors `commandToChange`; `applyCommands` maps sub-commands individually). `ir` must be the pre-mutation IR so page/node lookups for commands that omit `pageId` can resolve correctly. |
| [componentSourceKey](functions/componentSourceKey.md) | A stable identity for one Source, used as the cycle-guard stack entry and as the component component of a cache key. |
| [componentSourceLabel](functions/componentSourceLabel.md) | A stable, human-readable label for a Source — diagnostics and messages only. |
| [componentSourceRefsEqual](functions/componentSourceRefsEqual.md) | Structural equality for two Sources. |
| [computeAdjustmentColorMatrix](functions/computeAdjustmentColorMatrix.md) | Compile adjustments into the combined 3x4 color transform, or `null` when every color adjustment is neutral (blur alone never needs a matrix). |
| [computePolygonVertices](functions/computePolygonVertices.md) | Vertices of a regular polygon inscribed in `bounds`, one per `sides`, evenly spaced starting at the top and going clockwise (SVG's y-down convention). The ellipse `bounds` inscribes need not be a circle — a non-square box stretches the polygon along the wider axis. |
| [computeRequiredCapabilities](functions/computeRequiredCapabilities.md) | Derive the capability list this document's CONTENT requires. |
| [computeSnap](functions/computeSnap.md) | Compute snap deltas + smart-guide overlays for a candidate rect. |
| [computeStarVertices](functions/computeStarVertices.md) | Vertices of a star inscribed in `bounds`: `points` outer tips alternating with `points` inner vertices at `innerRadiusRatio` of the outer radius (0 collapses the inner ring to the center; 1 makes every vertex outer, tracing a regular `points`-gon whose extra vertices sit exactly on its edges' midpoints). |
| [computeStylePatch](functions/computeStylePatch.md) | FR-121: intersect a style payload with the target's compatible keys. Pure — the `node.applyStyle` command and any pre-commit UI reporting both call this, so what the command applies and what the UI reports never diverge. Applying `effects` also clears a legacy `shadow` so the target has one source of truth afterwards. |
| [createAudio](functions/createAudio.md) | FR-081 — asset-reference only. No playback/rendering is implemented in canvas-core. |
| [createCanvasIR](functions/createCanvasIR.md) | - |
| [createCanvasRuntime](functions/createCanvasRuntime.md) | Resolve a runtime from zero or more extensions. The zero-extension result is identical to the static exports; registering custom node kinds rebuilds the schema pair to include them. |
| [createChangeEmitter](functions/createChangeEmitter.md) | A minimal, framework-free pub/sub for change batches. Works outside React (e.g. in the host's collab/autosave plugins). |
| [createCommandRegistry](functions/createCommandRegistry.md) | Create a command registry. `builtins` names every built-in command type (e.g. `canvas-runtime.ts`'s `BUILTIN_COMMAND_TYPES`) so `register()` can reject an attempt to shadow one instead of silently accepting a handler that dispatch will never reach. |
| [createComponentIdFactories](functions/createComponentIdFactories.md) | Build the factory set every component-creating operation (M3) receives. Pass `base` for deterministic ids in tests and replays. |
| [createComponentInstance](functions/createComponentInstance.md) | Build a `component-instance` node (plan 0023 M1-09, plan 0021 T-012). |
| [createComponentResolutionCache](functions/createComponentResolutionCache.md) | - |
| [createEllipse](functions/createEllipse.md) | - |
| [createFrame](functions/createFrame.md) | - |
| [createGroup](functions/createGroup.md) | - |
| [createImage](functions/createImage.md) | - |
| [createLine](functions/createLine.md) | - |
| [createMigrationRegistry](functions/createMigrationRegistry.md) | Create a registry pre-seeded with [CANVAS\_IR\_MIGRATIONS](variables/CANVAS_IR_MIGRATIONS.md); `register` adds extension steps (re-registering a `from` overrides the earlier step). |
| [createNodeKindRegistry](functions/createNodeKindRegistry.md) | Create a node-kind registry, optionally seeded with built-in definitions. Seeding is exempt from the shadow guard; later `register()` calls are not — mirroring the built-in command-type protection in `createCanvasRuntime`. |
| [createPage](functions/createPage.md) | - |
| [createPath](functions/createPath.md) | - |
| [createPolygon](functions/createPolygon.md) | - |
| [createRect](functions/createRect.md) | - |
| [createResolvedView](functions/createResolvedView.md) | Wrap a resolved document in the [CanvasResolvedView](interfaces/CanvasResolvedView.md) read adapter. |
| [createRichText](functions/createRichText.md) | A rich-text block. `width` defaults to `bounds.width`, so the two agree unless a caller deliberately separates them — see [CanvasRichTextNode](interfaces/CanvasRichTextNode.md) for why both exist. |
| [createStar](functions/createStar.md) | - |
| [createSvg](functions/createSvg.md) | FR-016 — asset-reference only. There is no options field for inline markup. |
| [createText](functions/createText.md) | - |
| [createTreeAccess](functions/createTreeAccess.md) | - |
| [createVideo](functions/createVideo.md) | FR-081 — asset-reference only. No playback/rendering is implemented in canvas-core. |
| [decodeResolvedNodeId](functions/decodeResolvedNodeId.md) | Decode a resolved id back to its identity path. |
| [decomposeMatrix](functions/decomposeMatrix.md) | Decompose an affine matrix back into translate/rotate/skew/scale components, the exact inverse of [toAffineMatrix](functions/toAffineMatrix.md) (and of Konva's `Transform.decompose`). For a transform with no `skewY`, `decomposeMatrix(toAffineMatrix(t))` round-trips `t`; for the general case the recomposed matrix `toAffineMatrix(decomposeMatrix(m))` equals `m`. |
| [distributeRects](functions/distributeRects.md) | Per-rect displacement (along `axis`) to even out the gaps between rects, keeping the first and last (by position) fixed — like Figma's "distribute spacing". Returned array is index-aligned with `rects`; needs ≥3 rects, otherwise all zeros. |
| [encodeResolvedNodeId](functions/encodeResolvedNodeId.md) | Encode an identity path as an opaque resolved id. |
| [extractNodeStyle](functions/extractNodeStyle.md) | FR-120: extract the portable style of a node — only keys its kind owns per the matrix, only values actually present. A frame's `background` extracts as `fill`; a legacy `shadow` extracts as its `effects` equivalent. |
| [findComponentProperty](functions/findComponentProperty.md) | Resolve a property id WITHIN one definition — the only lookup scope that exists. There is deliberately no cross-definition index to consult, which is what makes cross-definition Property-ID reuse safe. |
| [findForeignComponentRefs](functions/findForeignComponentRefs.md) | - |
| [findNode](functions/findNode.md) | - |
| [findNodeInSubtree](functions/findNodeInSubtree.md) | Find a node by id within ONE tree — a page's or a Component Source's — including the root itself. The scoped complement of `findNode`, which is pages-only by contract (plan 0023 M3-01). Depth-guarded like every walker. |
| [findSizePreset](functions/findSizePreset.md) | Looks up a preset in [CANVAS\_SIZE\_PRESETS](variables/CANVAS_SIZE_PRESETS.md) by its `id`. |
| [firstDropShadow](functions/firstDropShadow.md) | The first drop shadow in a resolved effect list (what a one-shadow UI edits). |
| [flattenCanvasLayout](functions/flattenCanvasLayout.md) | Replace Auto Layout intent with the absolute geometry it currently resolves to. |
| [generateBrandComplianceReport](functions/generateBrandComplianceReport.md) | Walks `document` and reports every unresolved brand-token reference, forbidden color/font (per `brandKit.rules`), and off-brand literal color/font value (a literal that matches neither a brand color/font nor an `allowed-*` rule — only raised when the brand kit actually defines colors/fonts to compare against, so an empty kit never floods the report). Pure and read-only: never mutates `document`. |
| [getDefinition](functions/getDefinition.md) | Resolve a Source to its definition, from the local Registry or the admitted snapshot registry (TD §10). |
| [hitTest](functions/hitTest.md) | The top-most node whose box contains `world`. Nodes are taken in paint order (later siblings paint on top, matching `page.root.children`), so the last match wins. Returns null when nothing is hit. |
| [hitTestResolved](functions/hitTestResolved.md) | Resolved-record variant of [hitTest](functions/hitTest.md): same paint-order semantics (last match wins) and the same skip flags, read from each record's source node. |
| [insertNode](functions/insertNode.md) | - |
| [instantiateTemplate](functions/instantiateTemplate.md) | Instantiates a [CanvasTemplateDefinition](interfaces/CanvasTemplateDefinition.md) into a fresh, normal Canvas IR document (FR-022): every node/page gets a brand-new id (via `idFactory`, so the same template can be instantiated any number of times without id collisions), `variables` are resolved and stamped onto their slots' target nodes, and `lockedNodeIds` are never mutated regardless of what a variable would otherwise write there. |
| [invertMatrix](functions/invertMatrix.md) | Invert an affine matrix. Throws if the matrix is singular (its determinant is zero or non-finite), e.g. a zero-scale transform that collapses a dimension. |
| [isContainerNode](functions/isContainerNode.md) | True for any node that holds `children` — currently `group` and `frame`. |
| [isExternalSourceRef](functions/isExternalSourceRef.md) | Narrow a source ref to the external variant. |
| [isFrameNode](functions/isFrameNode.md) | True only for a literal `frame`. |
| [isGroupNode](functions/isGroupNode.md) | True only for a literal `group`. Prefer [isContainerNode](functions/isContainerNode.md) when the question is "does this node have children"; use this one only when the behaviour is specific to groups (e.g. ungrouping, which a frame does not support). |
| [isIdentityAdjustments](functions/isIdentityAdjustments.md) | True when the adjustment set changes nothing (safe to skip filtering). |
| [isLeafNode](functions/isLeafNode.md) | - |
| [isLocalObjectUri](functions/isLocalObjectUri.md) | The schemes that name **browser-local bytes**: `blob:` and `filesystem:`. |
| [isLocalSourceRef](functions/isLocalSourceRef.md) | Narrow a source ref to the document-local variant. |
| [isNodeLocked](functions/isNodeLocked.md) | Whether `nodeId` is locked by `definition`. The single source of truth for this check — canvas-m2-003's `instantiateTemplate` and any editor-side "can I edit this node" gate must call this rather than re-reading `lockedNodeIds` themselves. |
| [isNodeOfKind](functions/isNodeOfKind.md) | - |
| [layoutIssuesToExportWarnings](functions/layoutIssuesToExportWarnings.md) | Public face of the `layout/` domain — imported only by the root barrel. |
| [localComponentIdOf](functions/localComponentIdOf.md) | The registry key for a **local** Source, or `undefined` for an external one. |
| [marqueeHits](functions/marqueeHits.md) | Nodes selected by a marquee rectangle (in world coordinates). Uses each node's rotation-aware world AABB; `contained` switches from any-overlap to fully-enclosed. Returns matches in input order. |
| [marqueeHitsResolved](functions/marqueeHitsResolved.md) | Resolved-record variant of [marqueeHits](functions/marqueeHits.md): reads the resolver's world AABB instead of recomputing one, so nested/transformed ancestors are accounted for by construction. Returns matches in input order. |
| [materializeCanvasLayout](functions/materializeCanvasLayout.md) | Write a resolution's geometry into the document and stamp its freshness. |
| [materializeClipboardNodes](functions/materializeClipboardNodes.md) | Prepare a validated payload for insertion into `target` (FR-021): |
| [materializeExportVariant](functions/materializeExportVariant.md) | Derive an `export-variant` document from `ir`: detach every page instance (recursively — Source-tree instances vanish with the Registry), drop `components`, and strip the component capabilities from `compatibility.requiredCapabilities`. Pure — `ir` is never mutated. |
| [matrixBoundsExtent](functions/matrixBoundsExtent.md) | Axis-aligned bounding box of a `width`×`height` box anchored at the local origin, after applying an already-composed matrix. |
| [migrateCanvasIR](functions/migrateCanvasIR.md) | Parse + forward-migrate an untrusted/persisted IR to the current schema version, then validate it. This is the single seam for schema evolution. |
| [moveNode](functions/moveNode.md) | - |
| [multiplyMatrix](functions/multiplyMatrix.md) | Multiply two affine matrices. The result applies `m2` first, then `m1` — i.e. `applyMatrix(multiplyMatrix(m1, m2), p) === applyMatrix(m1, applyMatrix(m2, p))`. Use `multiplyMatrix(parentMatrix, childMatrix)` to compose a child transform into its parent's coordinate space. |
| [nodeCarriesLayoutIntent](functions/nodeCarriesLayoutIntent.md) | Does this node carry layout intent that a reader must understand `layout.auto.v1` to honour? |
| [nodeWorldAabb](functions/nodeWorldAabb.md) | World-space axis-aligned bounding box of a node, accounting for its rotation/scale/skew. Pass `parentMatrix` to place a node nested under a transformed ancestor; omit it for top-level (world-space) nodes. |
| [normalizeTypography](functions/normalizeTypography.md) | Applies a brand typography preset's `fontSize`/`fontWeight` uniformly to every `text` node (unlike [applyBrandColors](functions/applyBrandColors.md)/[replaceFonts](functions/replaceFonts.md), which only formalize an already-matching value, this one overwrites — "normalize" means bringing every text element onto the same scale, not linking an existing coincidental match). Font family is untouched — that's [replaceFonts](functions/replaceFonts.md)' concern. |
| [omitEmptyComponents](functions/omitEmptyComponents.md) | - |
| [pageCarriesLayoutIntent](functions/pageCarriesLayoutIntent.md) | Does any node on this page carry layout intent (see [nodeCarriesLayoutIntent](functions/nodeCarriesLayoutIntent.md))? One `walkPage` pass; used by consumers that need to know whether stored geometry alone can be trusted for a page — e.g. the SVG serializer deciding whether to warn `LAYOUT_UNRESOLVED`. |
| [pageOf](functions/pageOf.md) | - |
| [parentOf](functions/parentOf.md) | - |
| [parseClipboardPayload](functions/parseClipboardPayload.md) | Parse serialized clipboard text (byte-size cap → JSON → full validation). This is the entry point for text arriving from the SYSTEM clipboard — treat it as hostile (PRD §14.1). |
| [pointInNode](functions/pointInNode.md) | True when `world` falls inside a node's box, accounting for rotation/scale/ skew. A degenerate (zero-area / non-invertible) node contains nothing. |
| [pointInResolvedNode](functions/pointInResolvedNode.md) | Resolved-record variant of [pointInNode](functions/pointInNode.md): containment against the record's world transform. Takes no parent matrix — `worldTransform` is already fully composed. |
| [regenerateNodeIds](functions/regenerateNodeIds.md) | Deep-copy a node subtree and give every node a brand-new id, preserving hierarchy, order, and every other field (M0-05, PRD 0012 §9.2). This is THE shared id-regeneration primitive behind duplicate, paste, page cloning, and template instantiation — do not hand-roll another remap loop. |
| [removeNode](functions/removeNode.md) | - |
| [reorderChildren](functions/reorderChildren.md) | - |
| [replaceChildrenInParent](functions/replaceChildrenInParent.md) | Rewrite a single parent container's `children` in one immutable pass. The building block the command layer uses for batch sibling edits (group / ungroup) so they don't pay one full tree clone per affected child. |
| [replaceFonts](functions/replaceFonts.md) | Links every literal font-family that matches a brand font BY NAME (case-insensitive) to that font's `BrandTokenRef`. `rich-text` spans are out of scope here (canvas-m1-009's MVP applies span-level style uniformly via the editor, not per-property core transforms) — this covers `text` nodes, the common case. |
| [replaceLogoPlaceholders](functions/replaceLogoPlaceholders.md) | Fills every EMPTY logo-kind frame placeholder with the brand's logo (the first asset tagged `kind: "logo"`, else the first logo asset). A placeholder that already has an `assetId` or `assetToken` is left alone — this only fills gaps, it never replaces a deliberate choice. |
| [replaceNode](functions/replaceNode.md) | Replace the subtree rooted at `id` with `node` — id and kind may BOTH change, unlike `updateNode`, which pins them. The building block for detach-style materialization (plan 0023 M3-07): swap one node for another at the exact same tree position. A page root may only be replaced by another group (a page root is a group by contract); a Component Source root may be replaced by any node. |
| [replayChanges](functions/replayChanges.md) | Deterministically replay a sequence of change records onto an initial IR by re-applying each record's original `command` in order via `applyCommand`. Ignores each command's own inverse; only the resulting `ir` is threaded. |
| [resizeToVariants](functions/resizeToVariants.md) | Generates one new, fully editable page per preset from an existing page's content (FR-061, canvas-m3-007). Resolves PRD open question 3: variants are pages **within the same document**, not separate documents — this keeps a campaign's variants as one undo-able, exportable unit and matches how `instantiateTemplate` already batches `page.create` for templates (canvas-m2-003). A separate-document target was considered and rejected for this MVP; nothing here forecloses adding it later if a real product need surfaces. |
| [resolveCanvasDocument](functions/resolveCanvasDocument.md) | Resolve a document END TO END: expand every component instance into its virtual subtree, then run the Auto Layout solver over the expanded document, then attach provenance to every virtual record. The ONLY resolution path renderers, hit testing, a11y, and export may use (PRD §9.13) — never `resolveCanvasLayout` directly for component-bearing documents. |
| [resolveCanvasLayout](functions/resolveCanvasLayout.md) | Resolve a document's Auto Layout into one shared geometry tree. |
| [resolveCommentAnchor](functions/resolveCommentAnchor.md) | Resolve whether an anchor's target still exists in `ir`. Never throws — a missing target resolves to `"archived"` with a `reason`, never a thrown error or a silently dangling reference, so a comment UI can always render *something* for a stale anchor instead of crashing. |
| [resolveComponentInstance](functions/resolveComponentInstance.md) | Resolve ONE instance (T-RES-1): registry lookup → graph/depth/limit validation → override application → nested recursion → virtual subtree + origins + diagnostics + cache key. Pure and deterministic: identical inputs produce identical output, diagnostics in identical order (INV-5). |
| [resolveComponentVariant](functions/resolveComponentVariant.md) | Resolve a (possibly partial, possibly stale) selection to a concrete variant. |
| [resolveDefinitionVariant](functions/resolveDefinitionVariant.md) | Convenience: resolve against whatever variant set a definition carries. |
| [resolveFrameClipShape](functions/resolveFrameClipShape.md) | The ONE frame-clip resolver (ADR 0008 decision 2) — every consumer (the editor's Konva `clipFunc`, the SVG serializer's `<clipPath>`, the inspector) resolves a frame's clip through this, so canvas, export, and UI can never disagree about what a frame clips to. It is the mask counterpart of [resolveNodeEffects](functions/resolveNodeEffects.md) and follows the same contract: pure, total, and never throwing — a frame it cannot honour degrades rather than failing. |
| [resolveInlineExportDocument](functions/resolveInlineExportDocument.md) | Resolves a [CanvasExportJobSource](type-aliases/CanvasExportJobSource.md) to a validated, current-version `CanvasIR` — only when it carries the document inline. A `documentRef` source is a host/worker resolution concern; `canvas-core` never attempts to resolve one itself and this function throws rather than guessing. |
| [resolveNodeEffects](functions/resolveNodeEffects.md) | The ONE effect resolver (C-03, §9.4) — every consumer (the editor's Konva renderer, the SVG serializer, the inspector) resolves a node's effects through this so canvas, export, and UI can never disagree. |
| [resolveSpanStyle](functions/resolveSpanStyle.md) | Resolve one span against the host's defaults. Pure, allocation-only, and deliberately dumb — it is field inheritance, not layout. |
| [resolveTemplateVariables](functions/resolveTemplateVariables.md) | Fills in `definition.variables` against a caller-supplied value map: a supplied value wins, otherwise `defaultValue` applies, otherwise a `required` variable produces a structured warning (never a thrown error) and is simply absent from `values`. |
| [screenToWorld](functions/screenToWorld.md) | Map a screen (stage-pixel) point back to world (canvas) coordinates. Throws if `zoom` is 0 (a degenerate, non-invertible viewport). |
| [serializeDocumentToPdf](functions/serializeDocumentToPdf.md) | Serialize a [CanvasIR](interfaces/CanvasIR.md) document to a multi-page PDF, one PDF page per selected [CanvasPage](interfaces/CanvasPage.md), with each page's pre-rendered raster drawn to fill it. Page count always equals the number of selected pages; pages whose raster is missing/undecodable/unsupported are emitted blank with a corresponding entry in `warnings`. |
| [serializePageToSvg](functions/serializePageToSvg.md) | Serialize a single page of a [CanvasIR](interfaces/CanvasIR.md) document to an SVG string. |
| [settleComponentRevisions](functions/settleComponentRevisions.md) | Bump each touched Source's `revision` exactly once for one applied command/batch/transaction (plan 0023 M3-02, LC-PROPAGATE). |
| [shadowToDropShadowEffect](functions/shadowToDropShadowEffect.md) | Lift a legacy `CanvasShadow` into the effect model (spread = absent). |
| [snapRectFromExtent](functions/snapRectFromExtent.md) | View a world-space extent (e.g. a resolved layout record's `worldAabb`) as a [SnapRect](interfaces/SnapRect.md), so snap/align/distribute can consume resolved geometry without every call site hand-rolling the conversion. |
| [tidyUpRects](functions/tidyUpRects.md) | FR-072 "Tidy Up" (C-12): arrange a rough scatter into a clean grid. Rects cluster into rows by vertical-center proximity (threshold: half the median height), each row lays out left-to-right from the selection's left edge, and rows stack from its top edge — with uniform gaps derived from the MEDIAN existing horizontal/vertical gaps (clamped ≥ 0), so a tidy of an already-tidy grid is a no-op rather than a re-spacing. Returned array is index-aligned with `rects`; fewer than 2 rects → all zeros. Pure. |
| [toAffineMatrix](functions/toAffineMatrix.md) | Compose a transform into an affine matrix, replicating Konva's `Node.getTransform` order exactly: translate → rotate → skew → scale. (CanvasIR has no `offset`, so the trailing offset translate is always 0.) |
| [toResolvedNodeId](functions/toResolvedNodeId.md) | Brand a source node id as a resolved id. |
| [toSvgColorMatrix](functions/toSvgColorMatrix.md) | The 20-value `feColorMatrix type="matrix"` form: each RGBA output row is `[r g b a offset]`. Our 3x4 rows are `[r g b offset]` with no alpha dependence, so an alpha coefficient of 0 is inserted before each offset, and the alpha row is identity. |
| [transformedBoundsExtent](functions/transformedBoundsExtent.md) | Axis-aligned bounding box, in the parent coordinate space, of a `width`×`height` box anchored at the local origin after applying `transform`. Accounts for rotation/scale/skew, unlike a naive `x + width` extent. |
| [unitToPt](functions/unitToPt.md) | Convert a page dimension to PDF points. `in`/`mm` are physical; `px` is scaled through `dpi` so a screen-pixel page maps to a sensible physical size. Mirrors the unit semantics of `unitToPx` in `./svg.ts` (which shares `DEFAULT_DPI`). |
| [updateNode](functions/updateNode.md) | - |
| [validateAiDesignJobResult](functions/validateAiDesignJobResult.md) | Validates an `AiDesignJobResult`'s payload against the normal Canvas IR / command schemas before it is safe to apply to a real document (FR-052, canvas-m4-003). Pure — never mutates anything and never applies the command itself; it only decides whether the payload is safe, normalizing either payload shape to one ready-to-commit `CanvasBatchCommand`. A non-`"complete"` job, or one whose payload fails validation, is quarantined: this returns `ok: false` with a structured error rather than silently dropping content or force-coercing it into the document. |
| [validateCanvasIRInvariants](functions/validateCanvasIRInvariants.md) | Validate document-wide semantic invariants a Zod schema cannot express. Pure and read-only; never throws for a malformed-but-schema-valid `CanvasIR` — malformations are reported as issues, not exceptions (use [assertCanvasIRInvariants](functions/assertCanvasIRInvariants.md) for a throwing variant). Runs one `walk` over the whole document (O(n) in total node count) plus O(pages) and O(assets) passes. |
| [validateClipboardPayload](functions/validateClipboardPayload.md) | Validate an already-parsed candidate payload: version, schema, node-count cap, and tree-depth cap. Throws [CanvasClipboardError](classes/CanvasClipboardError.md); returns the schema-validated payload (unknown keys preserved). |
| [validateComponentGraph](functions/validateComponentGraph.md) | - |
| [validateComponentVariantSet](functions/validateComponentVariantSet.md) | Validate a variant set against the definition that carries it. |
| [validateLayoutInvariants](functions/validateLayoutInvariants.md) | Validate Auto Layout semantics across a document (TD §6.2, level 3). |
| [validateTemplateReferences](functions/validateTemplateReferences.md) | Checks that every [TemplateSlot](type-aliases/TemplateSlot.md)'s `nodeId` and every `lockedNodeIds` entry on `definition` actually resolves to a node inside `definition.document`. Returns an empty array when everything resolves. |
| [variantPropertyTarget](functions/variantPropertyTarget.md) | The node a property binds to for a given variant. |
| [viewportMatrix](functions/viewportMatrix.md) | Affine matrix mapping world (canvas) coordinates to screen (stage-pixel) coordinates: `screen = world * zoom + pan`. |
| [walk](functions/walk.md) | - |
| [walkDocument](functions/walkDocument.md) | Walk EVERY node tree a document owns: each page's tree (document order), then each Component Source tree in sorted component-id order (plan 0023 TD-001 — deterministic, so issue ordering built on this walk is byte-stable). Each root gets its own `MAX_TREE_DEPTH` budget, the same hostile-depth guard `walk` applies. |
| [walkPage](functions/walkPage.md) | - |
| [withComputedCapabilities](functions/withComputedCapabilities.md) | Return `ir` with `compatibility` refreshed from its content — the save-path helper. |
| [worldToScreen](functions/worldToScreen.md) | Map a world (canvas) point to screen (stage-pixel) coordinates. |
