---
title: "CanvasComponentOverrideSchema"
description: "@anvilkit/canvas-core · CanvasComponentOverrideSchema"
---

```ts
const CanvasComponentOverrideSchema: z.ZodType<CanvasComponentOverride>;
```

Defined in: [ir/validators.ts:765](https://github.com/ancyloce/anvilkit-studio/blob/main/src/ir/validators.ts#L765)

## File

Schema surface of the `components/` domain (plan 0023 M1-05).

The persisted schemas live in `ir/validators.ts` (rank 1) — see the
layering note in `index.ts` — and are re-exported here. Resource caps
(M1-07, D-3) join this surface from `limits.ts`.
