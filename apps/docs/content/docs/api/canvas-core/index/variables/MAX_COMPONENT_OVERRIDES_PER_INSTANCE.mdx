---
title: "MAX_COMPONENT_OVERRIDES_PER_INSTANCE"
description: "@anvilkit/canvas-core · MAX_COMPONENT_OVERRIDES_PER_INSTANCE"
---

```ts
const MAX_COMPONENT_OVERRIDES_PER_INSTANCE: 128 = 128;
```

Defined in: [limits.ts:136](https://github.com/ancyloce/anvilkit-studio/blob/main/src/limits.ts#L136)

Ceiling on override-map entries per instance — 2× the properties cap,
because orphaned overrides are retained rather than dropped (TD §10.3).
Enforced by `ir/validators.ts`.
