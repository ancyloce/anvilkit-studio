---
title: "PdfSerializeOptions"
description: "@anvilkit/canvas-core · PdfSerializeOptions"
---

Defined in: [serialize/pdf.ts:217](https://github.com/ancyloce/anvilkit-studio/blob/main/src/serialize/pdf.ts#L217)

## Properties

| Property | Type | Description | Defined in |
| ------ | ------ | ------ | ------ |
| &lt;a id="author"&gt;&lt;/a&gt; `author?` | `string` | Written to the PDF document's author metadata. | [serialize/pdf.ts:230](https://github.com/ancyloce/anvilkit-studio/blob/main/src/serialize/pdf.ts#L230) |
| &lt;a id="dpi"&gt;&lt;/a&gt; `dpi?` | `number` | Fallback px→pt DPI when a page omits `size.dpi`. Defaults to [DEFAULT\_DPI](../variables/DEFAULT_DPI). | [serialize/pdf.ts:226](https://github.com/ancyloce/anvilkit-studio/blob/main/src/serialize/pdf.ts#L226) |
| &lt;a id="pages"&gt;&lt;/a&gt; `pages?` | (`string` \| `number`)[] | Pages to emit, by id or index, in order. Defaults to every page in IR order. An unknown selector throws; an explicitly empty list throws (no PDF pages). | [serialize/pdf.ts:224](https://github.com/ancyloce/anvilkit-studio/blob/main/src/serialize/pdf.ts#L224) |
| &lt;a id="print"&gt;&lt;/a&gt; `print?` | [`CanvasPrintPdfMetadata`](CanvasPrintPdfMetadata.md) | Print-safety metadata (FR-043, canvas-m3-004). When present, every successfully-embedded raster's EFFECTIVE dpi (its natural pixel size divided by the page's print size in inches) is compared against `print.dpi` (default 150 DPI); a raster below it warns with `PRINT_UNSAFE` rather than failing the export. `colorMode` is advisory only — see [CanvasPrintPdfMetadata.colorMode](CanvasPrintPdfMetadata.md#colormode). | [serialize/pdf.ts:246](https://github.com/ancyloce/anvilkit-studio/blob/main/src/serialize/pdf.ts#L246) |
| &lt;a id="rasters"&gt;&lt;/a&gt; `rasters` | [`PdfRasterPage`](PdfRasterPage.md)[] | Pre-rendered page rasters. A selected page with no matching entry is blank. | [serialize/pdf.ts:219](https://github.com/ancyloce/anvilkit-studio/blob/main/src/serialize/pdf.ts#L219) |
| &lt;a id="title"&gt;&lt;/a&gt; `title?` | `string` | Written to the PDF document's title metadata. | [serialize/pdf.ts:228](https://github.com/ancyloce/anvilkit-studio/blob/main/src/serialize/pdf.ts#L228) |
| &lt;a id="validate"&gt;&lt;/a&gt; `validate?` | `boolean` | Run [CanvasIRSchema](../variables/CanvasIRSchema) over `ir` before emitting and throw on failure (default `false`). Independently of this flag, a page whose computed dimensions are non-finite throws a `RangeError` rather than producing a corrupt PDF (`pdf-lib` would otherwise receive `NaN` page bounds). | [serialize/pdf.ts:237](https://github.com/ancyloce/anvilkit-studio/blob/main/src/serialize/pdf.ts#L237) |
