---
title: "SvgSerializeOptions"
description: "@anvilkit/canvas-core · SvgSerializeOptions"
---

Defined in: [serialize/svg.ts:1172](https://github.com/ancyloce/anvilkit-studio/blob/main/src/serialize/svg.ts#L1172)

## Properties

| Property | Type | Description | Defined in |
| ------ | ------ | ------ | ------ |
| &lt;a id="fetchasset"&gt;&lt;/a&gt; `fetchAsset?` | [`SvgFetchAsset`](../type-aliases/SvgFetchAsset) | - | [serialize/svg.ts:1174](https://github.com/ancyloce/anvilkit-studio/blob/main/src/serialize/svg.ts#L1174) |
| &lt;a id="fonts"&gt;&lt;/a&gt; `fonts?` | [`SvgFontFaceDef`](SvgFontFaceDef.md)[] | - | [serialize/svg.ts:1175](https://github.com/ancyloce/anvilkit-studio/blob/main/src/serialize/svg.ts#L1175) |
| &lt;a id="images"&gt;&lt;/a&gt; `images?` | [`SvgImageMode`](../type-aliases/SvgImageMode) | - | [serialize/svg.ts:1173](https://github.com/ancyloce/anvilkit-studio/blob/main/src/serialize/svg.ts#L1173) |
| &lt;a id="nodekinds"&gt;&lt;/a&gt; `nodeKinds?` | [`CanvasNodeKindRegistry`](CanvasNodeKindRegistry.md) | Node-kind registry (typically `runtime.nodeKinds`). A node whose `type` is not a built-in kind is emitted via its registered `toSvg` hook; without one it is skipped with an `UNKNOWN_KIND_SKIPPED` warning. | [serialize/svg.ts:1193](https://github.com/ancyloce/anvilkit-studio/blob/main/src/serialize/svg.ts#L1193) |
| &lt;a id="pretty"&gt;&lt;/a&gt; `pretty?` | `boolean` | Newline-indent the output for readable/golden snapshots (default `false`). | [serialize/svg.ts:1179](https://github.com/ancyloce/anvilkit-studio/blob/main/src/serialize/svg.ts#L1179) |
| &lt;a id="resolvebrandtoken"&gt;&lt;/a&gt; `resolveBrandToken?` | [`SvgResolveBrandToken`](../type-aliases/SvgResolveBrandToken) | Resolve `BrandTokenRef` fills/fonts to concrete values. See [SvgResolveBrandToken](../type-aliases/SvgResolveBrandToken). | [serialize/svg.ts:1210](https://github.com/ancyloce/anvilkit-studio/blob/main/src/serialize/svg.ts#L1210) |
| &lt;a id="richtextdefaults"&gt;&lt;/a&gt; `richTextDefaults?` | `Partial`\&lt;[`RichTextStyleDefaults`](RichTextStyleDefaults.md)\&gt; | Fallbacks for span style fields a document leaves unset. Merged over the built-in defaults, which mirror `createText`'s. | [serialize/svg.ts:1208](https://github.com/ancyloce/anvilkit-studio/blob/main/src/serialize/svg.ts#L1208) |
| &lt;a id="skipinvisible"&gt;&lt;/a&gt; `skipInvisible?` | `boolean` | Skip nodes with `visible === false` (default `true`). | [serialize/svg.ts:1177](https://github.com/ancyloce/anvilkit-studio/blob/main/src/serialize/svg.ts#L1177) |
| &lt;a id="textmeasurer"&gt;&lt;/a&gt; `textMeasurer?` | [`CanvasTextMeasurer`](../type-aliases/CanvasTextMeasurer) | Lays out `rich-text` paragraphs. Core cannot measure glyphs (no DOM, no font metrics — see [CanvasTextMeasurer](../type-aliases/CanvasTextMeasurer)), so wrapping is only possible when the host injects its measurer here. The editor's measurer is the same one it renders with, which is what makes an export match what the user saw. Omit it and rich text still exports deterministically — one line per paragraph, flagged with `RICH_TEXT_WRAP_APPROXIMATE`. | [serialize/svg.ts:1203](https://github.com/ancyloce/anvilkit-studio/blob/main/src/serialize/svg.ts#L1203) |
| &lt;a id="validate"&gt;&lt;/a&gt; `validate?` | `boolean` | Run [CanvasIRSchema](../variables/CanvasIRSchema) over `ir` before emitting and throw on failure (default `false`). The serializer trusts its input and `fmt` coerces any non-finite number to `"0"` to keep the output well-formed; enable this to fail fast on a malformed/un-validated IR (NaN/Infinity, wrong types) instead of silently emitting zeros. | [serialize/svg.ts:1187](https://github.com/ancyloce/anvilkit-studio/blob/main/src/serialize/svg.ts#L1187) |
