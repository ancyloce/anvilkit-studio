---
title: "概要ページ"
description: "概要ページ — ヒーロー、ミッションセクション、統計、顧客ロゴクラウド。"
---

![概要ページ preview](/templates/about/preview.png)

<div className="anvilkit-template-meta">
	<code>@anvilkit/template-about</code> <span>·</span> <span>v1.0.2</span>
</div>

## CLI からスキャフォールド

```sh
npx anvilkit init --template about my-site
```

## 構成

コミット済みの `PageIR` は、ルートに次のノードタイプを配線します：

- `Navbar`
- `Hero`
- `Section`
- `Statistics`
- `LogoClouds`

## パッケージの詳細

会社概要 / ミッションページ。

ナビゲーションバー、ヒーロー、ミッションセクション、統計、そして顧客ロゴクラウド。

## インストール

```sh
npx anvilkit init --template about my-site
```

## 構成

このテンプレートは以下のコンポーネントパッケージを組み合わせています:

- `@anvilkit/hero`
- `@anvilkit/logo-clouds`
- `@anvilkit/navbar`
- `@anvilkit/section`
- `@anvilkit/statistics`

## 編集

正規の `PageIR` ツリーは `src/page-ir.json` にコミットされています。このパッケージのデフォルトエクスポートは、その IR をマニフェストフィールド（slug、name、description、preview、パッケージリスト）とともに `AnvilkitTemplate` にまとめます。

`AnvilkitTemplate` の契約については `docs/decisions/003-core-templates-subpath.md` を参照してください。
