---
title: "博客索引页面"
description: "博客索引页面 —— 导航栏、区块标题、文章列表。"
---

![博客索引页面 preview](/templates/blog-index/preview.png)

<div className="anvilkit-template-meta">
	<code>@anvilkit/template-blog-index</code> <span>·</span> <span>v1.0.2</span>
</div>

## 通过 CLI 脚手架生成

```sh
npx anvilkit init --template blog-index my-site
```

## 组成结构

提交的 `PageIR` 在根节点连接了以下节点类型：

- `Navbar`
- `Section`
- `BlogList`

## 包详情

一个博客落地页 / 索引页。

导航栏、板块标题，以及一个分页的文章列表。

## 安装

```sh
npx anvilkit init --template blog-index my-site
```

## 组合

该模板组合了以下组件包：

- `@anvilkit/blog-list`
- `@anvilkit/navbar`
- `@anvilkit/section`

## 编辑

规范的 `PageIR` 树提交在 `src/page-ir.json` 中。该包的默认导出会将该 IR 与清单字段（slug、name、description、preview、包列表）打包为一个 `AnvilkitTemplate`。

有关 `AnvilkitTemplate` 契约，请参阅 `docs/decisions/003-core-templates-subpath.md`。
