name: Generator smoke

# phase4-013 — weekly smoke test for the published
# `create-anvilkit-plugin` generator.
#
# Runs `pnpm dlx create-anvilkit-plugin@latest` against a tmp
# directory with representative flags, then installs, builds, and
# tests the generated output. Fails and pings the maintainer on
# regression.
#
# Intentionally OUT of PR gating: this exercises the published
# tarball, not the current branch. Per-PR safety for the scaffolder
# is covered by `pnpm -F create-anvilkit-plugin test` in `ci.yml`.

on:
  schedule:
    # Mondays 06:47 UTC — off-peak, weekly.
    - cron: "47 6 * * 1"
  workflow_dispatch:

jobs:
  smoke:
    name: Scaffold, install, build, test the published generator
    runs-on: ubuntu-latest
    permissions:
      contents: read
      issues: write
    timeout-minutes: 15
    steps:
      - uses: pnpm/action-setup@v4
        with:
          version: 10.33.0
          run_install: false

      - uses: actions/setup-node@v4
        with:
          node-version: 20

      # Pre-flight: confirm `@latest` resolves on npm. Catches the
      # "first-GA not yet cut" state explicitly with a readable error
      # instead of letting `pnpm dlx` fail with a generic resolution
      # message. phase5-019 phase4-013 — the first `@latest` publish
      # flips this step from a no-op past its first run.
      - name: Verify create-anvilkit-plugin@latest exists on npm
        id: check-dist-tag
        run: |
          set -e
          VERSION="$(npm view create-anvilkit-plugin@latest version 2>/dev/null || echo '')"
          if [ -z "$VERSION" ]; then
            echo "::error::create-anvilkit-plugin has no @latest dist-tag on npm."
            echo "::error::Trigger publish.yml with mode=publish-only after merging the Version PR,"
            echo "::error::or run 'npm dist-tag add create-anvilkit-plugin@<version> latest' manually."
            exit 1
          fi
          echo "Resolved create-anvilkit-plugin@latest => $VERSION"
          echo "version=$VERSION" >> "$GITHUB_OUTPUT"

      - name: Scaffold a plugin from the latest published generator
        id: scaffold
        run: |
          WORKDIR="$(mktemp -d)"
          echo "workdir=$WORKDIR" >> "$GITHUB_OUTPUT"
          cd "$WORKDIR"
          echo "Using create-anvilkit-plugin@${{ steps.check-dist-tag.outputs.version }}"
          pnpm dlx create-anvilkit-plugin@latest \
            --name hello-plugin \
            --display "Hello Plugin" \
            --category rail-panel \
            --dir "$WORKDIR"

      - name: Install generated package
        working-directory: ${{ steps.scaffold.outputs.workdir }}/hello-plugin
        run: pnpm install --ignore-workspace --no-frozen-lockfile

      - name: Build generated package
        working-directory: ${{ steps.scaffold.outputs.workdir }}/hello-plugin
        run: pnpm build

      - name: Test generated package
        working-directory: ${{ steps.scaffold.outputs.workdir }}/hello-plugin
        run: pnpm test

      - name: Publint generated package
        working-directory: ${{ steps.scaffold.outputs.workdir }}/hello-plugin
        run: pnpm publint

      # Open / reuse an issue if anything above failed. Avoids
      # silent cron failures — maintainer gets a PR-like ping.
      - name: Notify on failure
        if: failure()
        uses: actions/github-script@v7
        with:
          script: |
            const title = "generator-smoke: scaffold regression";
            const body = `Generator smoke failed on ${new Date().toISOString()}. ` +
              `See run: ${context.serverUrl}/${context.repo.owner}/${context.repo.repo}/actions/runs/${context.runId}`;
            const { data: existing } = await github.rest.issues.listForRepo({
              owner: context.repo.owner,
              repo: context.repo.repo,
              state: "open",
              labels: "generator-smoke",
            });
            if (existing.length > 0) {
              await github.rest.issues.createComment({
                owner: context.repo.owner,
                repo: context.repo.repo,
                issue_number: existing[0].number,
                body,
              });
            } else {
              await github.rest.issues.create({
                owner: context.repo.owner,
                repo: context.repo.repo,
                title,
                body,
                labels: ["generator-smoke", "bug"],
              });
            }
