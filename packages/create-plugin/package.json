{
	"name": "@anvilkit/create-plugin",
	"version": "0.1.2",
	"description": "Scaffolder for @anvilkit/* StudioPlugin packages. Generates a buildable plugin skeleton with tests, rslib build, biome config, and a minimal createXxxPlugin() factory.",
	"type": "module",
	"bin": {
		"create-plugin": "./dist/index.js"
	},
	"files": [
		"dist",
		"templates",
		"README.md"
	],
	"license": "MIT",
	"publishConfig": {
		"access": "public"
	},
	"scripts": {
		"build": "tsc -p tsconfig.json",
		"dev": "tsc -p tsconfig.json --watch",
		"typecheck": "tsc --noEmit -p tsconfig.json",
		"test": "vitest run"
	},
	"engines": {
		"node": ">=22.13"
	},
	"devDependencies": {
		"@types/node": "^26.0.0",
		"typescript": "6.0.3",
		"vitest": "^4.1.9"
	}
}
