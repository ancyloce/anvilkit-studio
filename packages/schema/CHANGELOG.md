# @anvilkit/schema

## 0.1.0-alpha.0

### Added

- Package scaffold (`phase3-005`).
- `extractFieldSchema` — maps Puck fields to `AiFieldSchema`.
- `isJsonSerializable` — validates JSON serializability.
