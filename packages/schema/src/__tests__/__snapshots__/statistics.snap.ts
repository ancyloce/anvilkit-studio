import type { AiComponentSchema } from "@anvilkit/contracts";

export const statistics: AiComponentSchema = {
	componentName: "Statistics",
	description: "",
	fields: [
		{
			name: "title",
			type: "text",
		},
	],
};
