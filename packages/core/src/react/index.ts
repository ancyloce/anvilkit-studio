// Barrel for `@anvilkit/core/react`.
//
// Filled in by core-013 (Zustand stores: export, ai, theme) and
// core-014 (`<Studio>`, `useStudio`, curried per-key override merge).
// This is the only subdirectory allowed to import React.

export {};
