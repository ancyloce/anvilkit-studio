// Barrel for `@anvilkit/core/runtime`.
//
// Filled in by core-008 (`compilePlugins`, `createLifecycleManager`,
// `StudioPluginError`) and core-009 (`createExportRegistry`,
// `composeHeaderActions`). MUST remain React-free — architecture
// constraint enforced in core-015 quality gates.

export {};
