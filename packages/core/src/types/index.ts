// Barrel for `@anvilkit/core/types`.
//
// Filled in by core-005 (`StudioPlugin`, `StudioPluginContext`, …) and
// core-006 (`ExportFormatDefinition`, `PageIR`, `AiGenerationContext`, …).
// Kept as an empty module so the subpath export resolves for consumers
// pointed at `dist/types/index.{js,cjs,d.ts}` even before any types
// ship.

export {};
