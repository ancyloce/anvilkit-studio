# @anvilkit/core

## 0.1.0-alpha.0

- Initial scaffold. Empty package with subpath exports (`./types`,
  `./runtime`, `./config`, `./react`, `./compat`) ready to be populated
  by the M2–M5 tasks in
  [`docs/plans/core-development-plan.md`](../../docs/plans/core-development-plan.md).
