/**
 * Shared Tailwind configuration for the monorepo.
 *
 * Consumers should define their own `content` globs locally and import this
 * config as a preset or merge it into an app-specific Tailwind config.
 */
export const config = {
	theme: {
		extend: {},
	},
	plugins: [],
};

export default config;
